# Subagent ledger

> One row per dispatched subagent. Lead maintains this file; subagents never edit it.

| Task ID | Wave | Role | Agent name | Branch/worktree | Owned writable files | Status | Commit | Handoff summary |
|---|---|---|---|---|---|---|---|---|
| P0-baseline | A | baseline-benchmarker | baseline-benchmarker | /tmp only | read + /tmp writes | PASS (artifacts verified by lead) | null | baseline_measurements.json; 6h fixture; numba finding; Trees=canopy-above-ground; SVF cache keyed by filename only (stale-veg hazard) |
| A-recon | A | repo-cartographer | repo-cartographer | read-only | none | PASS (report in lead context) | null | solver map: entry points, data flow, temporal state, GDAL IO sites, memory (4x146MiB patch stacks), file-ownership risks; Tg mutated in-place for water cells (aliasing hazard); repo_overlay frozen-mirror drift risk |
| A-recon | A | frontend-auditor | frontend-auditor | read-only | none | PASS (report in lead context) | null | examples/incremental_design_tool map: worker protocol, texSubImage2D integration point, spec-state gaps (Queued/Superseded/Failed weak, exact_result_version missing), 10-item API adapter gap list, prioritized P0-P3 changes |
| A-recon | A | validation-engineer | validation-engineer | read-only | none | PASS (report in lead context) | null | T0-T6 gap table; S01-S12 fixture plan (single generator rule: reuse fixtures.py); metrics/tolerances/T4 harness specs; pytest markers missing (scientific/nightly/benchmark); CI additions incl. gate-evidence enforcement |
| A-recon | A | oracle-auditor | oracle-auditor | read-only | none | PASS (idle, no independent report; scope covered by lead first-hand reads + cartographer) | null | no separate deliverable |
| P2-cache | B | cache-builder | p2-cache-builder | worktree-agent-ad95b41b391e94b58 | solweig_gpu/incremental/{cache,cache_builder,manifest}.py, tests/test_incremental_cache.py, incremental/__init__.py | PASS (52 req suite; full 93p/4s/2f pre-existing cli) | 127ca28 | SiteCache mmap warm path (zero GDAL), self-test-at-load w/ checksums, deterministic builder CLI; merged ad2fc57; note: branched from main@4c5e47d as anticipated |
| P3-packing | B | lead | lead (no subagent) | main tree | bitmask.py, pack_svf.py, tests/test_incremental_packing.py | PASS (18 tests; real-cache reductions ~6e-14) | d793bb0 | PACK-001/002 closed; PACK-003 partial-by-design (diffsh streaming deferred to P5 with verified bitwise-preserving design) |
| P4-trees | B | tree-rasterizer | p4-trees | worktree-agent-a55e8356b1f9069ed | solweig_gpu/incremental/{trees,spatial_index}.py, tests/test_incremental_trees.py, incremental/__init__.py | PASS (57 tests in worktree; 132 post-merge) | f308008 | TreeLayer + spatial index; local==full-sliced over 9 windows; max-combination; merged 525d515 |
| T3-harness | B | lead | lead | main tree | tests/scientific/{metrics,tolerances,conftest}.py, test_metrics.py | PASS (28 tests) | 695a337 | metrics machinery for P5 differential; S01-S12 scenarios land with P5 |
| svf-fix-review | B | code-review | svf-fix-reviewer | read-only | none | APPROVE-WITH-NITS (verdict 2026-09-01 ~19:50) | null | 38a3fd4 review: size-only check would have MISSED incident (geotransform comparison is the catch); no false-positive path; recompute heals cache; evidence hashes verified vs shasum. MEDIUM: gdal.UseExceptions made `ds is None` guards dead → corrupt-but-present cache crashed instead of fallback (fixed by lead: try/except RuntimeError → False + 3 corrupt-file tests, 20 passed). LOW (noted, unfixed): only svf.tif zip member extent-checked; CRS not compared; hardening test gaps closed by the 3 new tests. TRIVIAL WORKLOG 27-32% imprecision fixed |
| P5-worker | C | worker implementer | p5-worker | worktree-agent-a709a8594b5473245 | incremental/{solver,worker,result}.py, tests/test_incremental_worker.py, __init__.py append | PASS (25 fast + 8 scientific in worktree; bitwise local mode) | 86f0888 | merged c159f38. Found+guarded global amaxvalue march hazard; write margin 23px; veg SVF replay = exact svf_calculator accumulation on window; cold replay t=0; staging/publish with checksums + scene_revision guard; supersede watermark. Dense patch-cube slicing used (packed streaming → P8) |
| P5-review | C | code-review | p5-core-reviewer | isolated worktree, read-only | none | RUNNING (dispatched ~20:40) | null | independent scientific-core review of 86f0888 |
| P5-T3 | C | differential runner | p5-t3-runner | main tree read + one evidence file | docs/incremental_design_tool/benchmarks/2026-09-01-p5-scientific.json + /tmp | PASS full run (2026-09-02 ~02:00; pre-fix worker c159f38-lineage, files hash-verified) + post-merge subset rerun RUNNING | null | Sanity bitwise-PASS. SCI-001/003/004 PASS (SCI-004: S05 amaxvalue guard fired verbatim, full patch bitwise == oracle). SCI-002 strict-FAIL on S08 only — decomposed: 100% of diffs inside DISJOINT prior window w1, == job1's ulp drift; worker discipline + oracle invariance both bitwise-intact; criterion over-reached spec (see WORKLOG gate decision). Anomalies reported, not fixed: (1) S08 criterion; (2) local-vs-full ulp drift ≤7.25e-5 on real site, p99=0, deterministic; (3) REPO CACHE altitude_m=0.0 hard blocker — worker refused all jobs; runner built physics-identical cache_alt3 (altitude 3.0, all 26 arrays bitwise identical) to proceed; lead fixed repo cache + builder default (7328485); (4) harness ring-mask bug self-fixed mid-run (full-site masks before crop; parts regenerated, flagged); (5) S03 resize 917 s informational. Oracles cached /tmp/p5sci/oracles. Rerun (sanity+S01+S05+S08, repo cache, corrected SCI-002 grading) → benchmarks/2026-09-02-p5-scientific-postmerge.json, parts2/ |
| P6-api | C | server implementer | p6-api-server | worktree-agent-a75f87216edaa5f59 (from c159f38) | solweig_gpu/server/**, tests/test_server_api.py, tests/test_patch_codec.py, setup.py extras, requirements-server.txt | PASS initial (5e90fb6 merged 5488b54) + REJECT remediation PASS (f1f32ca merged b31c11b; 71 passed ×2 worktree + 71 main tree — lead-verified) | f1f32ca | initial: see prior row content — FastAPI [server] extra; SQLite WAL store/migrations/idempotency/optimistic versions; job runner coalescing+supersede; zstd codec; restart recovery. Remediation f1f32ca (+1191/−94): watermark via durable jobs.base_edit_watermark (published acked_sequence at insert) + adapter ledger replay counting fan-out edits (add/delete=1, move/update=2, reset=1/live tree); TestRealAdapterIntegration default-factory (bootstrap new-bytes, LOCAL sub-window byte-equal outside, create-without-baseline, progress contract); requeue_running_job + _queue.put in start(); _finalize catches ResultAlreadyPublished (checksum match→idempotent complete, mismatch→failed); POST /jobs/{id}/cancel; quota in-transaction; If-Match in fingerprint; resolve_requested 400; 409 cache_version_mismatch; Accept negotiation select_payload_media_type (identity when zstd unacceptable, 406 otherwise, ETag/checksum over SERVED bytes per encoding) — closes P7 H-2; decode_identity_payload. Hazard found: tiny fixture random 16-band shadowmat crashes Perez anisotropic in windowed solves → _real_site uses real _compute_baseline_svf. Re-review running |
| P5-fix | C | lead (review remediation) | lead | worktree-p5-review-fixes | incremental/{solver,worker,result,geometry,__init__}.py, tests/test_incremental_{worker,geometry}.py | PASS (220 non-scientific + 8 scientific in worktree; +11 tests) | 644cbe1 | fixes all actionable p5-core-reviewer findings: HIGH gvf-march pixel-scale (GVF_MARCH_METERS + gvf_march_pixels, per-site halo/write margin; 2m site margin 23→12px vs oracle 11px), MEDIUM amaxvalue negative-DSM guard, MEDIUM terrain-aware influence bounds (elevation_offset_m=DEM relief + oracle-bound length cap), MEDIUM manifest lon/lat vs raster-centre validation, LOW publish-loop supersede recheck + tree_base nodata clamp + missing-checksum rejection, TRIVIALs. MERGE HELD until p5-t3-runner completes (evidence provenance) |
| P6-review | C | code-review | p6-api-reviewer | read-only | none | APPROVE (final, 2026-09-02 ~01:35; R1 REJECT→f1f32ca, R2 narrow REJECT→9b28b23 both fully remediated) | null | Final verdict: API-001/002/003/004 ALL PASS. Independently verified: (1) bootstrap fix structurally sound (lowest published version is always a full-window bootstrap result on skip-0 path; stale-version publish refusal makes regression test deterministic) + reviewer re-ran own /tmp/bootstrap_gap.py brick repro against fixed main — was edit2-failed/stuck, now edit2 complete exact v2; (2) publish txn guard serializes against cancel on write lock, both orderings safe, rollback complete (get_result None asserted); (3) cancel semantics idempotent + race-consistent; (4) contract addition 58d13b1 accurate to implementation; (5) 75 passed re-verified main tree. Reset-409 on no-baseline site CONFIRMED correct semantics (nothing to reset to; P9 nicety: message could point at recovery path). Cosmetic non-finding: cancel-during-no-op-republish reports "superseded" cause. Round-2 LOWs accepted (identity decompress cache → P8, q-values documented) |
| P7-frontend | C | frontend implementer | p7-frontend | worktree-agent-a4dbbcf539a437906 (from 5488b54) | examples/incremental_design_tool/** only | PASS initial (48/48, merged 4863e60) + review remediation PASS (644033f merged 1cda682; 56/56 ×2 worktree, 56/56 main, smoke_proxy w/ auth) | 644033f | merged 4863e60. UI-001..005 implemented: api_client.mjs injectable-fetch seam, exact_session.mjs DOM-free state machine, one-POST-per-commit w/ Idempotency-Key+If-Match, 150ms preview/480ms commit debounce, 3-layer stale rejection (version manifest match, poll-generation guard, checksum+ETag+schema+window validation), texSubImage2D sub-rect only, Failed+retry same-key, model-scope card verbatim limitations. Zstd decision: JS DecompressionStream has NO zstd (Node 26.8.1 gzip/deflate only; canniuse 0%) — client feature-detects + Accept-negotiates identity fallback; P6 server must honor Accept (relayed to p6-api-server remediation). 3 self-found bugs fixed (getScenario double-encoding, bounds-check order, per-frame texture upload). 37 new tests. REMEDIATION (644033f): H-1 preview wired (connected branch + PREVIEW pill), M-1 propertyEditor.rebase mid-debounce, M-2 computeExactMetrics decoded-window, M-3 ready-flag+backlog replay, M-4 reset generation bump + pre-getJob/post-decode guards, M-5 heatGrid dims, L-1/3/4/5/6 fixed, L-2/7 documented; 56/56. Re-review running |
| P7-review | C | code-review | p7-frontend-reviewer | read-only | none | APPROVE-WITH-NITS (re-review 2026-09-01 ~00:15 UTC Sep 2; first review REQUEST CHANGES fully remediated) | null | Re-review verified every finding resolved in 1cda682: H-1 preview wired+pill, M-1 rebase timer semantics correct (callbacks read pendingTree at fire time), M-2 pure metrics fn w/ regression-shaped tests, M-3 backlog drain synchronous+ordered, M-4 both races regression-tested (payload-gate test strong), M-5 dims validated. New-defect sweep of backlog/rebase/generation areas: clean. UI-001..005 ALL PASS. 56/56 + smoke re-verified in main tree. Residual LOW → P8/P9: R-1 no reconnect path after failed connect (backlog accumulates silently), R-2 reset during connecting window doesn't clear backlog (held edits replay post-connect), R-3 serve.py Content-Encoding comment factually wrong (http.client never decodes Content-Encoding; behavior harmless/fail-closed), R-4 connecting badge says Exact before data. H-2 (server Accept negotiation) correctly remains P6-side — connected mode fail-closed-unusable in browsers until P6 lands identity serving |
| P8-perf | D | perf implementer | p8-perf | worktree-p8-perf (from d70137a) | solweig_gpu/incremental/**, solweig_gpu/server/{jobs,store}.py, tests/**, /tmp/p8perf evidence | PASS — MERGED (a1a0da4..104b300 incl. 104b300 review remediation; T3 ALL PASS; budgets adjudicated by lead; post-merge main 329p/5s + oracle bitwise 3/3 + node 56/56) | 104b300 | Re-enabled local mode on real terrain (per-tree LOCAL elevation offset replacing global relief; solar-series-based shadow cap; solar-driven dirty fraction 0.12-0.22 < 0.30 → LOCAL at h6/h10/h18), write-cropped per-cell physics with read-window marches, solweig.py/utci_process.py additive kwargs (sky_masks/precomputed_shadows/out_slice; legacy path when None), server O(new-versions) compose + identity payload cache + ledger retention, run_utci_window out_window validation. FINAL (post-remediation re-measure): p50 54.21s (pre-P8 89.32, −39%), p95 59.14s (−37%) — PERF-002 PASS @ original 60s; PERF-001 PASS @ 75s measured budget (20s placeholder replaced per cpu_optimization.md clause w/ derivation: 54.21 × 1.35 target-node factor, launch-bound per thread sweep); IO-001 PASS @ 2.67MB typical utci-only (5.86MB two-var full-day recorded + follow-ups); PERF-003/MEM-001/MEM-002/QUEUE-001 PASS original; PACK-003 not wired (MEM 3.3× headroom, documented). Night-march gate −10.7s, correctness-proven (differential 0.0 incl. night steps; T3 runner fresh oracle bitwise at 104b300). inside_max_abs claim corrected by T3: 1-ulp utci (2^-17) in 4/8 scenarios, tmrt/shadow bitwise. S03 917→64s. Evidence benchmarks/2026-09-02-p8-performance/ (19 files) |
| P8-review | D | code-review | p8-perf-reviewer | read-only (worktree .claude/worktrees/p8-perf) | none | APPROVE-WITH-NITS (2026-09-02 ~06:35) | null | Contract 1-5 ALL PASS: oracle kwargs keyword-only/None-gated incl. `_`-rebind hazard chased (diffusefraction rebinds before use); write-crop boundary correct (march+raywalk+Tg at READ extent, elementwise after gvf; early-exit amaxvalue argument verified against solweig.py:1114 loop — windowed steps strict prefix of oracle steps); local-offset early-return CAN under-estimate but _exact_influence_window per-cell flip mask is provable superset (Chebyshev+ds≥1+1cm slack); identity cache no poisoning (checksum-keyed, zstd deterministic); retention respects watermark. Tests genuinely strong (_assert_bitwise_inside = np.array_equal(equal_nan) vs real full oracle — fails on 1 ulp; conservativeness assertion catches halo misses). Findings: HIGH perf night-march waste (utci_process.py:731-743 marches at night, oracle discards — ~20-35% cut, correctness-neutral), MEDIUM identity-cache lock missing, MEDIUM multi-edit single-bbox inflation (deferred), MEDIUM idempotency-after-TTL undocumented, 5 LOWs. REJECTED the 52-55s "physics-bound floor" claim — floor lower, re-measure after night gate. Conditions before PERF-001 re-adjudication: night-march gate + identity-cache lock → remediation dispatched to p8-perf. Open questions for P9: (a) utci_calculator extent-dependence rationale empirically false on CPU — confirm on target device before REL; (b) disc-paints-height consistency with P4 untested directly. 2 test_cli fails = pre-existing PATH artifact |
| P8-T3 | D | differential runner | p5-t3-runner | read-only (worktree) + one evidence file | docs/incremental_design_tool/benchmarks/2026-09-02-p8-scientific.json + /tmp/p8sci | PASS — ALL GATES (2026-09-02 ~07:00; evidence 31927c4) | 31927c4 | SANITY + S01-S08 all PASS on worktree @ 104b300 (provenance resolved: remediation commit advanced HEAD past 8cf2bc1 mid-run; 7/8 executed files byte-identical between commits; night-skip empirically behavior-preserving — fresh 24-step full-tile oracle bitwise == pre-P8 baseline). LOCAL MODE RESTORED + POLICY-LIVE: S01-S04/S06-S08 local (dirty 0.0522-0.2496, walls 30.6-71.6s), S05 FULL 0.3902 by POLICY; 12-px margins exercised E2E every local job. INSIDE-WINDOW: P8 claim "inside_max_abs==0.0" FALSE as stated — utci 1 ulp (2^-17=7.63e-6) in S02/S03/S07/S08 (bitwise in S01/S04/S05/S06), tmrt bitwise ALL, shadow bitwise everywhere; 10× better than pre-P8 7.25e-5, inside envelope. RING/SEAM: ring max 0.0, bands flat at 7.63e-6, shadow ring 0.0, outside bands bitwise vs BOTH store AND edited oracle (0 cells — pre-P8 drift eliminated). Anomalies: (1) amaxvalue guard still policy-pre-empted on real site (SCI-004 on prior adjudication: full mode + tile-wide bitwise); (2) S09-S12 don't exist in harness — suite is sanity+S01-S08, flagged not improvised; (3) void sanity run detected+discarded (stale sys.path import, 0.5s); (4) S04 add+delete roundtrip bitwise this run (differs from pre-P8 drift, recorded as-is). No tolerance relaxed. POST-MERGE SPOT-CHECK PASS (2026-09-02 ~08:30, evidence 804c646 additive block post_merge_main_spot_check): merged main @ a408df5 plain checkout — SANITY fresh oracle 144.4s bitwise 3/3 (chain pre-P8 == 104b300 == merged main); S01 LOCAL (dirty 0.1600, 55.3s) inside utci/tmrt 0.0 + shadow bitwise, all identical to 104b300 evidence, outside bitwise vs store; S05 policy-full (0.390208 identical) tile-wide bitwise 3 vars. Runner disclosed+discarded a void first attempt (reused-oracle skip signature, 0.5s wall); graded run recomputed fresh, npy hashes cross-verified vs P8-era. P8-T3 CLOSED. Runner re-engages at U-C |
| P9-docs | E | docs fixer | p9-docs | worktree (isolated) | docs/**, docs/conf.py, 4 docstring files | PASS — MERGED (7c613b5+0be8978 → merge eb34ade; lead verified main: sphinx -W exit 0, docs test 13/13; worktree removed) | eb34ade | T5 CLOSED. Baseline ~179 in worktree (lead's 244 included main-only files + missed 3 docutils-ERROR + 3 RemovedInSphinx10Warning classes invisible to case-sensitive WARNING grep) → 0. Fixes: nbsphinx_prolog per-notebook titles + download-path fix via env.docname (~150 toc.no_title + notebooktitle + download.not_readable); http→text ×15, mermaid→text+note ×2 (+TextLexer alias for dependency_graph.md), math→{math} ×2 MathJax-verified, CONTRIBUTING xref→blob URL, docstring blank-line rst fixes ×4 files (lead-verified docstring-only: shadow, utci_process, preprocessor, create_inputs), ipython3 lexer defensive. DEVIATION ACCEPTED: suppress_warnings=['toc.not_included'] instead of exclude_patterns — exclusion breaks main's links into agent/universal_editing/benchmarks (empirically proven); pages stay in build. Merged-state pre-validated by agent (copied main files in, -W clean, 13/13) + lead re-verified in main post-merge |
| P9-rel | E | reliability engineer | p9-rel | worktree (isolated) | solweig_gpu/server/**, tests/test_server_*.py, +1 trees test, comment fix, smoke script, benchmarks/2026-09-02-p9-reliability.json | RUNNING (dispatched 2026-09-02 ~07:45) → COMPLETE, see result row below | 31defb4 | REL-001 crash-recovery proof test (kill mid-job → restart → no stuck rows/idempotency intact; fix holes minimally), REL-002 real-time classroom load test (8-10 scenarios, real solver, bounded <10min) + evidence JSON, T6 deployment-smoke pytest (health/create/edit/poll/result/payload both encodings/SSE/reset; deployment marker), review follow-ups: disc-paints-height pin test + stale tan(5°) comment |
| P9-frontend | E | frontend finisher | p9-frontend | worktree (isolated; agent reset worktree branch onto 987b9e2 since frontend code absent at worktree base) | examples/incremental_design_tool/** only | PASS — MERGED (5461313 → merge into main; node 61/61 verified in main post-merge; worktree removed) | 5461313 | P7 residuals closed. R-1: ExactSession.connect transient-failure reconnect, bounded backoff 1s/2s/4s ×3, held edits replay in order on landing, exhaustion releases+rejects, permanent 4xx fast-fail, reconnecting status pill. R-2: reset() during connecting releases backlog + poll-gen bump + held_edits_discarded + skips pre-ready server round-trip; connect re-reads baseline after mid-fetch reset race (previously crashed). R-4: exactBadgeForConnection — neutral Connecting until first verified baseline (onBaseline precedes ready), Exact only on connected. Stash-verified regression shape: fixes reverted → exactly 5 new/changed tests fail. README session-behavior + test list updated |
| U-B-core | U | engine-core implementer | u-b-core | worktree (isolated) | NEW: incremental/{edit_types,edit_graph,edit_registry,planner}.py + new tests + additive __init__ exports ONLY | PASS — MERGED (c68f120+dd138b1+017529a+c4b5164 → merge 486b2e4; lead verified main: 70/70 incl. 38 new engine tests; worktree removed) | 486b2e4 | Engine core delivered: contract records verbatim (ImpactPlan additively extended w/ scene_revision+transport_window per U-C ruling); 22-node/36-edge versioned graph w/ both YAML drift guards; registry enforces U-A rulings in code (water 7, transmissivity, blocked params, wind=fence-refusal per UEDIT-010 strict read); planner w/ heterogeneous coalescing (bitwise-deterministic), per-node write scopes, SafetyPolicy (0.30 default = worker policy), unsafe→FULL w/ reason, view-only zero-stage, stale-revision error. 10 ambiguities resolved conservatively (documented in docstrings; full list in report — notably: derived nodes inherit FULL from any dirty parent; REPLAY estimates acknowledged-underestimate w/ constructor param; model-param unknown keys rejected). Runtime yaml-free. Integration map delivered for adapter/executor/server/API packets |
| P9-rel | O | REL-001..003 + T6 smoke + review follow-ups | p9-rel | worktree @ 91df635/9ed2574/966e60c (base 987b9e2) | none (never pushed) | PASS — MERGED 31defb4 (2026-09-02 ~10:30). REL-001: SIGKILL mid-solve → frozen running row, idempotent reopen x2, requeue exactly-once, no orphan versions, 202 replay + 409 conflict; existing JobRunner requeue sufficient. REL-002: 10 scenarios/19 edits/1 worker, 11 complete + 8 superseded, 0 failed/dropped, p50 0.50s p95 3.99s ≤ 2x service, depth ≤2, drain 4.1s, RSS ≤282MB, 186s wall vs 600s budget; harness records actual steady/burst split. REL-003: 1MiB cap→413 (chunked covered), 30/min/IP→429+Retry-After (ops exempt), 20 edits/min/scenario, tree bounds via incremental-library validators→422, count cap→403, default-ON; TLS model + SSE buffering-off documented, proxy-headers proven over real HTTP. T6: real uvicorn — health/create/edit/SSE/manifest/payload both encodings (checksum/ETag/304/codec)/reset + tight security profile. Mandated 125/125; full 357 passed/4 skipped/1 failed — austin smoke failure PROVEN pre-existing on pristine origin/main@4c5e47d by lead (temp worktree, same TypeError) — not a regression of this branch. Doc notes: create_app default max_trees 200 vs ops-doc 100 (unchanged, set explicitly per deployment); models trunk_ratio [0,1) vs data_model.md [0,1] (doc lag) | 31defb4 | Evidence docs/incremental_design_tool/benchmarks/2026-09-02-p9-reliability.json + measure_load.py + reverse_proxy_tls.md. Lead post-merge re-verification: server suite running; austin pristine-main proof recorded |
| U-B-params | U | model_receptor_parameters adapter (#3) | u-b-params | worktree @ 19da08e (base c396a1b; agent left work uncommitted — lead committed in worktree) | none (never pushed) | PASS — MERGED (2026-09-02 ~11:30). 14 safe params code-cited (actual utci_process.py line numbers, zero baseline-manifest entries, vocabulary+refusal ready for U-C); blocked set enforced via adapter validate AND planner command-state + delta-payload paths; model_parameters→radiation closure only (4 nodes FULL, 16 reusable incl. walls/wall_aspect/visibilities/svf/time_shadow); mixed batch one plan (veg WINDOWS + params FULL); adapter plan == engine plan bitwise; wbgt never planned; PLUMBING_CLASSIFICATION executor seam (kernel_arg 10 — forwarded to kernel but not run_utci_window args; loop_local 4 — transVeg/height/leaf-days need body plumbing; anisotropic_sky double-sited :613+:767 both must be overridden; patch_option stays blocked SVF-cache invariant; before_value=None resolves at apply). 25 tests; __init__ additive-only (vegetation.py byte-identical). Lead re-verify at merged HEAD (35699a7+ semantics): 116 focused green + 262 incremental green, import-origin main tree | 19da08e |
| U-B-met | U | met/time forcing overlay adapter (#2; held for solver.py unblock post-p9-rel) | u-b-met | worktree; scope adapters/met_time.py + tests + additive __init__s + edit_registry.py ForcingDelta fence + solver.py cross-check relaxation (minimal, overlay-gated, legacy byte-identical) | none (never pushes) | RUNNING (dispatched 2026-09-02 ~11:40, base 0cd113d) | null | Copy-on-write overlay (baseline arrays immutable); per-variable fence vs registry met entry; temporal semantics per registry; site-global FULL for met chain only (walls/visibilities/svf stay reusable — verify from dependency_graph edges); wbgt never; mixed-batch one plan; solver legacy default-None path must still reject tampered arrays (new test); ~20-30 tests; commit ALL work in worktree |
| U-B-params-review | U | model_parameters adapter reviewer | u-b-params-reviewer | read-only (main @ 0cd113d/571bced → 3dc1f82) | none | Round 1 REQUEST-CHANGES (1H/1M/3L) → remediated 3dc1f82 → ROUND 2 FINAL VERDICT: APPROVE (2026-09-02 ~12:45; all fixes live-verified incl. mutation probes — isfinite-fence and staging-door tests genuinely exercise the checks; diff scope exact; 28/28 + 119/119 slice + 265 incremental green) | 3dc1f82 | H: NaN/+inf passed every value fence (nan fails all relationals, +inf slips low-only bounds; stdlib json parses the literals) — fixed math.isfinite in _check_value_in_domain (shared validate+seam+staging). M: apply_source_delta staged hand-built out-of-domain/blocked deltas — fixed via _check_declared_values(bounds=True) re-check pre-stage. L-a anisotropic_sky table note; L-b impact_plan SiteContext parity → U-C intake; L-c inverted '>= / >' bound text fixed. VERIFIED SOUND r1: 14 defaults exact vs utci_process.py; name fences all paths 40+ probes incl. case aliases (closed-world safe set catches); closure exact per YAML (svf reusable under transVeg CORRECT — svfbuveg/diffsh computed in run_utci_window from cached svf + transVeg :670/:674 forwarded :767, svf cache pure geometry); 5 dirty + 17 reusable = 22; plan equality bitwise; composition append-only vegetation byte-identical; tests non-vacuous (3 mutation probes caught); 116 focused + 262 incremental green |
| U-B-met-review | U | met/time adapter + solver overlay reviewer (scientific core — MANDATORY) | u-b-met-reviewer | read-only (main @ 8489a69 → 453d412) | none | RUNNING (dispatched 2026-09-02 ~13:10) → COMPLETE, see result row below | null | Dimensions: solver byte-identity (overlay=None all-guarded; SAME tolerances in masked re-check; declared-cell masking exactness); COW immutability proof; finiteness/bounds in met fence (params-reviewer NaN class); staging-door bypass; closure vs YAML (time_shadow dirty via meteorology->solar_atmospheric_state->time_shadow — verify edges); temporal REPLAY semantics; plan equality; YAML parity; mutation probes; worker+trees regression |
| U-B-lc | U | landcover adapter (#4, water-fenced) | u-b-lc | worktree; scope adapters/landcover.py + tests + additive __init__s (+edit_registry only if fence gap, report first) | none (never pushes) | RUNNING (dispatched 2026-09-02 ~13:20, base d58adbf) | null | Water-7 + invalid classes rejected on ALL paths incl. STAGING DOOR (params MEDIUM standard); finiteness (params HIGH); dirty/reusable from YAML edges; mixed batch one plan; plan equality bitwise; ~20+ tests; NOTE: parallel with u-b-bld — adapters/__init__.py appends will conflict, lead resolves at merge |
| U-B-bld | U | building massing adapter (#5, FULL-ONLY, last) | u-b-bld | worktree; scope adapters/building.py + tests + additive __init__s (+edit_registry only if gap, report first) | none (never pushes) | RUNNING (dispatched 2026-09-02 ~13:20, base d58adbf) | null | Every op → FULL whole downstream chain, zero windows (H2 engine path must match adapter path bitwise); staging door re-validates; finiteness; nothing geometry/vision-dependent reusable; wbgt pruned w/ reason; mixed batch building+veg+params one plan; U-C friction: executor runs full-tile regeneration for walls/wall_aspect/svf |
| U-B-lc-result | U | landcover adapter (#4) RESULT | u-b-lc | worktree @ 42f4e93 (base d58adbf, committed clean) | none | PASS — MERGED (2026-09-02 ~14:00). Water fence 4 layers (states both roles / validate_delta / STAGING DOOR / overlay __post_init__); remapped 0/3/4 + unknown 8 + negatives rejected; closure edge-justified (dirty radiation/STS/tmrt/utci; walls/visibilities/svf/time_shadow reusable per edges, matches engine pin); WINDOWS clamped-merged + STS REPLAY 0→bound + FULL @≥0.30 w/ reason; read_halo default 0 = bitwise plan equality; mixed batch one plan disjoint-union transport; refused polygon_assign/reset loudly; out-of-grid windows refused; payload all-int (finiteness N/A, documented); edit_registry UNCHANGED (no gap); mutation probe load-bearing; 33 tests; 188/188 slice -W error at HEAD; U-C friction: solver.py:1099-1108 overlay materialization, :1213-1216 full fallback, utci_process.py:571-604/967-979 + Tgmaps_v1.py:15-53 class consumption, solweig.py:130/:2104-2113/:2136-2144/:2194-2195 | 42f4e93 |
| U-B-bld-result | U | building adapter (#5) RESULT | u-b-bld | worktree @ 17fa52b (base d58adbf, committed clean) | none | PASS — MERGED 88c5085 (2026-09-02 ~14:45; lead resolved expected __init__ append conflicts — both lc+bld blocks kept, composed chain binds all 5 adapters). FULL-only every op (add/move/update/delete); raster_patch REFUSED loudly; staging door re-validates full payload; FULL dirty set 13 nodes edge-justified incl. STS REPLAY 0→requested; 9 sources reusable asserted both directions; engine parity adapter==H2-plan bitwise; edit_registry UNCHANGED; 35 fns/62 instances; U-C friction: NO incremental building rasterizer (absolute-elevation rule U-C's), wall pass solweig_gpu.py:255→walls_aspect.py:271 (walllimit 3.0 :25), SVF calculate_svf rewrites 3 cache artifacts (shadow.py:409), cache_builder.py:82, run_full_tile solver.py:1258 1:1 mapping | 17fa52b |
| U-B-met-review | U | met/time adapter + solver overlay reviewer (scientific core — MANDATORY) | u-b-met-reviewer | read-only (main @ 8489a69 → 3032486/453d412) | none | Round 1 APPROVE w/ 2 MEDIUMs + 2 open questions → R1 adjudicated+implemented (union semantics), MEDIUMs fixed 3032486 → ROUND 2 FINAL: APPROVE (2026-09-02 ~15:20; mutation-proven tolerance pin, staging probes refused, R1 plan shapes exact incl. degenerate + empty-window cases; 323 passed). 1 LOW → U-C: requested_times not fenced to available_times before union (out-of-series request extends REPLAY past series; executor must fence). Verbatim-verified: solver byte-identity empirical (pre-merge archive cmp, 7 arrays float64-identical), tolerances same rtol=1e-5/atol=1e-4, masking structural (mask==writes), col mappings vs utci_process.py:615-634, closure edges YAML:39/41, bitwise plan equality, PYTHONHASHSEED 0/1/42, mutation probes 5 | 453d412 |
| U-B-review-scope | U | review-policy note (lead) | — | — | — | lc + bld got NO dedicated reviewer rounds: neither touched scientific core (no solver/utci_process/solweig changes — pure adapter layer, same class as vegetation which the engine-core review covered by intersection). Params+met got reviews (first-of-class + solver.py touch = MANDATORY). U-E red-team wave audits all five adapters end-to-end. All 5 concrete adapters registered via composed chain at 88c5085; 254/254 adapter slice -W error | 88c5085 |
| U-B-review | U | engine-core reviewer | u-b-reviewer | read-only (main @ 486b2e4 → a0b4da9 → 35699a7) | none | Round 1 REQUEST-CHANGES (2H/4M/5L) → remediated a0b4da9 → round 2: NEW-1/NEW-2/nit/L4 found → fixed 35699a7/f15a0ac → FINAL VERDICT: APPROVE (2026-09-02 ~09:55). NEW-3 case-fold hardening also closed by lead @ f760024 (was defense-in-depth, adapter closed vocabulary already blocks canonical path) | f760024 | Round-2 findings all accepted+fixed: NEW-1 ancestor walk seeds only OUTPUT node ids; non-node producible layers accepted cleanly (executor transports, U-C); unknown names → PlanningError listing requestable vocabulary. NEW-2 pixel_size_m compared. Nit: literal "Patch_Option" alias. L4: drift guard asserts edge multiset length. Deferred LOWs w/ rationale → U-C intake: L1 dem delta family (terrain packet defines DemPatchDelta), L2 executor halo wiring (adapter windows pre-inflated via geometry influence bounds), L3 FrozenMapping constructor normalization, L5 canceled add+delete windows inflate dirty fraction (conservative-only: unnecessary FULL demotion, never wrong output). Reviewer-verified sound across both rounds: contract verbatim+source-compatible; determinism across PYTHONHASHSEED 1/42/99999; drift guards mutation-verified both directions; parity 22/36/9; replay-from-0; transaction staging; FULL-inheritance; M3 direction held (24-step context 376,832→4,898,816) |
| U-B-veg | U | vegetation adapter implementer | u-b-veg | worktree (isolated, reset to 486b2e4) | NEW: incremental/adapters/{__init__,vegetation}.py + tests/test_incremental_adapters_vegetation.py + append-only __init__ exports | PASS — MERGED (6389fd8+309a95f → merge into main; 3 wbgt expectations updated post-merge for M1 semantics, lead-adjudicated: tests written pre-a0b4da9 asserted old engine behavior; adapter itself needed no change; retest in background) | (merge after a0b4da9) | First EditAdapter delivered: VegetationGeometryAdapter + tree_edits_from_deltas/coalesced_batch_from_deltas/register_default_adapters. Registry enforcement passed through (validate_operation + validate_edit_state on BOTH states incl. transmissivity + raster_patch rejection); impact_plan DELEGATES to core EditPlanner → adapter plan == engine plan bitwise (==/hash/repr asserted). 22 adapter tests + cross-process determinism (PYTHONHASHSEED random vs 12345, identical digests). U-C friction list: sun-position/influence-config constructor injection (default = SVF-disc-only windows, schema/preview only); transaction all-or-nothing rollback, revision travels beside commit(); TreeLayer sequence replay via coalesced batch; dirty_windows_for_batch merge_gap_pixels=8 vs planner 0 (U-C passes 0); TreeSpec.transmissivity inert internally; read-halo executor expansion needed |
| U-B-params | U | model-params adapter implementer | u-b-params | worktree (isolated, reset to c396a1b) | NEW: incremental/adapters/model_parameters.py + tests/test_incremental_adapters_model_params.py + additive registration/exports ONLY | RUNNING (dispatched 2026-09-02 ~11:00) | null | model_receptor_parameters EditAdapter per ruled order (3rd adapter; audit A7: params already kernel args). Safe-set-only via registry paths; documented physical bounds per parameter (conservative where unknown); PARAMETER_DEFAULTS table w/ code-cited vs baseline-manifest split; reset semantics; plan delegates to core planner (params → radiation chain FULL, geometry caches reusable); heterogeneous batch w/ vegetation keeps local WINDOWS nodes (U-C mixed-scope); kernel-arg vs loop-local plumbing friction recorded for U-C |
| U-A | U | spec auditor | u-auditor | read-only (main) | universal_editing/audit/** (report only) | COMPLETE — report + lead rulings applied as eda60d1 (2026-09-02 ~09:15) | eda60d1 | Code-grounded reconciliation of dependency_graph.yaml + edit_registry.yaml vs oracle chain (thermal_comfort→Solweig_2022a_calc). 9 findings w/ file:line evidence: walls/aspect = Building_DSM-only preprocessing; SVF marches read `a` only; bush derived from relative geometry (utci_process.py:517); walls+wall_aspect feed time_shadow (solweig.py:2062-2063, utci_process.py:745); [model_parameters,utci] no direct path (calculate_utci.py:259); transmissivity inert (global transVeg utci_process.py:606); selected_date_time cache-pinned (manifest.py:287-304, solver.py:324-332); WATER BUG class-7-vs-lc_grid==3 dead branch (solweig.py:130/:2195); wind-coeff parity gap (solver.py:1090-1091/:1179); wbgt disabled; output extras local-mode only. LEAD RULINGS: water fenced from v1 paint classes; wind-coeff = hard fence (detect+refuse); transmissivity excluded from v1 schema; onlyglobal blocked (dead met cols 21/22); U-B order = vegetation → met/time overlay → model_params → landcover → building |
| U-C5 | U | building rasterizer + full regeneration chain (packet 5) | u-c5 | worktree @ 42abdc6 (base d92392d, committed clean) | none (never pushed) | PASS — MERGED 9895658 (2026-09-02 ~17:15). NEW incremental/buildings.py (556 ln: BuildingLayer COW apply/rasterize, per-cell DEM grounding dem+h NO interpolation matching veg convention utci vegdem=temp1+temp2, cell-centre boundary-inclusive even-odd fill == trees.py:130-134 disc mask cell-for-cell 64-gon proof, np.fmax paints-over-nodata, before-footprint reset unconditional-to-DEM disclosed via cells_reset, WALL_LIMIT_M=3.0 walls_expected disclosure incl. zero-cell + raster-edge) + incremental/regenerate.py (601 ln: stage_scenario_site overlap-guarded copytree → write_scenario_building_dsm atomic tmp+replace [+ trees] → regenerate_walls_aspect → regenerate_svf → rebuild_scenario_cache → run_scenario_full_tile; RegenerationError names completed stages, partial tree left as evidence, baseline hash-verified immutable incl. injected-SVF-failure) + tests/test_incremental_buildings.py (25). Executor NOT wired (u-c1 seam per scope). Lead post-merge: 101 focused green, import-origin ok. 148/148 in worktree; austin failure pre-existing @d92392d proven. Integration signature: regenerate_building_batch(edits, baseline_site_dir, baseline_cache, scenario_root, tree_layer=None, selected_date_str=None, requested_variables) → BuildingRegenerationResult | 42abdc6 |
| U-C2 | U | model-param physics plumbing (packet 2) | u-c2 | worktree @ e33836a (base d92392d, committed clean) | none (never pushed) | PASS-MERGED 0ba8fa6 (2026-09-02 ~17:40) — REVIEW PENDING (scientific core). utci_process.py +63/−3: model_parameters kwarg (None default bit-identical, npz-pinned); _MODEL_PARAMETER_NAMES frozenset :71-83 literal (physics never imports editing framework, tests pin sync); unknown-name ValueError pre-compute :520-528; override block :644-670 (transVeg/anisotropic_sky loop-local — loop-local assignment is single source of kernel arg, replaces both sites; 9 kernel-arg locals w/ default+line citations; kernel call byte-identical reading locals); leaf-days :698-703, height :716. Differential table max|Δ| 64x64 4-step DOY172: all 11 probed params change output where physics couples, zeros where decoupled (albedo_b leaves lup/ldown/shadow bitwise), windowed==full bitwise. 20 new tests + fixtures/params_physics_prechange_default.npz. LEAD RULING PENDING: cyl=False TypeError solweig.py:644-655 (torch.where bool-cond numpy idiom), anisotropic_sky=0 UnboundLocalError :804 (del never bound isotropic path) — pre-existing dead branches now reachable; plumbing delivers both (kernel-capture proven), crashes characterized in tests. Lead post-merge: 48 focused + 55 worker green | e33836a |
| U-C2-review | U | params-physics reviewer (scientific core — MANDATORY) | u-c2-reviewer | read-only (main @ 0ba8fa6) | none | RUNNING (dispatched 2026-09-02 ~17:45) | null | Dimensions: default-path bit-identity (npz + twin); override block completeness vs PLUMBING_CLASSIFICATION (all 14; anisotropic_sky both sites; loop-local 4); zeros-plausibility physics check; dead-branch ruling (fix vs fence); npz provenance; mutation probes |
| U-C1 | U | executor core + temporal store (packet 1) | u-c1 | worktree @ 9451b8d (base d92392d, committed clean) | none (never pushed) | PASS — MERGED (2026-09-02 ~18:10; lead resolved expected __init__ append conflict, u-c5 + u-c1 blocks both kept). NEW executor.py 1058 ln (PlanExecutor L295, execute L480, execute_plan L541, stage_read_window L236, detect_wind_coefficients L216, NotIntegratedError L198, ExecutedPlan L266, _prepare_vegetation_batch L661, _run_worker force-FULL routing L693 threshold 1e-12 restored-after, _undo/_replay_tree_edits L715/L734, _reconcile_scope L882; docstring documents u-c2..u-c5 seams) + store.py 246 ln (TemporalResultStore (node,time) references-only, atomic validate-all-then-write, stale=rev<recorded, same-rev/diff-job conflict, VARIABLE_TO_RESULT_NODE shadow→time_shadow) + planner fence_requested_times L129 + edit_types FrozenMapping L3 normalization (dedup last-wins key-sort) + coalesce L5 canceled-window drop. Revision discipline: publish at plan.scene_revision+1 (batch-created revision), failure/supersede rolls back state+store. E2E: 128x128 real-SVF scientific test REAL physics via executor == direct ExactWorker bitwise per var. 43 new tests. Lead post-merge: 68 executor+buildings, 145 engine+adapters green, imports ok. NOT covered (later): u-c2 folding, u-c3 met, u-c4 lc, u-c5 wiring (seam documented), veg sun-position refinement, ScenarioTransaction atomic binding, producible-extras blocked by ResultPatch.SUPPORTED_VARIABLES (recorded unpublished_variables), wind-coeff site execution. Orphan-patch edge documented: reconcile-fail-after-publish leaves job-unique patch dirs | 9451b8d |
| U-C3 | U | met overlay into worker/executor (packet 3) | u-c3 | worktree (isolated, reset to eae21a6) | solver.py overlay materialization, worker.py overlay thread, executor._scenario_forcing, NEW tests/test_incremental_met_integration.py | RUNNING (dispatched 2026-09-02 ~18:20) | null | Overlay-dropped-proof (run_full_tile/solve_window must materialize RESOLVED table, never met_path-copy-drop); R1 union E2E; oracle = physically edited met file in scenario site dir, bitwise inside write window; utci_process u-c2 sections + params_physics tests off-limits |
| U-C1-review | U | executor core + store reviewer (MANDATORY — orchestration of scientific core) | u-c1-reviewer | read-only (main @ eae21a6) | none | RUNNING (dispatched 2026-09-02 ~18:20) | null | Dimensions: stale-revision races/publish atomicity; force-FULL threshold restore on exception; tree undo/replay inverse round-trip; fence both call sites; store coverage_gaps/invalidate semantics; M2/L3/L5 absorption real; mutation probes; orphan-patch read-back hazard |
| U-C2-review | U | params-physics reviewer verdict (REPLACES dispatch row status) | u-c2-reviewer | read-only (main @ 0ba8fa6) | none | APPROVE-WITH-NITS (2026-09-02 ~19:00). Item-4 ruling ADOPTED BY LEAD: FIX dead branches, do NOT fence (upstream UMEP evidence — cyl if/else per Kside_veg_v2022a.py; del is GPU-port artifact upstream never had; defaults stay True/1 so default path untouched, npz gate pins it; fencing would shrink safe set 14→12 and bake port bugs into contract; isotropic longwave path aniso=0 never executed in port may hide more latent issues — fix packet validates differentially) | null | PROOFS: npz provenance INDEPENDENTLY verified (extracted d92392d package, ran base run_utci_window on byte-identical input bundle — base == modified-default == npz bitwise 7 vars); diff audit clean (single .get identical-literal each, no rebind); 14/14 reach physics; anisotropic_sky single-source CONFIRMED (:658 assign, :827 read); reviewer's OWN 12-param differential table matches physics exactly incl. transVeg/leaf-days psi-shadow being real; elvis=1 runs (no third dead branch). Findings: (1) MEDIUM pre-existing live contradiction cyl=False/aniso=0 SAFE-but-crash (resolved by u-c2b); (2) LOW fold seam np.float64 uncoerced → float() at fold; (3) LOW zero-side differential not pinned by committed test; (4) TRIVIAL shifted line citations. Tests 48/48 + worker scientific 8/8 re-run; mutation probes all refused (np.float32/0-d array/np.int64/bool-for-float, case/punct/space aliasing) |
| U-C2b | U | dead-branch fix remediation (cyl if/else, del guard, fold coercion, zero-side pins) | u-c2b | worktree (isolated, reset to df1d143) | solweig_gpu/solweig.py, incremental/adapters/model_parameters.py, tests/test_incremental_params_physics.py | RUNNING (dispatched 2026-09-02 ~19:10) | null | Upstream-faithful fixes w/ citations; default path byte-identical (npz + worker 55); all 4 cyl/aniso combos; zero-side bitwise pins; derived-only tolerances; round-2 verification back to u-c2-reviewer |
| U-C1-review | U | executor verdict (REPLACES dispatch row status) | u-c1-reviewer | read-only (main @ eae21a6, files verified byte-identical) | none | APPROVE-WITH-NITS (2026-09-02 ~19:55). 0H/4M/5L/4T; every failure path fails LOUD — no silent-wrong-results vector. Probe-evidenced: force-full finally-restore verified on mid-run exception; stale-rev discipline; single-thread store atomicity; fence both call sites; L3/L5 real; poisoned delta clean pre-layer rollback | null | M1 executor.py:637-648+:855-859 (root worker.py:560) post-publish failure leaves worker _acked_sequence advanced → retry coalesces to no-op, _record_outcome raises "double execution" false diagnosis, executor wedged (loud). M2 _record_temporal_entries merge_windows(...)[0] keeps only FIRST window — store under-reports multi-patch coverage (probe P0); latent because worker cannot publish >1 patch/job (pre-existing result.py:199/:222 dir-name collision). M3 store.py:152-157 shared-store conflict permanently wedges loser (rolls to rev 0, replays into conflict forever). M4 orphan rev-* patch dirs post-publish-failure — rev-glob readers (P6 pattern, U-D-bound) can read never-current patch. L5 pre-flight maps presence only — trunk_ratio=1.0 hand-built delta passes pre-flight, raises mid-real-pass, partial layer mutation (fix: validate_tree_spec in pre-flight). L6 store publish no lock. L7 same-rev same-job silent overwrite. L8 load_patch full SHA-256 verify just to index metadata. L9 superseded branch untested. T10 undo-raise masks original; T11 planner fence skip on empty available_times; T12 per-window reconcile conservative; T13 stage_read_window no production call (u-c4 scaffolding). REMEDIATION u-c1b QUEUED behind u-c3 (executor/worker overlap): fix M1-M4 + L5-L9; M3 lead decision = document store-per-executor single-writer discipline; M2/M4 pair with pre-existing multi-patch result.py fix (u-c1b designs backward-compat rev-* naming) |
| U-C2b | U | dead-branch fix RESULT (REPLACES dispatch row status) | u-c2b | worktree @ 696817c (base df1d143, committed clean) | none (never pushed) | PASS — MERGED 1b0f615 (2026-09-02 ~20:40) — round-2 verification PENDING. solweig.py: cyl torch.where-bool → EXACT upstream if/else (Kside_veg_v2022a.py 'Kside with weights': same guards aziX>(360-t)or<=(180-t) etc., same expr radI*shadow*cos(alt)*sin(aziX), KsideI=shadow*0; cyl==1 branch byte-untouched; zero-side uses grid torch.zeros — 0+b==b exact, judgment documented); del gated anisotropic_diffuse==1 (upstream has NO del — GPU-port artifact; all other del names audited bound); fold seam float() coercion for float-kind (np.float64 passes gate needs coercion, np.longdouble NOT float subclass already refused — docstring corrected); zero-side pins TestZeroSideOfTheDifferential (reviewer's verified decoupling table w/ physics justifications); dead-branch tests: cyl=False kernel-level differential unselected bitwise / selected vs bit-exact internal replication, DERIVED tolerance γ₂=2u/(1−2u) fp32 half-ulp observed ≤0.58× bound; aniso=0 Jonsson Ldown end-to-end; all-4 cyl/aniso gates parametrized, (True,1) bitwise default; old crash tests rewritten to assert fixed. 36+29+55 worker+108 neighbor+12 windowing green; npz default-path gate green. NOTES: met_time.py:261 + landcover test :99 citations shifted +20 by solweig.py fix, left out-of-scope, tests pass — lead sweeps in u-c4/u-d | 696817c |
| U-C3 | U | met overlay RESULT (REPLACES dispatch row status) | u-c3 | worktree @ 28effdb (base eae21a6, committed clean) | none (never pushed) | PASS — MERGED aa1d20a (2026-09-02 ~21:20) — review PENDING (scientific core solver.py +103). SiteForcing.overlay field + run_full_tile(overlay=None) kwarg + _forcing_with_overlay (resolved+different overlay → SolverInputError; baseline+overlay → re-materialize through load_site_forcing so masked re-check validates) + scratch stages RESOLVED table %.17g lossless (overlay=None still copies bytes verbatim); solve_window untouched (consumes met_table never met_path); worker stage_forcing_overlay + _published_forcing_overlay watermark mirroring _acked_sequence (consumed only on publication, superseded keeps pending); executor _scenario_forcing via _effective_forcing_overlay folding committed deltas over _applied_forcing_overlay accumulated across PUBLISHED batches (later veg batches never revert earlier met edits), last-write-wins per (variable,time_index), fence re-validated via overlay_from_deltas; met-only batches publish full jobs. E2E: patch BITWISE (equal_nan) vs full run on twin site w/ physically-edited met token + fresh cache, every band/cell/variable; unchanged times bitwise; shadow bitwise everywhere (fence: Ta can't move solar); decay carry utci .585/.013/.003; R1 union E2E REPLAY stop=3 bounds(1,3) FULL, t=1,2,3 served rev 1 bitwise; tampered overlays rejected through NEW seams (undeclared wind write, +1.0 coarse, +5e-4 sub-tolerance); legacy byte-identical. 157 passed (worker 55 unmodified spot-verified ✓, executor 44, met adapters 40, new 18); import-origin clean; utci_process + params tests untouched ✓. Deviations: executor integration edits beyond seam (EXECUTABLE_SOURCE_NODES+=meteorology, applied-overlay state, empty veg batches) — all required by integration; 2 pinned pre-u-c3 tests rewritten to assert integration | 28effdb |
| U-C3-review | U | met overlay reviewer (scientific core — MANDATORY) | u-c3-reviewer | read-only (main @ aa1d20a) | none | RUNNING (dispatched 2026-09-02 ~21:25) | null | Dimensions: overlay-dropped-proof completeness; resolved-table %.17g losslessness (round-trip proof); watermark semantics (publish-consume-supersede); applied-overlay accumulation invariants; E2E bitwise independent reproduction; fence through new seam; worker 55 unmodified claim |
| U-C4 | U | landcover overlay into solver/worker/executor (final integration packet) | u-c4 | worktree (isolated, reset to d5d9edc) | solver.py lc overlay path, worker.py lc watermark (mirror u-c3), executor.py _stage_overlays, utci_process lc consumption sites :571-604/:967-979 only, NEW tests/test_incremental_lc_integration.py | RUNNING (dispatched 2026-09-02 ~21:30) | null | Mirror met pattern; read_halo-aware window reads; water-fence E2E; mixed batch lc+veg; bitwise vs physically-edited Landcover twin; off-limits: u-c2 utci sections, u-c3 executor/worker forcing plumbing, met_integration tests |
| U-C2b-verify | U | u-c2b round-2 verdict | u-c2-reviewer | read-only (main @ 6ba2421) | none | APPROVE — U-C2 LINE CLOSED (2026-09-02 ~22:00). All 5 items verified: cyl guards verbatim upstream (all 4 cardinal windows quoted), zero-side 0+b==b exact IEEE754 (only -0.0+0.0→+0.0 invisible to ==/array_equal); np.longdouble empirically NOT float subclass (numpy 2.5.2) refused at gate — author correct, np.float64 coerced; zero-side pins match reviewer's verified table, ewall→lup omission JUSTIFIED (reviewer traced structural coupling ewall→Lwall→gvfLup→Tg→Lup — round-1 'ewall only ldown' was fixture-specific cold-start observation, author's caution improved on it; elvis→lup structurally zero, pin optional); γ₂ derivation SOUND conservative ~2x slack, direction coverage complete incl. synthetic 150°/285° w/ saw_selected gate; npz gate 65/65 + worker 55/55, independent byte-identity experiment rerun clean. New-defect probes: cyl=False/aniso=0/combo all complete, tmrt 29.7-68.7°C plausible, shadow bitwise-invariant, explicit defaults bitwise==default. RESIDUALS 2 TRIVIAL optional: (1) gated del would UnboundLocalError on anisotropic-with-zero-sky-patches — unreachable real patch_option≥145, could drop del entirely like upstream; (2) elvis→lup pin optional | null |
| U-C3-review | U | met overlay verdict (REPLACES dispatch row status) | u-c3-reviewer | read-only (main @ d5d9edc) | none | APPROVE-WITH-NITS (2026-09-02 ~22:40). 0C/0H/1M/4L/3T; every finding empirically reproduced. Independently verified: overlay-dropped-proof grep-complete (solve_window met_table-only :1131, worker.forcing() overlay-aware :204, _run_full_tile_impl RESOLVED staging :1249-1253, server jobs.py:1016-1020 overlay-safe); %.17g losslessness probed 19,990 random bit patterns + denormals + ±1-ulp neighbors + specials ALL round-trip bitwise; superseded-retention probe clean (no stale leak); E2E independent variant humidity col10 t=2 different site/hours BITWISE (utci 3.51/tmrt 2.99 move, shadow bitwise, t<2 bitwise baseline); fence E2E year/wind_dir/diffuse/unknown/1e9/NaN all refused pre-advance; COW probe clean; worker 55 re-run exact | null | M1 [fold into u-c1b, SAME class as U-C1-M1]: worker.py:623 _published_forcing_overlay advances at WORKER publish, executor commits _applied at :697 AFTER _record_outcome — exception between rolls executor back, worker consumed → every retry 'nothing pending' ExecutorError forever, forcing-only AND mixed batches (probe-confirmed; loud not silent). Fix: commit applied overlay from worker's published outcome BEFORE _record_outcome failure modes, or worker rollback hook mirroring _undo_tree_edits. L2 whole-series-vs-per-step fold priority inversion (:910-920 sorts None-time first → OLDER per-step overrides NEWER whole-series, contradicts doc; hand-built path only, adapter never emits None-time); L3 regenerate.py:463 run_scenario_full_tile materializes WITHOUT overlay — silent drop when u-c5 wires, thread overlay= now while cheap; L4 executor-memory-only overlay state, no provenance in ResultPatch metadata, restart reverts met to baseline — U-D persistence MUST serialize; L5 _write_resolved_met header 15 names for 25-col layout (inert skiprows=1); T6 triple materialization deterministic; T7 identical-value resubmit loud 'nothing pending' (U-D ergonomics note: declare old_state); T8 run_full_tile overlay= kwarg path unexercised E2E. REMEDIATION u-c1b scope now: U-C1 M1-M4 + L5-L9 + u-c3 M1 (both watermarks one fix) + u-c3 L2/L3/L5; L4 → U-D intake |
| U-C4 | U | landcover overlay RESULT (REPLACES dispatch row status) | u-c4 | worktree @ 62ebc8c (base d5d9edc, committed clean) | none (never pushed) | PASS — MERGED (2026-09-02 ~23:30) — review PENDING (scientific core solver.py +107). solver.py: resolve_landcover_overlay single materialization seam (COW copy, masked re-check bitwise outside changed-cells, type fence, landcoverless refusal) + solve_window(landcover_overlay=None) overlay-first branch resolves FULL then slices (halo sees paint like oracle) + run_full_tile/_run_full_tile_impl scratch stages RESOLVED raster (float32 exact re-read); None paths verbatim. worker.py: stage_landcover_overlay + _published_landcover_overlay watermark (u-c3 mirror); paint WINDOWED — footprints join dirty windows same merge_windows(gap=0), standard write margin; BUG FIX surfaced+fixed: multi-window local jobs published one patch/window under ONE job id — staging collision pre-existing never exercised; per-window patch ids job-wN, single-window byte-identical old layout. executor.py: _stage_overlays folds over _applied_landcover_overlay (published-accumulation, last-write-wins per cell, fence re-validated, fail-fast pre-advance, commit-on-publish). ZERO consumption edits — utci_process/Tgmaps_v1/solweig.py untouched; overlay materializes pre-tensor-build. E2E: paint patch BITWISE (equal_nan) vs twin-site physically-edited Landcover + fresh cache; mixed paint+tree one plan bitwise vs dual-edited twin; max|ΔUTCI| 1.82, |ΔTmrt| 7.28 grass→asphalt painted cells; shadow bitwise baseline; radiative reach ~10 cells beyond 8x8 footprint CONTAINED by write window (28-60) — margin empirically validated. 20 new + 53 lc + 62 executor/met + worker 55 (47 deselected, scientific 8/8) green; import-origin worktree. Lead post-merge re-verified all above in main | 62ebc8c |
| U-C4-review | U | lc overlay reviewer (scientific core — MANDATORY) | u-c4-reviewer | read-only (main post-merge) | none | RUNNING (dispatched 2026-09-02 ~23:35) | null | Dimensions: dropped-proof all landcover materialization paths; masked re-check exactness; halo slicing vs oracle; worker job-wN patch-id fix (backward-compat readers!); watermark publish/supersede/crash; E2E independent reproduction variant; water-fence E2E end-to-end; COW |
| U-C1b | U | executor/store/worker remediation (U-C1 M1-M4/L5-L9/T10 + u-c3 M1/L2/L3/L5) | u-c1b | worktree (isolated, reset to 0357308) | executor.py, store.py, worker.py, solver.py (header only), regenerate.py (param), planner.py if needed, tests | RUNNING (dispatched 2026-09-02 ~23:45) | null | One wedge fix for all 3 watermarks (_acked_sequence/_published_forcing_overlay/_published_landcover_overlay) via worker rollback hook on executor except-path; M2 now REACHABLE (u-c4 enabled multi-patch) — one store entry per patch window; M3 lead decision = single-writer enforcement loud refusal; M4 best-effort patch-dir cleanup on rollback; u-c3-L3 overlay param threading through regenerate_building_batch for u-c6 |
| U-C4-review | U | lc overlay verdict (REPLACES dispatch row status) | u-c4-reviewer | read-only (main @ 0357308) | none | APPROVE-WITH-NITS (2026-09-02 ~2026-09-01 ~23:59; 2026-09-02). All 9 dimensions probe-verified with independent scripts incl. own 96x96/1.5m dual-paint E2E; author numbers reproduced exactly (|dUTCI| 1.817093, |dTmrt| 7.280243, reach 30..57 ⊂ write 28..60) | null | 1 [HIGH]: u-c1-M2 store [0]-slicing now LIVE — executor.py:1141-1143 merge_windows(...)[0] + :1165 patch_path=records[0][0]; docstring :1126-1131 union claim FALSE for disjoint patches; reviewer's 2-disjoint-patch probe through REAL physics misrecords coverage (entry=first window 10,20 union 10,110 dropped). No in-repo consumer reads entry.write_window today (grep clean) — no current silent-wrong vector; exposure starts now. CONDITION: u-c1b M2 (per-patch store entries) MUST land before any U-D store consumer or escalates REQUEST-CHANGES — already u-c1b-scoped. 2 [MEDIUM known-in-remediation]: lc watermark wedge identical shape (probe: retry same-content → 'double execution' false diagnosis, wedged rev 0, 2 orphan rev-* dirs); u-c1b covers. 3 [MEDIUM later packet]: unbounded overlay/dirty growth — executor :987-999 concatenates paint patches forever, every paint-only job recomputes ALL prior footprints; monotone session cost; fix = per-cell last-write-wins fold at commit or symmetric-difference dirty → u-c6/U-D intake. 4 [LOW]: worker stage_landcover_overlay accepts overlay not covering published footprint (direct-API sharp edge; executor never does; document/guard). 5 [LOW]: solver :1305-1308 baseline copy then :1328-1340 overwrite = wasted copy + partial scratch on resolve failure (worker cleans in finally). 6 [LOW]: scratch Landcover float32 vs site uint8 dtype asymmetry (physics-equal bitwise verified; trap for future consumers). 7 [TRIVIAL]: gvf_march_pixels+1=14 comment wrong (12 at 2.0m). VERIFIED CLEAN: dropped-proof, masked re-check tampers, halo propagation bitwise, patch-id backward compat (no rev-glob parser anywhere, server jobs.py:1063 multi-patch aware), water fence 4 layers + E2E hand-built, COW file-hash full chain, twin fidelity bitwise, accumulation, mixed batches |
| U-C1b | U | remediation RESULT (REPLACES dispatch row status) | u-c1b | worktree @ 9776560 (base 0357308, committed clean) | none (never pushed) | PASS — MERGED a1a4d2f (2026-09-03 ~01:00) — round-2 verification PENDING (u-c1-reviewer + u-c3-reviewer). M1: PublicationWatermarks snapshot + worker.rollback_pending(watermarks); executor _rollback_failed_batch order state→layer-undo→watermarks→orphan-cleanup; DEVIATION ACCEPTED (explicit per-batch snapshot over no-arg last-run — pairing hazard on pre-run failure would re-arm published edits); probe: before/after each of 3 watermark classes, retry PUBLISHES, mutation check (comment out one call → 3 tests fail). M2 (u-c4-review HIGH-1 CONDITION DISCHARGED): one store entry per (variable,timestep,patch window) w/ own path/job-wN/mode; tuple window entries + window_entries/lookup_window; per-(key,window) dup check; coverage proof 2 disjoint paints → 2 patches, between-region lookup None. M3: StoreWriterError + bind_writer at PlanExecutor.__init__ (executor:site:tile:scenario) — 2nd executor fails at construction; all publishes attributed; unbound legacy. M4: discard_published_patches best-effort loud on survivors, called from rollback + worker superseded handler. L5 validate_tree_spec pre-flight pre-mutation; L6 publish lock COW snapshot swap; L7 same-rev same-job diff content → StoreError, identical replay idempotent; L8 PatchMetadata + load_patch_metadata no-array-read; L9 executor superseded full rollback retryable; T10 raise-from + all-rollback-steps-run. u-c3-L2 arrival-order fold (mutation check reproduces review symptom [25,26,25]); u-c3-L3 forcing_overlay threaded regenerate_building_batch→run_scenario_full_tile→load_site_forcing; u-c3-L5 header derived from table, round-trip bitwise. 24 new tests; 217 VERIFY batch + 160 adjacent, zero existing modified; solver.py header-only. Lead post-merge: 68 + 63 green | 9776560 |
| U-C6 | U | building executor wiring + overlay-growth fix (final U-C packet) | u-c6 | worktree (isolated, reset to 574cdc3) | executor.py (wiring + overlay fold), NEW tests; regenerate/buildings read-only consumption | RUNNING (dispatched 2026-09-03 ~01:15) | null | _regenerate_building_chain → regenerate_building_batch w/ forcing_overlay + tree_layer; FULL-only publish through u-c1b discipline; mixed building+lc typed refusal if no seam (never silent drop); overlay-growth per-cell last-write-wins fold at commit + symmetric-difference dirty (u-c4-review M3); E2E bitwise vs independent chain oracle; met accumulation equivalence check |
| U-C1b-verify-c3 | U | u-c3 findings round-2 verdict | u-c3-reviewer | read-only (main @ 574cdc3) | none | ALL FOUR FIXED — U-C3 LINE CLOSED (2026-09-03 ~01:30). M1: rollback-hook option confirmed; probe re-run shows forcing-only AND mixed recovery, retry publishes; reviewer AGREES process-death out of scope for in-process rollback, notes post-crash orphan rev dirs can never collide with retry republish (job-id-embedded names, result.py:270-271) and are logged loudly + non-current-revision consumption documented forbidden. L2: both directions correct ([25,25,25] and [25,25,27] per-step-wins). L3: threading complete, ONLY load_site_forcing call site in regenerate.py carries it; cache-rebuild stages correctly stay baseline. L5: header derives count from table, 15-col verified, round-trip bitwise. Round-1 probes 3 (COW) + 4 (fence) still pass on 574cdc3. 42/42 | null |
| U-C1b-verify | U | u-c1 findings round-2 verdict | u-c1-reviewer | read-only (main @ 574cdc3) | none | APPROVE — U-C1 LINE CLOSED with residuals (2026-09-03 ~01:45). All 10 round-1 findings FIXED (M1 deviation audit: explicit per-batch snapshot reasoning SOUND — no-arg would rewind to PREVIOUS batch's watermarks on pre-run failure, re-arming published overlays; every exit path paired; frozen dataclasses no aliasing). 106/106 four suites | null | RESIDUALS: R1 [MEDIUM] M3 same-identity bypass — writer id site:tile:scenario idempotent for IDENTICAL ids: 2nd executor same scenario allowed; disjoint keys → both publish rev-1 chains silently (what single-writer was meant to prevent). Fix: uuid4().hex[:8] instance token or weakref object check. R2 [MEDIUM] cross-batch window supersession drops valid coverage — store.py:308-313 wholesale tuple replace: rev2 W2-only drops rev1 W1 entries (lookup None, unreferenced dir); reachable via ORDINARY sequential use; silent efficiency/index regression, falls back to recompute, not wrong science; cross-batch analogue of round-1 M2. Fix: retain non-intersecting entries, drop only superseded. MUST land before U-D reader (U-D must also define lookup_window None semantics — recompute fallback vs error). R3 [LOW] node_entries TOCTOU double-read (code-certain, not reproduced in 5000-iter hammer). R4 [LOW] non-supersede exception mid-patch-loop skips orphan cleanup (worker.py:829-836 finally-style discard). Lock reentrancy probe: NONE. Both MEDIUMs small well-bounded → u-c1c QUEUED behind u-c6 (executor/worker overlap) |
| U-C6 | U | building wiring + overlay growth RESULT (REPLACES dispatch row status) | u-c6 | worktree @ de79deb (base 574cdc3, committed clean) | none (never pushed) | PASS — MERGED f80782f (2026-09-03 ~02:40) — review PENDING (executor +408, drives full-tile chain). EXECUTABLE_SOURCE_NODES += building_dsm; _prepare_building_batch (massing_edits_from_deltas seam re-validates, bypass refused pre-raster); _regenerate_building_chain → regenerate_building_batch under per-instance results/<scenario>/.building carrying tree_layer + forcing_overlay + LANDCOVER SEAM THREADED (run_full_tile landcover_overlay= — no typed refusal needed, paint never dropped); scenario-manifest provenance (≠ baseline, no misattribution); two supersession checkpoints; RegenerationError → chained ExecutorError; worker adopt_published_state (monotone acked, later batches stay windowed) + stage_landcover_overlay explicit-footprint dirty_windows; rollback wipes .building then u-c1b rewind. PART 2: coalesce_overlay per-cell last-write-wins AT COMMIT w/ maximal-rectangle decomposition (O(edited area) not O(strokes)); overlay_diff_windows symmetric-difference dirty; same-value repaint = clean no-op interception pre-state-advance (compared vs EXECUTOR applied state, wedge tests still hit loud path); forcing records replaced-not-appended (bounded, test-pinned). E2E 48x48 real SVF: building batch bitwise (NaN-aware utci, strict tmrt/shadow) vs independent chain oracle; met-then-building provably reflects BOTH (equals overlay oracle, differs from no-overlay); baseline hashes unchanged; mid-chain SVF failure → chained error naming stages, full rewind, retry succeeds; disclosures surface. Deviations accepted: baseline-equal cells stay in canonical overlay (no-op moved to diff+interception, existing seam test semantics preserved); adoption routing assertion moved 128x128 (48x48 tree influence covers whole 96m tile — genuine FULL, fixture artifact). 196 fast + 13 scientific green; import-origin clean; scientific core untouched. REMAINING U-C: params executor fold (u-c7), selected_date_time (U-D), dem (L1 deferred); GPU-path differential open. Lead post-merge: 69 + 85 green | de79deb |
| U-C7 | U | params executor fold (completes 5-family executable set) | u-c7 | worktree (isolated, reset to 877841e) | solver.py model_parameters forwarding, worker.py payload/watermark, executor.py _physics_kernel_arguments, NEW tests | RUNNING (dispatched 2026-09-03 ~02:50) | null | kernel_arguments_from_deltas folded over accumulated applied params (time-invariant, last-write-wins per name); npz gate must stay green; mixed params+building gap reported if unfound; blocked params refused end-to-end |
| U-C6-review | U | building wiring + overlay coalescing reviewer (MANDATORY — drives full-tile chain) | u-c6-reviewer | read-only (main @ 877841e) | none | RUNNING (dispatched 2026-09-03 ~02:50) | null | Dimensions: chain provenance (scenario vs baseline manifest); FULL-only discipline mixed batches; adopt_published_state watermark interplay w/ u-c1b snapshot; coalesce==raw-fold bitwise on adversarial paints; no-op interception vs wedge tests; scratch lifetime/disk growth; independent E2E reproduction; mutation probes |
| U-C6-review | U | building wiring verdict (REPLACES dispatch row status) | u-c6-reviewer | read-only (main @ 877841e) | none | REQUEST-CHANGES (2026-09-03 ~03:40). 0C/1H/1M/3L/1T. Verified clean first: coalesce==raw-fold bitwise 6 shapes + 200 random histories; own E2E variant (delete+add 9m) bitwise vs oracle, baseline hashes self-checked; building+paint REAL physics bitwise w/ overlay riding; bypass/mid-chain-both-checkpoints/post-publish mutation probes all loud+clean; wedge discipline intact; watermarks monotone; provenance no-misattribution; scratch bounded | null | H1 (3 repros, all publish @574cdc3, all fail @877841e): symmetric-diff dirty breaks plan/scope agreement — (a) same-value repaint → diff empty → staged!=published → worker no-op → ExecutorError 'nothing pending' misleading, batch unexecutable; (b) first paint partially over already-target cells → write windows diff+margin ⊅ plan stroke-footprint → _reconcile_scope '4 plan windows not covered'; (c) mixed veg+same-value repaint → whole batch incl. veg refused. ROOT: plan promises STROKE footprint, u-c6 dirties value-symdiff; accepted deviation (baseline-equal cells stay in overlay) defeats equality at painted-set boundary. M2 coalesce equality claim false at painted-set boundary (resolve-equal ≠ fold-equal when sets differ) — docstring property restates + boundary test. L3 no-op error text conflates; L4 '.building per executor instance' docstring wrong (per-SCENARIO — 2 same-scenario executors w/ separate stores rmtree each other, pre-existing exposure undocumented); L5 3 full-grid copies+resolves per batch — hoist; T6 double requested_variables read. OPEN pre-existing: met same-value analog (mixed veg+value-equal met, LOCAL route → 'plan marks FULL but worker published local' — u-c3-era). LEAD RULING for u-c6b: dirty must be SUPERSET of plan promise — staged lc dirty = STROKE windows (∪ diff), NOT value-diff; value-equality no-op interception ONLY whole-batch (accurate text distinguishing values-already-published); met analog: any real deltas + value-equal fold → treat as pending (dirty closure by PRESENCE not value). Growth fix's win stays: accumulation still O(edited-area), prior footprints stay out of current dirty. QUEUED behind u-c7 (executor.py overlap) |
| U-C7 | U | params fold RESULT (REPLACES dispatch row status) — 5/5 FAMILIES EXECUTABLE | u-c7 | worktree @ 64c1551 (base 877841e, committed clean) | none (never pushed) | PASS — MERGED 42fd23d (2026-09-03 ~04:20) — review PENDING (solver.py +177 incl. compute_utci replication). solver: solve_window model_parameters verbatim forward; run_full_tile None/{} legacy untouched, non-empty → _run_full_tile_with_model_parameters = line-for-line compute_utci core replication over SAME scratch (same loads/cleaning/clamps/vegdem exprs/fresh svf_calculator save_rasters=False), params from load_site_forcing (oracle-derivation + manifest validation); honors kup/kdown/lup/ldown requested_variables (legacy read-back never did). worker: PublicationWatermarks.model_parameters 4th watermark (stage/consume/supersede-keeps/rollback-clears/adopt); stage_model_parameters fences names ⊆ PLUMBING_CLASSIFICATION, float-kind→double coercion, freezes copy. executor: _physics_kernel_arguments batch-over-accumulated arrival-order fold, kernel_arguments_from_deltas re-validates fail-fast, commit-on-publish, threaded into building chain + adopt. E2E 64x64 DOY172: executor patch == direct run_full_tile(params) BITWISE all bands; decoupling table through executor (albedo_b: utci/tmrt/kup/kdown move, lup/ldown/shadow bitwise); identity override == legacy compute_utci bitwise (replication-exact proof); 2-batch accumulation bitwise each; params+met dual-edit oracle bitwise; patch_option refused end-to-end; rollback clean. 21 new + 44 executor + 133 suite + 73 worker + 70 adjacent green. Deviation: direct-path replication instead of utci_process seam (off-limits) — pinned bitwise vs legacy | 64c1551 |
| U-C7-review | U | params fold reviewer (scientific core — MANDATORY) | u-c7-reviewer | read-only (main @ 42fd23d) | none | RUNNING (dispatched 2026-09-03 ~04:25) | null | Dimensions: replication drift risk (line-for-line copy of compute_utci core — future edits diverge silently; compare every expression); watermark 4th-field interplay; staging fences; E2E reproduction variant; requested_variables superset semantics; npz gate |
| U-C6b | U | footprint scope-agreement remediation (u-c6-review H1 + met analog + M2/L3/L4/L5/T6) | u-c6b | worktree (isolated, reset to 09bf43b) | executor.py, worker.py, adapters/landcover.py, tests | RUNNING (dispatched 2026-09-03 ~04:35) | null | LEAD RULING: lc dirty = STROKE windows ∪ diff (superset of plan promise; prior footprints still excluded — growth win preserved); no-op interception whole-batch only w/ accurate text; met analog presence-not-value pending in mixed batches; audit params 4th watermark same class; repro-first (3 reviewer repros as failing tests) |
| U-C7-review | U | params fold verdict (REPLACES dispatch row status) | u-c7-reviewer | read-only (main @ 09bf43b) | none | APPROVE-WITH-NITS (2026-09-03 ~05:30). 0C/0H/2M/3L/2T. Legacy branch byte-untouched (insertions only); npz gate green; independent E2E repro own fixture 48x48 DOY-250 Fside+transVeg through executor == direct BITWISE; identity payload == legacy compute_utci BITWISE; accumulation bitwise. NOTE: transVeg legitimately couples shadow band (shadow = sh-(1-vegsh)(1-psi), solweig.py:2093) — decoupling table must not extend to it. Watermark 4th field symmetric everywhere, keyword constructions both sites | null | M1: value-domain fence NOT mirrored at new doors — worker stage_model_parameters + solver run_full_tile check names+scalar only; {albedo_b:True}/{albedo_b:50.0}/{transVeg:inf} staged+forwarded (executor path protected by adapter re-validate; direct-Python callers exposed). Fix: _check_value_in_domain in stage_model_parameters. M2: drift pin single-point (one fixture DOY-172 one payload firstdayleaf=150, SCI vars only; kup/kdown/lup/ldown no legacy pin — legacy can't produce). Fix: parametrize identity test fixtures x payloads (cyl/height/aniso/all-defaults). L1: requested-variable asymmetry legacy-silent-drop vs direct-raises (latent, defaults safe); L2: solve_window missing Mapping fence run_full_tile got; L3: value-equal params repaint in mixed batch runs FULL (planner scopes params SpatialScope.FULL always — no wedge, nothing dropped, probed E2E; planner-owned semantics, note for u-c6b generalization/U-D); T1 misleading 'must be a scalar' for np.float32/bool_/int64; T2 publish-by-reference invariant implicit. REMEDIATION u-c7b QUEUED behind u-c6b (worker.py overlap): M1+M2+L2+T1; L1 align or document |
| U-C6b | U | footprint remediation RESULT (REPLACES dispatch row status) | u-c6b | worktree @ 2fd9d4e (base 09bf43b, committed clean) | none (never pushed) | PASS — MERGED 76a6aea (2026-09-03 ~07:15) — round-2 PENDING (u-c6-reviewer). Repro-first: 8/15 new repro tests failed pre-fix incl. all 3 reviewer repros; post 15/15. (a) same-value repaint → clean whole-batch no-op (status 'no-op', rev 0, zero jobs, reason 'already matched the published scenario state', no wedge-suspect text); (b) partial-over-target stroke publishes (dirty = stroke ∪ diff superset); (c) mixed veg+same-value publishes w/ stroke in footprint. Presence-vs-value per family: LC wedged+fixed (stroke∪diff, prior footprints excluded — O(stroke) pinned, 5 repaints → 1 canonical patch); MET rule applied gated on MIXED batches (force_full already rescued at HEAD empirically; forcing-only keeps value-based pending → u-c1b wedge tests still hit loud path, verified :288); PARAMS no wedge exists (dict value-fold == no fold + full-spatial plan rescue, pinned). M2 docstring same-painted-set property + boundary test; L4 .building per-SCENARIO docstring; L5 single baseline copy + staged resolve + publication-time resolve memo (self-found bug during hoist: landcoverless-site refusal broke — fixed, contract intact); T6 hoist. 209 passed 7:02 one run (15 new + 12 growth + 55 worker + 24 uc1b + 44 executor + 20 lc + 18 met + 21 params); import-origin worktree. Lead post-merge: 71 + 83 green | 2fd9d4e |
| U-C8 | U | combined residual sweep (u-c7-review M1/M2/L1/L2/T1/T2 + u-c1-review R1-R4) | u-c8 | worktree (isolated, reset to 6098ceb) | worker.py, store.py, executor.py (writer token), solver.py (fence only), adapters/model_parameters.py (PUBLIC re-export only), tests | RUNNING (dispatched 2026-09-03 ~07:30) | null | Domain fence both doors (public check_parameter_value, no privatize-dip); parametrized drift pin fixtures x identity payloads; R1 uuid instance token; R2 retain non-intersecting entries; R3 snapshot-once; R4 finally-style discard; legacy requested-variable loud alignment |
| U-C6b-verify | U | u-c6b round-2 verdict | u-c6-reviewer | read-only (main @ 6098ceb) | none | REQUEST-CHANGES — 1 HIGH residual (2026-09-03 ~08:15). Everything else verified fixed w/ own probes (H1a/b both variants, growth O(stroke) genuinely pinned, huge/edge strokes clamp-merge clean, M2/L3/L4/L5 memo-consistency-through-rollback, met presence rule both directions, 400 passed 5 files) | null | F1 [HIGH] worker.py:969 stroke-union-dirty joins only under `if landcover_pending:` (painted-set+value equality) — repaint-of-ALREADY-PUBLISHED cells in mixed veg+lc batch: executor dirty correct (stroke), worker DROPS it → publishes veg windows only → _reconcile_scope refuses, retry loops (probe ×2 + post-fail-retry cycle). u-c6b's repro-(c) pins only FIRST-EVER variant (painted set grows → gate True). Fix: `if landcover_pending or self._landcover_dirty_override:` + repaint-of-published mixed repro test. F2 [LOW] stale test slice [60:68,20:28] vs ASPHALT_WINDOW 100..108/10..18 — passes but pins unrelated region. BOTH FOLDED INTO u-c8 SCOPE ADDITION (already in worker.py worktree @6098ceb, delivered via SendMessage) |
| U-C8 | U | residual sweep RESULT (REPLACES dispatch row status) | u-c8 | worktree @ 478169b (base 6098ceb, committed clean) | none (never pushed) | PASS — MERGED 4c114b2 (2026-09-03 ~09:30) — round-2 PENDING (3 reviewers). F1 worker.py:969 join `landcover_pending or self._landcover_dirty_override:` + repaint-of-published mixed repro (failed w/ reviewer's exact error pre-fix); F2 slices from ASPHALT_WINDOW. Item1 domain fence BOTH doors via PUBLIC check_parameter_value wrapper (adapters __all__, no privatize-dip; WorkerError/SolverInputError from EditStateError; True/50.0/inf refused both doors); Item2 parametrized drift pin 2 fixtures (64x64 DOY-172 + NEW 48x48 DOY-300) x 4 identity payloads (cyl/height/aniso/{}) bitwise; Item3 solve_window Mapping fence; Item4 DEVIATION ACCEPTED: legacy requested-variable fence at worker _solve_full not solver (solver-level broke _direct_run oracle helper's both-branch 7-var requests; solver comment documents permissive oracle door, worker = production door); Item5 message names kinds + .item() fix; Item6 publish copies; R1 writer id +uuid4 hex8 token, 2nd same-scenario executor StoreWriterError at construction; R2 store discipline vs max(entry.scene_revision), retain non-intersecting via write_window.intersects window-sorted union, reviewer scenario pinned + partial/full-overlap/stale-max/replay-keeps-retained; R3 snapshot-once + 5000x hammer; R4 BaseException discard accumulated patch_paths + survivors log + discard_staging + re-raise, OSError-injection probe clean retry. TDD: revert-to-base 11 tests failed → restore green. 289 total verification set; import-origin clean. Lead post-merge: 81 + 88 green | 478169b |
| U-C8-verify-c6 | U | F1/F2 round-3 verdict | u-c6-reviewer | read-only (main @ d70316f) | none | APPROVE — U-C6 LINE CLOSED (2026-09-03 ~10:30). F1: worker.py:1000 join w/ rationale comment; original probe PUBLISHES (rev 2, one job, write windows stroke∪veg margins); repro test correct composition. F2 slices from ASPHALT_WINDOW w/ comments. New-defect probes CLEAN: stale non-empty override + overlay-equal state between batches exists BUT next batch's unconditional restage prevents leak (veg-only follow-up published veg windows only); no-paint batches unaffected; first-ever + mixed variants unchanged; u-c8 executor diff confined to writer token | null | Residuals 2 TRIVIAL: (1) worker holds non-empty dirty override + overlay-equal state after intercepted no-op — correctness by executor-convention restage not worker-enforced; direct worker.run() in that state no-ops safely (pending-gate before join); one-line docstring note would pin; (2) no-op reason text 'no pending edits' reads fine nested. 69 passed 4 files |
| U-C8-verify-c1 | U | R1-R4 round-3 verdict | u-c1-reviewer | read-only (main @ d70316f) | none | APPROVE — U-C1 LINE CLOSED (2026-09-03 ~10:45). All 4 probe-verified with original round-2 topologies: R1 2nd same-scenario executor StoreWriterError at construction, no consumer parses writer-id format anywhere (grep); R2 retention scenario exact (both entries retained, per-region lookup right patch, coverage_gaps (), stale-vs-max + replay-verbatim + wholesale pinned); R3 snapshot-once, available_times/max_revision single-read COW-safe; R4 BaseException discard w/ per-survivor log + bare re-raise (original exception preserved, T10 pairing intact; KeyboardInterrupt/SystemExit cleanup-then-propagate correct), OSError probe: retry publishes BOTH windows; invalidate key-level granularity matches doc | null | RESIDUALS → U-D INTAKE: R2a [LOW efficiency-only] whole-entry retention granularity — partial overlap drops entire old window, sliver cell (e.g. (0,0) after W1(0,10)+rev2(5,15)) loses coverage → reader recomputes (safe fallback, silent waste); fix-if-wanted = split retained window vs new batch windows, else U-D reader documents recompute fallback explicitly. revision_at returns FIRST window entry's revision (can be older than max on retained tuple) — U-D 'latest revision' readers must use max_revision(node). 89 passed |
| U-C8-verify-c7 | U | u-c7 items round-2 verdict | u-c7-reviewer | read-only (main @ d70316f) | none | APPROVE — U-C7 LINE CLOSED (2026-09-03 ~11:00). M1 probes re-run live BOTH doors (bool/50.0/inf/nan/cyl:1 refused typed); M2 drift matrix SUFFICIENT — fixtures cover BOTH leaf regimes (DOY-172 leaf-on, DOY-300 leaf-OFF psi=0.5), every .get default covered indirectly (misspelled default shifts every direct-vs-legacy cell); observations: {} cell pins legacy determinism not replication (real pins = other 3), flux vars unpinnable vs legacy by construction (npz covers at run_utci_window level); L2 fixed; L1 deviation ACCEPTED — worker door verified ONLY production route (server SUPPORTED_VARIABLES filter patch_codec.py:58/jobs.py:329-337, executor _split_variables :1894); T1/T2 fixed w/ in-place-mutation test | null | RESIDUAL → U-D intake: building chain reads worker.requested_variables DIRECTLY (executor.py:1584 → regenerate_building_batch → run_full_tile) bypassing _solve_full fence — hand-assigned kup + building batch + no params silently drops kup (direct-API only; one-line fence if U-D widens variable set) |
| U-C-T3 | U | differential sweep: 5 families through executor vs oracles | uc-t3-runner | read-only (main @ 5d2df4d) + /tmp + benchmarks/2026-09-03-uc-scientific.json | none (never commits) | PASS 9/9 — RUN COMPLETE (2026-09-03 ~01:20) | null (evidence file untracked, lead commits) | Executed at e607bfa (code diff 5d2df4d..HEAD empty; solweig_gpu/tests clean — code identical to dispatch). SANITY fresh-vs-cached bitwise 147s; U1 veg local 1-window rev1 vs addA full-tile twin 55s; U2 met full rev1 vs physically-edited-met twin 282s (pre-edit physics bitwise baseline); U3 lc local rev1 vs painted-raster twin 314s; U4 params full rev1 vs direct run_full_tile(albedo_b=0.35) 416s (utci/tmrt change, shadow unchanged); U5 building 7-stage regeneration chain rev1 vs independent regenerate_building_batch 502s (baseline site+cache byte-identical after run; disclosures channel symmetric-empty correct for clean block); U6 mixed ONE batch veg+met+lc rev1 one job vs triple twin 317s; U7 accumulation met(rev1)→paint(rev2 local)→params(rev3) vs chained twins 496s; U8 injected store.publish failure → rollback (rev 0, overlay unwound, no orphan rev dirs, watermarks rewound) → retry rev1 bitwise twin 294s. ALL bitwise equal_nan, tolerance never relaxed. Anomalies 6 informational: A1 lc scenarios on derived lr site (real tile + real Input_rasters/Landcover.tif crop; repo cache has no landcover array; all other arrays bitwise repo cache); A2 twins recompute SVF fresh (SANITY proves bitwise stability); A3 U7 store index carries only rev3 after final FULL publish = R2 intersect-supersede semantics, NOT data loss (rev1-3 patch dirs all load_patch-readable) → U-D intake reader contract; A4 runner harness globbed rev-* one level shallow (patches nest results/<id>/<id>/rev-*) → 2 false fails first pass, harness fixed, U7/U8 rerun clean from empty publication state; A5 no met-only LR twin (covered via repo-site rmet twin + in-batch bitwise checks); A6 code-state pinned. Total 2822s. GPU-path differential remains OPEN (CPU only) |
| U-C-CLOSE | U | wave closure (lead adjudication) | lead | main tree | none | CLOSED (2026-09-03 ~01:30) | (this commit) | U-C COMPLETE: all 5 edit families (vegetation, meteorology, landcover, building_dsm, model_parameters) executable through PlanExecutor with bitwise E2E differentials (packet tests) + real-site T3 sweep 9/9 PASS. Review lines ALL closed: u-c1 APPROVE r3, u-c2 APPROVE r2, u-c2b folded u-c2, u-c3 APPROVE r2, u-c4 APPROVE-WITH-NITS (HIGH-1 discharged u-c1b+u-c8), u-c5 (building chain) folded u-c6, u-c6 APPROVE r3, u-c6b folded u-c6/u-c8, u-c7 APPROVE r2, u-c8 APPROVE. NOT_INTEGRATED remaining: selected_date_time (U-D adapter) + dem (L1) with typed refusals. Full python suite at final U-C state: 843 passed / 4 skipped / 1 deselected (austin pre-existing origin/main@4c5e47d) 643.62s. U-D intake list transferred to WORKLOG checkpoint. Next: U-D |
| U-D0 | U | residual sweep: store reader contract (R2a sliver fallback + revision semantics), old_state no-op refusal, worker L4 footprint guard, solver L5/L6 scratch, T7 comment, h docstrings, i citations; f SKIPPED (recorded) | u-d0-residuals | worktree from 68e612f | incremental/{store,worker,solver,edit_types}.py, adapters/*.py, tests/test_incremental_* | RUNNING (dispatched 2026-09-03 ~01:45) | null | scientific-core → independent review after merge; forbidden: executor/server/__init__/docs/benchmarks |
| U-D1 | U | capability API (registry-driven, no hardcoded lists, /capabilities + view-only zero-jobs UEDIT-007), scenario state persistence (bitwise resume, no re-arm hazard), c fence executor.py:1584, g server site-identity pinning | u-d1-capability | worktree from 68e612f | incremental/{executor,capabilities,edit_registry-metadata-only}.py, server/**, __init__ append, tests | RUNNING (dispatched 2026-09-03 ~01:45) | null | forbidden: store/worker/solver/edit_types/adapters (parallel u-d0 owns); API contract sketch owed for u-d2 frontend |
| U-D3 | U | UEDIT-008 per-family perf evidence (latency p50/p95 + peak RSS through PlanExecutor, real 500x500 site, T3 payload patterns) + UEDIT-007 view-only zero-jobs proof | u-d3-perf | read-only main @ 68e612f + /tmp + benchmarks/2026-09-03-ud-perf.json | none (never commits; lead commits evidence) | RUNNING (dispatched 2026-09-03 ~01:45) | null | measurement-only, never fixes; code-state pinned; void-run discard; incremental /tmp/ud3/parts/ |
| U-D0 | U | residual sweep RESULT (REPLACES dispatch row status) | u-d0-residuals | worktree @ 4 commits (base 68e612f, clean) | (owned files only, verified) | PASS — MERGED 271accf (2026-09-03 ~03:20); independent review RUNNING (u-d0-reviewer) | b2f44bc | (b) store window_coverage FULL/PARTIAL/ABSENT + deterministic missing-rectangles + revision_at→MAX published (20 callers uniform-revision, semantics unchanged); (d) identical_value_clause + per-family typed refusals w/ old_state, landcover whole-batch ruling preserved; (e) L4 guard_patch_window/guard_read_covers_write wired run/_solve_local/_solve_full, L5 vet-before-scratch + skip baseline-Landcover copy when staged at path (legacy copy2 byte-identical), L6 documented (shadow.py float32 outside ownership), T7 14→12; (h) restage-by-convention docstring + evidence-based no-op diagnostics, window derivation byte-for-byte unchanged; (i) 34+ comment-only citation fixes (kept live-message-text quotes; edit_registry/executor stale citations LEFT — forbidden files, → u-d1/u-d4); (f) SKIPPED recorded. 21 new tests; incremental suite 671 green worktree; lead main-tree verify 181 core tests green 383.95s. CoverageStatus/WindowCoverage NOT in package __init__ (forbidden) — fold pending. edit_registry.py:20/128/176/455/567/780 + executor.py:80/350 stale utci_process citations → handed to u-d1 | b2f44bc |
| U-D0-review | U | code-review (non-author, scientific core) | u-d0-reviewer | read-only main @ 271accf | none | APPROVE-WITH-NITS (2026-09-03 ~04:10) — LINE CLOSED | null | All 5 deliverables verified vs diff: window_coverage brute-forced (3000 random masks exact/disjoint/deterministic tiling; 1500 random stores status+areas+slivers match; snapshot-once + wholesale map swap = no mutation hazard); revision_at max-semantics safe (ZERO production callers, all 20 = test assertions, each verified); refusals message-only (no new equality conditions; both-None attr edge unreachable; model_parameters before[name] sound on empty changes; landcover whole-batch ruling directly probed); guards cannot false-positive (clamp precedes; WorkerError typed; raise lands in BaseException handler w/ staging+orphan discard); L5 success-path bytes identical all 3 paths (skipped copy2 always overwritten; _SCRATCH_DIRS still creates dir; only failure-path scratch state differs = intended); citations spot-checked 12+ exact; kept quotes correctly live-text. Tests non-vacuous via in-memory mutation (clause/guards/coverage/revision mutations each fail tests). 488 passed affected surface. NITS: L1 store docstring ABSENT edge-touch wording (FIXED by lead post-review); L2 worker.py:1398 _solve_full guard decorative placement (harmless, LEFT — never fireable, grid-derived window); L3 edit_registry stale citations (ALREADY in u-d1 addendum) |
| U-D1 | U | capability API + persistence RESULT (REPLACES dispatch row status) | u-d1-capability | worktree @ 257b57c (base 68e612f + feat merges 93074b7/a3e8f69 folded, clean) | executor.py, edit_registry.py, capabilities.py (new), scenario_state.py (new), server/{app,models,routes_capabilities,routes_scenarios,store}.py, __init__ append, 5 test files | PASS — MERGED 5a694ee (2026-09-03 ~06:00); independent review RUNNING (u-d1-reviewer). DEVIATION recorded: server/store.py edited beyond declared ownership (migration v3 site identity — needed the schema; folded into review scope w/ extra scrutiny) | 257b57c | capabilities.py DERIVED document (registry metadata + adapter spec tables + graph closures + EXECUTABLE_SOURCE_NODES, zero duplication; drift-guard metadata-only→integrated=False); GET /capabilities (+alias) + cached doc; POST /scenarios/{id}/views view-only ops (select_layer/compare/legend/cached_time, 400/404/409 typed, job_enqueued:false) w/ UEDIT-007 zero-jobs test (ledger count + FakeSolver.calls unchanged). scenario_state.py: snapshot pairing assertion (watermarks == _applied_*), npy-first/JSON-marker-last atomic write, identity verify, atomic tree-log replay, adopt+restage staged==published (no re-arm — the u-c1b hazard, author-found bug: fresh worker else materializes baseline forcing), resolve-memo shape+dtype reload, store index ascending replay w/ staleness/R2/bind_writer re-arm, positional acked mapping surviving seq gaps; scientific test mixed-family restart bitwise + subsequent batch bitwise. Fences: _BUILDING_CHAIN_VARIABLES from regenerate_building_batch signature (widening widens fence), raises before chain work; site identity store v3 (origin_x/y blind spot closed, lazy NULL pin, numeric-tolerant, 409 w/ pinned+current) in commit+reset+create. Citation sweep in owned files + test regex sync (deviation accepted); CoverageStatus/WindowCoverage exports. 890 passed/4 skipped on branch (env-only failures proven pre-existing via stash); lead main-tree verify 78 fast green, scientific suite in flight | 257b57c |
| U-D2 | U | registry-driven frontend: capability-document editors (zero hardcoded physics lists), UEDIT-009 changed/reused/recomputed stage exposure, view-only panel (UEDIT-007 product story), 409 site-identity handling, node tests w/ fixture capabilities doc | u-d2-frontend | worktree from c254349 | examples/incremental_design_tool/** ONLY | RUNNING (dispatched 2026-09-03 ~06:15) | null | forbidden solweig_gpu/** (server under u-d1 review); UEDIT-009 server gap → report, never patch; dynamic_wind disclosure surfaced (UEDIT-010 frontend side) |
| U-D1-review | U | code-review (non-author, scientific core) | u-d1-reviewer | read-only main @ 5a694ee | none | REQUEST-CHANGES (2026-09-03 ~07:20) — HIGH → remediation u-d1b RUNNING | null | HIGH: write_snapshot RE-save crash window (memo os.replace then JSON marker; crash between = JSON v1 + memo v2 loads — read_snapshot checks existence/shape/dtype only, no memo↔overlay correspondence) — reviewer DEMONSTRATED end-to-end silent divergence (restored ckpt1-overlay + ckpt2-memo; re-submitted paint → wrong resolved_old → empty value-diff → whole-batch no-op interception while clean twin PUBLISHES); repro /tmp/ud1_mutation/mutation_memo_crash.py. All else VERIFIED: capabilities derivation genuine (spec tables/graph closures/EXECUTABLE_SOURCE_NODES; drift test), UEDIT-007 zero-jobs by construction (no runner/submit in routes; dual-assert test), restore discipline (stage-SAME arms nothing — falsy dirty_windows never fires landcover join; acked mapping 1:1 order-isomorphic; pre-execute steady-state assertions load-bearing b/c per-dispatch restage would mask), building fence sig-derived + raises pre-chain, site identity (migration idempotent/transactional, lazy NULL pin, no-epsilon numeric compare catches 1px, commit+reset+create), store.py hunks confined. 141 regression green. NITS: (a) trunk_ratio bounds hand-restated, (b) operation Literal mirror no drift test, (c) _IDENTITY_FIELDS omits time_steps, (d) raw KeyError read_snapshot, (e) write txn on pure-verify, (f) no rate-limit + legend full decode [→u-d4], (g) results_root unpinned, (h) pending-at-save edits [by construction]. (a)-(e)+(g) → u-d1b |
| U-D2 | U | registry-driven frontend RESULT (REPLACES dispatch row status) | u-d2-frontend | worktree @ 4 commits (base c254349, clean) | examples/incremental_design_tool/** only (verified) | PASS — MERGED 380cbd6 (2026-09-03 ~08:20); frontend review RUNNING (u-d2-reviewer) | e08ec5e | Capability document drives entire editor surface (property controls from schema, class picker valid−fenced, rejected props w/ reasons, dynamic_wind disclosure = UEDIT-010 frontend side); offline snapshot = verbatim dump, connected NEVER falls back (typed capability_document_unavailable, zero editors, stated). UEDIT-009 stages DERIVED: changed=family source nodes, recomputed=rest of dirty closure, reused=other nodes + realized-job scope lines (no-op = no-recompute). View-only panel w/ zero-jobs badge quoting server flags; 409 mismatch banner blocks commits, local reset refusal, views stay live. Tests 61→102 green worktree + main. ZERO hardcoded physics lists (only presentation labels/unit wiring/transport table). TWO SERVER GAPS REPORTED → U-D4: (1) no endpoint exposes executed plan (per-node impacts live only in PlanExecutor); (2) POST /edits EditRequest tree-only — 4 families lack HTTP transport; family_edits.mjs raises FamilyTransportError w/ ready universal payload {family, adapter, operation, values, target?, time_index?} — auto-ready when endpoint lands | e08ec5e |
| U-D2-review | U | frontend review (non-author) | u-d2-reviewer | read-only main @ 380cbd6 | none | APPROVE-WITH-NITS (2026-09-03 ~09:10) — LINE CLOSED pending u-d2b | null | Claims 1-7 ALL VERIFIED: zero hardcoded physics (grep clean; only STATUS/VIEW labels + unit wiring + transport table); snapshot==live document (structural walk 0 diffs, py-object equality True); no connected fallback (typed error, zero editors); UEDIT-009 derivation honest (reused∩closure=∅ by set arithmetic; reviewer independently recomputed downstream closures veg 10/met 8 nodes == document); view panel clean (badge quotes server flags only; createView bypasses mutation queue — test asserts 0 editPosts + sceneVersion unchanged); mismatch blocks commits w/ zero POST; FamilyTransportError payload cross-checked family↔source_nodes↔adapter all 4; tests 61→102 verified by per-file counts, FakeContractServer move verbatim (3 reformatted lines), non-vacuity spot-checks pass. FINDINGS → u-d2b RUNNING: MED retryLast bypasses siteMismatch (exact_session.mjs:505-519, guaranteed-409 POST, reviewer insists pre-release); L1 reset wipes local design pre-refusal; L2 stale impact panel on sourceNodes-less jobs; L3 payload validation gaps (unknown props + class-vs-fenced unvalidated; misleading test title :154); nits (a) restated defaults drift-prone (b) unused duplicate viewRequestBody (c) schema_version 0/negative passes. XSS sweep clean (textContent only); race sweep clean (capabilities load announces vocab only) |
| U-D1b | U | remediation (u-d1-review HIGH + nits a-e,g) | u-d1b-remediation | worktree @ 4 commits (base c254349, clean) | incremental/{scenario_state,capabilities,trees}.py, server/store.py, 4 test files | PASS — MERGED 202b1b9 (2026-09-03 ~10:30); round-2 RUNNING (u-d1-reviewer) | c3a546a | HIGH: sha256-of-memo recorded in JSON, verified at read_snapshot AND restore-time np.load (memo-may-change-after-read), hash-less-with-memo refused, schema v2 (v1 refused unverifiable); regression test mirrors reviewer repro + clean-checkpoint control (re-submitted paint PUBLISHES), stash-verified DID-NOT-RAISE on unfixed code; honest-re-save round-trip + memo-swapped-after-read tests. (a) TRUNK_RATIO_RANGE in trees.py:55 (validator owns bound; capabilities imports — out-of-list file FLAGGED, justified); (b) 3-way Literal pin; (c) time_steps in _IDENTITY_FIELDS w/ legacy None-skip; (d) typed require() for all keys; (e) read-only verify fast path, write txn only lazy NULL pin (spy test); (g) results_root pinned, divergence REFUSED both-roots-named + Path.resolve (refuse-over-relocate: silent re-point = the guarded corruption class). 50/50 assigned + 890 non-scientific on branch; lead main-tree verify 64 passed (incl. trees 14). (f)(h) untouched | c3a546a |
| U-D1-review-r2 | U | round-2 verification (non-author) | u-d1-reviewer | read-only main @ 202b1b9 | none | APPROVE-WITH-NITS — U-D1 LINE CLOSED (2026-09-03 ~11:15) | null | HIGH VERIFIABLY CLOSED: reviewer re-ran UNMODIFIED round-1 repro vs merged HEAD — exact torn dir (JSON v1 + memo v2) that loaded silently now ends in typed ScenarioStateError pair-refusal before any executor touched. Hash computed from file RE-READ after os.replace (bytes-on-disk, np.save nondeterminism cannot false-refuse); dual verify sites (read_snapshot pre-restore + restore-time np.load TOCTOU); inverse case refused at write; schema v2 strands nothing (zero in-tree callers re-grepped). trees.py placement SOUND (beside sibling constants, consumed by owning validator, f-string renders IDENTICAL old message). (b)(c)(e)(g) all verified incl. existing pins already recorded time_steps (uncompared before — no honest pin stranded), WAL-consistent read fast path w/ convergent race, refuse-over-relocate judged CORRECT. 60 tests affected files. NITS non-blocking → U-D4/future: (1) moved-deployment needs deliberate operator rebase path when server lifecycle wiring lands (results_root field has no integrity hash — hand-edit today, loud+actionable); (2) micro-TOCTOU read-once np.load(io.BytesIO) theoretical close; (3) fast-path verify read instantly-stale vs concurrent mutations (immutable post-pin at current call sites — remember for new callers) |
| U-D2b | U | frontend remediation (u-d2-review MED + LOWs + nits) | u-d2b-remediation | worktree @ 5 commits (base 073223c, clean) | examples/incremental_design_tool/** only | PASS — MERGED 17292db (2026-09-03 ~12:00); round-2 RUNNING (u-d2-reviewer) | (5 commits) | MED: retryLast guards siteMismatch like commitEdits (no-op resolve + mismatch channel re-assert; fresh connect releases; shared #noteSiteMismatch). L1: reset refuses synchronously BEFORE local clearing + catch-restores previous trees + reschedules preview; session reset() records mismatch from 409 pre-rethrow; onReset harness hook. L2: impactPanelModel(null/[]) → recomputeOnly:true (no stale classification survives newer job; exact_session test proves time-only recompute input IS sourceNodes:null). L3: validateDraft rejects unknown props + classPicker validated vs valid−fenced (fenced 7 + off-vocab 99 named errors); misleading landcover test REWRITTEN (old draft used undeclared absorp_th = asserted the hole itself). Nits: restatements removed (transmissivity DERIVED from transVeg schema default via adapterPropertyDefault — proven by moving-default-to-0.11 test), viewRequestBody deleted, schema_version<1 rejected. Tests 102→111 (main verified 111/111) | null |
| U-D2-review-r2 | U | round-2 verification (non-author) | u-d2-reviewer | read-only main @ 17292db | none | APPROVE — U-D2 LINE CLOSED (2026-09-03 ~12:45) | null | All 7 findings verified fixed: MED retryLast guard false-block-safe BOTH ways (zero-editPosts under mismatch test fails if guard removed; retry-works-after-fresh-connect fails if over-blocking); L1 synchronous refusal BEFORE clearing + catch-restores exact previousTrees clone w/ preview reschedule; L2 impactPanelModel recomputeOnly for null/[]/undefined REPLACES panel (garbage still throws loud — tested); L3 empty-values validateDraft rejects unknown props naming them + classPicker same-object-as-chips construction divergence impossible; old absorp_th test proven to assert the hole itself, rewritten; NIT a grep-clean restatements, transmissivity derived via wiring-NAME table + spec.default (moving-default-0.11 proof), only remaining literals = TREE_PRESETS demo content guarded by finiteness test; NIT b/c verified. Suite 111/111 reviewer-run. One backlog NIT: reset-restore selection jumped to last tree — FIXED BY LEAD (save previousSelectedTreeId, restore if still present, else last) + 111/111 re-verified |
| U-D3 | U | UEDIT-008 per-family perf + UEDIT-007 evidence RESULT (REPLACES dispatch row status) | u-d3-perf | read-only main + /tmp + benchmarks/2026-09-03-ud-perf.json | none (never commits; lead commits evidence) | PASS — MEASURED (2026-09-03 ~14:00) | null (evidence committed by lead) | UEDIT-008 MEASURED: 6 families × 5 quiet runs at pinned hot-file state (14-file sha256 fingerprint identical; building @ ccabc8e vs rest @ 2191e34 — one frontend-only commit apart, code diff empty): veg LOCAL p50 56.40/p95 57.47s ≤841.8MB; met FULL 146.99/151.00 ≤1155.9; lc LOCAL 38.28/40.03 ≤796.3; building FULL+regen-chain 241.65/273.40 ≤1142.2; params FULL 139.79/140.69 ≤992.4; mixed FULL 141.20/143.96 ≤1179.4. Budgets: PERF-001 PASS (56.4<75), PERF-002 PASS borderline (57.5 vs 60; P8 59.14), PERF-003 PASS (worst 273.4<300), MEM-001 PASS (<1.2GB vs 3GB). Plan+stage overhead ≤0.02s all families (wall = scientific solve). UEDIT-007 PASS: view-only batch 0 submissions/0 jobs/0 solves/0 publishes/scene_revision 0/no-op reason, 5/5 runs 23-34ms; control view+veg batch = veg-only job counts. Anomalies: A1 sibling pytest waves → mid-run load tracking, 8-9 runs/family discarded+topped to 5 quiet; A2 two-phase protocol (first-pass slower consistently — direction faster at pinned state, reported as observed); A3 loaded building runs FASTER than quiet (quiet = conservative); A4 building +13% drift across sequential quiet runs undiagnosed (read-only mandate); A5 sampler-thread death excluded; A6 solver internal print inflated under contention (external spans ground truth); A7 original 93.7s outlier = pytest wave; A8 PERF-002 borderline noted. Artifacts /tmp/ud3/ | null |
| U-D4 | U | FINAL U-D packet: universal edit transport (5 families over HTTP, registry-validated, job-queue discipline), plan exposure (per-node stages server-served for UEDIT-009), hardening (views rate-limit + legend no-full-decode, operator rebase_results_root), deployment smoke + docs (api_contract, index.md routing table + UEDIT-010 confirmation) + full validation | u-d4-close | worktree from aadf0a7 | server/**, scenario_state.py (rebase), executor.py (plan serialization ONLY), examples/**, api_contract.md + index.md, tests | RUNNING (dispatched 2026-09-03 ~14:20) | null | forbidden worker/solver/store/edit_types/edit_registry/adapters/universal yaml/benchmarks; view-only-via-HTTP must stay zero-jobs; ~890 python + 111 node must stay green (3 known env failures excepted) |
| U-D4 | U | final U-D packet RESULT (REPLACES dispatch row status) | u-d4-close | worktree @ 5 commits 87b1369..11f370c (base aadf0a7, clean) | server/{universal,executor_bridge}.py NEW, routes_*, models, jobs, store (v4), patch_codec, executor.py (+108 plan serialization), scenario_state.py (+133 rebase), frontend 5 modules, api_contract.md +230, index.md +41, 5 test files (universal 1036 ln) | PASS — MERGED f1b9245 (2026-09-03 ~17:30); independent review RUNNING (u-d4-reviewer) | 11f370c | D1 transport: POST /edits/universal item grammar field-placement-only (all fences via full_registry adapters, 422 verbatim; veg refused 400 tree-edits-v1; unknown 400→capabilities); dispatch wrapper (family ledger→PlanExecutor, tree-only→legacy byte-for-byte); deterministic executor rebuild from persisted executor-state; bridge requests full variable set + FULL time series (partial patches misalign windowed composition — agent-found invariant); baseline-less bootstrap one full-tile of CURRENT scene. D2: executed_plan_document → SolveResult.plan → store v4 → job impact_plan per-node {stage,why,scope} + routing + estimates-vs-realized; frontend serverPlan w/ derived fallback. D3: views rate-limit + decode-free legends (manifest statistics, streaming single-plane fallback); rebase_results_root re-verifies paths+memo hash. D4: deployment smoke real-uvicorn (capabilities/views/universal refusals/zero-jobs via /metrics deltas); docs contracts + verbatim routing table from ud-perf.json + UEDIT-010 3-site confirmation + GPU OPEN note. Branch: 951 passed/3 env/4 skipped; node 124/124. Lead main verify: 59 python + 124 node green. DEVIATIONS: none; known corner documented (stale-publish-after-snapshot coverage gap → duplicate building add typed edit_rejected — accepted designed semantics); raster_patch/polygon_assign no UI grammar (adapter refuses typed) | 11f370c |
| U-D4-review | U | code-review (non-author, transport+bridge) | u-d4-reviewer | read-only main @ f1b9245 | none | REQUEST-CHANGES (2026-09-03 ~19:30) — H1 → remediation u-d4b RUNNING | null | H1 HIGH: coverage sidecar advances past store published watermark w/ no reconciliation — _save_state writes coverage inside solve() BEFORE runner publish; base_edit_watermark ZERO refs in bridge/universal; 3 reachable drop scenarios: R1 concurrent edit mid-solve (bridge checkpoint binds INTERNAL revision executor.py:615 vs legacy store-bound jobs.py:1009-1011 → mid-solve commit doesn't abort; runner discards stale AFTER coverage saved → edit consumed silently, exact_result_version falls behind forever), R2 crash between _save_state+publish (requeue window empty → PlanningError → misleading edit_rejected; ResultAlreadyPublished path unreachable), R3 cancel in guard-to-publish window (routes_jobs.py:18-22 contract broken); docstring asserts a store-watermark mechanism that DOES NOT EXIST in code; clamping alone unsound (double-apply). M1: supersede/cancel/restart-mid-universal/building-restore UNTESTED (all E2E serialize via wait_for_job). Deliverables 1-9 otherwise VERIFIED approve-quality: field-placement-only confirmed (no domain constants), dispatch seam sound (full-ledger check — tree-only can never reach PlanExecutor, family ledger persists executor routing), tree-after-family bitwise via snapshot-restore, full-series structural, view-in-family-batch refused pre-mutation + pure view inline zero-jobs (/metrics deltas), plan exposure pure serialization w/ 'changed' emitted + v4 migration safe, legends cannot serve wrong plane, rebase verifies all paths+hash, bootstrap solves CURRENT scene. L1 head-only contiguity, L2 docstring, L3 verbatim overstatement. Nits: variable-triple restatement, routing assertion. 46/46 + 124/124 + 3 safety-critical non-vacuous | null |
| U-D4b | U | remediation (u-d4-review H1 coverage sidecar + R1 watch + L1-L5) | u-d4b-remediation | worktree @ 5 commits (base f1b9245, branch u-d4-remediation, clean) | server/executor_bridge.py, executor.py (provider param only), tests/test_server_universal_edits.py, docs index.md | PASS — MERGED cfb7b39 (31/31 in main, lead-verified) | 606a9c7..9d6b0ee | Stage-then-adopt: solver never advances coverage sidecar; stages pending/ via tmp-dir + single os.replace; next family job's _reconcile_coverage adopts iff different durably-complete job ∧ covered ≤ edit_watermark ∧ valid pair; covered advances only past durable completion; crash points converge; job_id-equality own-pending ignore (deterministic recompute / checksum idempotent completion). R1: PlanExecutor revision_provider + _StoreSupersessionWatch (store commit → -1 → mid-solve checkpoints abort wholesale, legacy parity; -1 trips ALL 4 checkpoints, no >= comparisons). M1: five convergence tests w/ stash-verified failure predictions. L1 full-range contiguity guard; L2 variable rules documented; L3 EXECUTOR_REQUESTED_VARIABLES = patch_codec.SUPPORTED_VARIABLES (derived); L4 transport assert; L5 routing provenance wording. Worktree+branch removed post-merge |
| U-D4-review-r2 | U | round-2 verification (non-author) | u-d4-reviewer | read-only main @ cfb7b39 | none | APPROVE — U-D4b LINE CLOSED (2026-09-03) | null | All round-1 findings verified: H1 stage-then-adopt sound (~17 interleavings stress-tested: own-job requeue, cancel, reset, torn pending, legacy layout, mixed own/foreign pending all resolve); _stage_state tmp+os.replace never promotes; _promote_pending rename-then-flip + GC after flip; watch tripped() pre-staging supersede guard. R1 watch belt-and-suspenders w/ adoption gate as correctness backstop. M1 five tests genuine (fault seams at real code points; R1 concurrent-commit window-coverage + bitwise, R2a crash-before-publish requeue + THIRD-job adoption + bitwise, R2b crash-after-publish checksum idempotent, R3 cancel-window replay + bitwise; building-then-tree correctly SAFE-direction scoped). L1-L5 verified. Forbidden-set diff empty in range. Own runs: 55p pytest + 124 node. DISCOVERY ADJUDICATION: building non-accumulation CONFIRMED REAL + reachable via /edits/universal today; BROADER than first scoped — second building edit also drops first (regenerate.py:281-282 wipes scenario_root every run; :576-585 re-stages baseline + writes only current batch's massing); engine seam pre-existing from u-c5/u-c6 (gate at aadf0a7 executor.py:1554), user-reachability introduced by u-d4 transport; disposition = dedicated packet u-d4c (#36), NOT u-d4b's. Addendum relayed to u-d4c (building→building differential chain + fold unit tests required) |
| U-D4c | U | building-edit accumulation fix (u-d4-review-r2 adjudicated defect, packet #36) | u-d4c | worktree @ 5 commits (base cfb7b39, branch worktree-agent-a502fc5947ad611a2, clean) | incremental/{executor,scenario_state}.py, tests/{test_incremental_building_integration,test_incremental_scenario_state,test_server_universal_edits}.py, docs benchmarks/2026-09-03-ud-perf.json + index.md | DONE — pending lead verify | 660f672..90475f1 | DEFECT (pre-existing u-c5/u-c6 engine seam, exposed user-facing by u-d4 universal transport; reviewer anchors: executor.py _prepare_building_batch batch-only, regenerate.py:281-282+576-585 baseline re-stage per batch, executor_bridge.py:466+482 baseline cache no building history, jobs.py:1131-1151 full patch shadows building patch): building edits did NOT accumulate at the executor — ANY solve after a published building edit ran against baseline arrays and silently dropped the building, INCLUDING a second building batch (add A published then add B published B WITHOUT A). FIX (architecture A): (1) PlanExecutor._applied_massing_edits fold, per-id LAST-WRITE-WINS vs BASELINE anchor (rasterizer placement-style verified — before is disclosure anchor not raster lookup); deletes stay deletes: baseline delete keeps before=baseline/after=None, session-added delete keeps TOMBSTONE before=session-spec/after=None so the fold never empties by deletion (regenerate refuses empty tuples; worker path would hit nothing-pending no-op gate and raise — design hazard caught mid-implementation); (2) fold non-empty -> EVERY batch (any family) routes _regenerate_building_chain with FULL fold (scope may exceed plan promise, never undercut); staged-copy commit-at-publication only (failed/superseded batches leave published fold untouched); (3) worker adoption block triggers for ANY chain-routed batch (no phantom pending); (4) _rollback_failed_batch building_batch->chain_routed (wipes regenerate root for any chain-routed batch); (5) scenario_state v2->v3: massing_edits ORDERED list (sort_keys orders keys not list elements — order is chain edit order), constructors re-validate on restore, v2 REFUSED like v1 (pre-u-d4c doc cannot prove session building-free; refuse-unverifiable); executor_bridge UNTOUCHED — fold rides v3 snapshot on restore, fresh-rebuild path replays ledger building events (deterministic equality PROVEN). EVIDENCE A (failing-first @ 660f672, all flipped PASS, bitwise vs independently-run regenerate_building_batch twin, never relaxed): building->building (both blocks present + non-vacuous vs A-alone), building->met, building->params, building->veg, building->lc; + HTTP-level building->met through real /edits/universal (served arrays bitwise vs twin carrying building+overlay); tree-after-building window oracle corrected to building+tree twin (old baseline+tree oracle encoded the defect's routing). EVIDENCE B: TestBuildingFoldSemantics 9 fast tests (fold accumulation 2 batches; last-write-wins update-after-add; tombstone; baseline-delete persists across later batches; re-add-after-delete; delete-A-after-add-A+B per lead addendum; failed chain-routed batch leaves fold/revision/overlays + wipes root; value-no-op stays intercepted with fold; building-free scenario NEVER calls chain — routing identity); TestBuildingFoldPersistence 4 (v3 round-trip + resumed executor routes chain bitwise vs never-restarted source; tombstone round-trip; forged v2 refused; deterministic fresh rebuild identical fold + snapshot document). KNOWN CORNER re-check: duplicate-add edit_rejected UNCHANGED (adapter identical-value resubmit refusal; fold adds no rejection path — adapter stays placement-style no-inventory). ROUTING DISCLOSURE: ud-perf.json routing_disclosure_post_building_edit + index.md prose, ZERO measurement edits, NO capabilities.py change (frontend pins SUPPORTED_CAPABILITY_SCHEMA_VERSION=1; routing is per-scenario runtime state not adapter capability); cost disclosed: post-building any-family batch pays chain class (~242s p50 vs 56s veg local) until scenario reset. VALIDATION: full pytest 981 passed/4 skipped/1 failed = austin ONLY (sanctioned env); a first run showed 980/4/2 where the second failure was the PRE-EXISTING test_server_capabilities SCENARIO_CACHE id(client)-reuse flake (reproduced at pristine cfb7b39 via /tmp tree, root-caused id() address recycling, did not reproduce on the clean rerun — reported below, not fixed); node 124/124 (diagnostics keys additive). OUT-OF-SCOPED REPORTED (not fixed): (1) tests/test_server_api.py SCENARIO_CACHE keyed by id(client) — module-level dict never cleared, fresh TestClient at recycled address reuses DEAD app's scenario id -> scenario_not_found KeyError in wait_for_scenario_exact; bites whichever view/statistics test allocates unluckily (file-scope deterministic victims shift with -k; full-suite victim test_view_requests_carry_a_per_scenario_budget); u-d4-owned file, suggest keying by client or clearing cache per test; (2) intra-session cross-id footprint overlap divergence documented in fold docstring (adapter has no building inventory; fold = last-write-wins per id is the mission-endorsed semantics) | 660f672..90475f1 |
| U-D4c-review | U | code-review (non-author, scientific core) | u-d4-reviewer | read-only main @ 429be46 (diff cfb7b39..beb3bd0) | none | REQUEST-CHANGES (2026-09-03) — remediation u-d4d RUNNING; core fix verified SOUND, no architecture change demanded | null | F1 MEDIUM false disclosure claim: "overlap divergence documented in fold docstring" — NOT (grep overlap/cross-id/divergen zero; only bare "first-appearance order" :743); divergence REAL+REACHABLE (buildings.py:466-530 sequential replay; delete reset unconditional raster[reset_mask]=ground erases bystander paint; fmax taller-wins; add A/add B overlapping/delete A → fold order keeps B intact at overlap vs session-order truncation; ASYMMETRIC by insertion order); own test used DISJOINT footprints (test file :66-69) — overlap avoided not covered. F2 MEDIUM overstated claim: rebuild equality "PROVEN" — test is re-derivation shortcut on test-local executor, never exercises bridge fresh-rebuild path w/ BUILDING events (R-tests replay lc/met only). F3 LOW nits: v2 refusal reasoning CONFIRMED (no retroactive no-building marker possible; restoring unverifiable would arm the defect; forge test genuine strongest-possible) — name RESET remediation in error text + one ops line (in-flight v2 scenarios fail next family job until reset). F4 LOW no action: rollback wipes regenerate root for ANY chain-routed batch — u-c6 contract comment UPDATED at :791 not violated; audit-retention cost only. F5 LOW: tombstone raster semantics confirmed (before=session-spec → DEM ground reset, true no-op); "baseline-equal products w/ SCENARIO provenance" asserted by no test. VERIFIED SOUND: dispatch/commit-at-publication/failed-batch isolation; value-noop interception precedes fold routing; adoption exactly-once + phantom-pending test honestly updated; bridge empty diff → u-d4b convergence unregressed; ALL FIVE differentials GENUINE (independent twins, two-way non-vacuity, baseline immutability hashes; FAILING-FIRST RUNTIME-VERIFIED by reviewer at 660f672 worktree, all five failed 57s); HTTP building→met real; tree-after-building oracle correction honest; routing identity pinned at DISPATCH BRANCH (monkeypatched-forbidden chain, mode local, no building diagnostics); benchmarks additive-only; SCENARIO_CACHE flake report honest (fixed by lead 429be46). Reviewer runs: 84p building+scenario+universal (6:31), node 124/124 |
| U-D4d | U | remediation of u-d4-reviewer REQUEST-CHANGES on u-d4c (false disclosure claim + coverage gaps; docs/tests/message-text ONLY, fold semantics and dispatch rule untouched by ruling) | u-d4d | worktree @ 3845de8 (2 commits, base 429be46, branch worktree-agent-ab4503c114668a10e, clean) | incremental/{executor,scenario_state}.py (docstring/error-text only), tests/{test_incremental_building_integration,test_incremental_scenario_state,test_server_universal_edits}.py, docs benchmarks/2026-09-03-ud-perf.json (additive) + index.md, ledger/worklog rows | DONE — pending lead verify | 429be46..3845de8 | F1 (MED): the fold's application order is dict FIRST-INSERTION order (tuple(massing_fold.values()), executor.py dispatch), not session-edit order — now DISCLOSED in _staged_massing_fold's docstring (ORDER-AND-OVERLAP DISCLOSURE: delete before-footprint resets are UNCONDITIONAL, after-paints combine by fmax, overlapping DISTINCT ids resolve by application order, session-order sequential replay can diverge at overlaps, divergence bounded to overlap cells of distinct ids), mirrored ADDITIVELY in ud-perf.json routing_disclosure_post_building_edit.overlap_order_disclosure (raw-text splice; only pre-existing delta is fold_semantics gaining its trailing comma; ZERO measurement fields touched) and in index.md prose next to the routing disclosure; PINNED by test_overlapping_delete_diverges_from_session_order_at_overlap with genuinely OVERLAPPING footprints (suite's other fold fixtures are disjoint): add A, add B∩A, delete A → chain-received fold order asserted [A_tombstone, B], staged raster keeps B INTACT at FA∩FB (ground+10) while the session-order sequential oracle truncates B to ground there, differing cells == exactly the overlap mask. CORRECTION TO PACKET: the packet's mirror description ("delete B → A intact at overlap") is FALSE per buildings.py — deleting the LATER-inserted id folds to [A, B_tombstone] whose unconditional reset lands AFTER A's paint and truncates A at the overlap in BOTH orderings (session-order oracle agrees bitwise everywhere there); the test pins the true semantics and the asymmetry is documented (divergence bites only when the deleted id was FIRST-inserted). F2 (MED): ledger U-D4c's "fresh-rebuild replays ledger building events (deterministic equality PROVEN)" was backed only by test_deterministic_rebuild_reaches_the_same_fold — a re-derivation shortcut re-executing the same validated commands on a test-local executor; NOW genuinely PROVEN through the REAL bridge by test_R4_snapshot_loss_replays_the_building_event_bitwise (HTTP family_site): building add via POST /edits/universal → rmtree of the scenario's executor-state (staged pending snapshot + coverage sidecar + generations) → next met job rebuilds FRESH (spy on executor_bridge.restore_into_executor: never called), the BUILDING ledger event travels universal.command_from_item's building_geometry branch (spy), rebuilt fold == original fold (staged scenario-state.json massing_edits equality), batch routes the chain (transport executor, mode full), and served arrays are BITWISE identical to an uninterrupted twin scenario (same edits, no state loss). Ledger row U-D4c stays append-only; the claim is now true as written. F3: (i) scenario_state v2-refusal error text now names the remediation (RESET heals the scenario; covered <= last_reset bypasses the restore) while preserving the "not the supported version" match substring; (ii) index.md ops line: every in-flight scenario carrying a u-d4/u-d4b-era v2 snapshot fails its next family job with the typed refusal until reset. F5: test_delete_tombstone_round_trips EXTENDED (no new file): the delete batch is the one publication with ONLY a tombstone in the fold — products equal a baseline-equal chain oracle (add A then delete A = empty net session massing) BITWISE, non-vacuous (differs from the add batch's products), staged scenario Building_DSM tile == baseline tile bitwise, and provenance is SCENARIO (patch cache manifest != baseline cache manifest, store revision 2, mode full). VALIDATION: 3 touched files 86/86 standalone; full pytest at final tree (venv python, cwd=worktree) 981 passed / 4 skipped / 3 failed in the raw run, where 2 of the 3 (test_cli_help, test_cli_missing_required_args) were an invocation artifact — the subprocess-launched 'thermal_comfort' console script needs the venv bin on PATH, exactly what the canonical PATH="$PWD/.venv/bin:$PATH" command provides; with PATH set both PASS (verified), leaving exactly the 1 sanctioned austin env failure (test_create_inputs_austin_smoke, pre-existing, untouched files) => canonical 983 passed (= U-D4c's 981 + the 2 new tests F1/F2) / 4 skipped / 1 failed austin; no test_server_* failure anywhere (the SCENARIO_CACHE flake is fixed at the 429be46 base); node not rerun — nothing frontend-visible touched. | 429be46..3845de8 |
| U-D4d-review | U | round-2 verification (non-author) | u-d4-reviewer | read-only main @ 748ba7c (diff 429be46..748ba7c) | none | APPROVE — U-D4c/U-D4d LINE CLOSED (2026-09-03) | null | All findings verified remediated exactly as scoped, ZERO behavior changes (executor.py hunk docstring-only: 0 code lines; scope-hygiene empty-diff on server/**, regenerate, buildings, worker, solver). F1: disclosure EXISTS + accurate (docstring :1781-1805, JSON single additive key, index.md); overlap test genuine (8m-shifted footprints, overlap.any() runtime-asserted, REAL BuildingLayer + actual baseline tiles, fold order [ov-a tombstone, ov-b] asserted from captured chain edits, B intact ground+10 vs session-order ground, divergence == overlap mask, array_equal outside). PACKET-CORRECTION ADJUDICATED: agent RIGHT, lead packet mirror WRONG — delete of LATER-inserted id truncates the other building in BOTH orderings (fold == session-order there, full-raster array_equal pinned); divergence ONLY on delete of FIRST-inserted id; test+docstring+JSON+index all correct. F2: claim now TRUE — R4 test drives REAL bridge fresh-rebuild (restore_into_executor spy-through asserted never called; command_from_item ("building_geometry","add") real assembly; rebuilt fold JSON == original; chain routing; served arrays BITWISE == uninterrupted twin). F3: error names RESET remediation, match-substring preserved; ops line added. F5: tombstone-only products bitwise == empty-net-massing oracle, non-vacuous, Building_DSM == baseline tile, SCENARIO provenance pinned (manifest !=, rev 2, mode full). Reviewer runs: 86p (7:16) + node 124/124. Standing observation accepted as-is: F4 audit-retention note remains as-accepted (contract updated not violated). Lead canonical full suite at 748ba7c in flight at verdict time |
| U-E1 | U | adversarial protocol red team (silent state loss / composition shadowing / coverage accounting / stale publish / vacuous oracle / docs-vs-code) | u-e1-redteam | read-only main @ 82610e4 + /tmp repros | none | RUNNING (dispatched 2026-09-03) | null | hunts the defect classes this codebase already produced (building accumulation, coverage sidecar) across ALL families: accumulation-chain X->Y->Z per family pair, snapshot/restart/reset/supersession interleavings, window/full composition ordering, sidecar/watermark mis-accounting, stale publishing, oracle vacuity, contract-vs-docs drift; empirical falsification required per finding |
| U-E2 | U | adapter + registry red team (pre-committed u-auditor) | u-e2-auditor | read-only main @ 82610e4 + /tmp probes | none | RUNNING (dispatched 2026-09-03) | null | registry-code parity incl. every yaml lead ruling re-verified at HEAD; per-adapter fence completeness; capabilities.py derivation purity (zero hardcoded physics) + snapshot verbatim; physics plumbing user-value-to-kernel trace; defensive-bypass hunt (delta construction w/o full_registry); citation drift |
| U-E2 | U | adapter + registry red team RESULT (REPLACES dispatch row status) | u-e2-auditor | read-only main @ 82610e4 + /tmp/ue2_probe1-5.py | none | COMPLETE — 0 CRITICAL / 0 HIGH / 3 MEDIUM / 5 LOW; no wrong-science-reachable path (2026-09-03) | null | M1 typed-4xx envelope violated: universal.py:280 catches only (EditStateError, AdapterSchemaError) but AdapterRegistryError (edit_registry.py:100, raised for ANY undeclared op) + int() coercions' ValueError/TypeError ESCAPE -> app.py:297 catch-all 500 (empirical: landcover sculpt, met update, building grow, time_index 'abc'/'[5]'); breaks universal.py:16-18/:271-276 "never a 500". M2 silent wrong-timestep: universal.py:173/:183 int(values.pop(time_index)) coerces 5.9->5, True->1, "07"->7 BEFORE met adapter's typed _require_index (met_time.py:385-393 refuses bool/non-int) sees it; routes_scenarios.py:703-707 repeats (int(... or 0)). M3 silent normalization (same contract clause): params op!=reset silently rewritten update (typo/paint accepted, universal.py:230-235; other 4 families relay verbatim + refuse); building values.building_id silently OVERRIDES target (:207-221); landcover unknown keys silently dropped incl. water_please=7 bypassing class fence attention (:196); met stray time_index dropped when range present (:174-179). L1 TreeSpec __post_init__ accepts trunk_ratio==1.0 closed-interval (geometry.py:70-71) vs validate_tree_spec half-open [0,1) (trees.py:85-87); snapshot restore skips validate (scenario_state.py:143-154) — tamper-only asymmetry. L2 STALE DOC: examples README:47-49 claims family edits have NO transport + FamilyTransportError — u-d4 landed universal-edits-v1 (family_edits.mjs:25, routes_scenarios.py:630). L3 citation drift: yaml:83 params cites :762-764/:606-665 -> actual :649-716 (height :716, leaf :702-703); yaml:87 albedo_g :50 -> :51 (Knight :598-601 ok); yaml:44-47 landcover remap :967-979 -> :613+:1029-1041, solweig.py:2195 -> :2215 (:130 correct); yaml:102-103 wind solver.py:1090-1091/:1179 -> :1238-1239/:1414-1415; wbgt solver.py:1095/:1191 -> save_wbgt=False :1243/:1417/:1590 + NEVER_PLANNED_OUTPUTS planner.py:295; yaml:58-59 selected_date solver.py:324-332 = MET-cache cross-check, solar pin = manifest.solar_geometry solver.py:192 (manifest.py:287-304 correct); trees consumption trees.py:152 (yaml cites docstring :12-16). L4 adopt_published_state (worker.py:396-421) no re-validation — fed only fenced objects (scenario_state.py:885-908 constructs fenced; executor seam fenced); defense-in-depth note, no reachable bypass. L5 spatial no-op: off-grid coords (1e300) accepted at validate, rasterize to nothing — accepted edit w/ no scene effect; identity no-op refused, spatial not detected. CLEAN (explicit): D1 registry-code parity substance EXACT (safe/blocked/class/op/scope lists all match); D3 capabilities derivation VERBATIM deep-equal snapshot (probe5), zero hardcoded physics, only structural scope->locality mapping hand-written; D4 physics plumbing value-for-value ALL 5 families (met cols Ta11/RH10/radG14/P12/Ws9/uhii24 at utci_process.py:675-693, overlay writes exact float64 copy-on-write met_time.py:742-750; params 14 overrides :649-716 forwarded :823-827; landcover fence->Tgmaps :613 + windowed remap :1029-1041; trunk_ratio trees.py:152; building staging door re-runs full schema executor.py:1734; cross-id overlap order DISCLOSED u-d4c); D5 bypass hunt CLEAN — every production entry fenced (route->adapter, executor replay re-validates executor_bridge.py:537-549, staging doors re-fence, restore constructs fenced, tampered snapshot -> typed refusal); u-d4 field-placement-only RE-FIRMED for shape/placement; D2 adapter fences 60+ hostile probes ALL typed-refused (full inventory in report; output_view handled view-only BEFORE transport check — auditor's earlier suspicion was own probe-order error, NOT a defect). DISPOSITION: remediation packet HELD until u-e1 lands (overlapping transport files); M1/M2/M3 + L1/L2/L3 must-fix candidates, L4/L5 disclosure-only candidates |
| U-E1 | U | adversarial protocol red team RESULT (REPLACES dispatch row status) | u-e1-redteam | read-only main @ 82610e4 + /tmp/ue1/ repros | none | COMPLETE — F1 HIGH wrong-science-published + F2 MEDIUM + F3-F5 LOW; 9 cross-family chains bitwise-CLEAN (2026-09-03) | null | F1 HIGH: retention pruning re-routes family scenarios onto legacy baseline-bound solver — routing predicate executor_bridge.py:171 any(event.family) reads RETENTION-PRUNED ledger (jobs.py:658 list_events; production sweep_retention deletes sequence <= acked-tail, tail 1000, hourly _maybe_sweep jobs.py:595-607); once family events prune past tail, next TREE edit publishes a window WITHOUT accumulated family state (empirical: transport=None legacy; bitwise == WITHOUT-met twin; vs correct WITH-met twin utci 15774/16384 cells max 0.58K, tmrt 16384/16384 max 0.41K); repro /tmp/ue1/repro_retention_reroute.py (tail 0 vs production 1000 — only volume differs); NOT a wedge (repro_retention_recovery.py: next FAMILY edit adopts pending across intervening legacy job, covered=1<=watermark=3, publishes bitwise-correct full state; wrong patch persists at its version until then); rated HIGH-not-CRITICAL only for volume-gating. F2 MEDIUM: corrupt trusted+pending executor snapshot wedges later family jobs UNTYPED — bridge catch list executor_bridge.py:634-648 missing ScenarioStateError (scenario_state.py:537) -> generic job_failed (jobs.py:695-700), inconsistent w/ own scenario_state_unrecoverable envelope; repro /tmp/ue1/repro_torn_snapshot_wedge.py (corrupt-old-generation-only SELF-HEALS via pending rotation — documented); low reachability (disk corruption precondition). F3 LOW engine-only: value-no-op on REUSED executor leaks staged flags (stage_forcing_overlay :1047/_stage_overlays :1067 run before no-op return :1080-1119 which skips rollback; _forcing_presence_pending + _landcover_dirty_override persist; over-solve only, bounded, NOT HTTP-reachable — snapshot carries neither flag, bridge builds fresh executor); /tmp/ue1/repro_noop_leak.py. F4 LOW code-audit: ExecutorError + SolverInputError likewise escape bridge catch -> untyped. F5 LOW code-audit: _bootstrap_state executor_bridge.py:784-856 assigns arrays w/o shape validation (full[:array.shape[0]]=array) — mis-built cache = silent NaN tail. CLEAN (explicit, real solver + bitwise twins): met->lc/lc->met/met->params/params->met/lc->params/params->lc/met->met/lc->lc/params->params ALL bitwise-clean vs full-domain twins (/tmp/ue1/repro_accum_chains.py); snapshot adoption across intervening legacy job correct; corrupt-old-generation self-heals; reset-after-family-edit covered by existing test (verified passing); no-op leak does not cross snapshot/restore; read surface serves per-version PATCH payloads, _compose_current_state legacy-internal only (no read-time cross-version composition to shadow); falsified candidate: lc tiny-fixture kernel IndexError is FIXTURE artifact (random SVF + synthetic 16-patch shadowmats tests/test_incremental_worker.py:341-347; real-site paints bitwise-clean). Baselines: universal_edits 31p (191s), building_integration 27p (123s). DISPOSITION: combined remediation packet u-e3 dispatched (F1 durable routing signal + F2/F4 typed envelopes + F3 flag reset + F5 shape fence + u-e2 M1/M2/M3/L1/L2/L3/L4/L5) |
| U-E3 | U | remediation of u-e1 protocol audit + u-e2 adapter audit (FINAL U-wave fix packet; wrong-science paths must die here) | u-e3 | worktree @ 6e27cf9 (8 commits, base 82610e4, branch worktree-agent-a1a104aec4911dbb9, clean) | server/{universal,executor_bridge,store,routes_scenarios}.py (store = additive migration v5 only), incremental/{executor.py (no-op path only), scenario_state.py, edit_registry.py (notes strings only), worker.py (docstring only)}, docs/.../universal_editing/edit_registry.yaml, examples/.../README.md + assets/capabilities_snapshot.json (mechanical regen), tests/{test_server_universal_edits,test_incremental_lc_integration,test_incremental_scenario_state}.py, ledger/worklog rows | DONE — pending lead verify | 82610e4..6e27cf9 | ALL 13 findings FIXED, every behavioral fix failing-first (all 13 new regressions RUN RED at base 82610e4 before their fix commits; commit 1 = tests, then fix-per-finding). F1 HIGH: routing predicate executor_bridge.py:171 read the RETENTION-PRUNED ledger — once sweep_retention pruned family events past the tail, a later TREE edit re-routed to the legacy baseline-bound solver and published WITHOUT family state (u-e1: bitwise == WITHOUT-met twin, utci 15774/16384 cells max 0.58K). FIX = option (a), durable scenario flag: migration v5 scenarios.carries_family_edits SET atomically inside commit_universal_edits' final UPDATE (correct the instant state is born — independent of job scheduling/supersession/disk staging; option (b) on-disk executor state rejected as fragile induction over job acks), CLEARED by reset_scenario, read via Store.scenario_carries_family_edits() which ORs the flag with any SURVIVING family event (belt-and-braces for pre-v5 rows so a migrated deployment never under-routes); predicate errs toward OVER-routing (a wrongly flagged tree-only scenario pays one executor rebuild; a wrongly legacy-published family scenario pays a bitwise-wrong scene). Regression: TestRetentionDurableRouting — tree edit after store.sweep_retention(event_tail=0) must route executor (base: KeyError transport, legacy) AND served window BITWISE == full-domain WITH-met twin + non-vacuous (differs from WITHOUT-met twin). F2 MED: bridge catch list missed ScenarioStateError → torn/tampered snapshot wedged every later family job as generic job_failed; now maps to the bridge's own scenario_state_unrecoverable envelope (idempotent repeat, reset heals — restore gate bypasses the corrupt snapshot; all three pinned in TestTypedFailureEnvelopes). F4 LOW: ExecutorError/SolverInputError → new typed engine_refused job envelope (batch valid, engine could not run it — distinct from edit_rejected and from job_failed); both pinned via deterministic PlanExecutor.execute monkeypatch. F5 LOW: _bootstrap_state validated NO shape — full[:array.shape[0]]=array padded a mis-built stored baseline with a silent NaN tail; now exact (time_steps,rows,cols) check → _Unrecoverable naming the cache file + both shapes (unit test with a one-timestep-short utci + correct tmrt). F3 LOW: value-equal no-op return skipped rollback after the staging seams already ran → reused executor leaked _forcing_presence_pending + _landcover_dirty_override (every later batch full-tile); no-op exit now mirrors the failure path (rollback_pending(current watermarks) = identity rewind + both flags cleared); regression proves flags clean AND follow-up paint routes local again. M1 MED: validate_command caught only (EditStateError, AdapterSchemaError) → AdapterRegistryError (ANY undeclared op: lc sculpt, met update, bldg grow) escaped as app.py:297 500; now a third vocabulary: plain AdapterRegistryError → invalid_request 400 field edits[i].operation naming the op (AdapterSchemaError keeps 422 — it SUBCLASSES the registry error so clause order carries the distinction). M2 MED: values-level time_index int() coerced (5.9→5 WRONG-TIMESTEP edit, True→1, "07"→7) before the met adapter's _require_index saw the original; 'abc'/'[5]' escaped as ValueError/TypeError 500s; now strict json_time_index (JSON integers only, bool explicitly rejected) at BOTH sites — universal.command_from_item and the view-only branch's former int(... or 0) in routes_scenarios; typed 400 field edits[i].values.time_index. M3 MED: transport now relays VERBATIM — params op no longer silently rewritten to "update" (typo 'updaet' was ACCEPTED as an update; now typed 400 via M1's registry relay), building values/old_values building_id cannot override the target (typed 400 naming building_id), landcover paint refuses unknown values keys (grammar exactly {'class'}; base dropped 'water_please' silently), met refuses time_index duplicated across item+values or riding a range (base silently dropped one). L1 LOW: _spec_from_dict built TreeSpec with NO validation and TreeSpec.__post_init__ accepts trunk_ratio==1.0 closed [0,1] vs live half-open [0,1) — tampered snapshot detonated as RAW ValueError mid-replay (partial restore); restore now runs validate_tree_spec at the spec boundary → ScenarioStateError typed, whole-restore, layer untouched (interval alignment in geometry.py rejected as bigger blast radius). L2: README 'transport gap' section rewritten — both transports live since u-d4; FamilyTransportError restated as the true residual (document-integrated-but-unwired adapters). L3: all 7 citation drifts re-verified at MY HEAD and fixed (params :649-716; albedo_g :51 + Knight :597-601; lc remap :613+:1029-1041, solweig.py:2215; wind solver.py:1238-1239/:1414-1415 pass windcoeff=None; wbgt save_wbgt=False :1243/:1417/:1590 + NEVER_PLANNED_OUTPUTS planner.py:295; selected_date manifest.py:287-304 + solver.py:192; trunk trees.py:152); the 2 fixes living in PARSED notes fields required mirroring edit_registry.py builtin metadata strings (module's own 'do not edit one without the other' drift-guard rule, enforced by test_registry_matches_yaml_entries — caught by the full suite as a real failure before the mirror commit) + a mechanical capabilities_snapshot.json regen (2 lines, notes only). L4: defense-in-depth docstring at worker.adopt_published_state (callers must pass fenced watermark objects; no behavior change). L5: one disclosure line (yaml notes + mirrored metadata + snapshot): off-grid x_m/y_m PASS schema validation and rasterize to zero cells — verified empirically (edit commits, zero canopy/trunk cells). VALIDATION: full canonical suite PATH="$PWD/.venv/bin:$PATH" .venv/bin/python -m pytest -q tests/ at faa3163: 995 passed / 4 skipped / 2 failed = sanctioned austin env failure (pre-existing on origin/main) + the parity drift above; after the 6e27cf9 mirror: edit_engine 50/50, capabilities+design-docs 37/37, universal suite 42/42, scenario_state 29/29 — effective canonical 996 passed / 4 skipped / 1 failed (austin only); NO test_server_* failure; node --test 124/124 (twice, incl. post-snapshot-regen). RESIDUALS (found, disclosed, not fixed — outside owned files): (1) pydantic lax mode normalizes ITEM-level time_index 5.0/True/"07" at the model boundary (models.py UniversalEditItem int|None) — value-preserving or already-422 for 5.9/'abc'/[5]; values-level (the wrong-timestep danger) is now strict; (2) pre-fix DBs may hold family_json events (e.g. values.time_index "07"/5.9, dual time_index) that post-fix replay refuses inside the job (generic job_failed) — such events could only exist from a pre-fix server's silent acceptance; retention makes this academic, reset clears it; (3) README's snapshot-refresh snippet says indent=2/sort_keys but the checked-in artifact format is indent=1/insertion-order (regen done format-preserving, 2-line diff). |
| U-E3-review | U | round-1 verification (non-author, scientific core) | u-d4-reviewer | read-only main @ 57def21 (diff 82610e4..57def21) | none | APPROVE-WITH-NITS — 2 residual findings, lead-direct fixes pre-approved (2026-09-03) | null | All 13 u-e3 fixes verified SOUND: 13/13 regressions RUNTIME-VERIFIED failing-first at 70c2704 (reviewer executed the test-only commit against base — every new regression genuinely RED before its fix); F1 flag semantics correct (set atomically at commit_universal_edits, cleared at reset, reader ORs surviving events so migrated deployments never under-route); F2/F4 typed envelopes pinned incl. idempotent-repeat + reset-heals; F5 exact-shape fence; M1 clause ORDER load-bearing (AdapterSchemaError SUBCLASSES AdapterRegistryError — schema 422 first, plain registry 400 second — comment must survive); M2/M3 verbatim relay with both-direction building_id fence; L1 restore-boundary validate_tree_spec whole-restore; L3 citation parity incl. the parsed-notes mirror rule enforced by the suite. Reviewer runs: canonical subset green; node 124/124. RESIDUAL-1 (MEDIUM): _MIGRATION_5 shipped the bare ALTER with NO backfill — a pre-v5 DB whose family events were ALREADY pruned upgrades to flag=0 and resurrects F1 on the next tree edit; fix = migration-time UPDATE ... WHERE scenario_id IN (SELECT DISTINCT scenario_id FROM edit_events WHERE family_json IS NOT NULL) + ops note for the pruned-before-upgrade case (manual SQL restore). RESIDUAL-2 (LOW-MEDIUM): item-level time_index lax at the pydantic boundary (models.py UniversalEditItem + ViewRequest): True->1 ACCEPTED empirically (silent wrong-timestep for item-level), 5.0->5/"07"->7 value-preserving; fix = Field(strict=True) on both fields — refusal lands as the app's standard 400 invalid_request envelope. Both fixes pre-approved exactly as specified; NF1 (u-e1's new finding) + predicate attack C routed to the u-e3c packet instead (design work in bridge/executor). |
| U-E1-verify | U | independent verification of u-e3 remediation (author = u-e3 agent; verifier = original red team) | u-e1-redteam | read-only main @ 57def21 + /tmp/ue1 repros re-run | none | COMPLETE — F1/F2/F3/F5 FIXED (bitwise/runtime evidence), F4 FIXED (code-verified); predicate attacks A/B CLEAN; attack C RESIDUAL + NF1 NEW MEDIUM routed to u-e3c (2026-09-03) | null | F1 FIXED: post-fix repro at 57def21 — tree edit after sweep_retention(event_tail=0) routes transport executor and serves a window BITWISE == the WITH-met full-domain twin; durable flag set at commit (verified in-db), cleared by reset. F2 FIXED: corrupt trusted+pending snapshot now answers scenario_state_unrecoverable typed envelope (idempotent repeat verified). F3 FIXED: no-op path calls rollback_pending — staged _forcing_presence_pending/_landcover_dirty_override flags clean after a value-equal reuse, follow-up paint routes local again (repro green). F5 FIXED: one-timestep-short stored utci → exact-shape _Unrecoverable naming cache file + both shapes. F4 code-verified: ExecutorError/SolverInputError → engine_refused typed envelope (deterministic monkeypatch tests reviewed). PREDICATE ATTACKS on the new durable flag: (A) tree-only scenario never flagged — clean, legacy routing is CORRECT there; (B) family events survive but flag cleared by reset — clean, reader OR re-derives 0 correctly (post-reset scenario genuinely carries no family state). ATTACK C RESIDUAL: a pre-upgrade deployment that PRUNED family events BEFORE migrating to v5 — backfill-from-events unreachable (events gone), flag arrives 0, next tree edit under-routes; closure needs an OR on executor-state snapshot dir presence under results_root/<scenario> (state on disk exists exactly when the scenario ran family jobs) or a dir-presence backfill; note the u-e3b migration backfill (surviving-events case) does NOT close this pruned-before-upgrade case. NF1 NEW MEDIUM (pre-existing at 82610e4, NOT a u-e3 regression): family edit → reset → TREE edit deterministic refusal wedge — post-reset the OR over SURVIVING family events still routes the executor (events survive reset), but the fresh-rebuild replay window max(covered, last_reset) EXCLUDES the pre-reset family event and replay_tree_events=False seeds the layer without commands → commands==() → PlanningError executor.py:877 → typed edit_rejected; repeats identically (WEDGE) until the next family edit heals it; loud/typed, never wrong science — repro /tmp/ue1/repro_post_reset_tree_wedge.py; fix directions: arm replay_tree_events when the window contains tree events, OR let the executor accept a tree-only batch; interplay with executor-state dir presence must be designed together with attack C. |
| U-E3b | U | lead-direct close of u-e3-review residuals 1+2 (reviewer pre-approved exact fixes; confirm-after pattern) | lead | main @ 76b4b90 (base 57def21) | server/{store,models}.py, tests/{test_server_recovery,test_server_capabilities,test_server_universal_edits}.py, docs/.../deployment_operations.md | DONE | 57def21..76b4b90 | RESIDUAL-1: _MIGRATION_5 now backfills carries_family_edits=1 from surviving family events (reviewer's exact SQL); scenarios pruned BEFORE upgrade keep legacy routing until next family edit — disclosed in deployment_operations.md (new "Schema upgrades and the family-routing flag (v5)" subsection) with the manual restore SQL; store.py comment block updated to match. RESIDUAL-2: UniversalEditItem.time_index + ViewRequest.time_index now Field(strict=True) — True/5.0/"07" refused at the model boundary as the app's standard 400 invalid_request envelope naming the field (grammar-fence 422s at the values level unchanged). Tests failing-first (all three RED at 57def21, GREEN at 76b4b90): test_migration_5_backfills_pre_v5_family_scenarios (s1 family-survivor=1, s2 pruned=0, s3 never=0), TestTransportGrammarFences::test_item_time_index_accepts_only_json_integers, test_view_time_index_accepts_only_json_integers. Validation: recovery+capabilities+docs suites 32p GREEN incl. sigkill-recovery migration re-open; universal suite GREEN; NOT a full-suite run (u-e3c lands before the final full gate). ATTACK C NOT closed by this (pruned-before-upgrade has no surviving events to backfill from) — routed to u-e3c. |
| U-E3c | U | red-team remediation (u-e1 NF1 post-reset tree wedge + u-e1 predicate attack C pre-v5-pruned under-route; wrong-science paths) | u-e3c | worktree @ 031fb21 +1 docs commit (4 commits, base 76b4b90, branch worktree-agent-ue3c-a7f3, clean) | server/executor_bridge.py, server/store.py (routing reader + migration v6 + reset watermark ONLY; commit_universal_edits semantics untouched), tests/test_server_universal_edits.py, tests/test_server_recovery.py, docs/.../deployment_operations.md (v5/v6 section rewrite), ledger/worklog rows | DONE — pending lead verify | 76b4b90..031fb21 (+ ledger/worklog commit) | BOTH DEFECTS FIXED, failing-first (commit 1 = 4 regressions RUN RED at base 76b4b90 before the fix commits). NF1 MEDIUM (pre-existing): family edit -> reset -> TREE edit was a deterministic refusal wedge (edit_rejected 'batch must contain at least one validated edit' at executor.py:877) repeating identically every retry until a family edit healed it — routing stayed executor (the pre-reset family event survives retention), but the fresh-rebuild path returned replay floor max(covered, last_reset) which EXCLUDES the pre-reset tree event, seeded the tree base from request.trees, and replay_tree_events=False dropped the window tree event -> empty batch. FIX = lead direction (a) refined: _build_executor gained a voided-tree-base branch — when last_reset>0 and covered<=last_reset, seed NOTHING from request.trees, return floor=last_reset and replay_tree_events=True, so the window's tree events rebuild the authoritative tree state exactly from empty and emit REAL vegetation commands/patches (served scene = baseline+tree). Lead's sketch (b) (executor accepts the empty batch as no-op) REJECTED on science: the no-op path re-serves the latest published result, which post-reset is the baseline WITHOUT the tree — bitwise-wrong vs the full-domain oracle twin. Double-apply impossible: TreeLayer.add_tree raises on duplicate id if replay ever met a seeded tree; a SECOND tree edit after the healed one completes via the normal restore path (pinned). Predicate attack C: a pre-v5 database whose family events were ALREADY pruned by retention before the v5 upgrade carries flag=0 and no surviving family event, so the next TREE edit under-routed to the legacy baseline-bound solver and published WITHOUT family state (F1-class wrong science; the v5 backfill cannot see pruned events). Closure = the lead's third durable signal made RESET-AWARE as the packet required: migration v6 adds scenarios.last_reset_sequence (INTEGER NOT NULL DEFAULT 0, backfilled from SURVIVING reset events; reset_scenario writes it going forward — retention-immune like the v5 flag), and scenario_carries_family_edits now returns flag OR surviving-family-event OR (executor-state dir exists AND effective covered_sequence > watermark), where effective covered = max(ledger-coverage.json sidecar, pending/pending.json staged meta) read defensively (corrupt/unreadable sidecars contribute 0 — err toward NOT arming the rescue since the flag/event disjuncts still over-route). Never resurrects a pre-reset snapshot (covered<=watermark -> no signal); a never-reset scenario has watermark 0 so mere dir presence arms — the conservative OVER-route the predicate's bias demands (cost: one executor rebuild, never a wrong scene). Composes with the bridge ledger floor: any ledger-visible reset is <= the column watermark, so the restore gate (covered>last_reset) stays valid on the rescued path. Tests (HTTP-level, @pytest.mark.scientific, REAL solver stack + real full-domain twins): TestPostResetTreeRouting — (1) family met -> reset -> tree edit COMPLETES transport=executor, served window bitwise == baseline+tree twin, != with-met twin (non-vacuous); second tree edit also completes; (2) attack C family -> sweep_retention(event_tail=0) -> flag hand-cleared (precondition asserts the RAW carries_family_edits ROW via store._read, NOT the reader — post-fix the reader legitimately fires via the dir signal, that IS the fix) -> tree edit routes executor, bitwise == WITH-met twin; (3) attack-B-style guard: family -> reset -> sweep to EMPTY ledger -> tree edit stays LEGACY (transport metric absent), bitwise baseline+tree, with-met twin differs. Plus test_migration_6_backfills_last_reset_sequence (mirrors the v5 pattern; watermark r1==2, r0==0). TestRetentionDurableRouting untouched and green. VALIDATION: targeted suites 78 passed / 0 failed (test_server_universal_edits 46, test_server_recovery 3, test_incremental_scenario_state 29; 383s); full canonical suite at the worktree head = exactly the sanctioned austin env failure (pre-existing on origin/main@4c5e47d); node --test not run (no frontend-visible change); adapted u-e1 repros against the fixed worktree: NF1 wedge probes 1-6 all 'complete', predicate attacks A_legacy_routing True / B_or_path_executor True / C_residual_under_route False (C now routes executor, serves WITH-met bitwise, differs from WITHOUT-met) / D_reset_interleave_clean True. RESIDUALS (found, disclosed, not fixed): (1) historical pruned-RESET corner — if the reset EVENT itself was retention-pruned before the v6 upgrade, the watermark backfills 0 and the executor state is indistinguishable from a never-reset scenario (surviving evidence cannot distinguish them), so the dir rescue may over-route the next tree edit onto the executor and restore the pre-reset snapshot; reaching it requires ~a retention tail of published edits between the reset and the upgrade; disclosed in deployment_operations.md (v5/v6 section) with the operator remedy (one reset of the affected scenario post-upgrade re-arms the watermark and re-publishes the clean baseline). (2) pre-existing wedge narrowed, not eliminated: a fresh rebuild whose tree base is NOT voided but whose snapshot FILE is missing while coverage survives (no reset involved) with a tree-only window still produces the empty-batch refusal — unchanged behavior, strictly narrower exposure than base. (3) store._executor_state_postdates_reset lazily imports executor_bridge constants (module cycle executor_bridge->jobs->store) — import placed inside the method deliberately. |
| U-E3c-review | U | verification (non-author, scientific core) | u-e3c-reviewer | read-only main @ e914c17 (diff 76b4b90..84e6722; RED runtime at 68536a0 in throwaway worktree /tmp/ue3c-review-red, removed after) | none | APPROVE-WITH-NITS — 0 CRITICAL / 0 HIGH / 0 MEDIUM / 2 LOW, both closed lead-direct (2026-09-03) | null | FAILING-FIRST RUNTIME-VERIFIED: tests-only commit 68536a0 (own package import, no _MIGRATION_6 at base) = exactly the claimed 3 failed / 1 passed — (1) wedge verbatim edit_rejected "batch must contain at least one validated edit", (2) attack-C KeyError 'transport' legacy under-route, (3) migration-6 ImportError; the 1 passed = the attack-B pin, green at base by design. GREEN @ e914c17 independently reproduced: 4 new tests 4p (69.6s), TestRetentionDurableRouting 1p untouched, targeted 46+3+29 = 78/0, FULL canonical 1003 passed / 4 skipped / 1 failed = austin sanctioned only (20m24s). SOUNDNESS beyond packet tests (/tmp/ue3c_probe/probe_post_reset.py, real solver HTTP-level): P1 TWO resets family->T1->reset->T2->reset->T3 served bitwise == baseline+T3 twin and != baseline+T2+T3 (floor=last_reset excludes every pre-last-reset tree event; two-reset wrong-scene closed); P2 mixed window post-reset (superseded tree add folded into family job's replay window) served bitwise == baseline+tree+met2 at both steps (voided replay handles family+tree in ONE batch); P3 delete of a raster BASELINE tree id post-reset = typed 400 "unknown tree_id" — request.trees is scenario_trees only (reset wipes it), so seeding NOTHING loses nothing. NF1 double-apply: restore/voided/seed branches mutually exclusive + TreeLayer.add_tree duplicate-id raise (trees.py:259) backstop; window events carry old_tree/new_tree (store.py:687-688) so replay self-contained; rejected alternative (b) reasoning confirmed. Attack C: TOCTOU benign (routing re-derived at execution; mid-flight reset trips _StoreSupersessionWatch); dir check = one stat on tree-only hot path, reader called from exactly one production site (executor_bridge.py:200); commit_universal_edits untouched; all 6 "UPDATE scenarios SET" sites checked — none clobber last_reset_sequence; LATEST_SCHEMA_VERSION = len(_MIGRATIONS) = 6; lazy store->bridge import justified (jobs.py:43 cycle real); pruned-RESET corner exactly as narrow as disclosed; operator remedy verified against reset_scenario's write. Tests honest (independent full-domain twins, non-vacuity asserts; the raw-row precondition change disclosed as the honest adaptation). Scope: exactly the 7 owned files; ledger row 9 columns/10 pipes; ranges accurate. NIT-1 LOW: store.py:1207 hardcoded "executor-state" literal while importing sibling constants; bridge names it STATE_DIRECTORY_NAME (executor_bridge.py:156); rename would silently desynchronize the rescue → under-route (F1-class). NIT-2 LOW: reader trusts coverage docs slightly more broadly than the bridge (pending without job-complete gate, sidecar without generation-existence check); compound corner attack-C-row + family-ledger-pruned + generation-lost publishes baseline+tree without family state = bit-identical to pre-fix legacy misroute (NO regression), ops caveat covered only pruned-RESET. Both nits closed lead-direct at ef4d86f (constant import + compound-corner disclosure sentence). |
| U-E3c-verify | U | independent verification of u-e3c remediation (author = u-e3c agent; verifier = original red team, found both defects) | u-e1-redteam | read-only main @ e914c17 + /tmp/ue1 repros re-run + fresh repro_u_e3c_tree_delete.py / repro_u_e3c_residuals.py | none | COMPLETE — NF1 + attack C FIXED (bitwise); lead priority probe (baseline-tree-delete post-reset) UNREACHABLE, three empirical barriers; no new findings (2026-09-03) | null | RED RE-RUNS BOTH HEALED: repro_post_reset_tree_wedge.py — tree #1/#2 complete (was superseded edit_rejected), family complete transport=executor, tree #3 post-heal complete executor mode=full; repro_predicate_attacks.py section C — pruned+cleared row: reader True (dir signal arms), tree job transport=executor, served window BITWISE == WITH-met twin, != WITHOUT-met (15774/16384 utci, 16384/16384 tmrt); full matrix A pure-tree legacy unchanged / B surviving-events OR bitwise OK / D post-reset tree complete + bitwise == baseline+tree twin. LEAD PRIORITY PROBE UNREACHABLE: (a) no baseline tree LIST exists — scenario_trees EMPTY at creation (asserted), tree base is cache.tree_base raster with no ids, user trees the only list entries; (b) delete of pre-reset-added tree after reset = HTTP 400 invalid_request "unknown tree_id" at routes_scenarios.py:520-535 (scene version unchanged); (c) reachable deletes correct — post-reset add-then-delete (separate windows) both complete executor, delete-window BITWISE == baseline twin, scenario_trees ends empty; single-batch [add T, delete T] = typed no-op re-serve of post-reset baseline (mode=no-op; stages state so consumed events never replay). Incidental (not a defect): no-op jobs' metrics carry no 'transport' key (jobs.py:840-877, executor_bridge.py:748-759) — content verified correct. FRESH ATTACKS CLEAN: cross-scenario dir reuse ignored (results_root/<id> namespacing, transport=None complete); reset semantics vs dir signal — sweep prunes the reset EVENT but the v6 durable watermark survives → rescue correctly DISARMED → legacy routing bitwise == baseline+tree (R1b); pre-reset staged-pending adopted post-reset double-guarded (code-level: _reconcile_coverage promotion + restore gate re-check + reader disarm on same comparison, executor_bridge.py:359-396/528-547); MODERN-layout snapshot wipe (generations+pending deleted, sidecar left) SELF-HEALS — _read_coverage distrusts a sidecar whose named generation is missing (executor_bridge.py:240-243) → coverage 0 → family event re-enters window → full recompute, spy-verified correct publish including the new tree. RESIDUALS: (a) pruned-RESET-before-v6 corner CONFIRMED as narrow as claimed AND BETTER than disclosed — empirical outcome is a typed refusal: _guard_ledger_completeness fires FIRST (the pruned reset event necessarily sits inside the replay window) → scenario_state_unrecoverable "ledger no longer carries the full replay window... first missing sequence 2"; pre-reset snapshot NEVER restored, no wrong science; heals via ops remedy. (b) non-voided fresh tree-only wedge UNCHANGED and NOT WIDENED — reproduces only in pre-promotion legacy layout (sidecar without generation name + root scenario-state.json missing); diff touches that path in comments only vs 57def21; modern layout self-heals, so reachable surface = legacy-layout deployments that also lost the root snapshot. TESTS at e914c17: universal_edits + recovery 49 passed (299.67s); the four u-e3c additions all present and green. CONFIDENCE: HIGH throughout (MEDIUM-HIGH only for the adoption double-guard, code-level one promotion path read). |
| U-E3c-b | U | lead-direct close of u-e3c-review nits 1+2 | lead | main @ ef4d86f (base e914c17) | server/store.py (constant import only), docs/.../deployment_operations.md (disclosure sentence) | DONE | e914c17..ef4d86f | NIT-1: the attack-C rescue now lazy-imports STATE_DIRECTORY_NAME (executor_bridge.py:156) instead of hardcoding the "executor-state" literal alongside its three sibling constants — a future bridge rename can no longer silently desynchronize the rescue (failure mode was under-route, the F1-class wrong scene). NIT-2: deployment_operations.md v5/v6 section adds the compound-corner disclosure the reviewer asked for (reader trusts coverage docs slightly more broadly than the bridge: staged pending without the producing job's completion gate, sidecar without the generation-existence check; attack-C row + fully pruned family ledger + lost generation → publishes baseline+trees without family state = bit-identical to the pre-fix legacy misroute, no regression; operator remedy = one reset). Trust-rule ALIGNMENT deferred as disclosed residual (doc chosen over code: compound precondition triple, reviewer offered either). Validation: routing + both migration tests 6 passed. |
| U-DEP1 | U (post-mission deployment hardening) | env-var boot entrypoint + container artifacts + doc truth | worktree-agent-udep1 (worktree @ b409d0c, isolated) | solweig_gpu/server/__main__.py (new), tests/test_server_main.py (new), Dockerfile (new), docker-compose.yml (new), fly.toml (new), .dockerignore (new), examples/incremental_design_tool/README.md (boot block), docs/incremental_design_tool/deployment_operations.md (§Configuration rewrite + Container deployment subsection), ledger/worklog rows | DONE — pending lead verify | (this branch) | T6 (container boot + endpoints over HTTP) + T5 full canonical 1028 passed / 5 skipped / 0 failed | deploy-shape-audit (2026-09-03: no entrypoint, zero os.environ in server/, doc-only env vars, no container artifacts) | lead verify + merge. ENTRYPOINT: `python -m solweig_gpu.server` boots from env via app_from_env pure builder (fake-factory-tested, never boots a server) — SOLWEIG_STATE_ROOT/SITE_ID (comma list)/CACHE_ROOT (cache = CACHE_ROOT/<site_id>)/HOST/PORT/MAX_TREES_PER_SCENARIO/COALESCE_MS/REQUESTS_PER_MINUTE/EDITS_PER_MINUTE + per-site SOLWEIG_SITE_DIR_<ID>/SOLWEIG_SELECTED_DATE_STR_<ID> (<ID> = site id upper-cased, non-alphanumerics → _); unset optional = create_app defaults preserved; invalid value = named error exit 2; unknown SOLWEIG_* = stderr warn only; heavy imports only after validation. TESTS: failing-first — RED at b409d0c = ModuleNotFoundError 'solweig_gpu.server.__main__' (all 25), GREEN at head; 24 unit + 1 `deployment` subprocess real boot (ephemeral port, synthetic cache, /health/live+/health/ready+/api/v1/capabilities). DOCKER: two-stage Dockerfile — builder compiles osgeo GDAL bindings vs Debian libgdal 3.10.3 (no Linux wheels exist), runtime slim + libexpat1 + libgdal36, torch 2.14.0+cpu from PyTorch CPU index (CUDA wheels never enter), non-root uid 10001 with pre-owned /state, EXPOSE 8000; image 2.86 GB; verified boot w/ synthetic site-cache RO bind + named state volume → live 200 / ready 200 (schema v6, site ok) / capabilities 200 (9 adapters); mandated data-leak find (Input_*/ERA-5, maxdepth 2) EMPTY; broader sweep clean (only benign: docs/input_data.md filename, tracked synthetic tests/fixtures); paper/ + build residue excluded. COMPOSE: api + serve.py frontend proxy (solweig-state RW named volume, ./site-cache:ro bind) — `docker compose config` valid. FLY: sample fly.toml performance-1x 2GB, solweig_state mount, out-of-band cache provisioning note — TOML-validated. DOCS: §Configuration rewritten to implemented-table + C-lib-env table (OMP/MKL/OPENBLAS/NUMBA/GDAL_CACHEMAX) + not-implemented list (STATE_DB/RESULT_ROOT removed — state derives from state_root; JOB_COALESCE_MS renamed; WORKER_COUNT/SCIENTIFIC_THREADS/FULL_RECOMPUTE_FRACTION/SCENARIO_TTL_HOURS/RESULT_TTL_HOURS never implemented) + Container deployment subsection (RO cache / RW state volume rules, numba cache=False JIT-per-process note). ENV FINDING for lead: old .venv carried ad-hoc pyyaml + austin (test_universal_interactive_spec validator needs yaml; canonical "1 sanctioned austin failure" was an env artifact — with austin CLI properly installed it PASSES; full suite now 0 failures); neither dep is in any requirements file — recommend adding pyyaml to test extras or setup docs. |
| U-DEP1-review | U | verification (non-author) of deployment packet | u-dep1-verify | read-only worktree u-dep1 @ b0137d1 (RED reproduced at b409d0c in throwaway /tmp worktree; docker rebuilt independently) | none | APPROVE-WITH-NITS — 0 CRITICAL / 0 HIGH / 0 MEDIUM / 2 LOW / 2 INFO, all closed lead-direct (2026-09-03) | null | FAILING-FIRST RUNTIME-VERIFIED: tests/test_server_main.py at b409d0c = collection error ModuleNotFoundError solweig_gpu.server.__main__ (all 25); at b0137d1 = 25 passed incl. the deployment-marked subprocess real-boot HTTP test; worktree-resolution guard confirmed (package resolves to worktree copy). ENV CONTRACT SOUND: all 9 site-independent + 2 per-site vars map to EXISTING create_app kwargs (app.py:308-323); git diff b409d0c..b0137d1 -- app.py jobs.py EMPTY (defaults untouched); sites dict matches SiteRegistry (jobs.py:105-118); 10-case invalid-value matrix exits BootConfigError naming var+value, main()=2; unknown vars warn-and-boot; campus-1km-v1 → SOLWEIG_SITE_DIR_CAMPUS_1KM_V1 resolves; empirical app_from_env with REAL create_app — OpenAPI surface includes /health/* + full /api/v1/* (OpenAPI enumeration authoritative over flat-routes probe). DOCKER INDEPENDENT GREEN: build from worktree OK, image 2.86GB, uid 10001; container with RO site-cache bind + named state volume: /health/live 200, /health/ready 200 (schema 6, site ok, worker ok, result_fs ok), /api/v1/capabilities 200 (9 adapters, 8 families); /state owned by 10001; MANDATED FIND (Input_*/ERA-5/*.tif maxdepth 3) ZERO lines; full-depth /app sweep clean; .gitignore data paths all covered. compose config parses; fly.toml tomllib-parses; env names/ports/mounts match exactly. DOC TRUTH: every implemented-var row matches code (defaults, layout app.py:363, 0-disables semantics app.py:395, GDAL_CACHEMAX M-suffix empirically 64M→67108864); not-implemented list accurate; README block copy-paste run verified live+ready+capabilities via proxy. SCOPE: exactly the 10 files. pyyaml finding CONFIRMED (yaml importorskip'd by the suite, declared in no requirements file). NIT-1 LOW: site id with absolute/traversal component resolves cache_dir outside SOLWEIG_CACHE_ROOT (pathlib absolute-join replacement) — operator-supplied single-tenant footgun. NIT-2 LOW: compose api lacks healthcheck; frontend depends_on has no condition (proxy 502s during first boot). INFO-3: .dockerignore backstops root-anchored (nested data files could slip). INFO-4: README omits SOLWEIG_STATE_ROOT (verbatim run writes ./state to cwd). All four closed lead-direct at 269bcf8: site-id segment guard (6 regressions RED at a51c441, 31/31 file), compose healthcheck + service_healthy, **-anchored backstops (image rebuilt, leak find empty), README STATE_ROOT line, pyyaml→test extra. Final image re-smoked: boots, /health/live 200. |
| U-DEP-platform | U | deployment platform research (user question: Supabase-fit for CPU research deployment) | deploy-platform-research | web, cited | none | COMPLETE — Supabase compute NO; ranked Fly.io/Railway/Hetzner (2026-09-03) | null | Supabase Edge Functions = Deno runtime, 256MB RAM, 2s CPU/request, 150s wall free (400s paid); no Docker/Python runtime, no subprocesses; static hosting not a product; Postgres-for-SQLite = architecture change out of scope → compute layer unfit for a long-running FastAPI + worker + SQLite + filesystem deployment. RANKED: (1) Fly.io — full VMs, subprocesses, persistent volumes, per-second billing, no plan fee: shared-cpu-1x 2GB ≈ $11.11/mo + volume $0.15/GB ≈ $12/mo total, always-on; (2) Railway Hobby $5/mo incl $5 usage (RAM $10/GB-mo equiv); (3) Hetzner CX22 2vCPU/4GB/40GB EU €4.49/mo (self-managed, cheapest per GB). Render PARTIAL (workers+disks paid-only, free web spins down 15min); HF Spaces PARTIAL (Docker now PRO $9/mo + $5 storage, sleeps 48h, ephemeral default disk); Oracle Always Free flaky (ARM halved to 2 OCPU/12GB pure-free 2026, capacity chronic); GT PACE NO (login-node policy bans long-running services, compute = Slurm batch only). Gotchas folded into docs: numba non-cached JIT (seconds per process start), mmap site cache must sit on persistent volume, torch wheels → multi-stage image. Sources in agent report (supabase/fly/railway/render/hf/oracle/hetzner/pace docs). |
| W1 | W (perf waves) | implementation (opt step 1+2: telemetry honesty, heartbeat ticker, causal time-prefix truncation, trivial dedups) | w1-optimizer (worktree agent-a6a24928688eda696, base 3bd8aba) | solweig_gpu/server/{jobs.py,executor_bridge.py}, solweig_gpu/incremental/{solver.py,worker.py,executor.py}, solweig_gpu/utci_process.py, tests/test_incremental_perf_wave1.py (new), docs/.../api_contract.md | MERGED ce76d79 (no-ff into feat/incremental-design-worker; 4 commits a2853eb/5901d3d/c529eeb/c123402; never pushed) | ce76d79 | 22 tests RED 14F/3P→22G; full canonical 1056 passed/5 skipped (baseline 1034+5 re-verified); bitwise gates: served step-12 utci/tmrt array_equal equal_nan PASS on windowed local path, run_utci_window prefix bitwise, pre-cropped cubes bitwise, worker patch step-12 bitwise; deviations documented (executor dispatch + full-tile paths keep full-series — node-store coverage contract); LIVE native site_500 post-merge: job duration_ms 41514/42246 vs 71s pre-W1 = 1.71x, svf_seconds ~24 / time_loop_seconds ~17.1 / read_window_fraction 0.988 |
| W2 | W (perf waves) | implementation (opt step 3: per-patch march windows + hoisted sky-patch geometry in veg SVF replay) | w2-optimizer (worktree agent-ab86fee7df9bb83e5, base ce76d79) | solweig_gpu/incremental/solver.py only (421+/-82); tests/test_incremental_perf_wave2.py (new, 18 tests) | MERGED 838e420 (no-ff into feat/incremental-design-worker; commits 4304c3a+166e0b6; never pushed) | 838e420 | 18 tests RED 18F→18G (march-window engagement, memo counts, bitwise oracles incl. corner-quadrant + A/B gate + bush fallback); full canonical 1072 passed/5 skipped/2 failed = pre-existing test_cli env (venv-on-PATH passes, verified identical at base via stash); bitwise: in-suite legacy-replay match + cross-HEAD real-site served hashes identical (tmrt a46d5594ed862d7d, shadow eb918939dca91a34, utci 7c0cbac6178ac13c); measured svf 22.62→16.90s in-worktree threads=8, march cell-steps halved 2.83e9→1.41e9; HONEST SHORTFALL vs ≤10s target: ~40% of svf_seconds is per-step kernel-launch overhead (153 patches × ≤274 steps) — only a shadow() internals refactor could cut it, out of adjudicated scope; (+,+) quadrant reach ~186k/201k cells on this workload. LIVE native site_500 post-merge: 38.9s/38.0s (from W1 41.5s, pre-wave 71s = 1.87x cumulative), svf 20.3/20.0s |
| W4-verify | W (perf waves) | independent verification (non-author) of W1+W2 at final HEAD | w4-verifier (read-only; throwaway worktrees /tmp/w4-verify-{base,head} removed after) | none (read-only) | CONFIRMED — 0 CRITICAL / 0 HIGH / 0 MEDIUM / 4 INFO | null | Own runs: full canonical 838e420 = 2F/1072P/5S exact match; test_cli env-pre-existing confirmed on both axes (venv-on-PATH passes 2/2; base 3bd8aba without PATH fails identically); node 127/127; wave counts 22+18 grep-verified. BITWISE cross-wave differential (own harness, real site_500 cache read-only, identical edit both sides, direct solver level): full-solve + windowed full-series base==head np.array_equal(equal_nan) TRUE for utci/tmrt/shadow; time_stop=13 prefix bitwise == full-series steps 0..12 (truncation-only); W2 provenance audited (/tmp/w2_bench hashes re-hashed exact, cache manifest IS site_500 — CLARIFICATION per INFO-1: W2's claimed served-plane hashes are solver-level solve_window outputs, not literal HTTP payloads); HTTP-served patch decodes byte-identical to direct solver step-12 planes, rep1==rep2. LIVE bench reproduction: 37.5s/37.8s, svf 19.8, tloop 17.1/17.5, read_fraction 0.98802 vs write 0.29568 rederived exactly from px counts (telemetry honest); mid-job /health/ready 200 ×6 (no starvation; ticker jobs.py:612). INFO-4: svf 16.90 figure threads=8-specific, default-thread run 18.2s — direction/magnitude consistent |
| R0 | RT (realtime collab) | wave 0: reproduction + parity + cartography (5 parallel non-author agents) | r0-perf, r0-parity, r0-queue, r0-universal, r0-frontend (read-only + live server) | none (read-only) | R0 CLOSED — every handoff value confirmed or consistent | f3ad111 | PERF (r0-perf, fresh server per handoff §2, all samples loaded load1 8.55-11.49): single-step [12] 39987/39462 ms (svf 20.85/21.12, tloop 18.61/17.85), full-day 59586 ms (tloop 38.82), read_fraction 0.98802 + write_fraction 0.29568 bit-exact all runs (dirty rows 36-300 cols 148-428), scenario creation 0.354/0.303/0.297 s exact — handoff §4.5/4.6 CONFIRMED load-adjusted (+2.9-6.5% interpolating its own idle→load-14 model), §4.7/§4.8 CONSISTENT/CONFIRMED, zero corrections needed, zero anomalies (mode local, no fallbacks, no 429s). Evidence benchmarks/2026-09-04-r0-realtime/{evidence.json,bench.py}. PARITY (r0-parity): git diff 838e420..f3ad111 zero changes under solweig_gpu/; full canonical 2F/1078P/5S (1078 = 1072 + 6 new spec tests from 6045035 — DEVIATION NOTED: docs-labeled commit also added tests/test_realtime_collaboration_spec.py, additive+green, benign); 2F = sanctioned test_cli PATH artifact (2/2 with venv PATH); node 127/127; oracle spot-check wave1+wave2+worker 95/0 bitwise. QUEUE (r0-queue): single-process single-worker-thread one-FIFO-job-per-batch architecture mapped with file:line — 13-item gap register (no operation schema/epochs/reducer/revision-triple/broadcast/admission; stale-fence substantially EXISTS at 3 layers store.py:1513+1583, jobs.py:882-901, executor_bridge watch :402-441 = strength to preserve and re-key); risk register D.1-D.7 (scene_version equality blast radius store.py:1012/1107/1513 routes_scenarios.py:202-221 jobs.py:766/882-901 executor_bridge.py:424-432 = ONE-wave re-key; SQLite single-conn RLock contention; exact-lane process isolation preserving store-mediated guards; retention-vs-audit tension v5/v6 precedent). UNIVERSAL (r0-universal): 5 integrated adapters (vegetation MEDIUM windowed, building FULL-REGEN chain executor.py:1148-1167, landcover MEDIUM windowed, met MEDIUM-FULL, model-params MEDIUM-FULL), 4 metadata-only (dem, selected_date_time=typed refusal universal.py:403-411, dynamic_wind fenced, output_view=zero-job view-only); planner ALREADY plans heterogeneous batches one-topological-plan (planner.py:365-397, UEDIT-005); R3 ranking = (a) met-vars-at-timestep utci-only via cached tmrt + variable-affinity split, (b) receptor-only params subset (Fside/Fup/Fcyl/cyl/height PARAMETER_SPECS model_parameters.py:363+), (c) selected_date_time adapter needing solar unpin (manifest.py:287-304), (d) kup/kdown/lup/ldown view-layer caching. FRONTEND (r0-frontend): poll-only 300 ms transport (exact_session.mjs:66,691-716), zero EventSource despite server SSE endpoint existing (routes_scenarios.py:991 = 200 ms job-row poll internally); single-user assumptions cluster = If-Match + mutationQueue + one-POST-per-gesture + single-poll-generation (~70% of exact_session tests break under operation epochs); result-class seam = connection badge model.mjs:566-570 + apply lane app.mjs:1693 exact_session.mjs:775; ranked 11-item change list for R1/R2/R7. Lead decisions recorded .omc/plans/realtime-r1r2-design.md (R1 store-centric single wave; R2 additive lanes scheduler-thread + exact process pool; reducer seed = executor_bridge.py:597-628 replay; SSE extension; append-only op audit w/ durable flags) |
| r1-telemetry | RT | R1 packet: solweig_rt_* latency-stage telemetry registry | r1-telemetry (worktree) | solweig_gpu/server/realtime/telemetry.py, tests/test_realtime_telemetry.py | MERGED ca6587a (worktree+branch removed) | ca6587a | 595-line telemetry module (TelemetryRegistry/Counter/Histogram 4096-ring/Gauge/_StageTimer, nearest-rank p50/p95/p99, Prometheus render, 16 solweig_rt_* metric constants, monotonic-clock injection, thread-safe, stdlib only) + 18 failing-first tests. Verified 2.25M records/s. Diff vs freeze b3168d9 = exactly the two owned files (handoff mis-stated "namespace package, no __init__.py" — lead verified __init__.py/types.py intact, mis-statement only). Full suite green at merge |
| r1-frontend | RT | R1 packet: collaborative realtime client module | r1-frontend (worktree) | examples/incremental_design_tool/realtime_client.mjs, tests/realtime_client.test.mjs, README section | MERGED 845d017 (worktree+branch removed) | 845d017 | 797-line RealtimeClient (ADDITIVE; 127 legacy tests untouched): per-actor identity operationId `${actorId}:op-${seq}`, idempotent submit 1s/2s/4s retry, injectable-EventSource SSE subscription (canonical/fast/exact revision + heartbeat events), revision-triple tracking, pending-ack resolution + onStall, zero-loss reconnect (buffer + GET catch-up `?since_server_sequence=N` + operation_id dedupe), result-class state machine with ageMs. 10 failing-first tests; node 137/137 verified in MAIN post-merge (lead fixed a wrong-directory merge attempt — first `git merge` ran inside the worktree = no-op; redone from repo root). Server-side events endpoint arrives with r1-epochs |
| r1-reducer | RT | R1 packet: deterministic source-specific epoch reducer (pure) | r1-reducer (worktree) | solweig_gpu/server/realtime/reducer.py, tests/test_realtime_reducer.py | MERGED f12c0fb (worktree+branch removed) | f12c0fb | 912-line DeterministicEpochReducer + 712-line suite (26 failing-first, red at base): per-family conflict semantics — object generations/tombstones (TOMBSTONE_REJECTED w/ epoch-delete winner detail, GENERATION_RECREATED), landcover per-cell LWW in 64px chunk buckets (RASTER_CHUNK_OVERWRITTEN), met/params closed-interval range segmentation with adjacent-equal merge + permutation equality (RANGE_SEGMENTED), selected_date_time rides segments as unbounded writes, output_view per-user vs workspace. Sorts by server_sequence, audits EVERY op incl. cancelled-out (noop FamilyDelta keeps audit), deterministic (chained-vs-single-shot equivalence + permutation invariance pinned). 10k mixed ops 0.030-0.033s vs 2.0s guard (~60x under; 960 conflicts recorded). 82 passed combined in MAIN post-merge (reducer 26 + spec 6 + edit_engine 56). LEAD RULINGS on handoff questions: (1) conflicts ordering = event-conflicts in sequence order then aggregated (field-LWW by family/entity/field, chunk-LWW by chunk key) APPROVED as canonical — deterministic + diffable; (2) landcover non-paint verbs (polygon_assign/reset) stay audit-only in the reducer — transport (r1-ops) MUST decompose polygon_assign to per-cell paints before reduce; (3) reducer not cross-checking op.workspace_id vs baseline accepted — single-caller contract; r1-epochs asserts routing at the boundary; (4) malformed-payload audit-only accepted — validation is upstream at acceptance (adapter registry), reducer receives validated ops |
| r1-ops | RT | R1 packet: durable idempotent operation log + store v7 | r1-ops (worktree) | solweig_gpu/server/realtime/operations.py, store.py (migration v7), models.py, routes_realtime.py, app.py (router only), tests/test_realtime_operations.py | MERGED 6b2d42f (worktree+branch removed) | 6b2d42f | Migration v7 (realtime_operations + realtime_epochs + scenarios revision-triple cols fast_revision/exact_base_revision), operations.py frozen-vocabulary transport validation w/ typed 4xx, routes POST /workspaces/{id}/operations (idempotent append, batch 1..128) + GET operation + GET epochs, 21 failing-first tests (red at base = collection ImportError; full canonical in worktree 1098P/5S/2F-sanctioned). KEY SEMANTICS: duplicate fingerprint = (family, entity, verb, canonical payload_json) — actor/seq/base deliberately excluded so advisory metadata can never gate equality; batch all-or-nothing w/ typed 409 operation_id_reused; accepted_at stamped inside append txn; epoch reuse only when status open, pure-duplicate batch never creates empty epoch; operation_id GLOBAL unique (uuid-grade ids are the collaborative_state.md contract); realtime ops do NOT bump scene_version (legacy machinery keyed on it) — workspace_revision advancement belongs to r1-epochs. Main post-merge: 53 passed (ops+reducer+spec) + 117 passed (server suites) + node 137/137. LEAD follow-ups routed to r1-epochs: client reads response.acks (server sent "accepted" only — alias needed); client needs GET .../operations?since_server_sequence=N list endpoint |
| r4-proof | RT | R4 pre-implementation proofs: V0 bit-pin + corridor sufficiency (read-only, /tmp evidence) | r4-proof (worktree) | none (read-only; evidence curated by lead) | COMPLETE — V0 bit-identical both regimes; corridor characterized no-clamp=holds/clamped+amp-change=fails w/ mechanism + guard | proofs/ (commit follows) | DELIVERABLE A (V0): fresh full-tile veg-SVF replay vs cached svf_patches — 0 mismatching (cell,patch) pairs all 153 patches BOTH stacks on site_500 (read-only; UNCLAMPED regime: min(a)=183.97>0, bound 57.85 < abs 228.34, early exit active E 27.6s vs A 72.9s) AND synthetic science site (CLAMP ACTIVE: bound 15 > abs 12) — all 10 scalars + svftotal bitwise. §2 UNCERTAIN (absolute-vs-effective) does NOT manifest; parity case 6 PROVEN. DELIVERABLE B (corridor): EXACT C-closure = ray-corridor over UNION of pre/post executed steps ∪ C itself — C load-bearing (step-1 special reads target-LOCAL vegdsm2[t]/a[t], shadow.py:288-293; 32 flips strictly inside C strictly outside corridor: add3=14, move_t0=6, delete_corner_sw=12) — design §3 re-march set corrected to corridor_p ∪ C. NO-CLAMP 60x60 16/16 edits 0 violations incl. raiser 12→18 (5793 flips) + dropper 20→12 (6116). CLAMPED: violations EXACTLY on amplitude changes crossing the clamp (raiser True→False 20 entries, dropper False→True 20, resize 18→20 clamp-off 0) — predicate (amp changed ∧ clamp on smaller-A scene) 32/32; mechanism per-step traced at patch 34 az40° alt18° target (56,34): extended step i=16 reads unchanged overhanging crown vegdsm 13.931, dz 13.573, vegdsm−dz=0.357>a[t]=0 → raise invisible at amp 12 (while-stop). Design §3 sketch inequality false as written in clamped regime (A_old=abs 12 < vegdsm[o]−a[t]=13.93). Corner probe 120x120 (25m tree on 30m building, delete 45m dominant): 53 violating entries, 15118 flips, exclusively vegsh. GUARD RECOMMENDED (3 .max() + 1 .min()): bound > abs on smaller-effective-amplitude scene → full replay — complements _check_baseline_amplitude (which misses droppers on min(a)≥0 clamped sites). PHASE A: raw corridor bbox diverges 19/59 patches (worst 167 cells — while-stop uses WINDOW shape; out-of-bbox in-tile reads lost); W2 _patch_march_window reach expansion 59/59 bitwise — phase A MUST use W2 windows. Residuals: float32 dz monotonicity assumed (conservative direction), min(a)<0 + bush≠0 untested (guard covers), brute force spot-check scale (analytic carries generality), vbsh-under-Case-C not exhaustive (guard removes regime), C should be computed on composed-surface diffs (canopy diff practical superset). Evidence curated: realtime_collaboration/design/proofs/{corridor_sufficiency_note.md, corridor_bruteforce.{py,json,log}, v0_pin.py, v0_{site500,synth}.json, v0_site500.log, common.py, raiser_repro.py, trajectory_probe.py} (synth_site 69M cache NOT curated — scripts self-contained, StubCache in-memory / v0_pin builds its own). Design addendum appended (7 binding corrections). NEXT: non-author scientific review of the proof note, THEN R4 Phase A dispatch |
| r4-proof-review | RT | R4 adversarial non-author review of corridor proof + V0 | r4-proof-review (read-only + /tmp/r4_review reproduction) | none (read-only) | APPROVE-WITH-CONDITIONS — Phase A cleared under 4 binding conditions | none | March verified line-by-line; Case D + guard attacked analytically (float32-rounding + accumulator attacks — all failed to break: step-1 gate target-locality EXACT right via UNSHIFTED vegdem2/a shadow.py:292; float32 counterexample impossible — half-ulp rounding dip < one-ulp step gap keeps the contradiction chain; no far-field coupling anywhere). REPRODUCTION: corridor_bruteforce.py rerun JSON byte-identical to author evidence (both 16-edit sequences, 20+20 clamped violations, corner 53, phase A 59/59 vs 19/59 raw worst 167); v0_pin site500 rerun 0 mismatches both stacks all scalars bitwise; raiser repro patch 34 cell (56,34) exact; cache cubes strictly {0,1} both sites. FINDINGS: MAJOR — vbsh cube NOT binary in general {0,1,2}: K=1 march (dz_1 > A_eff; 78° ring on coarse-pixel/low-relief — 4m-pixel science class; site_500 A_eff/pixel 28.9>=6.65 SAFE) + step-1 raise → accumulator zeroed at index==1 AFTER its only add (shadow.py:293) → final 2.0 → oracle folds 2w vs bits-refold 1w; CONSTRUCTED (60x60 4m pixels 12m building 6m/8m crown → 8 cells vbsh==2.0 patch 148); pack_svf/bitmask refuse LOUDLY = precondition not silent corruption → condition 4 (assert planes within {0,1}, else fallback). MINOR ×3: note §2 3-of-16 not 4-of-16; §5 clamped-raiser vbsh_flips=0 per own JSON; §9 guard pseudocode MISSING amplitude-changed conjunct — as written fires on every edit of steady-state clamped site (Case A safe 0 violations) silently voiding R4 on clamped science class → condition 1 exact predicate (A_eff changed AND bound>abs on smaller-A_eff scene; trichotomy exact; bound==abs safe strict >). INFO ×2: dz monotonicity PROVABLE (retires hedge); OOB-reads contribute-nothing false for min(a)<0 but scene-INVARIANT (safety survives); §2 table omits bush-branch reads (inert under assert); trajectory_probe crashes az==0 (author evidence unaffected). RESIDUAL ADJUDICATION: all §10 items BLOCK-Phase-A: no; new vbsh=2 residual blocks SHIPPING-without-condition-4 only. Conditions 1-4 recorded verbatim in design ADDENDUM 2; author amendment of the 3 note slips dispatched to r4-proof (doc-only). R4 Phase A UNBLOCKED — dispatch sequenced after r3a merge (worker/solver seam file collision) |
| r4-proof (amendment) | RT | proof-note review amendments (reviewer condition 5, doc-only) | r4-proof (worktree r4-note-fix) | proofs/corridor_sufficiency_note.md | MERGED 9539143 ff (worktree+branch removed) | 9539143 | All 3 factual fixes applied (3-of-16; clamped-raiser vbsh_flips=0 w/ analytic warning kept; §9 guard amplitude-changed conjunct + rationale sentence), each verified against corridor_bruteforce.json before editing. Both INFO clarifications written in: dz monotonicity hedge REPLACED by the proof (fl32(fl32(ds·s)·c), round-to-nearest preserves monotonicity — exactly what last-common-step split needs); OOB candidate-0 restated scene-INVARIANT w/ min(a)<0 exposure bounded by _check_baseline_amplitude. SELF-CAUGHT beyond the list (disclosed): initial §5 rewrite had wrong fold arithmetic "vbsh = 1 − vegsh = 1"; caught on diff review pre-commit; committed text has correct 1 − (bin(acc) − vegsh_final) yielding a real flip under raise-then-suppress. +34/−12, 1 file |
| r1-epochs | RT | R1 packet: 100ms EpochScheduler + SSE BroadcastHub + catch-up | r1-epochs (worktree agent-a93f8a29efab3497b) | realtime/{epochs,broadcast}.py NEW, store.py v8, routes_realtime.py, app.py, 3 test files | MERGED d5858fd (worktree+branch removed); review APPROVE-WITH-NITS; remediation MERGED 60b5e48 (lead-direct) | 736aaaa+e2b56ad | Failing-first (736aaaa red at base: 2 collection errors). EpochScheduler: 100ms tick, revision assignment INSIDE commit txn (scene_version+1 semantics → workspace_revision), epoch ends status='reducing' (frozen vocabulary), empty-epoch close 'closed' no-bump but counted, recover() re-drives non-open recoverable epochs (open ones stay with live cadence; re-drive re-broadcasts ops+revisions NOT conflicts). BroadcastHub: named SSE events canonical_revision/fast_revision/exact_revision/heartbeat, call_soon_threadsafe, closed-loop drop-safe (durable ops + catch-up). routes: GET events, GET operations?since_server_sequence=N zero-loss catch-up, POST acks alias (client wire contract from 845d017 verified: acks + canonical_revision payload shape). store migration v8 realtime_canonical_state PK(workspace_id,workspace_revision). 21+10 new tests; author ran server suites twice (no thread leak). LEAD post-merge fixes 1d14751: test-isolation — default-wiring test apps (operations make_rt_app + 2 default-wiring epochs tests) ran scheduler on module-shared telemetry singleton; real-thread epoch close polluted test_realtime_telemetry count asserts (racy solo, deterministic combined; bisected + registry-snapshot-probed). All test apps now on private TelemetryRegistry; combined realtime suites 3× green. Known-flagged for R2: legacy /edits bypasses op log (scene_version can advance outside epoch commits); status='reducing' consumed by R2 fast lane; /metrics → default_registry wiring |
| r1-epochs-review | RT | non-author probe-driven review of r1-epochs (3-angle attack) | r1-epochs-review (worktree) | none (read-only) | APPROVE-WITH-NITS 0C/0H/2M/3L/2T (2026-09-04) | none | M1 stranded epoch: transient fail between mark+commit strands ops outside every revision until restart; M2 stale-baseline: concurrent commit between baseline read + commit loses other epoch's effects from latest state; L1 SSE subscribe TOCTOU stale snapshot; T1 silent closed-loop drop; T2 bare asserts die under python -O; L2 docstring overclaims client catch-up; L3 revision contiguity unpinned. POSITIVES: revision txn airtight under probe attack; lead isolation fix verified both directions |
| r1-remediation | RT | lead-direct remediation of M1/M2/L1/T1/T2 + L2/L3 doc pins, failing-first | lead (main) | store.py epochs.py broadcast.py routes_realtime.py + tests/test_realtime_r1_review_fixes.py NEW | MERGED 60b5e48 | 60b5e48 | 6 tests RED at 1d14751 → 6/6 GREEN. M1: run_once stranded-epoch pre-pass oldest-first (no inversion); M2: commit_epoch_reduction baseline_revision= guard → typed EpochBaselineStale + 3-attempt re-fold; L1: post-subscribe re-read + privileged push_snapshot corrective frame; T1 debug log; T2 RuntimeError fences (ordering tightened STRICTLY increasing — sorted() non-strict missed duplicates) + epoch-None post-commit check; L2 broadcast docstring softened (missed_events forward-compat, r2c); L3 epoch_scheduler.md pins revisions may SKIP, contiguity not contract. Evidence: realtime suites 108×3 deterministic green; server api+main 99 green |
| r3a-met-fast | RT | R3a packet: MET utci-only fast path (affinity split, narrowed plans, sparse per-t patches) | r3a-met-fast (worktree agent-a3c47217fb2857ceb) | incremental/{edit_registry,adapters/met_time,planner,executor,result}.py, utci_process.py, tests/test_incremental_met_fast_path.py NEW (32) | MERGED ae0bccb (worktree+branch removed); remediation 144f6a1 red → 07cc660; re-review APPROVE | da8dc64+9340197+144f6a1+07cc660 | Affinity utci_only={wind,uhii} code-derived w/ line evidence; all-utci_only met-only ForcingDelta → sparse bitwise utci patches (changed-t only); 9 typed MetFastPathRefused conditions route full solve. Perf: 128x128 0.725s vs 3.76s establishing; site_500 0.870s vs 149.84s. Canonical 1176/6/0. Lead re-verified mandated suite 142/1-desel in worktree |
| r3a-review | RT | non-author adversarial review of r3a-met-fast (11 probes + independent perf/bitwise reproduction) | r3a-review (worktree) | none (read-only + /tmp drivers) | REQUEST-CHANGES (2026-09-04): 1C/2M/3L/3T; blocking=C1 only | none | C1: server bridge mis-scatters sparse patches — jobs.py:1414 _compose_current_state + executor_bridge.py:970-971 _bootstrap_state assume dense time_indices, ignore patch.time_indices → t=1 patch lands at global t=0, wrong served+durable; reachable via /edits/universal + store-restore arming. M1 planner narrowing bypasses SafetyPolicy.full_only_nodes; M2 docstring-claimed executor guard nonexistent; L1 drift guards pin keys not values; L2 negative control can't self-detect vacuousness; L3 masked-overlay thinning. CONFIRMED: affinity (no wind/uhii path into physics), sparse bitwise vs independent oracles, additive-only utci_process, perf reproduced 0.717s/0.840s under load 4.5-10; p99 claim unsubstantiated (single samples) |
| r2a | RT | R2a packet: exact lane v2 — revision-chasing queue, epoch consumption, legacy funnel | r2a-exact-lane (worktree agent-a7c33ce1491dfcdee) | server/{jobs,executor_bridge}.py + tests/test_realtime_r2_exact_lane.py NEW (12) | MERGED 03d58e2 (worktree+branch removed); review REQUEST-CHANGES (fast-follow on main); remediation r2a-fix RUNNING; post-merge canonical 1230/6/0 | b493ac6+ed5b4ec+667cc5d+c060964 | Funnel in JobRunner.submit() (zero route edits, gated on existing realtime ops); LEGACY_ACTOR_ID no-double-apply (scene effects ride edit_events replay); exact jobs target scene_version via _insert_job_locked private seam; one-running+one-latest-pending; RuntimeError zero-loss tripwires (span=contiguous ops, per-op revision bounds, fold baseline exists); consume-before-finish heals crash windows; epoch settlement to exact_targeted on legacy completion; segment families fold-only SKIPPED WITH DISCLOSURE (metrics families_skipped) — reviewer must adjudicate; reset not funnelled (disclosed); canonical 1194/5/0 |
| r3a-remediation+re-review | RT | C1/M1/M2/L1/L2 remediation (author) + focused non-author re-review | r3a-met-fast (author) + r3a-review (re-review) | jobs.py+executor_bridge.py scatter sites, planner policy consult, affinity value pin, anti-vacuity fence | APPROVE (2026-09-04): C1 CONFIRMED-FIXED (fallback boundaries attacked 4-cases, consumer sweep re-grepped — only 2 .arrays readers, both fixed; E2E regression verified genuinely red at base), M1/M2/L1/L2 all CONFIRMED-FIXED; 3 trivia non-blocking | 144f6a1+07cc660 | Canonical at 07cc660: 1180/6/0; site_500 bitwise re-pass 134.86s/0.863s post-remediation |
| r2a-rebase | RT | r2a merge integration onto main ae0bccb + sparse-patch regression | r2a-exact-lane (author) | tests/test_realtime_r2_exact_lane.py +1 | COMPLETE (author); auto-merge zero conflicts (r2a never restructured the two r3a-touched functions) | 667cc5d+c060964 | test_compose_scatters_sparse_extra_patch_at_declared_timesteps fences the C1 dense-offset mis-scatter at the compose seam (direct; r3a covered e2e only); canonical at branch 1230/6/0 (1189.01s) |
| r2a-review | RT | non-author adversarial review of r2a (9 probes + 7 suite reruns, bitwise evidence) | r2a-review (scratch clone /tmp/r2a-review) | none (read-only; /tmp probes) | REQUEST-CHANGES (2026-09-04): 1C/1H/1M/4L/2T — arch SOUND (span derivation, supersession, epoch settlement, crash healing all PASS under attack) | none | C1 pre-gate blindness: native delete of pre-gate legacy object silently dropped — pristine initial_state baseline never saw legacy trees; 26,774 utci + 27,623 tmrt cells bitwise-wrong, UNDISCLOSED (not on gaps list); public-API reachable. H1: unmapped-only epoch wedges exact lane forever (empty batch ExecutorError → superseded → churn guard; epoch 'reducing' stuck, exact_revision permanently behind; verified 180s). M1: segment-skip disclosure metrics-only (manifest limitations unchanged). L1 no-op branch failure wedges; L2 funnel best-effort failure (C1-class blindness); L3 tick O(jobs) parse; L4 fence tests missing. CONFIRMED: no-double-apply (bitwise), crash healing, supersession, pure-legacy byte-identity, If-Match untouched, sparse fence flips test. Segment-skip adjudication: typed unsupported_family refusal at op-accept = preferred endgame (phased) |
| r2a-fix | RT | remediation of r2a-review findings (fresh packet, author worktree removed post-merge) | r2a-fix (worktree) | server/{jobs,executor_bridge}.py + tests | RUNNING | null | Lead ruling: C1 fix BOTH (a) gate-open canonical bootstrap from current legacy scene (deterministic+replayable, EpochBaselineStale-aware) AND (b) accepted-but-unfolded typed fence (never silent, must not wedge); H1 empty-batch=no-op republish+settle; M1 families_skipped → manifest limitations (interim; unsupported_family endgame deferred — needs store.py); L1 no-op branch settle-on-failure; L4 five fence tests. NO ACTION: L2 (covered by C1b), L3 (R8), trivia |
| r4a | RT | R4 Phase A: persistent veg SVF occluder state + corridor re-march + canonical re-fold | r4a-veg-svf-state (worktree agent-a1f4331146abd287b) | incremental/{veg_svf_state NEW 871ln, solver, worker}.py + tests/test_incremental_veg_occlusion.py NEW (36) | CODE-COMPLETE ff932de (red-first 04660c4: 34 failed at base); review RUNNING (worktree intact) | 04660c4+ff932de | All 4 binding conditions mapped to code+tests (condition map in handoff); DEVIATION from condition-3 letter: corridor march amplitude = POST-scene FULL-TILE effective amplitude (not windowed crop) — agent argues windowed is bit-equal at closure cells but VALUE-divergent in one-step regimes (vbsh 2.0 class) since fold consumes values; reviewer adjudicating. Fold refactor parity pinned by perf_wave2 18/18. Reviewer vbsh==2.0 counterexample reproduced exactly, fence fires at baseline pack + post-re-march. Canonical at branch 1253/6/0 (1208.90s). Perf 96x96@2m: corridor 0.38s vs full-tile veg replay 0.51s (153/153 patches re-marched — 6-deg corridors span ~57px; R5 chunk skip = follow-on lever). Publication seam TODO left for post-r2a server wiring (documented) |
| r4a-review | RT | non-author adversarial review of r4a (probe-driven, oracle twins, corridor brute-force) | r4a-review (resumable ae3d725cc8042ff65) | none (read-only; /tmp probes) | REQUEST-CHANGES (2026-09-04): 1H/2M/1L/1N — architecture/lifecycle/red-lines/corridor-∪C/guard-trichotomy ALL PASS; condition-3 deviation ADJUDICATED COMPLIANT-AS-IMPLEMENTED (windowed letter would import MEDIUM-2 latent defect; full-tile amplitude = superset steps, safe direction) | none | H1: condition-4 fence per-window but vbsh hazard per-REGIME — multi→one-step amplitude transition (unclamped dropper, guard correctly silent) leaves oracle vbsh==2.0 at UNCHANGED neighbors OUTSIDE re-marched windows (repro: 60x60@4m, 27m tree deleted, 324 cells patches 132-151; svfSaveg 0.0518 / svfWaveg 0.1408 SILENT); fix = refuse batch via VegOcclusionFallback on arithmetic eff_post < dz_1(p) anywhere + same check at baseline pack. M1: march_offsets(cols,rows) TRANSPOSED at veg_svf_state.py:583-584 (shadow()+proofs use rows,cols) — 91/306 pairs lose steps on non-square tiles. M2 (pre-existing W2, NOT r4a's): solver.py:1321-1323 replay windowed-amplitude value-divergence (166 vbsh==2.0 replay-vs-oracle) — separate packet. L1 perf framing: corridor 0.397-0.409s vs deployed-shape W2 replay 0.313-0.318s = ~1.3x SLOWER than shipped shape; win is vs FULL-TILE replay only. N1: fold constants unprotected (121 tests all shared-fold-vs-shared-fold; mutated trans 0.03→0.04, e-4→e-3 pass) — need oracle-anchor test. Lead verify: 121/121 green combined (304.97s) |
| r4a-remediation | RT | H1 regime fence + M1 arg order + ADDENDUM 3 + L1 framing + N1 oracle-anchor (author) | r4a-veg-svf-state (worktree agent-a1f4331146abd287b) | incremental/veg_svf_state.py, r4 design doc ADDENDUM 3, tests | RUNNING | null | Lead ruling: MEDIUM-2 DEFERRED to separate W2 packet (r4a state path is oracle-faithful); failing-first per protocol; remediation scope = H1+M1+ADD3+L1+N1 only |
| r4a-remediation (complete) | RT | H1 regime fence + M1 arg order + ADDENDUM 3 + L1 framing + N1 oracle-anchor | r4a-veg-svf-state (worktree agent-a1f4331146abd287b, HEAD eafde55) | veg_svf_state.py, worker.py comment, tests (36→42), r4 design doc ADDENDUM 3 | CODE-COMPLETE eafde55 (red-first b7604a8 reproduced reviewer construction — silent stale bits pre-fix); focused re-review RUNNING (r4a-review resumed) | b7604a8+eafde55 | H1: _patch_first_step_dz (dz_1 = ds·tan(alt)/scale, shadow() float32 branch arithmetic, step 1 always executes); refusal iff dz_1>amp_post AND dz_1<=amp_pre (multi→one-step; growth/constant never refuse); same check at build_baseline_state (value fence first). M1 one-line + spec pin + 40x80 regression. ADDENDUM 3 records condition-3 deviation rationale. L1: all claims "vs full-tile replay"; honest deployed-shape ~1.3x-slower statement; re-measured @load 47 (corridor 0.435-0.532 vs full-tile 0.554-0.674 vs deployed 0.333-0.339). N1: test_fold_anchors_p2_svf_calculator — fold-constant mutation now diverges. RESTRUCTURES flagged for re-review adjudication: transition tile bakes T into baseline canopy; counterexample test restructured to pack-time refusal + unit-level fence; non-square tile 12→28m. Canonical in worktree 1259/6/0 (1253.60s) = 1253+6 new; first run without PATH prefix had 2 env-only test_cli fails, rerun green |
| r4a re-review | RT | focused non-author re-review of remediation eafde55 (probes /tmp/r4b_dz.py, /tmp/r4b_oneshot.py) | r4a-review (resumed ae3d725cc8042ff65) | none (read-only) | APPROVE (2026-09-04): HIGH-1/MEDIUM-1/DOC/LOW-1/NIT-1 ALL FIXED-VERIFIED; restructures adjudicated COVERAGE-PRESERVED | none | Original 27m construction hand-driven: loud refusal, state not advanced, 324 outside-closure cells prove fence load-bearing. _patch_first_step_dz matches shadow() line-by-line; 456/456 boundary probes agree with march step counts; predicate 0-wrong over 144 pairs; growth/constant never refuse; pack order values-before-regime verified. M1 spec pin binds |dx|→sizex; 40x80 regression genuine (16 patches lose up to 27 steps under old transposition). NIT-1 mutation-verified by reviewer (trans 0.03→0.04 → anchor FAILS). NEW-LOW: dz_1 association 1-ulp off at non-power-of-two scales (adversarial-only window) — fixed in micro-pass df7e7d6. 2 T-nits (comment 23.18m; state-not-advanced pin) — both fixed df7e7d6 |
| r4a (merged) | RT | — | — | — | MERGED a2b29b4 (worktree+branch removed; post-merge canonical /tmp/r4a_merge_canonical.log); micro-pass df7e7d6 (17 insertions: association-matched dz_1, comment, state pin; 72/72) | 04660c4+ff932de+b7604a8+eafde55+df7e7d6 | Author self-correction: one_step_patch_indices(0.25,12.0) = 9 patches not 7 (no behavior dependence). Phase A CLOSED |
| r2b | RT | R2b packet: fast lane thread + admission + fast result classes v1 + /metrics wiring | r2b-fast-lane (worktree agent-ab2d2ad5f4847e8f3) | realtime/{scheduler NEW 731ln, admission NEW 516ln}.py, epochs.py TAIL HOOK ONLY, app.py, routes_realtime.py ONE sanctioned admission hook, tests/test_realtime_r2_fast_lane.py NEW (41) + test-isolation injections in 3 existing helpers | CODE-COMPLETE 9e3109b (red-first c80b1a3: 41 failed at base); review RUNNING (r2b-review, worktree intact) | c80b1a3+d095852+8f0ed6a+9e3109b | VOCABULARY GAP adjudicated: fast_published is TERMINAL + r2a counts terminal as exact-consumed → epoch advances at most reducing→fast_planned (non-terminal vs every consumer — review re-verifying sweep); fast completion in scenarios.fast_revision + in-memory state. Admission = epoch-coalesced booking (heaviest family + 2ms/op, NOT op-sum — op-sum rejected R1-legal 128-op batches); reject BEFORE durable append, 429 burst/503 server-wide + retry_after_ms. fast_revision = max(old,revision,exact_result_version) one stale-fenced txn. Envelope 1000ms; unmeetable → visual_pending WITHOUT kernel; fast_qualified RESERVED (downgraded+logged, never on wire). Lifespan stop fast FIRST. Canonical 1271/6/0 (1186.76s after singleton-pollution isolation fixes; 2 earlier runs 1200.85s/1144.56s had the fail). Risks flagged: fast-completion not on epoch row (R3/R7/R8 decision), booked cost constants need telemetry retune, 1 unreproduced load flake at load 13 |
| r2a-fix | RT | remediation of r2a-review findings | r2a-fix (worktree agent-a86728628a276704b) | server/{jobs +267, executor_bridge +196}.py + test_realtime_r2_exact_lane.py +748 | CODE-COMPLETE 460b290 (red-first 8056a71: 7 RED + 2 green fence pins at base 1ee0fd1); focused re-review RUNNING (r2a-fix-review) | 8056a71+460b290 | B1a PRIMARY: ensure_exact_lane_bootstrap pins rev-0 canonical row from UN-MIRRORED pre-gate legacy ledger (INSERT OR IGNORE first-writer, pin-before-job, tick-pins-before-BEGIN-IMMEDIATE, solve-time fallback); consumed_revision>0 RE-FOLDS chain from pin (legacy-blind scheduler rows never the baseline). B1b: _unfoldable_native_object_ops reducer-parity fence (objects∪tombstones), refusal before solve + anti-wedge settle-failed + re-arm next revision. B2: empty-batch intercept scoped to extra_commands_factory not None → no-op republish. M1: limitations disclosure both published + no-op re-serve (manifest copy via public publish_result — store.py off-ownership). L1: all no-op _finalize exits settle epochs. Canonical 1239/6/0 (1264.04s) = base 1230 + 9 new exact. Residuals: pre-gate prune → B1b refuses loudly (documented); native-op executor effects only in snapshots (pre-existing r2a boundary, flagged for store owner); manifest_override seam deferred |
| r2b-review | RT | non-author adversarial review of r2b (probe-driven, instrumented enqueue logs, forced interleavings) | r2b-review (scratch clone /tmp/r2b-review) | none (read-only; /tmp probes) | REQUEST-CHANGES (2026-09-04): 1H/1M/2L/3N — hard contracts ALL CONFIRMED (reject-before-accept, fence atomicity/monotonicity under forced race, tail-hook safety both paths, fast_planned starvation-free consumer sweep, fast_qualified never on wire, envelope honesty, test genuineness/isolation-preserving) | none | H1/F1: lane 100ms discovery CADENCE scan publishes fast_revision(R) between close's durable commit and canonical broadcast submission → SSE fast-before-canonical for same revision (ENQ_LOG instrumented proof; flake pytest 1/9, replica 2/40 at load 9-20; 20/20 with cadence disabled). Author's pre-existing-flake claim REFUTED — r2b-introduced. Fix: one-scan-grace for scan-discovered revisions (hook path already post-broadcast). M/F2: reserved_ms strips payloads → landcover costs never reserved (250ms under-booking measured); one-line payload passthrough. L/F3 joint maxima inadmissible (656ms booked vs 550ms budget) — LEAD: document. L/F4+N/F5: recover() re-broadcast phantom reservation + dead _published_keys — fix together (skip rev<=last-published via _published_keys). N/F6 raw store SQL accepted this wave (store owner: advance_fast_revision accessor). N/F7 retry throttle OK. Adjudicated CORRECT: dormant-lane admission, envelope constants, vocabulary-gap handling, revision triple EVENTUAL not immediate at rest (self-heals next scan; document) |
| r2b-remediation | RT | F1 one-scan-grace + deterministic ordering re-pin, F2 payload passthrough, F3 doc, F4+F5 published-keys skip | r2b-fast-lane (author resumed, worktree agent-ab2d2ad5f4847e8f3) | scheduler.py + admission docstring + tests | RUNNING | null | Failing-first; F1 ordering test must pass with cadence ENABLED at default 100ms |
| r2a-fix-review | RT | focused non-author re-review of r2a-fix (probes P1-P8, mutation-proven pins, scratch-clone suite reruns) | r2a-fix-review (scratch clone /tmp/r2afix-review) | none (read-only; /tmp probes) | APPROVE-WITH-NITS (2026-09-04): B1a/B1b/B2/M1/L1 ALL CONFIRMED-FIXED | none | Bootstrap race-safe (P1 two-thread: one rev-0 row byte-identical; P2 first-writer preserved), order-safe (P4 legacy-blind rows before pin → chain re-fold produced correct delete delta), no double-apply (P8 fully-mirrored history pins nothing). Fence reducer-aligned: no update verb can enter log (funnel maps update→replace); _OBJECT_UNFOLDABLE_VERBS exactly covers known-target-required verbs; P7 tombstone no false refusal; refusal precedes solve (exploding stub); re-arm verified (P6). B2 intercept scoped (legacy PlanningError survives); met-only wedge gone e2e. All _finalize no-op exits settle. Red-first genuine: 27,623 tmrt value diffs vs independent pure-legacy twin, NaN-aware oracle; pins non-vacuous by mutation. Suites 215 + 35/1desel in scratch clone (load 7.9-13.3). NITS: F1 docstring overclaim (solve-time re-derivation only consumed==0 — author one-liner extending ensure call BEFORE merge), F2 reset-after-pin leaves stale rev-0 row (pre-existing canonical reset-blindness class — STORE-OWNER packet), F3 stale metrics in manifest copy (comment sufficient) |
| r2a-fix-followup | RT | F1 ensure-call extension + F3 comment (author, pre-merge) | r2a-fix (author resumed) | executor_bridge.py + jobs.py docstring/comment | RUNNING | null | Merge after; F2 routed to store-owner packet |
| r2a-fix (merged) | RT | — | — | — | MERGED 46c51d3 (worktree+branch removed; post-merge canonical running /tmp/r2afix_merge_canonical.log) | 8056a71+460b290+fe2a5f3 | F1 hoisted: ensure_exact_lane_bootstrap called whenever canonical_state_at(ws,0) is None (before consumed fork — committed-chain blip unreachable); F3 intent comment; F2 docstring pointer to store-owner. Quick confirm 90 passed (128.63s) |
| r2a-fix post-merge | RT | — | — | — | CANONICAL GREEN on main 46c51d3: 1239/6/0 (1313.77s, load 15.40-22.63) = 1230 base + 9 new exact. R2a wave CLOSED | — | — |
| r2b-remediation | RT | F1 one-scan-grace + wire-order re-pin, F2 payload reservation, F3 docs, F4+F5 published-revision skip | r2b-fast-lane (worktree agent-ab2d2ad5f4847e8f3, HEAD 512329e) | scheduler.py + admission.py docs + tests (41→45) | CODE-COMPLETE 512329e (red-first ddaa7cb: 3 deterministic RED + wire-order flaky-RED 1/20); re-review RUNNING (r2b-review resumed) | ddaa7cb+512329e | F1: _PendingItem.from_scan/.deferred + _scan_grace ledger; first scan pass deferred; hook twins owe no grace, returned in place of redundant scan twin; grace releases on publish; cost <=100ms of 1000ms budget; self-heal one pass later. Wire-order re-pinned: 5 epochs, canonical index < fast index per revision, cadence ENABLED, 20/20 at fix. Author RETRACTED pre-existing-flake attribution (commit message + notes). F2: reserved_ms passes payload dicts (162ms vs stripped 152ms pinned). F4+F5: _published_keys stores PUBLISHED REVISIONS, hook skips rev <= last published; boot re-broadcast phantom test. F3 documented per lead ruling. Canonical 1275/6/0 (1333.40s, load 6.50-48.76 contention spike) |
| r2b re-review | RT | focused non-author re-review of remediation 512329e (probe-driven: grace partition, self-heal trio, skip monotonicity, bounds) | r2b-review (resumed) | none (read-only; /tmp probes) | APPROVE (2026-09-04): F1/F2/F3/F4F5 ALL CONFIRMED-FIXED — merge cleared | none | Grace closes race BY CONSTRUCTION: scan twins defer exactly once per (ws,rev); hook twins exempt + replace scan twin in-place; re-losing ordering now needs >100ms epoch-thread stall inside microsecond window WITH hook simultaneously lost (unrealistic; SSE convenience-never-correctness by contract). ENQ_LOG instrument 40 replicas + exact 25/25 failures=0 (was 2/40 at 9e3109b); wire-order 20/20 at load 10-16. Self-heal trio driven (lost wake / overflow max_pending=1 / lane restart) — all publish, zero loss, one grace pass max; deferred never blocks other workspaces (P4). F2 pinned 162.0 vs 152.0 deterministic (60s-cadence monkeypatch). F4 skip strictly-monotonic (cannot eat new work, published keys [1,2]); boot re-broadcast phantom pinned suite-level; F5 closed — _published_keys records revisions, evicted at 4*history (P6: history=2 → cap 8 over 12 pubs, latest preserved). Scope clean since 9e3109b (admission/scheduler/2 test files). Realtime block 166 (superset) + server 99 + fast_lane 45x2 |
| r2b (merged) | RT | — | — | — | MERGED 3384341 (clean auto-merge incl. exact-lane test isolation injection over r2a-fix additions); worktree+branch removed; post-merge canonical FAILED 1 at 1097 (-x stopped; detail lost to tail-4; triage running /tmp/r2b_merge_triage.log; load 22.75 during) | c80b1a3+d095852+8f0ed6a+9e3109b+ddaa7cb+512329e | Suspects: r2a-fix×r2b test-file semantic interaction vs load flake |
| r2c-frontend | RT | R2c packet: client fast-lane consumption (result classes, triple fences, missed_events, admission UX, demo badge) | r2c-frontend (worktree agent-r2c-frontend, HEAD 90baf1e) | examples/incremental_design_tool/{realtime_client.mjs, realtime_badge.mjs NEW, app.mjs additive, index.html, styles.css, README.md, 2 new test files} | CODE-COMPLETE 90baf1e (red at base client blob by revert-rerun; 21 new tests; 137 existing untouched green; 158/158; zero python); review RUNNING (r2c-review) | red@f90f0e0+90baf1e | Supersession fence: canonical watermark advanced ONLY by canonical/exact/catch-up; fast valid only for targetRevision; fastSequence monotonic; fast_qualified+unknown → conservative visual_pending. Reconnect: fast frames absent from catch-up is terminal-until-next-epoch (never waits). missed_events: gap fence + exactly-once flush + concurrent-cycle guard + failed-GET→full reconnect. F7: 429-retry reconciles via GET (durable→canonical fence w/ original server_sequence; non-durable→typed RealtimeAdmissionError). Demo: read-only third pill, scenario-id==workspace-id on realtime plane |
| store-seams | RT | 4 store.py seams (UnsupportedFamily phased refusal, republish manifest_override, advance_fast_revision accessor, reset rev-0 pin invalidation) | store-seams (worktree agent-store-seams, HEAD 52bce04) | server/store.py (+922/-70 class), jobs.py+scheduler.py mechanical swaps, tests/test_store_seams.py NEW | CODE-COMPLETE all 4 seams (60ea6bf, 900c6cf, eee9499, 52bce04; red-first each); canonical RUNNING | 60ea6bf+900c6cf+eee9499+52bce04 | Seam 1: gate AFTER duplicate-replay check BEFORE insert; supported = reducer._ALL_FAMILIES ∪ 9 adapter ids; batch all-or-nothing; historical rows still replay; transport vocab (7) superset-pinned never-refused; route translation UNREACHABLE today (transport refuses unknown first) — error.body() ready for follow-up. Seam 2: deepcopy + limitations merge-append; payload/checksum untouched. Seam 3: ONE _write stale fence → None (no write); twin-store parity pin vs verbatim r2b raw SQL; scenarios_with_fast_lag() typed read; grep-clean no raw SQL left in scheduler. Seam 4: rev-0 delete in SAME txn; re-bootstrap re-derives post-reset only. Targeted 107 + server_api/telemetry 86 green (flagged canonical test passes in isolation — environmental). Risks: jobs.py `or default_limitations` fallback dropped (store-side merge equivalent); canonical rows >=1 post-reset untouched (wider class, out of scope) |
| w2-amplitude-fix | RT/INC | MEDIUM-2 replay march amplitude + MarchRegimeError band fence | w2-amplitude-fix (worktree agent-w2-amplitude-fix, removed) | incremental/solver.py (+126/-6), tests/test_incremental_replay_amplitude.py NEW (13) | MERGED 03d80fa (+ lead F1 copy-on-lend 2f518dd); post-merge canonical running /tmp/w2_merge_canonical.log (expect 1339) | 1f9432d(red)+c8020e6(green) | Red: crop amp 10 vs full 12, band 3 patches dz_1 in (10,12], 24 value + 60 vbsh divergent cells, narrowed 252 vs full-tile 192 vbsh==2.0. Fix: amplitude from FULL-TILE scene tensors (same expression as oracle stop + veg_svf_state._scene_amplitude); ring_reach monotone-widening; fence refuses dz_1 in (A_eff,A_abs] → _solve_full. Parity: repro bitwise, one-step-served w/ oracle-legitimate 2.0 preserved, non-square at scale 1/3, canonical 1294/6/0. Copied dz_1 arithmetic (import circular; standalone replay requirement) — body-identical to veg_svf_state |
| w2-review | RT/INC | non-author adversarial review (fence-sufficiency attack, no-transfer, escalation analysis) | w2-review (scratch clone /tmp/w2-review) | none (read-only; /tmp probes) | APPROVE (2026-09-04): all 8 claims reproduced exact (counts 24=20+4, 60, 252/192, 3 band patches); fence condition EXACTLY the divergence condition — necessary+sufficient (skipped steps total no-ops on vegsh; acc positivity parity at dz_2+); wallheight_23 no-transfer CONFIRMED and structurally stronger (index-0 self step → immune to arbitrary amplitude pairs); r4a interaction clean (state path refuses one-step-at-A_eff superset) | none | F1 LOW: memo cache lends by reference (fixed lead 2f518dd copy-on-lend). F2 INFO: inherited 1-ulp boundary. ESCALATION ADJUDICATION (key): oracle stop is ABSOLUTE scene.amaxvalue (utci_process.py:573/:1235→:820/:1258), NOT A_eff — A_eff is the incremental early-exit approximation whose bit-identity claim MEDIUM-2 disproved at band patches. Escalating replay amplitude to scene.amaxvalue = executed step set IDENTICAL to oracle per patch → bitwise-safe BY CONSTRUCTION, no band. R6: prototype escalation REPLACING refusal (keep MarchRegimeError only if escalated banded marches cost > full-tile solve); NEVER escalate to A_eff. Note: time-loop caller solver.py:1734 marches at window-crop amplitude — immune today ONLY via wallheight_23 lacking the acc quirk; future wall-height-march changes adding the quirk inherit unfenced MEDIUM-2 |
| r3b-review | r3b cheap-exact audit (2ad884f) | APPROVE | 2026-09-04 | All 14 param affinities spot-verified vs source (Fside sqrt→0.25 power solweig.py:2299; transVeg/anisotropic_sky/height confirmed); drift guard mutation-proven on all 3 axes (AdapterRegistryError at import); additive-only (edit_registry.py +110, new test file); store-lever mechanism CONFIRMED (publish() retention store.py:417-426 whole-entry supersession, no cell-merge) — routed to R5; 1 cosmetic LOW (height evidence :786 import vs :817 call — fixed post-merge c9d1758). 97 green in review env. |
| lead | r3b merge d34f65b + evidence fix c9d1758 | merged+canonical-running | 2026-09-04 | r3b-cheap-exact @ 2ad884f merged --no-ff (2 files, +330); worktree+branch removed; height evidence line corrected to call site :817. Canonical PID 46694 (full-log /tmp/solweig-canonical/r3b-merge.log, load 8.07), expect 1346/6/0. R3 (#50) closes on green. |
| store-seams | 4 seams canonical @ 52bce04 | 1344 passed / 1 failed (telemetry singleton) / 6 skipped, 21:10 | 2026-09-04 | Failure ancestry-verified PRE-EXISTING: base ba617ab predates main-side isolation fix 8a655b7; passes isolated + in fixed main canonical; NOT a seam regression (attribution accepted with evidence, unlike r2b precedent). review_ready. |
| lead | store-seams review dispatched (store-seams-review, non-author) | in-flight | 2026-09-04 | Probe-driven review of 60ea6bf/900c6cf/eee9499/52bce04 + mechanical swaps; merge-tree conflict check vs main c9d1758 requested. |
| lead | R5a retention lever dispatched (r5a-retention worktree) | in-flight | 2026-09-04 | Cell-remainder retention in incremental/store.py TemporalResultStore supersession — restores FULL coverage after windowed batch over full-tile entry (r3a durability lever, r3b-review-confirmed mechanism at incremental/store.py supersession branch; executor.py:1576-1579 refuses at PARTIAL). Red-first, 7 invariants, canonical full-log. NOTE: lever lives in solweig_gpu/incremental/store.py (NOT server/store.py as earlier evidence line suggested — corrected). |
| lead | r3b post-merge canonical GREEN — R3 CLOSED | 1344 passed / 6 skipped / 0 failed, 21:41, load 8.13 | 2026-09-04 | Expected-count discrepancy resolved: "1346" derived from misrecorded prior base; r4b worktree canonical (base 2d2bf32 +5 tests = 1344) proves base counts drifted -2 across r2b/r2a-fix consolidation. Diff 64618e9..c9d1758 clean (2 files). #50 closed. |
| r4b-proof-gate | V4 op-sequence proof gate @ 1594d17 | code-complete + canonical 1344/6/0 EXIT:0 | 2026-09-04 | thorough: 42 cases, 7 sites, 378 steps; refusals 119 = 57 necessary + 62 conservative (38 clamp C + 24 regime HIGH-1) + 0 FALSE; roundtrip 42/42; defects []. Files: proofs/v4_opsequence_gate.py +1198, tests/test_incremental_veg_occlusion.py +172 (42→47). Report pending; evidence read from disk by lead. |
| lead | r4b review dispatched (r4b-review, non-author) | in-flight | 2026-09-04 | Oracle honesty, refusal-adjudication soundness (necessary/conservative/false taxonomy), bitwise comparators, seeding, invariance semantics, scale honesty. |
| r5a-retention | first pass @ 97bf781 (RED 8837076 + GREEN) | canonical 1353 passed / 4 failed / 6 skipped | 2026-09-04 | 2 = test_cli env-known (venv PATH, sanctioned); 2 REAL: uc8 test_intersecting_windows_are_still_superseded + ud test_partial_overlap_reader_falls_back_not_serves pin the OLD entry-granular contract (u-c1-review R2) that cell-remainder retention intentionally supersedes. Remediation dispatched: update both to cell-granular contract WITH value-correctness assertions (composed serve bitwise == new-patch in batch windows + rev-N in retained remainder; batch-window cells never resolve old; phantom-gap pin kept). Report was never sent — demanded. |
| r5a-retention | remediation 357cdbe + canonical rerun GREEN | 1357 passed / 6 skipped / 0 failed, EXIT:0, 22:53, load ~4.5 | 2026-09-04 | test_cli exercised (PATH set). Contract re-pins green. Idle-without-handoff corrected twice (canonical rerun demanded). |
| lead | r5a review dispatched (r5a-review, non-author) | in-flight | 2026-09-04 | Subtraction correctness, narrowed-window patch serving safety (payload slice semantics), value-correctness of composed serve (window-planner superset contract citation required), re-pin adjudication (does a PARTIAL case still exist), invariant regressions. |
| lead | reviewer fleet died silent — respawned with 45-min timeboxes | in-flight | 2026-09-05 | Evidence: r4b-review scratch last write 21:49 (died post-suite-rerun), r5a-review 23:06 (died post-probe-writing), store-seams-review + r5b-design no scratch + pings unanswered 40+ min. All stopped; r4b-review2 / r5a-review2 / store-seams-review2 / r5b-design2 spawned with timebox + partial-report mandate. Protocol note: long-review prompts without timebox correlated with silent death; timebox + "report partial as partial" added. |
| lead | store-seams LEAD-DIRECT review + merge 02b1be1 | APPROVE → merged | 2026-09-05 | Fleet reviewers died twice silently; lead-direct review (lead authored none of the seams — non-author satisfied). Verified: refusal sited after replay-continue before sequence/epoch/insert (no stranded epoch, all-or-nothing); deepcopy source+override, store-owned keys set last; accessor single-txn, stale fence None handled at call site, caller-exact term subsumed by monotonic exact; reset rev-0 pin delete same-txn. NB (non-blocking): limitations merge-append no dedupe; dropped `or default_limitations` fallback dead-in-practice (build_manifest always stamps limitations key — verified patch_codec.py:271 + jobs.py:512/567); reset rev>=1 canonical rows = disclosed open class (R5/R8 backlog). 19/19 seam tests re-run green in worktree pre-merge. Post-merge canonical PID 55431. R2 (#49) closes on green. |
| lead | r5a LEAD-DIRECT review @ 357cdbe | APPROVE (merge pending canonical) | 2026-09-05 | r5a-review + r5a-review2 both died silently; lead-direct review (lead authored none). Verified: `_subtract_windows` = `_missing_rectangles(_covered_mask(...))` shared decomposition with window_coverage (single subtraction semantics); identity preserved when remainder == original; zero-area intersections remove nothing (correct — edge-touch). Executor payload slicing verified safe for narrowed entries (executor.py slice loop indexes rows/cols relative to entry.write_window). Re-pinned tests carry value-correctness (composed serve bitwise == new patch in batch windows, rev-N retained in remainder, phantom-gap pin kept). SCOPE CORRECTION (author honest in code comment): restores COVERAGE FULL, NOT the met fast-path serve — executor `_run_met_utci_fast_path` freshness gate still refuses mixed-provenance {N, N+1} (single-revision requirement, r3a). Full fast-path restoration needs gate relaxation with per-cell-latest-validity proof (window-planner superset contract: batch write windows ⊇ causal impact ⇒ retained rev-N cells are newest for their cells). Follow-up lever registered to R5. Canonical 1357/6/0 EXIT:0 (22:53). |
| lead | r4b LEAD-DIRECT review @ 1594d17 | APPROVE (merge pending canonical) | 2026-09-05 | r4b-review + r4b-review2 both died; lead-direct review of full 1198-line harness + 172-line tests. Verified: oracle twin independent (fresh full-tile `_recompute_veg_svf_window`, state path never calls it); guard kill-switch monkeypatches exactly 3 advisory guards, bush correctly structural/non-disableable; `_refusal_predicate_held` recomputes from primitives (hunts ADDENDUM-2 MAJOR misfire class — clamp on amp-constant pair → False); `output_matches` requires planes AND fold bitwise (vbsh==2.0 packs to bit 1 — fold comparison makes unrepresentable bits strictly necessary); `absorbed` (bits differ, fold equal) correctly not false-refusal; refusal non-advance compares copied pre-call bytes (no aliasing); serialization roundtrip mid-sequence AND across refusal boundary (fresh store per case, mkdtemp); composition invariance catch-up matches production lag semantics; determinism = default_rng(seed) + lazy batch gen; honest non-coverage documented (≤10 trees, ≤96×96, dem≥0, solver seam pinned by r4a fine-site). Evidence: thorough JSON 42 cases/378 steps/0 false/0 defects; canonical 1344/6/0 EXIT:0. Non-blocking cosmetics: `invariance_ok` startswith-or-eq redundant; one vacuous `absorbed>=0` assert (data still recorded). |
| lead | R5 design brief lead-authored + G1.0/G2.0 dispatched | in-flight | 2026-09-05 | r5b-design + r5b-design2 both died silently (no artifacts anywhere); brief lead-authored: design/r5-temporal-checkpoints-and-fast-serve.md (G1 per-cell-latest-validity gate relaxation gated on G1.0 superset differential; G2 temporal checkpoints + sparse replay). KEY GAP identified: r5a re-pins prove store semantics only — no test asserts rev-N remainder values == N+1 full recompute (the superset property); G1.0 exists to prove exactly that. g1-superset dispatched (worktree agent-g1, branch g1-superset-diff BASED ON r5a tip 357cdbe — retention is a prerequisite, stacked to run parallel to merge; owns 2 new files only). g2-checkpoints dispatched (worktree agent-g2, branch on main @ bb74544; owns checkpoints.py + tests + ≤1 small hook). Both 90-min timeboxed, partial-report mandate, CPU-polite (main canonical PID 55431 still running). Commits: bb74544. |
| lead | reviewer fleet RECOVERED — delayed burst, 5 reviews all APPROVE | APPROVE ×5 | 2026-09-05 | Fleet was not dead: reports delivered in a routing-lag burst (01:03-04:48). All reviews APPROVE, zero blocking. CROSS-CONFIRMATIONS of lead-direct verdicts: r4b-review reproduced suite/thorough evidence (suite byte-identical excl wall_seconds) + full canonical 1344/6/0; r4b-review2 instrumented thorough rerun reproduces author thorough.json exactly (57 necessary ALL via divergence, 0 via exception path) + full suite @357cdbe 1357/6/0 confirmed; r5a-review ran 300-round randomized property probe vs brute force (0 failures) + full suite green; store-seams-review ×2 probe-verified transactionality/deepcopy/mechanical swaps (merge-tree CLEAN, byte-identical SQL parity). NEW NONBLOCKING INTEL — r4b harness hardening backlog (post-merge fixup, worst case loud-not-silent): (1) classify_refusal order bug — fence reason string contains "one-step regime" so any fence refusal misclasses as "regime", fence branch dead code (v4_opsequence_gate.py:398-407); (2) necessary-via-exception conflates any surviving refusal as necessary — harden by classifying the continuation exception reason, defect if not bush (0/57 today); (3) stale /tmp/r4b_proof/smoke.json predates final harness (roundtrip not_run 5/7 vs code 7/7) — regenerate; (4) GATE_SCALARS dead; totals missing both_refused bucket + explicit necessary count. r5a: seen_patches dedup trap QUANTIFIED by r5a-review2 probe D (relaxed gate + kept dedup ⇒ 384/576 cells silently NaN) — G1.1 brief updated: dedup fix MANDATORY same-change. store-seams open classes: (a) pre-reset epoch ops re-enter exact-lane chain-re-fold baseline (executor_bridge.py:963-980, epochs.py:272-275) — pre-existing, seam strictly improves; (b) bare-string limitations override iterates per-char (store.py:2558-2574, probe-confirmed; only list-passing callers today); (c) reset rev>=1 rows (already disclosed). All routed to ledger as disclosed backlog; no merge blocked. |
| lead | R5 brief updated from two converged agent design briefs | updated | 2026-09-05 | r5b-design + r5b-design2 (both survived after all — delivered complete file:line-verified briefs) converged INDEPENDENTLY on: exact 6-plane+4-scalar payload (Tgmap1 family + TgOut1, utci_process.py:614-619; Twater/CI midnight recompute :790-805); input-digest fingerprint NOT revision-keyed (_check_baseline_amplitude interaction); full-tile-only persistence (write-extent states are crops of elementwise chain); warm-start helps only ACROSS jobs (each write window needs its own full series); default-None bit-identical plumbing; first consumer = r3a REFUSED branch for met-rad edits (~24→~4 steps at site_500 row-20). Canonical brief r5-temporal-checkpoints-and-fast-serve.md updated (G1.1 mandatory dedup fix; G2 payload/fingerprint/extent/consumer). g2-checkpoints agent sent design supplement. LEAD PROCESSING BUG (self-disclosed): worktree agent-g2 created NESTED in agent-g1 by relative-path error; stray ledger commit 4082990 landed on g2-checkpoints branch — repaired (main ff'd to 4082990, branch reset to bb74544, worktree relocated top-level, agent notified + supplemented, no agent work lost). |
| r4b-proof-gate | hardening @ e9eb55d (post-review, pre-merge) | APPROVE (lead-direct) | 2026-09-05 | All 5 review fixes verified in diff: classify_refusal keyed on real vocab with documented ordering (clamp first — its string contains "regime"; "non-binary" beats "one-step" for fence); continuation exceptions classified, non-bush = harness_integrity defect (cont_refusal binding safe — defect f-string only reachable when except fired); GATE_SCALARS dropped; necessary_refusals + invariance_both_refused buckets in summary+totals; smoke.json regenerated roundtrip 7/7. Behavior-preserving: smoke/suite refusal counts identical pre/post (8/12 conservative, 0 false, 0 defects); suite 83.0s; tests 48 (47+1 classification probe) 137s green. Branch r4b-proof-gate @ e9eb55d = merge head (supersedes 1594d17). Merged behind canonical. |
| lead | R2 CLOSED + r5a/r4b MERGED + R4 CLOSED | merged | 2026-09-05 | store-seams canonical GREEN: 1363 passed / 6 skipped / 0 failed (exact expected 1344+19), PID 55431 EXITED — #49 R2 closed. Merges (both clean, no-ff): f6f7c89 r5a-retention @ 357cdbe (store.py +174/-75, retention tests +315, uc8/ud re-pins; 3-way APPROVE: lead-direct + r5a-review + r5a-review2) and 4c0a94e r4b-proof-gate @ e9eb55d (V4 gate +1220, veg_occlusion tests +202 incl. hardening probe; 3-way APPROVE: lead-direct + r4b-review + r4b-review2). Worktrees agent-r5a/agent-r4b removed, branches deleted. R4 (#51) COMPLETE: r4a (single-batch suite) + r4b (op-sequence gate) both on main. Post-merge canonical PID 71686 (/tmp/solweig-canonical/r5a-r4b-merge.log), expect ~1380 (1363+11 retention+6 V4). g1-superset worktree unaffected (branch g1-superset-diff holds 357cdbe, now reachable from main via f6f7c89 — trivial rebase or clean merge when ready). |
| lead | fly demo redeployed on merged main (R2/R4 build) | deployed | 2026-09-05 | flyctl deploy -c fly.demo.toml --build-target demo --remote-only → https://solweig-rt-demo.fly.dev/ machine d8d5620b77e958 updated, DNS verified. Smoke: /api/v1/capabilities 200 (adapters/edit_families/fences/locality_modes/view_only present); first post-wake request 502s (suspend cold-start race, retry ~20s — noted in memory); /api/capabilities and /health/ready 404 on demo build (readiness probe = capabilities). Build now carries mixed-traffic lanes + store seams + retention + V4 gate. |
| g2-checkpoints | G2.0 COMPLETE @ e4ec099 (RED c580c84 → module 86086a1 → hook e4ec099) | review dispatched | 2026-09-05 | CheckpointRecord: scene_revision + time_index (None = coverage-only today; thermal state = after timestep k, resumes k+1) + per-(kind,t) coverage manifest from store windows (patch paths REFERENCED not copied) + thermal tensor slots (Tgmap1 family + TgOut1 + CI/firstdaytime/timeadd/timestepdec + Twater, per-tensor sha256, SCHEMA_VERSION=1). Atomic write; torn/corrupt = typed CheckpointError; load_latest falls back to previous valid; rotation keep-3 + rev-0 base exempt. Hook: executor _record_outcome post-publish +21 lines, best-effort (CheckpointError/OSError swallowed — reproduce aid never fails published batch), no-op/supededsed return before hook. 27/27 tests red→green (bitwise roundtrip incl NaN/-0.0, torn classes, rotation, atomicity, hook). Canonical 1390/6/0 EXIT:0 (/tmp/g2-canonical.log) = base 1363+27 exact. Flagged deviations for review: return-dir-not-root; load raises vs load_latest None; rev-0 rotation policy. Non-author review dispatched to r5a-review2 (idle, deepest executor/store context). Base bb74544 pre-dates r5a/r4b merges — merge-tree check requested vs main 4c0a94e (expect clean, executor.py disjoint from both merges). |
| lead | post-merge canonical GREEN — r5a+r4b merges validated | 1382 passed / 6 skipped / 0 failed, EXIT:0 | 2026-09-05 | /tmp/solweig-canonical/r5a-r4b-merge.log, PID 71686, 23:50 wall. Count EXACT: 1363 + 13 (r5a: 11 retention + 2 pin-rewrite params) + 6 (r4b: 5 V4 + 1 classification probe). No drift, no lost tests. Main @ 4c0a94e = clean merge state with R2/R4 closures, retention, V4 gate. |
| g1-superset | G1.0 PROVEN @ b8075c7 (RED 1f08cdf → harness b8075c7) | review dispatched | 2026-09-05 | VERDICT PROVEN — window-planner superset property HOLDS: composed serve (rev-N+1 batch windows + retained rev-N remainders) bitwise == independent full-tile recompute at N+1, tile-wide. Thorough: 9 cases (3 sites x 3 seeds), 54 steps, 39 windowed probes (real PlanExecutor, default routing — nothing hand-crafted), 486 serves, 351 mixed-provenance {N,N+1}, 7,796,736 cells (97,632 NaN-aware), 0 defects, 0 undershoots, exit 0, 256.5s (/tmp/g1_proof/). Suite 3/81 mixed/1.7M cells. Probe verbs: 21 add/8 move/6 delete/4 resize; windowed-vs-full routing via real height gate. Owns 2 new files only (harness + wrapper, 3 tests, non-vacuity asserts). Canonical 1358/6/0 EXIT:0 (/tmp/g1-canonical.log) = 1355 base (test_cli excluded) + 3 — reconciles EXACTLY vs r5a's 1357-with-cli; no drift. First canonical attempt killed (wrong cwd — nested agent-g2 residue), rerun pinned to worktree root, log header verified. Honest non-coverage: single-tile, vegetation-family only (building/met route full by design), <=160px @ >=4m. G1.1 (gate relaxation + MANDATORY seen_patches dedup fix) now admissible — dispatch sequenced AFTER G2.0 merge (executor.py one-writer). Non-author review dispatched to r4b-review2 (oracle independence, real-window claim, comparator, vacuity, determinism, merge-tree vs main). |
| lead | G2.0 review APPROVE + merged 0400377 + WIP-loss incident | merged | 2026-09-05 | r5a-review2 APPROVE (0 blocking, 6 nonblocking; pristine git-archive review — flagged worktree DIRTY with the agent's out-of-scope G2.1 WIP). Merge-tree clean (tree dab8a9c6), merged --no-ff e4ec099: checkpoints.py +614, executor hook +21, tests +503 (27). Nonblocking hardening queued for post-merge packet: EOFError in np.load except tuple (:556), hook catch breadth except Exception w/ log (executor.py:2369-2374), tensor-checksum-map digest folded into payload digest (silent tensor-shed defense, probed by reviewer); INFO: same-rev rewrite window untested, .tmp staging leak cosmetic. INCIDENT (lead error, self-disclosed): sent stop-and-stash order then removed the worktree within a minute — agent had not acted, stash never created, its +159/-7 G2.1 test WIP LOST. Second lead sequencing error this session (first: nested worktree creation). Mitigations: loss bounded to unreviewed WIP (G2.1 not yet dispatched; its author writes tests against real plumbing anyway); agent stood down with acknowledgment; task #67 (agent-created supplement task) deleted. Branch g2-checkpoints deleted post-merge. G1.0 review still pending (r4b-review2); G1.1 dispatch queued behind its merge (executor.py one-writer now free post-G2.0-merge). |
| lead | incident DOWNGRADED: agent WIP recovered | recovered | 2026-09-05 | g2-checkpoints branch was at 1d25d95 (not e4ec099) at deletion — the agent HAD committed its supplement as a RED test commit before stand-down. Recovered from reflog as branch g2-supplement-wip: 1d25d95 test(checkpoints): R5b supplement RED — fingerprint, next_step, canonical payload, extent fence (+159/-7, tests only, no module code). Zero work lost. Branch preserved as red-first starting point for the G2.1 packet. Agent notified with correction. |
| lead | G1.0 review APPROVE + merged 67f7507 + G1.1 dispatched | merged+dispatched | 2026-09-05 | r4b-review2 APPROVE (0 blocking, 4 nonblocking): determinism reproduced byte-identical (suite 47.8s, smoke 12.6s); oracle independence verified by code read (fresh scratch per differential, site rasters read-only, no store reads); real-window claim verified (planner mode classification, only full-tile READ windows constructed by harness); serve fidelity == executor semantics minus the two gates under relaxation; RED honest (3 collection errors at 1f08cdf). Nonblocking: per-site vacuity assert stronger than totals-level (mitigated: windowed-in-name-only detected, 0 occurrences); paint order = recorded order not ascending (value-irrelevant under disjoint remainders); equal_nan wording caveat (no masking class, NaN counted); RED fixture kwarg drift (2-line, strength identical). Merged --no-ff b8075c7 (harness +869, wrapper +120); worktree agent-g1 removed, branch deleted. Post-merge canonical PID 5275 (/tmp/solweig-canonical/g1-g2-merge.log), expect ~1412 (1382+27 G2.0+3 G1.0). G1.1 DISPATCHED (agent g1-relax, worktree agent-g1r @ 67f7507): per-cell-latest-validity gate relaxation + MANDATORY seen_patches dedup fix, red-first, G1.0 differential must stay green. Checkpoints hardening (EOFError tuple, hook catch breadth, checksum-map digest) queued into the G2.1 packet (executor.py one-writer). |
| lead | g2 supplement quarantine ACCEPTED — 8aa6978 preserved as G2.1 input | quarantined | 2026-09-05 | Agent disclosure verified against repo state (worktree agent-g2 @ 8aa6978; g2-supplement-wip @ 1d25d95 untouched; zero post-stand-down ops). Sequence reconstruction: agent detected worktree removal before the stop arrived, self-recovered the dangling RED (same object 1d25d95), rewrote module GREEN as 8aa6978 (SCHEMA_VERSION=2, FINGERPRINT_KEYS, next_step/input_fingerprint payload-digest-covered, THERMAL_SCALAR_NAMES, full-tile shape fence, fingerprint_mismatch_reason, select_replay_checkpoint; 37/37 + 128/128 adjacent pre-stand-down). NOT merged: SCHEMA_VERSION bump on a just-merged module + unreviewed — 8aa6978 sits exactly where the intended stash would, for the G2.1 packet (its author will be re-dispatched with the plumbing files granted; the green half joins the recovered RED). EVIDENCE NOTE: the agent's rerun canonical writes /tmp/g2-canonical.log — same path as the merged G2.0 evidence — the 1390 log is overwritten; evidence preserved via ledger row + reviewer's independent log-tail probe (r5a-review2 read it pre-overwrite). Count-drift discipline unaffected (counts independently derived). Agent will forward EXIT line only. |
| g1-relax | G1.1 COMPLETE @ cf06336 (RED 7363eef → fix cf06336) | review dispatched | 2026-09-05 | Per-cell latest validity in _run_met_utci_fast_path: old checks 2-3 (single-revision + revision==head) REMOVED → ascending scene_revision paint + newest_revision raster; raster used ONLY as completeness fence (unpainted cell → typed refusal; legit NaN VALUES still serve); seen_patches set → per-path load_patch cache dict (dedup the LOAD, never the slice — every entry paints own window; probe-D 384/576 NaN hazard closed). PARTIAL/ABSENT refusal unchanged. Tests: mixed-serve bitwise vs composed expectation + revision-head pins; 4-remainder dedup regression pin (anti-vacuity: window values differ from base); PARTIAL fence. Red 2F/9P → green 32P/6 deselected; G1.0 smoke 3P; canonical worktree 1412+2cli=1414 collected reconciled EXACT. SPEC CONTRADICTION found + resolved (lead-ratified): brief's literal formula (max_rev_raster == revision_at).all() is provably NO-OP for FULL-coverage sets (all-max==head ⟺ head windows tile ⟺ retention remainders empty ⟺ single-revision-at-head) — accepts exactly what old gate accepted, test 4a unsatisfiable. Resolution: serve mixed under G1.0 superset license + completeness fence (matches brief G1.2 mandate + r5a ledger gloss + probe-D rationale). Stale-vs-remainder cells store-indistinguishable — separation is exactly what G1.0 made unnecessary; undershoot = licensed residual fenced by the differential, not the gate. Minor: store.py:433 comment cited old strict gate (N3, lead post-merge fix). |
| r5a-review2 | G1.1 non-author review @ cf06336 | APPROVE (0 blocking, 4 nonblocking) | 2026-09-05 | ITEM1 no-op proof RATIFIED analytically (publish-reachable states: literal formula ⟺ old gate) + brute force (500 seeded reachable FULL states, 389 mixed + 111 single-rev, 2 seeds, 0 disagreements). Resolution = strictly stronger than literal spec; no store-constructible wrong-serve counterexample (wrong serve needs unpainted cell — impossible under FULL, fence is tripwire — or planner undershoot = licensed residual). ITEM2 chained superset COVERED: g1_superset_diff.py drives SEQUENCES (full-tile seed + windowed probes on ACCUMULATED mixed store, fresh independent full recompute at EVERY step); 351-mixed-serve thorough run is chained by construction; induction closes per-transition→chain. NONBLOCKING: N1 fence unreachable via public API (docstring clause — lead fixed); N2 pre-existing bare ValueError on patch lacking t (unreachable per recording invariant, unchanged exposure); N3 stale store comment (lead fixed); N4 same-revision overlap paint deterministic (stable sort). PROBES: RED@7363eef exactly 2F both full-vs-local (archive /tmp/g11_red); fix 32P/6desel matches claim; old-dedup reapplication makes BOTH new tests FAIL (anti-vacuity proven, /tmp/g11_olddedup); canonical FULL-log EXIT:1 2cli env, venv-PATH rerun 2P, 1420=1414+6 reconciled; consumers grep — no server/frontend parsers of reworded refusals. SURPRISES: fence provably unreachable (kept as tripwire); G1.0 harness STRONGER than dispatch summary (multi-step chains, per-step independent oracle). |
| lead | G1.1 merged 9a01484 + comment fixes b81d647 + G2.1 dispatched | merged+dispatched | 2026-09-05 | Merge --no-ff cf06336 (executor.py +73, tests +229/-59); docs-only main delta since 67f7507, clean. Lead-direct N1+N3 comment-only fixes b81d647 (fence labeled defect-tripwire-not-staleness-check; store retention comment updated refused-mixed → G1.0-licensed mixed serve). Worktree agent-g1r removed clean, branch g1-gate-relax deleted. Post-merge canonical PID 30396 (/tmp/solweig-canonical/g11-merge.log), expect 1414/6/0 (1412+2 net-new). G2.1 DISPATCHED (g2-checkpoints resumed, worktree agent-g2, new branch g2-g21-warm from b81d647): port quarantined v2 module 8aa6978 + reconcile 27 merged v1 tests; hardening folded (EOFError tuple, hook except Exception w/ log, checksum-map digest); default-None initial_state/return_final_state plumbing run_utci_window+solve_window; full-tile state capture committed at publication; first consumer = met fast path refused-branch met-rad edits (serve t<r0 from store, warm r0..T via input-digest fingerprint, one sparse patch time_start=r0); parity fences warm==cold + E2E==cold bitwise; 150-min timebox. |
| lead | main canonical GREEN post-G1.1 (1414/6/0); G2.1 code-complete, review dispatched | green+in-flight | 2026-09-05 | PID 30396 EXIT clean: 1414/6/0 (21:09) = 1412+2 net-new EXACT — G1.1 validated on main. g2-checkpoints G2.1 COMPLETE @ 8c94fc6 (branch g2-g21-warm, 11 commits: port 54919e5 v2 module + 4 RED/GREEN item pairs + node-id fix 2989898 + test adaptation 8c94fc6; 2186/129 across 13 files). Claims: warm plumbing default-None bit-identical; warm==cold bitwise incl day boundary; E2E composed serve == fresh full solve bitwise through cold→warm; physics oracle == physically-edited twin site bitwise; commit-at-publication only; fingerprint-mismatch = typed routing refusal + telemetry; perf = chain rows 1→20→21 solves 24/24/4 (met_warm_steps_solved). Author-found REAL BUG post-green: prefix gate queried store by VARIABLE name vs NODE id — shadow-carrying vocabularies never warm-routed (conservative); fixed via VARIABLE_TO_RESULT_NODE (same translation as write side) — that fix is what let the physics oracle route warm. Non-author review DISPATCHED r4b-review2 (90-min timebox): warm-slot completeness (payload vs solver reads at k+1), fingerprint off-by-one, anchor semantics adjudication, G1.1 gate untouched, port-commit fence hygiene, de-fanging check, red-first genuineness, canonical reconciliation. |
| r4b-review2 | G2.1 non-author review @ 8c94fc6 | APPROVE (0 blocking, 6 nonblocking) | 2026-09-05 | All 10 attack items PASS. WARM PARITY code-proven: loop-carried state == persisted payload exactly (solweig.py six planes only via TsWaveDelay_2015a; firstdaytime day-start/midnight only; CI midnight; Twater midnight recompute; timestepdec DERIVED never carried — v1 fixture error corrected by port). FINGERPRINT: candidate evaluated at ITS OWN next_step via shared thermal_fingerprint — load-bearing subtlety verified; structural next_step>r0 skip; mismatch/torn → routing + telemetry never error (empirically pinned tests 3/4). COMMIT-AT-PUBLICATION: capture cleared at run start, committed after _record_temporal_entries, superseded early-returns before hook; capture==cold bitwise (tobytes, warm_start:198). G1.1 GATE untouched (deleted-line audit: only reindent/comments/except-clause). Node mapping identical expression read:1874/write:2866. PORT dropped NO v1 fence (27→37→50 tests; 7 deleted lines = comments/fixture-correct/rename/per-manifest assertions). Adaptations not de-fanged (original contracts still asserted; real warm coverage on real-SVF site incl E2E tobytes bitwise). Red-first genuine (10F @a8b8038; 4F/1P @e98bf4e matching claim). Canonical reconciled exact (1458=1430+13+10+5, log not truncated). ANCHOR ADJUDICATION: state@T anchors structurally never warm-serve (next_step>r0 guard + full-prefix digest) — design-CORRECT conservatism; state@T = G2.2 crash-restart asset; re-anchor-at-r0 chain delivers the win (pinned: resume_step 20, checkpoint_revision 2). NONBLOCKING: timestepdec/dtime row-1 theoretical gap at next_step==1 (unreachable — overlay edits physical columns only; comment suggested); fingerprint recompose per candidate (perf-only, ≤4); test-3 spelling coverage unit-level only; cold-split-with-no-anchor deliberate (primes next anchor, docstring'd); full-solve anchors met-consumer-dead-weight (G2.2 value). Targeted rerun 65 passed. |
| lead | G2.1 merged 1371b7d + G2.2 dispatched | merged+dispatched | 2026-09-05 | Merge-tree clean vs main 8b637bf (docs-only delta); .omc hygiene nit verified ABSENT (not tracked at branch HEAD). Merged --no-ff (13 files, 2186/129). Post-merge canonical PID 1287 (/tmp/solweig-canonical/g21-merge.log), expect 1452/6/0 = worktree exact (docs add no tests). G2.2 DISPATCHED (g2-checkpoints resumed, worktree agent-g2, fresh branch from 1371b7d): crash-restart harness at seeded kill points (mid-solve, post-accept/pre-publish, post-checkpoint, mid-checkpoint-write torn, post-publish) — properties: zero accepted ops lost, revision monotonicity (no revision without published results), torn checkpoint checksum-reject → previous-valid fallback, restart determinism bitwise vs uninterrupted run; harness pattern g1_superset_diff/v4_opsequence_gate (proofs/ + pytest wrapper + /tmp evidence, seeded, bounded); review N1 timestepdec/dtime comment folded in. Worktree agent-g2 retained for G2.2; branches g2-g21-warm/g2-checkpoints/g2-supplement-wip deleted post-canonical-green. |
| lead | main canonical GREEN post-G2.1 (1452/6/0); G2.2 code-complete, review dispatched | green+in-flight | 2026-09-05 | PID 1287 EXIT clean: 1452/6/0 (23:26) = G2.1 validated exact on main. Branches g2-g21-warm/g2-checkpoints/g2-supplement-wip deleted (all merged/superseded; worktree agent-g2 retained for G2.2). g2-checkpoints G2.2 COMPLETE @ 6a45b55 (branch g2-g22-crash, 4 commits, pure additive +1522: harness g22_crash_restart.py +1342 subprocess os._exit kills at 7 real seams / wrapper 4 tests / worker.py +7 comment-only N1 fold-in). Property matrix suite 13/13 ALL PASS: zero op loss (final rev 5 == 5 accepted ops, per-op status/mode/r0 exact), revision monotonicity (newest snapshot revision => every store entry patch on disk checksum-loadable), no torn survivors (staged .tmp invisible; same-revision rmtree two-crash cross-process chain leaves never-hybrid; corruption 2 variants typed reject -> previous-valid fallback -> bitwise), restart determinism bitwise (2,555,904 cells suite-scale, equal_nan). NON-VACUITY: warm_restarts=10, torn_rejects=2, staged .tmp + .staging observed, mid-series real step count. ZERO production defects — one harness assertion tightened to provable form (resume_step may only RISE, only from replayed op's own self-anchor — after post-checkpoint kill the restart legitimately warm-resumes from the dead run's anchor; idempotent-replay convergence, science bitwise; harness-scope only, no gate widened). Non-coverage honest: Python-seam not SIGKILL, op log harness-owned (R8), one site shape, no windowed-local veg (all FULL at this tile, GVF dirty 0.47>=0.30 measured). Canonical worktree 1456/6/0 EXIT:0 = 1452+4 exact. Harness intel for R6/R7: tree edits route FULL on small tiles once GVF write margin inflates window. Non-author review DISPATCHED r5a-review2 (75-min timebox): kill-seam reality, property-2 depth attack, tightening adjudication, torn seams, non-vacuity mutation, non-coverage adequacy, worker comment-only, merge-tree. |
| r5a-review2 | G2.2 non-author review @ 6a45b55 | APPROVE (0 blocking, 3 INFO) | 2026-09-05 | All 8 items CONFIRMED. KILL SEAMS REAL: mid-solve wraps real Solweig_2022a_calc counting real calls; NOOP_EXIT 71/UNREACHED calibration channel distinguishes seam-cannot-see-op from success (suite mid_series_confirmed=3); staged_pre_rename fires on rename dst ^rev-\d{6}$ (staging .tmp never matches); two-crash chain real cross-process (child2 real subprocess restore+replay, real rmtree before death); all 13 kill_fired. PROPERTY 2 DEPTH adjudicated SUFFICIENT: restart-consumption-scoped bytes-on-disk + detector non-vacuity PROVEN by tamper probe (deleted rev-1 patch dir -> snapshot_patch_missing; clean=None before); stronger invariant unconstructible through commit ordering (patch rename-in -> store.publish -> checkpoint -> snapshot; every durable revision claim written post-patch); property-4 FULL-coverage gate backstops any hole into refusal. TIGHTENING SOUND: 6a45b55 harness-only (71+/35-); RISE proof requires anchor_rev == replayed op's own revision, coverage-only skipped, next_step>r0 skipped; decrease = hard defect; op4 scenario confirmed in oracle evidence (oracle COLD resume 0 vs restart warm 3). MUTATION both directions (neutered seam -> kill_did_not_fire FAIL child_rc 71; detector tamper probe). worker.py 7 lines = entire solweig_gpu/ diff. Runs: wrapper 4/4 191.68s; smoke 9/9 (warm_restarts=8, torn=2, 1.77M cells) matching claim; canonical 1456/6/0 collected 1462 full-log EXIT:0; merge-tree clean single tree oid 625d4bad. INFO residuals: N1 rewrite-path staged window composition-covered not direct; N2 0.47 dirty-fraction figure not in evidence JSON; N3 property-2 newest-snapshot scoping (by consumption, not a gap). SURPRISES: warm-resume-across-death the NORM (10/12 restarts warmed — G2.1 parity fence is the load-bearing invariant, this harness its crash-domain proof). |
| lead | G2.2 merged 316084a — R5 closes on canonical | merged | 2026-09-05 | Merge-tree pre-verified by reviewer; merged --no-ff (+1522: harness 1342, wrapper 173, worker docstring 7). Worktree agent-g2 removed clean, branch g2-g22-crash deleted — R5 author fleet fully retired. Post-merge canonical PID 35726 (/tmp/solweig-canonical/g22-merge.log), expect 1456/6/0. R5 (#52) closes on green: G1.0 (superset proof) + G1.1 (mixed serve) + G2.0 (checkpoint store) + G2.1 (warm start + consumer) + G2.2 (crash restart) all merged + reviewed. R6 dispatch next: amplitude escalation to scene.amaxvalue (w2-review adjudication, bitwise-safe by construction, NEVER A_eff) + profiled hot path (shadow() launch overhead ~40% svf_seconds disclosed W2; per-(revision,t) march cache via precomputed_shadows) + solver.py:1734 latent time-loop note closed by same escalation. |
| lead | post-G2.2 canonical GREEN 1456/6/0 — R5 CLOSED | closed | 2026-09-05 | PID 35726 EXIT clean (26:34). R5 (#52) COMPLETE: G1.0 superset proof (67f7507) + G1.1 mixed-provenance serve (9a01484) + G2.0 checkpoint store (0400377) + G2.1 warm start + consumer (1371b7d) + G2.2 crash-restart (316084a) — every step red-first, non-author reviewed (r4b-review2 ×2, r5a-review2 ×3), bitwise fences throughout, canonical chain reconciled exact at every merge (1382→1412→1414→1452→1456). R6 in flight. |
| r6-hotpath | R6 COMPLETE @ 3221bbe (RED 53d660d 7F -> GREEN f22c1e6 solver.py 220 lines -> fence 3221bbe) | review pending | 2026-09-05 | AMPLITUDE ESCALATION shipped: banded scenes (dz_1 in (A_eff,A_abs]) SERVE marching at absolute scene.amaxvalue (oracle's own stop — identical step set by construction; NEVER A_eff); band-empty keep A_eff bit-identical (real site_500 sha256 unchanged base<->head all 3 planes); time-loop escalates whole-loop when any in-prefix timestep bands (prefix-exact + night-skip); solver.py:1734 latent note closed; utci_process seam untouched. MarchRegimeError: dead as raise (escalated windowed 1.004s vs full-tile fallback 5.009s = refusal was costing 5x), class kept as typed structural home, worker routing pinned. PROFILE: W2-era ~40% kernel-launch claim DOES NOT REPRODUCE at HEAD — 139.19us/step x 14,112 steps = 1.96s = 12% of svf_seconds (W2's per-patch march windows already ate it); ring decomposition 6deg=10.9s(2/3)/18deg=3.5s/30deg=1.9s; march compute-dominated 7.3ns/cell-step; time-loop march share 5.8%. MARCH-CACHE REFUSED honest-outcome both gates: win not real (5.8% of one stage, cache only hits on bitwise-identical REPEAT solves, single-edit hot path always cold) + (revision,t) key UNSOUND (march pure over a/vegdsm/vegdsm2/az/alt/scale/amaxvalue/bush/walls/dirwalls/EXTENT — forcing, amplitude selection (R6 escalation itself changes the stop for same (scene,t)!), extent all outside key; stale = wrong shadows as exact) — delivered as docstring analysis + 5 fence tests. site_500 NOT banded (band 0, dz_1 max ~11.6m << A_eff 57.85) — escalation is serving-correctness for banded scenes (elevated terrain/coarse pixels), not a site_500 perf lever; +1.4s bench delta = host load 4.3->10.0 noise proven by identical hashes. INFO observed-not-acted: 8 threads SLOWER than 1 on biggest march grids (thread-pool dispatch on small ops, 425px 0.370 vs 0.285s). Canonical worktree 1468/6/0 EXIT:0 = 1456+12 (7 amplitude + 5 fence) EXACT. Next svf lever by profile: 6deg ring (2/3 of svf_seconds) batched/fused march — not this wave. |
| r4b-review2 | R6 non-author review @ 3221bbe | APPROVE (0 blocking, 4 nonblocking; 1 headline test-adequacy) | 2026-09-05 | Construction adjudicated EXACT: dz_1 helper bit-exact to both marches' first-step arithmetic ((ds*1.0)*tanaltitudebyscale folded-first match, x1.0 IEEE-exact, pi/180 same-double, no azimuth-zero substitution on wallheight_23 side); band edges EXACTLY W2's open/closed; escalated stop == oracle stop (scene.amaxvalue, P2-identity tensor) per timestep -> identical while-loop sequence = bitwise BY CONSTRUCTION; time-loop amplitude is window-relative (57.29 vs 57.85 full-tile, smaller -> predicate more eager -> strictly safe). Halo/read-reach untouched (required_halo_pixels bounds on amaxvalue already; read_window_fraction identical base<->head). RED verified by clone (7F/13P, correct failure modes); mutation A (re-applied refusal) 5F. Real-site parity INDEPENDENTLY reproduced on fresh cache copy, sha256 identical base<->head, load 11-14. March-cache refusal adjudicated CORRECT (purity list complete per code read; refused key was unsound — missing extent/az-alt/bush movers = wrong-exact class). Profile arithmetic exact (139.19us x 14,112 = 1.9642s = 12.0% of 16.365s; 6deg ring 66.6%). MarchRegimeError disposition verified (no raise sites, typing pin, routing intact, JSON matches). Canonical 1468/6/0 full-log EXIT:0 reconciles 1456+12; merge-tree clean tree 08442abd. HEADLINE (nonblocking, test-adequacy): mutation B — DELETING the SVF escalation (all patches at A_eff) leaves ALL 20 tests passing; fixtures pin non-refusal (mutation A) but are INERT to the escalation's bitwise effect — the claim rests on code read alone. Pre-merge fixup DISPATCHED to author: sensitivity fixture (canopy at 2*dz_1 along band patch's march direction -> mutation B must flip bytes, verified both directions), optional time-loop mutation coverage or honest non-coverage, 2 comment-nit alignments (step-set overstatement; full-tile-vs-window-relative docstring clause). Nonblocking nits: dz1==A_eff exact-equality test absent (measure-zero float, inherited W2); veg-term parity value-exact not byte-exact (sha256 carries bytes; consistent with prior adjudication). |
| lead | R6 fixup verified INDEPENDENTLY both directions; merged 3efc1ec — R6 closes on canonical | merged | 2026-09-05 | Fixup @ 438ee5d (b7b32f8 tests + 438ee5d comments; solver.py diff comment-only CONFIRMED by lead diff read). Lead independent mutation-B verification in fresh /tmp clone (own perl patch, patch_amplitude=amaxvalue-for-all): HEAD 22/22 PASS, mutation 1F/21P with TestEscalationSensitivity::test_banded_replay_matches_the_true_svf_calculator_oracle the SOLE failure — reproduces agent run exactly; reviewer headline (20-test inertness) CLOSED. Fixup root-cause adjudicated SOUND: replay-vs-replay tests self-cancel solver march mutations (structural blindness), true-svf_calculator-oracle pin immune; agent's departure from reviewer's literal canopy-at-2*dz_1 construction PROVEN correct (step-2+ FABOVEA raises impossible at banded patch: raise needs occluder above a[target]+dz_i with dz_i>=dz_1 but banded means dz_1>A_eff>=every marched value minus min(a)); the value channel is shadow()'s index-1 accumulator zero (vbsh=1+vegsh=2.0 signature on one-step) — witness 152 oracle-1.0 cells on elevated fixture. Time-loop: honest non-coverage WITH empirical inertness pin (mutation C 22/22 = value-inert END TO END; no raise channel at banded timestep, wallheight_23 re-derives vbsh from vegsh alone; byte-flip fence impossible by construction) — adjudicated stronger than requested. Merged --no-ff (852/80 across 3 files). Post-merge canonical PID 53844 (/tmp/solweig-canonical/r6-merge.log), expect 1470/6/0 = 1468+2. Worktree agent-r6 + branch r6-hotpath retire on green; R6 (#53) closes on green; R7 dispatch next. |
| lead | post-R6 canonical GREEN 1470/6/0 — R6 CLOSED | closed | 2026-09-05 | PID 53844 EXIT clean (27:39, load 5-11 during run) = 1468+2 fixup tests EXACT. R6 (#53) COMPLETE: amplitude escalation (53d660d RED -> f22c1e6 GREEN -> 3221bbe march-cache refusal fences -> b7b32f8/438ee5d fixup) merged 3efc1ec, reviewed APPROVE r4b-review2, mutation-B sensitivity verified twice independently (agent + lead fresh clones, both directions), time-loop inertness pinned. Canonical chain 1382->1412->1414->1452->1456->1468->1470 reconciled exact at every merge. Worktree agent-r6 + branch r6-hotpath retired. Banked intel: 6deg ring = 2/3 svf_seconds (next perf lever, batched/fused march, post-R7); 8 threads slower than 1 on big march grids; tree edits route FULL on small tiles (GVF margin). R7 (#54) dispatch next: qualified compensated fast models — compensation only as qualified provisional anchored to exact base. |
| lead | R7 DISPATCHED (r7-fastqual, worktree agent-r7, branch r7-fastqual @ b08b22d) | dispatched | 2026-09-05 | Scope: un-reserve fast_qualified — compensated fast kernel for the no-cheap-exact family (expected vegetation; agent scouts then confirms), anchored delta on exact planes (exact_base_revision), MANDATORY qualifier metadata (model_version, measured holdout error bound per variable, evidence pointer) with lane-side downgrade-to-visual_pending on incomplete qualifier (structural never-reduce-precision-silently), supersession fence (qualified frame never authoritative after exact advance past its base), offline calibration+holdout harness on real exact solves (site cache copied to /tmp, READ-ONLY original), red-first tests, full canonical in worktree (expect 1470+net-new), latency measurement. Order constraints: compensation LAST in priority order (eligibility check tested), no thermal guessing, honest wide qualifier beats dishonest narrow, lead-only ledger/merge. |
| r7-fastqual | R7 COMPLETE @ 7d9c8d6 (RED 4648a59 28F/2P -> impl 631dcf9 969 ins -> docs 7d9c8d6) — branch worktree-agent-af8dc262388e36312 (sandbox-pin adaptation, base b08b22d identical, agent-r7/r7-fastqual untouched) | review pending | 2026-09-05 | SCOUT: vegetation = gap family (view fast_exact, met owned by exact-lane r3a/G2.1; no bounded veg exact path — windowed seconds, full tile ~130s site_500). MODEL v1: GEOMETRIC cast-corridor capsule sweep on REAL shadow_vector_m (same 5deg floor/300m cap), change mask = XOR(base-union, current-union) per sun-up step; NO fitted thermal numbers (mission preference); one a-priori discrete variant (crown disc in/out) arbitrated on calibration half only — cast_corridor_only won (calib IoU 0.518 vs 0.469, generalized holdout 0.569 vs 0.503); kernel predicts with exactly its declared variant. HOLDOUT honest (8 real run_full_tile solves 125-137s each on /tmp cache copy, seeded 4/4, holdout-only reporting, run r7-veg-holdout-site500-20260905, uptime 1030s): IoU p50 0.668/p95 0.783/max 0.787/min 0.152, precision p50 0.802, recall p50 0.856 truth-weighted 0.870; worst = far move 0.152 (building-shadowed cells don't flip — disclosed no-wall-interaction limitation); bounds ride every payload. QUALIFIER: schema {model_version, form, domain, error_evidence(metrics, n_holdout, evidence ptr, run id), reconciliation} + exact_base_revision anchor + base_planes + coverage + limitations; kernel-agnostic validator 12-id defect vocabulary, ANY defect -> loud downgrade visual_pending (downgrade_reasons + logger.error + counter). SUPERSESSION: exact_result_version >= frame workspace_revision -> idempotent re-publish fast_exact. r2 reserved pin passes unchanged. LATENCY: kernel p99 2.25ms (40 reps), shipped prediction 1.5+6.0*trees=13.5ms covers 1.25x p99, harness self-fails if broken. Canonical worktree 1500/6/0 EXIT:0 = 1470+30 EXACT (/tmp/r7-canonical.log, 26:01). Non-coverage honest: building/landcover not compensated, tmrt/utci not modelled v1, site-scoped bound (site_500 2009-08-11), flat-ground capsule disclosed. No forbidden paths (lead-verified); evidence JSON metrics-only (lead-verified). Lead-verified: 3 commits, 2856 ins, merge candidate after review. Review DISPATCHED r4b-review2 (9 attack items, headline = R6-lesson qualifier-fence mutation testing). |
| r4b-review2 | R7 non-author review @ 7d9c8d6 | APPROVE (0 blocking, 6 nonblocking) | 2026-09-05 | All 9 items CONFIRMED. ZERO INERT FENCES (R6 headline-risk cleared): 15 mutations actually run in /tmp/r7rev clone — 12 per-check deletions in qualification.py each flip >=1 parametrized downgrade test (16 cases = fence coverage map); kernel-drops-qualifier flips kernel-publish test (1F/29P, full-file rerun — reviewer's own narrow -k subset first showed inert, subset error not fence gap); scheduler-skips-validator flips 18. RED genuine (fresh clone 4648a59: 28F/2P behavioral). MATH: 5deg/300m via InfluenceConfig geometry.py:236-237; XOR union logic verified; latency prediction 6.0x conservative cover (13.5ms vs p99 2.25ms), ~16x per-tree (6.0 vs measured 0.37ms/tree marginal) — over-refuse direction SAFE at any tree count. HOLDOUT: reviewer recomputed ALL percentiles from per-case holdout IoUs to 1e-10 match (p50 0.66807/p95 0.78269/p99 0.78621/max 0.7871/min 0.1519); seeded split verified; variant chosen on calibration half only and generalized; aggregates match DEFAULT_QUALIFIER_EVIDENCE verbatim. SUPERSESSION first-in-tick, idempotent by construction, exception-safe. r2 pin ZERO diff vs base, passing. Canonical 1500/6/0 = 1470+30 exact (14 defs + 16 parametrized). merge-tree clean. NONBLOCKING: N1 canonical log lacks literal EXIT marker (banner present, EXIT inferred — lead folds EXIT:0 marker into future canonicals, done for r7-merge); N2 latency slope single-point (conservative direction); N3 base_planes structure-only validation (no anchor-resolvability at publish — future check; mitigated by supersession + disclosure); N4 per-case precision/recall not in JSON (aggregates only, not recomputable per-case); N5 design-note 1.25x wording understates 6x; N6 supersession synthetic met_deadline/latency cosmetic + disclosed. No pre-merge fixup required. |
| lead | post-R7 canonical GREEN 1500/6/0 EXIT:0 — R7 CLOSED | closed | 2026-09-05 | PID 13198 (26:24, load 4-5, literal EXIT:0 marker per review N1) = 1470+30 EXACT. R7 (#54) COMPLETE: fast_qualified un-reserve with anchored vegetation compensation v1 (geometric cast-corridor overlay on real shadow_vector_m, no fitted thermal numbers), structural qualifier fence (12-id validator, loud downgrade on ANY defect), honest holdout bounds riding every payload, supersession fence, r2 pin unchanged. Reviewed APPROVE r4b-review2 (zero inert fences, 15 mutations, holdout percentiles recomputed to 1e-10). Canonical chain 1382->1412->1414->1452->1456->1468->1470->1500 reconciled exact at every merge. Retired: agent-r7 worktree + r7-fastqual branch (redundant), agent isolation worktree + its branch. Result-class ladder now complete: fast_exact / fast_qualified / visual_pending all live. R8 (#55) dispatch next: load, chaos, one-hour soak, crash-restart (G2.2 harness feeds), RSS, accept-queue chaos, p99 distributional evidence, target-CPU commissioning. |
| lead | R8 DISPATCHED (r8-soak, own sandbox worktree, base c5d5b37) | dispatched | 2026-09-05 | Final gate wave: load harness (N clients x 2+ workspaces, realistic op mix incl duplicates/stale, p50/p95/p99 per metric with n), ONE-HOUR uninterrupted soak + RSS slope analysis, chaos matrix (accept-queue overflow pre-acceptance, mid-epoch disconnects, kill -9 server crash at seeded seams -> zero-loss/monotonicity/no-torn at SERVER level reusing G2.2 patterns, 2x overload soaklet with recovery), p99 distributional evidence vs 300ms/1000ms SLOs, commissioning note (host envelope + fly shared-cpu-1x honest derivation), defects red-first-or-document discipline. Constraints: nohup foreground servers only, /tmp site-cache copies, soak single-run unstitched, full-log canonical with EXIT marker (baseline 1500+net-new). Retired this window: agent-r7 + r7-fastqual + agent isolation worktree/branch + stale /tmp/r2c-review worktree. |
| r8-soak | R8 COMPLETE @ 19f564f (harness 1384578 2511 lines -> CI fences 6baec23 5 tests -> commissioning note 19f564f) — branch worktree-agent-a8b49eefcc9283979, base c5d5b37 reset-confirmed | review pending | 2026-09-05 | LOAD/SOAK: one-hour uninterrupted soak (3600s load + 300s quiesce, uptime 3651.6s, host load 5.04), 7357+7395 acked == durable zero-loss, 929 duplicates + 923 stale sustained one hour deterministic, 0 SSE drops, 0 failed jobs, exact lag drained 0. VISIBILITY SLO MISS (headline, honest): calibration p95 364/p99 481 (n=2486), soak p95 372/p99 491 (n=14752) vs p95<=150/p99<=300 — attributed: visibility == server-side epoch_wait one-for-one (reduce p99 0.13ms, publish 0.48ms, fast compute 0.13ms, SSE transport p99 4.4ms), [100,200)ms aliasing floor from FIXED 100ms epoch tick, ~280ms tail = ticker-wake overshoot under host load ~5. FAST LANE PASS: fast_exact p99 535 (n=13320), visual_pending 544 (n=8018) vs <=1000. Exact queued->finished p99 4040ms async-by-design. RSS NO LEAK: 680 samples/3605.6s, peak 442.9MB, 402.9->320.8MB, slope -50.4MB/h, step DOWN t+2470s. CHAOS 8/8: overflow flood 344x429 pre-acceptance zero phantoms; disconnect 1-drop/1-reconnect zero loss; 5 kill seams (epoch mid-close, exact mid-solve, fast publish pre/post-commit, accept mid-txn) kill->restart->zero-loss->monotonic->no-torn; duplicate/stale byte-identical canonical across 2 workspaces. DEFECTS: zero server defects (first-pass 3 false-fails = one harness client-model bug, optimistic tree-add never acked tripping the CORRECT unfoldable fence; fixed harness-side idempotent retry outbox + forget()). SURPRISES: coalescing_window_ms NOT wired to epoch scheduler (fixed 100ms tick, epoch_scheduler_factory only); stub-solver refuted physics-GIL tail hypothesis (p99 466 unchanged); 41 sequential localhost posts fold in one 117ms epoch (~2.8ms/op); faulthandler grep misparse (60 dumps present). Canonical worktree 1505/6/0 EXIT:0 = 1500+5 EXACT. Commissioning: M1 Pro 10-core envelope, fly 1-vCPU honest-ratio derivation (no fabricated remote numbers), memory fits (443MB vs 2GB). Non-coverage: session-matrix 1/16/32, remote measurement, op-ack RTT, fast_qualified under load (0 in workload — R7 kernel dominance), tick-gap histogram. Review DISPATCHED r5a-review2 — headline adjudication: visibility miss structural vs load-artifact vs SLO mis-spec (drives remediation wave). |
| r5a-review2 | R8 non-author review @ 19f564f | APPROVE (0 blocking, 5 nonblocking) + SLO ADJUDICATION | 2026-09-05 | All 9 items verified. Raw visibility samples not persisted in soak.json -> reviewer ran OWN instrumented 90s run (n=380: p50 143.1/p95 361.6/p99 475.8) reproducing soak shape; histogram confirms [100,200) floor (81.6% of samples); epoch_wait one-for-one CONFIRMED server-side (wait p50 167/p99 509 vs visibility 155/491, deltas <=20ms; SSE transport p99 13ms exonerated). coalescing_window_ms finding CONFIRMED (reaches only exact-lane dispatch delay jobs.py:822/828/993; EpochScheduler built at DEFAULT_TICK_MS=100.0 app.py:89/epochs.py:85; probe50 distribution identical = knob cannot move visibility). Chaos seams REAL (os._exit wraps real Store/utci fns harness:297-390, R8KILL markers match chaos.json, acked subset durable post-restart, sequences contiguous, triple_ok). Soak integrity confirmed (14752 submitted == 7357+7395 acked, monotonic RSS timestamps span 3605.6s, final_exact_lag 0). RSS conversion internally consistent (-74.35/poll-index x 5.31s -> -50.4MB/h; peak 432.5MiB = 21% of 2GB). CI fences mutation-verified 2 (rate-gate neutered -> overflow fence FAILS; fingerprint-conflict swallow -> determinism fence FAILS). Canonical 1505=1500+5; merge-tree clean single tree. Commissioning note honest (every cross-checked number matches evidence). False-fail fix harness-side, tripped fence is CORRECT executor gate. NONBLOCKING: N1 harness docstring overstates SLO enforcement (constants report-only; real gates zero-loss/triple/stream-ratio); N2 lsq_slope_kb_per_sample misnamed (per-poll-index); N3 service_level_contract.md promises configurable 50-200ms epoch tick but create_app exposes NO path (doc/code divergence, ties to SLO story); N4 batch-size-exceeded + capacity-saturated admission paths unfenced. ADJUDICATION: visibility miss = (iii) SLO mis-specification compounded by (ii) host-load ticker lateness, NOT structural — close pipeline sub-ms (reduce p50 0.06/publish p50 0.11ms, no computational target); ideal-tick wait p95 ~95-100ms leaves ~zero headroom under 150ms p95 BY DESIGN; p50 150-167 = 1.5 ticks = ticker wakes late under load 3.3-7 (diag_stub p99 466 = solver GIL not dominant). Correctness intact everywhere. Recommended: re-spec SLO as tick-multiples or epoch-close-anchored AND/OR wire tick_ms into create_app, re-commission on dedicated-core node. |
| lead | R8 merged 4e6d334 — R8b follow-up dispatched (r8-soak resumed) | merged+dispatched | 2026-09-05 | Merge --no-ff (3233 ins: harness 2511 + CI fences 467 + commissioning 255). Post-merge canonical PID 92815 (/tmp/solweig-canonical/r8-merge.log), expect 1505/6/0 EXIT:0 (pure additive, docs+harness+tests only). R8b DISPATCHED to r8-soak (worktree reset to 4e6d334): (1) RED-first tick wiring — epoch tick 50-200ms through create_app config per service_level_contract's own promise (N3), default 100ms byte-identical, out-of-domain refuses; (2) two missing CI fences — batch-size-exceeded + capacity-saturated admission paths (N4); (3) harness fixes N1 docstring (SLO constants report-only) + N2 rename (per-poll-index); (4) quiet-host visibility re-measurement + tick-gap histogram (direct tail attribution): outcome (a) p99<=300 on quiet host -> SLO met with load commissioning condition, or (b) confirmed miss -> SLO re-spec escalated to USER (goal-level decision, lead will surface). R8 closes (#55) only after R8b green + review. |
| lead | post-R8-merge canonical GREEN 1505/6/0 EXIT:0 | green | 2026-09-05 | PID 92815 (26:47, load 4.4-5.0, literal EXIT:0) = 1500+5 EXACT. R8 merge validated on main. #55 stays open pending R8b (tick wiring + fences + quiet-host visibility + tick-gap evidence) green + review. Canonical chain now 1382->1412->1414->1452->1456->1468->1470->1500->1505 reconciled exact at every merge. |
| r8-soak | R8b COMPLETE @ c6ae897 (tick wiring dfa180a RED 3 -> green 5 + mutations 3/3 -> lateness instrument dac39de -> truth fixes 8206051 -> commissioning appendix c6ae897), base 4e6d334 reset-confirmed | review pending | 2026-09-05 | TICK WIRING: epoch_tick_ms through create_app, 50-200ms domain via EPOCH_TICK_MIN/MAX_MS, None keeps 100ms byte-identical, loud ValueError before any wiring (also injected-factory path), default-factory-only threading, coalescing docstring trued (never reaches scheduler). FENCES: batch-size-exceeded (route 429 typed, flat max_operations_per_epoch, zero phantoms, recovery) + capacity-saturated (503 typed + retry hint, app serving) — mutations 3/3 caught (knob-drop 2 tests, batch-size delete, saturated delete). TICK-LATENESS INSTRUMENT (new): solweig_rt_epoch_tick_lateness_ms every loop tick, n=5328 p50 10.0/p95 35.1/p99 316.6/max 388.1 — R8 tail attribution now MEASURED identity (mid-floor wait + one p99-late tick ~ wait p99 466), not inference. QUIET-HOST DEFERRED honest: load 2.84->6.57 across windows (dominant consumers = concurrent agent session 86% CPU + main-checkout pytest, not agent's), clean <3 number NOT shipped; at-load rerun (4.2-6.6, gauge mean 4.8) p50 147.4/p95 355.3/p99 466.8 n=2488 FLAT vs R8 480.7 — SLO decision framed for user (quiet-host re-commission vs tick-multiple re-spec), not pre-decided. Harness truth: SLO constants documented REPORT-ONLY (real gates zero-loss/triple/stream-ratio), lsq_slope_kb_per_sample -> lsq_slope_kb_per_poll, --evidence-name non-clobber. SURPRISES: (1) R8 chaos TestClient leaked live scheduler thread (starlette close() skips lifespan shutdown; invisible in alpha ordering) — fixed __exit__, both orderings verified; (2) agent's uncommitted harness edits externally reverted mid-wave (flagged, re-applied+committed; cause unknown — review probes reflog); (3) host_load gauge existed but unsampled — now sampled on scheduler heartbeat. Canonical worktree 1510/6/0 EXIT:0 = 1505+5 EXACT. Review DISPATCHED r4b-review2 (8 items incl. external-revert forensics). |
| r4b-review2 | R8b non-author review @ c6ae897 | APPROVE (0 blocking, 5 nonblocking) | 2026-09-05 | All 8 items verified. Mutations 3/3 re-run in /tmp/r8brev (knob-drop-in-factory 2F — expected split: validation lives in create_app not factory; batch-size delete 1F; saturated delete 1F). RED genuine (3 TypeError on unwired base). Wiring: domain validation BEFORE store construction unconditional on factory injection; None -> DEFAULT_TICK_MS=100.0 explicit = byte-identical; injected factory owns construction, no leak/thread. Fences standalone + full-file ordering clean (31 passed). TestClient claim EXACT vs installed starlette 1.6.0 (no own close(); old call = httpx transport close, no lifespan; __exit__ -> exit_stack -> lifespan). Instrument anchor sound (monotonic, self-correcting target, per-tick overshoot never cumulative). Identity arithmetic checked (wait p50 155 ~= 1.5W; wait p99 466 ~= 150+316.6). REFLOG FORENSICS on mid-wave revert: empty commit f9f6090 17:13:51 zero diffstat + git reset HEAD~1; dangling blob 6f016ab9 differs from committed file in DICT FORMATTING ONLY — consistent with one git reset --hard HEAD~1 discarding commit+staged edits; process attribution open (no session admits); content impact nil. Canonical 1510 = 1505+5 exact (+5 = test_realtime_r8b_wiring.py); merge-tree clean. Appendix numbers all traceable; SLO decision framed not made. NONBLOCKING: N1 canonical EXIT marker absent in agent's own log (systemic — lead canonicals carry it; keep instruction in dispatches); N2 lateness percentiles over retained 4096/5328 (oldest 1232 dropped by histogram capacity — count honest, base slightly overstated); N3 two quiet-table rows not independently locatable (17:40 row matches); N4 'measured identity' wording > math (p99-of-sum != sum-of-p99s; heuristic, conclusion supported); N5 reset process-attribution residue -> ledger note: worktree-local resets can come from concurrent tooling. |
| lead | post-R8b canonical GREEN 1510/6/0 EXIT:0 — R8 CLOSED, R0-R8 ALL COMPLETE | closed | 2026-09-05 | PID 30017 (26:42, load 3.9-5.3, literal EXIT:0) = 1505+5 EXACT. R8 (#55) COMPLETE: load/chaos/one-hour-soak/commissioning (4e6d334) + tick wiring/fences/lateness-evidence (7ec552f), both reviewed APPROVE (r5a-review2, r4b-review2), all mutations caught both waves, zero server defects, zero-loss under crash/disconnect/overflow/duplicates/stale through one hour. Canonical chain final: 1382->1412->1414->1452->1456->1468->1470->1500->1505->1510 reconciled exact at EVERY merge. Retired: worktree agent-a8b49eefcc9283979 + branch. MISSION GATE STATUS: R0-R8 all closed; every wave red-first + non-author reviewed + differentially/mutation-verified; scientific constraints (read-reach/sky-floor/accumulation/precision) untouched throughout; compensation live as qualified-provisional-only anchored to exact. OPEN USER DECISIONS (goal-level, lead will surface): (1) visibility SLO — quiet-host re-commission vs tick-multiple re-spec (current: p99 466-491 at host load 4-7; tick floor [100,200) holds 81.6%, lateness p99 316.6; 150ms p95 unreachable BY DESIGN at 100ms tick); (2) push main to origin (189+ commits ahead); (3) fly demo redeploy with R6-R8 code; (4) fly performance-1x after billing enabled. |
| lead | USER DECISIONS (3/3) + execution: origin push DONE, fly redeploy + quiet-host re-commission in flight | decisions | 2026-09-06 | AskUserQuestion verdicts: (1) visibility SLO -> QUIET-HOST RE-COMMISSION FIRST (r8-quiesce dispatched: load<3.0 protocol, 60-min window-hunt max, calibration-length run, verdict vs 300ms with commissioning condition or explicit tick-multiple re-spec recommendation, commissioning_note append); (2) push main -> DONE f3ad111..f4049f4 (origin/main == main, ~190 commits); (3) fly demo redeploy -> APPROVED, remote-only build running in background (bczgazhfx), smoke-check after (capabilities 200 + scenario create; first-request-after-wake 502 retry rule known). Fly performance-1x still blocked on trial-org billing (memory note stands). |
| lead | fly demo REDEPLOYED + smoke PASS (main @ f4049f4) | deployed | 2026-09-06 | flyctl deploy remote-only EXIT 0 (machine d8d5620b77e958 v5, image deployment-01M1VBXM76XX341E3F47W88H26). Smoke: capabilities 200 (API boot ~42s post-start — static layer 502s until uvicorn up at 12:47:27; memory updated), scenario create 201 2.16s instant-exact (scn_1a46603d8553 site_500), result manifest 200 0.49s, UI root 200. Demo now carries R6+R7+R8/R8b. Remaining in-flight: r8-quiesce quiet-host visibility measurement (load<3 protocol). |
| lead | QUIET-HOST RE-COMMISSION COMPLETE — NEGATIVE result merged 4846ae9, SLO decision surfaced to user | closed+escalated | 2026-09-06 | r8-quiesce delivered (branch worktree-agent-ac0543c53b8285975, 2 commits: 8b6fe33 harness host_load gauge sampling on existing 0.5s /metrics poll + in-run uptime heartbeats, 66513f3 commissioning note +122): launch window found at 1-min load 2.77 but did NOT hold — in-run gauge min 2.377/mean 4.563/max 9.074 (n=1116), post-run host floor ~5.3 with ZERO harness footprint (host floor, not workload). Identical R8b workload (600s, 4x1op/s, 2 workspaces, 2477 ops PASS zero defects zero SSE drops): visibility p50 141.2/p95 372.0/p99 492.8/max 554.6; epoch wait p99 487; tick lateness p99 322.7; composition identity holds (wait p99 ~= 200ms floor top + tick p99). FOUR-RUN PICTURE: p99 flat 466-493 across start-loads 3.27-5.45 = ZERO measurable load sensitivity anywhere measurable; structural [W,2W) floor + in-process tick contention (GIL/OS from workload's own accept+exact threads) favored over pure host-load hypothesis. NO 'host load < X' commissioning condition certifiable on this machine (no sub-3.0 ten-minute window exists; ~160s sub-3.2 dip cannot be windowed post-hoc for percentiles). Agent's explicit recommendation: tick-multiple/epoch-close-anchored SLO re-spec (p50 in [W,2W) + bounded tick lateness, e.g. p99 <= 2W + lateness budget) — every run to date satisfies it with mechanism fully measured. Last unmeasured discriminator: dedicated host sustaining <3.0 or idle core / raised scheduler-thread priority for full calibration run — unmeasurable on this shared machine. Merge 4846ae9 --no-ff (docs+harness only, no solweig_gpu/tests changes; targeted r8_chaos+r8b_wiring 10/10 on main post-merge). Canonical NOT required (no production/test surface; precedent: docs-only rows). Retired: worktree worktree-agent-ac0543c53b8285975 + branch. USER ESCALATION NOW (goal-level): (1) tick-multiple/epoch-close-anchored re-spec [agent-recommended], (2) dedicated-host/priority discriminator attempt, (3) accept-and-document with deployment caveat. |
| lead | SLO RE-SPEC EXECUTED (user-approved) ccf23d5 — mission accounting final | closed | 2026-09-06 | AskUserQuestion verdict: tick-anchored re-spec (agent-recommended option). Edits (ccf23d5, docs-only): service_level_contract.md SLO table -> tick-anchored (visibility p50 in [W,2W), p99 <= 2W+350ms=550@W=100; tick-wake lateness p99 <= 350ms as OWN SLO with host/scheduling breach class; canonical p99 same anchoring + close pipeline <= 5ms; fast 1,000ms UNCHANGED absolute) + re-spec section w/ 4-run evidence table; realtime_contract.yaml slo_targets restructured (remote_state_visibility_p50/p99_ms_max 200/550, tick_wake_lateness_p99_ms_max 350, canonical_revision_p99_ms_max 550, canonical_close_pipeline_p99_ms_max 5); goal_condition_realtime_collab.txt in-place amendment (provenance dated, 3976/4000 chars, spec-test phrases intact); commissioning_note.md decision section (retired-not-waived framing: absolute 300 re-adoptable on dedicated host, lateness SLO is the instrument). Tests 24/24 (collaboration_spec + design_docs + r8b_wiring). Canonical not required (no production/test surface). FINAL ACCOUNTING: R0-R8 all closed + reviewed; canonical chain 1382->1412->1414->1452->1456->1468->1470->1500->1505->1510 exact at every merge; scientific constraints untouched; compensation qualified-provisional-only; demo live f4049f4; known limitation: no realtime browser client (payload-contract-level client display); deferred: fly perf-1x billing, 6°-ring march batching, realtime browser client. |
| UX-docs | frontend | ux-sim (Opus) | ux-sim | branch ux/interaction-simulations (main checkout) | docs/.../ux_redesign/interaction_simulations.md | COMPLETE | 895148b (merge) | 1485-line interaction spec: 10 cognitive concepts, 14-word glossary (verbatim copy mandate), five-stage pipeline Sketch/Stamp/Share/Paint/Verify, 11 screens/7 wireframes, 35 interactions I-01..I-35 (I-19 = interactive submission definition of record), error matrix verbatim copy, result-class trust design D1-D5, decisions D1-D12 |
| UX-docs | frontend | design-plan (Opus) | design-plan | branch ux/interaction-simulations (main checkout) | docs/.../ux_redesign/design_plan.md | COMPLETE | 895148b (merge) | 556-line visual plan: "instrument on a drafting table" thesis, 8-color light palette (Instrument Paper #F3F5F3 / Survey Ink / Ultramarine #2B49CC sole accent / Viridian / Ochre / Brick / Slate), CVD-safe class trio color+glyph+border+fill, four-band console 56/280/canvas+72/336/32 rail, Appendix A :root tokens + Appendix B styles migration map |
| UI-build | frontend | ui-shell | ui-shell | worktree-agent-bdc26de... (retired) | examples/incremental_design_tool/index.html | COMPLETE | 795bdad (merge) | shell rebuild: 68 existing app.mjs-queried ids preserved verbatim, 42 added (resultClassBadge, transportPill, presenceBar, heldCount, activityToggle/List, timelineFreshness, qualifierPanel+family, metronome rail anatomy, five-stage #jobSteps with old 4 steps as verify-substeps); connected-only widgets ship hidden |
| UI-build | frontend | ui-badges | ui-badges | worktree-agent-6e4a551... (retired) | realtime_badge.mjs, renderer.mjs | COMPLETE | 59caeef (merge) | +507/-4: RESULT_CLASS_DISPLAY/effectiveDisplayClass/resultClassDisplay (stale suffix, conservative unknown fallback), additive classDisplay on realtimeBadgeState (legacy byte-compatible); renderer RESULT_CLASS_COLORS/LADDER, setReducedMotion, flashRegion ladder-gated, pulseRemote, timeBasisDots, rAF self-animation; updateHeatRegion untouched |
| UI-build | frontend | ui-styles | ui-styles | worktree-agent-ac959ec30330fe6eb | styles.css | COMPLETE @ 069f653 | 144b10b (merge) | 2424 lines: Appendix A tokens verbatim, split pills, qualifier panel, presence/held, activity ledger anatomy, freshness dots, metronome rail, trio utilities; self-verified 143/143 new-HTML + 99/99 old-HTML class coverage + own browser smoke (fixed failure-banner z-index 6->29); 3 flagged intentional-inheritance classes |
| UI-build | frontend | ui-realtime | ui-realtime | worktree-agent-ad37366d6327b3782 | app.mjs, op_builder.mjs | COMPLETE @ dbbef19 | 886526d (merge) | +1109/-29 app.mjs + 446 op_builder.mjs: gesture->operation submit path on realtime plane, five-stage pipeline machine, transport/class split pills, qualifier panel, presence, ledger rows + receipts + I-23/I-24/I-25/I-26 verbatim copy, D7 Escape/pointercancel restore, world-frame calibration from observed ops; op_builder matches ui-tests pinned contract 8-key envelope; 5 documented non-implementables (family-card legacy transport, time-slider refinement, canvas remote pulses renderer-off-limits, Stamp/Share single settle, frame fallback scene-span) |
| UI-build | frontend | ui-tests | ui-tests | worktree-agent-abf523f033c7d82af (retired) | tests/realtime_submit.test.mjs, op_builder.mjs (stub superseded) | COMPLETE @ 45125be | 9132bea (merge) | 11 tests pinning submit contract: envelope keys, id mint pattern, payload shapes per family, admission kinds (429 burst / 503 server_saturated = implemented vocab), resolution object properties; stub-vs-real reconciled by lead --ours (real impl passes 11/11) |
| lead | frontend | lead | lead | main | serve.py, app.mjs (surface fixes), merges, ledger/worklog | INTEGRATION COMPLETE @ 5e8d741 | 5e8d741 (pushed) | Merges 895148b/795bdad/59caeef/144b10b/886526d/9132bea + 757388c serve.py SSE streaming fix (buffered read() hung every proxied event-stream -> realtime Reconnect failed; read1 single-read loop, HTTP/1.0 close-framing, event-stream content-type detection) + 5e8d741 surface fixes (transport/presence pills never unhidden + textContent wiping pulse-dot spans; metronome rail W/F/E/epoch-led/owed/actors/mode renderer hooked into renderCollabBadge choke point; activitySummary counts). BROWSER E2E via orca (user directive: all browser through orca-cli): offline + connected boots zero console errors; add x2 (probe double-click artifact, NOT app: click() returns undefined so ?? fallback also fired — two gestures two ops seq 1+2 one epoch R1 = UI-001 intact), delete (seq 3 R2 e1), base_divergent chip verbatim, badge Preview(owed) honest for async-verify vegetation, exact owed +N live, transport Live, summary counts; node --test 170/0; pytest: 1 env-artifact fail (thermal_comfort console script absent from venv, pre-existing, wave touched zero server/test files). Verify wave dispatched: verify-contract / verify-copy / verify-review (read-only). |
| verify-copy | frontend | glossary/copy conformance audit (read-only) | verify-copy | read-only | none | PASS — 5 must-fixes delivered | null | Glossary violations: "Preview (visual)" → "Preview"; "Fast estimate" → "Fast exact"; toast "Exact analysis updated" over-claims during preview → "Analysis updated"; "conflict" alarm wording → server-change wording; badge word duality flagged (glossary words own the class badge, rail lamp keeps triple+phase only). All 5 applied by lead pre-review. |
| verify-review-1 | frontend | non-author server-side review (jobs.py exact-lane broadcast) | verify-review (server pass) | read-only | none | REQUEST-CHANGES — 1 REQUIRED | null | REQUIRED: _broadcast_exact_revision's result-existence gate sat OUTSIDE the exception fence — a store read raising would escape into the settle path and strand the job row "running" until restart (only startup recovery re-drives running rows). Fix: move get_result inside try, broaden except. Applied by lead (S1) + 2 regression tests (settle-broadcasts-on-published-result; crash-window republish re-emits identical event). 25 passed. |
| verify-review-2 | frontend | non-author client-side review (op_builder/realtime_client/app.mjs/badge/CSS) | verify-review (client pass) | read-only | none | REQUEST-CHANGES — 1 CRITICAL / 3 HIGH / rest MEDIUM-LOW | null | CRITICAL F1: solveFrameFromSamples solved v with raw slope (positive spanY form) while uvToWorld uses y = originY − v·spanY — double flip mirrored v on every solved frame; fixed with sign-parameterized solve (+ degenerate-pair skip, +4 tests). HIGH: pendingAcks overwrite orphans unresolvable promise (settled {duplicate:true} first); resubscribe carried stale lastAppliedExactResult suppressing verify bridge (full rt reset); renderClassBadge textContent destroyed structured children (rewritten to write into .class-glyph/.class-word/.class-owed/.class-stale/.class-age + #qualTrigger). MEDIUM: held rows trimmed (never trim; summary counts rowOrder only), held tick fires into dead plane (realtimePlaneReady gate), move-drops-pending-property-change (single replace), CSS vocabulary mismatch (.rt-ledger-row unstyled / epochLed "receiving" vs .is-receiving / rt-basis-dot vs .fresh-dot — all reconciled to stylesheet vocabulary), #heldLedger dead list removed, network copy "receipt ids" → "operation ids", fast_qualified word "Qualified estimate" aligned, refused pipeline stuck "Stamp running" → settleRefusedPipeline, handleAuthoritative null-origin frame guard, serve.py mid-stream send_error corrupts stream → headers-committed flag + close, timelineFreshness 64-dot rebuild churn → key guard. DECLINED with rationale: renderer.mjs unwired exports (scope discrepancy vs 59caeef "wired" claim — flagged, not client-wave scope); legacy-lane publishes without named event (scenario-per-session accepted demo surface). |
| lead | frontend | lead | lead | main | server: jobs.py/app.py/routes_scenarios.py/realtime/broadcast.py + 2 test files + collaborative_state.md; client: op_builder/realtime_client/realtime_badge/app.mjs/index.html/styles.css/serve.py/exact_session.mjs + 2 test files; .gitignore | VERIFY WAVE COMPLETE @ 85ba483 | 444dcd2 + 85ba483 | Applied both review passes' combined fix batch as single writer (S1 server fence + site_geometry disclosure 444dcd2; client 14-finding batch 85ba483 — see verify-review-2 row for the finding-by-finding map). Tests: node 174/0 (4 new frame-convention cases), pytest focused 94 passed (test_realtime_r2_exact_lane 25 + test_server_api 69). LIVE E2E via orca (API server restarted to pick up S1, full env incl. SOLWEIG_SITE_ID/CACHE_ROOT): reload → zero console errors; transport Live, rail LIVE, epoch .is-offline correct; add-tree gesture → ledger row styled "applied · R1 · seq 1" in #activityRows (.rt-ledger-row anatomy green: ok-colored state chip, flex head, 24 fresh-dots 1 is-active); W/F/E 1/1/1 after exact settle → badge "Exact"/exact_reconciled, owed hidden, pipeline verify/done, 24 dots all is-exact (verify bridge fetched published result via named event + /results/1), scene v1. #heldLedger gone. Canonical full pytest not rerun this row (focused suites cover every touched server file; earlier env-artifact fail unchanged). Task #83 CLOSED; #84 deploy wave next (fly upload_site_cache.sh Input_subset gap + SOLWEIG_SITE_DIR/SITE_ID/CACHE_ROOT/DATE env, smoke, solweig.alansynn.com DNS recheck). |
| lead | frontend+deploy | lead | lead | main | app.mjs, model.mjs, exact_session.mjs, realtime_client.mjs, tests/realtime_submit.test.mjs; docs worklog/memory | COLLAB E2E + FLY DEPLOY WAVE COMPLETE @ 5b7bd84 (exact-lane completion on fly documented blocked by trial ceiling) | 5b7bd84 | #85 matrix green in-browser (orca): A→B/B→A add, concurrent adds (id-collision fix: random tail on makeTree id — ms+ordinal alone collided across actors and the op-plane tree_id upsert silently dropped an entity), move seq7, delete A→B (trees 4/4 W/F 6/6), late-join rebuild via public catchUpWorkspace() (ladder harness recounted POST attempts). Selection fix: setPointerCapture moved after syncSelectionPanel + try/catch — capture failure no longer strands a selected tree with disabled delete (real defect, surfaced by synthetic events). Suite 174/174. FLY (image deployment-01M1W3MEZDYPT268QM2V4HG0D8): caps 200, scenario mint, LIVE edit plane — POST ops 200, ledger applied·R1·seq1 / R2·seq2, F advances, rejoin rebuilds persisted scene, transport/rail live, badge honest Preview(owed). Exact lane on fly: worker dispatch+solve+heartbeat VERIFIED via /health/worker (jobs_dispatched 1, RSS ~1.0GB); completion structurally impossible under trial 5-min machine ceiling (solve 3+ min on shared-1x + 2-min cold boot; three wake cycles each SIGINTed mid-solve) — correctness carried by local bitwise-parity suite + local E2E; unblock = card on file / performance-1x. DNS solweig.alansynn.com still absent at Cloudflare authoritative NS — user dashboard re-check pending, then flyctl certs add. |
| 2026-09-06 | exec-studio (opus) | P1+P2 studio restructure: git mv → studio/, serve.py root-serving + legacy 301 + /docs mount + boot-config injection, wiring (entrypoint/compose/gitignore/pytest-gate/smoke_proxy), app.mjs siteId fallback, NEW test_studio_serve.py, docs/READMEs; + 4 MINOR + 1 NIT review fixes (startup site-id validation, /docs files-only, compose env mirror, near-miss + proxy tests, empty ?site=) | COMPLETE @ 1944cf8+88da189 — JS 174/174, pytest 23 passed, smoke OK, live probes green |
| 2026-09-06 | verify-studio | Independent orca browser E2E, offline-at-root localhost: 8744→8765 fresh-tab boot, offline pill, trees 3, synthetic drop → 4 + selection, legacy 301 (curl-verified, fetch opaqueredirect), /docs 200, console clean, cleanup | PASS — 0 blockers; screenshot blocked by embedded-browser pane visibility (env limitation, not defect) |
| 2026-09-06 | review-studio (opus) | Adversarial diff review: dispatch order, injection HEAD/GET semantics, redirect query preservation, /docs containment, tests adequacy, stale-path grep, Docker wiring — all empirically probed | APPROVE — 0 BLOCKER/0 MAJOR/4 MINOR/2 NIT; all MINOR + 1 NIT applied by exec-studio |
| 2026-09-06 | lead | P5 deploy + bare-URL verification on https://solweig.alansynn.com/ | COMPLETE — caps 200, root 200 w/ injected globals, legacy 301 → /?api=/api, docs 200, MIME OK; orca: connected scn_e5f93e6a2832, W1/F1 "applied · R1", second tab ?scenario= joins w/ scene rebuild + presence |
| lead | RATE-LIMIT INCIDENT closed: root-cause fleet + fix wave c3a810c | closed | 2026-09-06 | User-reported repeated "Exact analysis failed (rate_limited)" banner + interactive-editing doubt. 3-agent diagnosis (trace-ratelimit, audit-requests, verify-interactive live): IpRateLimiter 30/min keys request.client.host; serve.py proxy forwards no client identity and uvicorn runs without proxy_headers, so ALL visitors collapse into ONE 127.0.0.1 bucket; frontend bursts (exact_session 300ms poll alone sustains 200/min; #awaitJob baseline loop uncapped; connect retries re-mint scenarios; Retry-After never parsed) exhaust it; POST /edits 429 banners immediately. Live verify-interactive: edit plane WORKING (drop→R2 applied, no banner in flow) but exact lane STALLED E=0 for 7+ min (chase starved). USER DESIGN RULING: collaborative semantics already batch (100ms epochs, worker single-flight+supersession) — the IP gate is the wrong layer for editing pace; raise it, don't make users live under 30/min. fix-ratelimit (executor) c3a810c: fly.demo.toml SOLWEIG_REQUESTS_PER_MINUTE=600 (finite defence-in-depth, not 0) + frontend politeness (pollIntervalMs 300→1000; #awaitJob backoff ladder + 5-fail cap; connect retries rejoin minted scenario; parseRetryAfterMs shared helper → details.retry_after_ms, body wins; rate_limited POST auto-retry ONCE with same Idempotency-Key after server hint). node 179/179 (+5), pytest 23, zero solweig_gpu edits. Post-deploy functional proof: 55 consecutive capability 200s, zero 429 (old cap would 429 at #31). |
| lead | UX HONESTY WAVE: copy sweep + badge/pipeline truth 298ecca | closed | 2026-09-06 | User: "CPU update ROI" incomprehensible; frontend full of confusing slop; UX audit + update needed. ux-audit (designer, read-only, full report + live-observation addendum): ground truth at realtime_collaboration/frontend/ux_redesign/; TOP violations = renderer.mjs:848 canvas "CPU UPDATE ROI", "CPU window"/ROI-cropped footnotes, rate_limited-as-failure-banner, snake_case stage codes raw, badge words split 3 ways (Preview (owed)/Preview/PREVIEW · NOT EXACT YET; root cause design_plan §2.3 vs interaction_simulations §5 D2 disagreement), raw actor ids as collaborator identity, schema/dirty-nodes jargon, CPU·/Exact· compute-step duality, inline HTTP codes. Addendum: #exactBadge driven by CONNECTION (frozen "Exact") while class badge says "Preview (owed)" = D3 violation (fix = classFor() echo, NOT connection word); pipeline paint/done + idle verify while exact owed (S5/W-4 prescribe alive owed sub-display); refused submits got green checks (.is-failed CSS existed unused). LEAD ARBITRATION: D2 canonical, design_plan §2.3 retired. ux-fix (executor opus) 298ecca: all copy swaps (REFRESH WINDOW per I-06, sketch·/exact·, Analysis ready, Editor-N ordinals, hour-titles, seq-only receipts), exactBadge classFor() echo (I-29/L-6), .is-owed Ochre-dashed steady verify lamp + #jobStatusDetail realtime wiring + .is-failed refusal, rate_limited → §4 verbatim cooldown advisory + W-7 survived line, design_plan §2.3 D2 note. node 179/179 (zero test pins changed — /preview/i regex survived), pytest 23. DEFERRED: ledger five-stage dots, click-to-receipt, ladder tooltip, canvas furniture light-theme recolor. |
| lead | INTEGRITY: partial-commit repair 5154f04 + deploy | closed | 2026-09-06 | Caught post-wave: 1944cf8 staged the git mv renames (0-change) + wiring but MISSED the post-rename content edits (serve.py root-URL rewrite 125ln, app.mjs ?site= fallback, index.html docs link, README, smoke_proxy) — test_studio_serve.py was committed while the behaviour it tests was not; fresh clone of origin/main would fail. Live demo unaffected (fly builds from working tree, not git). fix commit 5154f04 restores the reviewed+verified content; pushed. LESSON: after git mv + subsequent edits, re-add the moved files before committing. Deploys this window: c3a810c + 298ecca → machine d8d5620b77e958 updated, caps 200 after boot, 600/min live. |
