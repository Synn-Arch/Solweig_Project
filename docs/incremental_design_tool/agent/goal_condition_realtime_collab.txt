Lead autonomous implementation of a collaborative real-time universal SOLWEIG editor in AlanSynn/solweig-project. Tree edits are only one adapter example. Support all enabled building, vegetation, land-cover, time/meteorology, model/receptor and view edits through the existing universal dependency graph.

Read first: docs/incremental_design_tool/realtime_collaboration/*, benchmarks/2026-09-04-perf-optimization-handoff.md, universal_editing/*, agent/mission_realtime_collab.yaml and agent/multi_agent_realtime_collab.md. Treat handoff numbers as evidence to reproduce, not truth. Maintain WORKLOG and SUBAGENT_LEDGER.

SLO: use 100 ms micro-epochs. Every operation durably accepted inside the measured admission envelope must be included in exactly one canonical workspace revision, visible remotely p99 <= 2W + 350 ms tick-anchored (W = micro-epoch tick; 2026-09-06 user re-spec after structural-miss evidence), and represented in a fast analysis revision p99 <=1,000 ms from acceptance. Never omit an accepted edit. Reject/backpressure before acceptance when the envelope cannot be honored.

Do not falsely require arbitrary exact geometry SOLWEIG in one second: measured native geometry edit is ~38 s. Separate canonical state, fast revision and exact revision. Fast result classes are fast_exact, fast_qualified with validated error/provenance, or visual_pending with no current numerical claim. Exact lane asynchronously reconciles the latest canonical snapshot. Stale jobs never publish.

Queue architecture is mandatory: durable idempotent operation log; deterministic source-specific conflict reducer; per-workspace epochs; heterogeneous edit coalescing; one dependency plan per epoch; deadline/fair fast queue isolated from exact CPU; exact latest-state revision-chasing queue with at most running+latest pending target per workspace. Supersede compute targets, never operations.

Computation priority: reproduce timing; instrument all latency stages; implement cheap exact paths for cache/view, UTCI/receptor and selected forcing; then persistent object-to-ray and per-cell directional occluder state for exact add/move/delete delta visibility/SVF; temporal checkpoints and sparse replay; scratch reuse/copy removal; profile-backed fusion/Numba/C++ only with independent differential proof. Never shrink physical read reach, raise the 6° sky floor, reorder accumulation while claiming bitwise parity, or reduce precision silently.

Compensation is allowed only as qualified provisional output anchored to an exact base revision. Define finite qualification domains by edit family/magnitude/context/time and validate max error, MAE, RMSE, p95/p99, boundary error, cross-family interaction, accumulated drift and out-of-domain detection. Outside qualification publish visual_pending, not fabricated thermal values.

Use maximum useful subagents in isolated worktrees: operation/CRDT, scheduler/admission, cheap exact kernels, geometry delta-SVF, temporal checkpoints, compensation, frontend collaboration, load/chaos, scientific reviewer, independent benchmarker and red teams. One writer per file. Every scientific change needs non-author review+differential runner; every performance claim independent reproduction. Lead integrates and commits only green gates.

Validate deterministic replay, conflicts, duplicates, heterogeneous epochs, zero edit loss, p99 deadlines, overload/backpressure, stale-result fences, exact oracle parity, compensation holdouts, one-hour soak, crash/restart and target-node RSS/latency. Report raw samples, host load, commit/model/cache hashes and admission envelope.

Stop only for material scientific ambiguity, controlled oracle non-determinism, required scope change without criteria, access/license blocker or unresolved scientific semantic conflict. Do not stop for ordinary failures. Done only when R0-R8 gates, full tests/docs/UI/deployment, scientific and compensation evidence, target CPU commissioning and clean main commits pass. Begin now.
