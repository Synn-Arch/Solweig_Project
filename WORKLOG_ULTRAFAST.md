# WORKLOG_ULTRAFAST

`docs/incremental_design_tool/ultrafast_bitwise/` 기준 task 실행 기록. Lead 소유(에이전트 직접 편집 금지).
Status vocabulary: PASS / FAIL / BLOCKED / NOT_RUN / TARGET_MISSED.

## Goal

GOAL_PROMPT.ko.txt: exact incremental editing engine의 초경량 CPU/CUDA 구현. Raw-bit parity vs 고정 full-domain oracle. 목표(미달성 설계 목표): site_500 selected-time exact E2E p95 CPU 100ms, canonical CUDA 33.3ms.

## Infra (lead-set)

- Main tree: `/Users/alansynn/Workspace/solweig` @ `e0d19fc98220a333ea312efe4604780803bbdd17` (== design review SHA, source diff 없음).
- Oracle pinned worktree: `/Users/alansynn/Workspace/solweig_oracle_e0d19fc` @ e0d19fc (detached). `Input_subset`, `Input_rasters`, `site-cache`, `.venv` symlink read-only. 모든 oracle 실행은 이 worktree 에서.
- Scratch artifact root (repo 밖, private planes 허용): `/Users/alansynn/Workspace/solweig_ultrafast_artifacts/`. Raw logs 반드시 여기 저장(/tmp 단독 금지).
- Design package installed: `docs/incremental_design_tool/ultrafast_bitwise/` (SHA256SUMS 전부 OK, comparator self-test 18/18 PASS @ 2026-09-07).
- Env(main venv): py 3.14.7, numpy 2.5.2, torch 2.14.0, numba 0.67.0. Host: macOS M1 Pro (darwin 25.5.0). 4-core 고정 reference host는 별도 확보 과제(당장 local dev baseline).
- Real inputs: `Input_subset/{DEM,Building_DSM,Trees}.tif` + `site-cache/site_500/`(manifest, static, svf, forcing, metfiles, baseline_results) — read-only.
- Incident 상태: incident-3 (#140) OPEN, fix agent 진행 중(worktree `agent-a0e362222c327873a`, locked — 간섭 금지). Incident-2 (#138) fix merged 58b4dfd+deployed, joint live verify 대기. T15 regression test 요구사항에 반영 예정.

## Entries

### T00 — baseline audit (PASS, 2026-09-07)

- Task: #141. Owner: uf-t00-baseline agent. HEAD: 시작 시 e0d19fc(== review SHA), 진행 중 lead docs commit 4f7d44a(source 0 변경, diff 확인).
- Files: `benchmarks/ultrafast/{harness_solve,build_manifests,compare_repeats,aggregate_runs,make_synth_fixtures}.py` + `baseline/`(11 manifests). Artifacts: `~/Workspace/solweig_ultrafast_artifacts/`(276MB, oracle_full/e1_r1/e1_r2/e1_warm/e2-e4/logs/raw_records.jsonl/parity_repeat_e1.json).
- Key evidence(lead 직접 spot-verify): oracle origin = `solweig_oracle_e0d19fc/solweig_gpu/__init__.py`(per-run `origin` field), shadow.py sha256 51551ca7 양쪽 동일(lead 재계산). E1 rep1↔rep2 raw-bit 0 mismatch(shadow/tmrt/utci, signed-zero/NaN-payload 감지 comparator).
- 측정(당일, 본 host, site_500, 24 steps): full oracle 317.6s(loop 315.8, svf 0.31 cached). E1 add-tree(248×220): cold+cold-page 161.5s, warm-page 58.9s, warm-process 57.1–62.9s(n=7 rows; svf ~34%, time loop ~63%). E2 58.4s / E3 55.3s / E4 63.7s.
- KEY FINDING: page-cache warmth가 cold penalty 지배(~2.7×). Cold/warm 분리 보고 원칙(TARGETS)의 실측 근거.
- Limitations: M1 Pro(4-core pinned reference 아님), host load 28–72(upper bound), radiation/thermal/UTCI sub-stage 미계측(source instrumentation 없음, T00는 source 수정 금지 → T01 harness seam에서), E5 met warm-start NOT_RUN, GPU 전 gate BLOCKED(장치 없음), n=2 cold/2+5 warm(≥30 paired는 T01+).
- Handoff 부정합 기록: agent가 주장한 per-record source_sha256는 run_record.json에 없음(origin path만 존재). Lead가 hash 직접 재계산으로 보완. 치명적 아님(경로+pinned worktree+동일성 확인).
- Status: PASS(제한 명시). 다음: T01(#142).

### T01 — bitwise harness + primitive matrix + CLI (PASS, 2026-09-07)

- Task: #142. Owner: uf-t01-harness agent (T00 author 아님 — role 분리). Base: 63ed145.
- Files: `solweig_core/{__init__,profile}.py`, `tests/ultrafast/`(bitwise_harness, primitive_matrix, test 3종+CLI), `benchmarks/ultrafast/run.py`, `baseline/{primitive_matrix,numeric_profile}.json`. Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t01/`.
- Lead 직접 verify: `pytest tests/ultrafast/ -q` = **84 passed / 1 skipped / 5.17s**. Mutation bite 실측 — 엔진에 1-bit flip → FAIL(1), -0.0 치환 → FAIL(1), identical → PASS(0). Forbidden comparator grep: docstring 금지 문구만, acceptance path 무결. frozen JSON 2종 존재(primitive_matrix 829KB, numeric_profile 3.9KB).
- RED witnesses 전부 작동(+0/-0, NaN payload/sign, 1-ULP, wrong time index, wrong window origin, shape, byteorder, empty, sentinel-vs-NaN, oracle origin swap — byte-identical이어도 origin path 틀리면 FAIL).
- **Primitive matrix 핵심 발견(T09 feed)**: torch 2.14 CPU vs numpy 2.5.2 — sin(30,598 elem)/cos(34,884)/tan(31,301)/pow_float(19,042 + signed zero)/exp(6,559)/log(7,050 + NaN-sign) 1-ULP급 불일치. torch dense↔compacted sin/cos는 실E1 extent(248,220)에서만 1-ULP 발산(lane/tail dispatch 민감). CLEAN: add/sub/mul/div/eq/lt/pow_int/round/sqrt. torch NaN-sign canonicalization(log/max/min), promotion trap(0-d f64/np.float64 scalar weak).
- T00 gap 폐쇄: 모든 신규 run_record에 source hash 5종 + origin 인증(T00 e1_r1/r2도 retro-certify PASS).
- CLI: baseline/compare/bench/report 4 subcommand live. fresh E1 ↔ T00 e1_r1 = 0 mismatch(전 24-time coverage).
- Limitations: CUDA row BLOCKED(장치 없음), host-pinned frozen profile(타 hardware에서 실패 시 JSON 재기록, test 완화 금지), T00 dir sidecar 부재로 strict cross-era compare는 metadata 비대칭 FAIL(by design), E5/trace-exporter NOT_RUN(T03 seam).
- Status: PASS. 다음: T02(#143 예정).

### T02 — buffer ABI + adapter seam (PASS, 2026-09-07)

- Task: #149. Owner: uf-t02-abi agent. Commit: **6977086** (base ce8715b).
- Files: `solweig_core/{abi,request,status,dispatch}.py`(신규, torch-free — tokenize 검증), `solver.py` seam(`solve_with_core_adapter`, 파일 끝 additive, `__all__` 추가), `worker.py`(`ExactWorker(core_adapter=False)` opt-in, default 분기 verbatim), tests 5종(100 tests).
- Lead 직접 verify: `pytest tests/ultrafast/ -q` 182P/2F/1S(2F = test_profile oracle-identity — 첫 sanctioned source touch로 by design, T01 owner에게 re-pin fix 발송). E1 gate: `samples.sample0.all_equal = true`(top-level False는 reference의 pre-seam solver hash pin뿐). mutation marker 잔존 없음.
- Gate: synthetic 40×80 + site_500 E1 adapter↔direct raw-bit 동일(e1_r1 plane hash 재현). 변환 counter delta 0(정적 AST + runtime guard). test_incremental_worker 55P(default 불변).
- Mutation 2종 bite: origin flip, seam 내부 loop 변환.
- 사고 기록(해결): agent 세션 중 cwd-drift cp가 oracle worktree의 solver.py를 seam 위에 덮어씀 → test contract로 재구성 + 게이트 재검증(E1 hash 재현으로 입증). 규칙 재확인: 절대경로만.
- 신규 reference: `t02_adapter_e1`(post-seam solver hash pin) — 이후 compare의 기준.
- Status: PASS(test_profile re-pin 대기 중 잠정 — fix 커밋 후 suite 전체 green 확정). 다음: T03(#154 진행 중).

## Entries (incident track, 병행)

### T03 — exact step tables + amplitude policy (PASS, 2026-09-08)

- Task: #154. Owner: uf-t03-tables agent(전달 후 세션 종료 — lead가 evidence 직접 검증). Base: cfeb3bf.
- Files: `solweig_core/step_tables.py`(454L, torch-free — StepTable/StepTableKey, amplitude policy registry 5종, canonical digest, trace producer), `tests/ultrafast/trace_exporter.py`(원본 코드 경로 trace 캡처), `tests/ultrafast/test_step_tables.py`(26 tests), `tests/ultrafast/step_table_trace_manifest.json`(74 traces sha256). Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t03/`(traces 74 + manifest).
- 방법론(설계 7.2 준수): dx/dy/실행 count는 원본 `march_offsets` import·실행으로 캡처. dz bits는 원본 one-line 산술 재표현 후 **nextafter double-probe**로 원본 stop 거동과 bit-exact 입증(count(c) >= k+1 ∧ count(nextafter(c,-inf)) == k). kernel-level 진실은 원본 `shadow()`/`shadowingfunction_wallheight_23` 직접 호출 replay gate. 두 kernel variant 절대 혼합 없음(svf_shadow: index 1 시작, azimuth 0→1e-12 치환, first-step reset / wallheight_23: index 0 시작, 치환 없음, (ds*index)*(tan/scale) 몫 먼저).
- **R6 amplitude bits**: `capture_amplitude_policy`가 site_500 실제 composed scene에서 effective/oracle-absolute bits + banded-escalation patch indices + timeloop escalation 캡처. RED witness: banded patch를 A_eff로 march하면 FAIL. site subset gate: 6°/42°/78°/90° + 모든 escalated patch가 원본 `march_offsets`와 bit 일치.
- Lead 직접 verify(전부 본 세션): (1) `pytest tests/ultrafast/ -q` = **224 passed / 2 skipped**(T03 39P/1S 포함). (2) **수동 source mutation** — producer에서 overshoot step 제거 → 20 failed + 15 errors(길이 fence + byte gate), revert → 39P 재현. (3) repo manifest ≡ artifacts manifest(sha256 90640c8e… 동일). (4) frozen 74 traces = synthetic_probe 정책(실경로 정책은 site gate에서 live 캡처 — manifest에는 미반입, T04+ producer가 진입점).
- Limitations: 범용(입력 임의) Torch-free table producer는 T14 — build_table_from_trace는 trace-derived만. CUDA row BLOCKED(장치 없음). trace 재생산은 exporter CLI로 가능(명령 header 기록).
- Status: PASS. 다음: T04(dense serial Numba march — 본 table 소비, G2 gate).

#### T03 addendum — author report received post-verification (2026-09-08)

- Author report consistent with lead verification (39P/1S file, 224P/2S suite, manifest sha 90640c8e, mutations bite). Env-gated full sweep: SOLWEIG_ULTRA_STEPTABLES_FULL=1 → all 153 site_500 patches == march_offsets bit-exact.
- **KEY FINDING (T04/T14 feed, recorded NOT fixed — solver.py read-only)**: azimuth EXACTLY 225.0°/315.0° — wallheight_23 KERNEL branch constants (f32 tensor 5π/4 = 0x407B53D2) pick the cos branch; helpers (_sun_first_step_distance/march_offsets/shadow(), python-float → f32 0x407B53D1) pick sin → ds differs 1 ulp. T03 tables match the KERNEL (marker-probe verified). The helper diverges on exactly those 2 wallheight fixtures — pre-existing source inconsistency, candidate for separate correctness change with its own oracle evidence.
- Zenith alt 90 is NOT no-march: tan(f32(π/2)) < 0 → dz negative → amplitude stop never binds → march runs to domain boundary (stop_reason=boundary). _patch_first_step_dz's -1 sentinel is classifier convention, not march truth.
- HONESTY note: site_500 has NO R6-banded patches (A_eff 57.85321044921875, A_abs 228.33999633789062, banded ∅) — escalation is pinned synthetically + by policy record, NOT by this site's geometry.
- Limitations (author-disclosed): wallheight replay compares march outputs only (vegsh/sh/vbshvegsh; wall/aspect tail march-independent); source_geometry_hash includes solver.py so unrelated commits drift the KEY (numerics pinned by content digests; drift prints regen marker, never fails alone); boundary-limited dz ladders expression-only (amplitude probe unreachable by construction).

### T04 — dense serial Numba march (PASS, 2026-09-08)

- Task: #155. Owner: uf-t04-march agent(보고 후 세션 사망 — report 전달 완료, lead 검증으로 마감). Base: 511092a.
- Files: `solweig_core/numba_cpu/{__init__,march}.py`(신규, torch-free), `tests/ultrafast/test_march_dense.py`(29 tests). Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t04/`(red_first_run, bit_gate_74_run, site_gate_run, final_full_run, timing_probe.py, timing_site_patch.txt, numba_cold_cache/, mutations/).
- 구현: `_svf_shadow_kernel` + `_wallheight23_kernel` — njit(cache=True, nogil=True, fastmath=False), serial. StepTable 소비(실행 step set + overshoot 포함, previous-state stop 재유도 금지). `_torch_maximum` NaN 전파 max. Python wrapper에 ABI ArrayView seam + variant/shape/dtype/bush guard. branch constants: svf=python-float f32-cast-at-compare, wallheight=f32 tensor 산술(225°/315° 1-ulp split 재현 — T03 addendum 1). zen리th boundary-limited march 특수화 없음.
- Gates(전부 author 보고 + lead 재검증): (a) RED first — march.py 부재 시 ModuleNotFoundError(red_first_run.txt). (b) 74 frozen traces vs 원본 shadow()/wallheight_23 raw-bit 0 mismatch(compare_plane_bits, signed-zero/NaN payload 포함). (c) site_500 SUBSET_ALTITUDES 20+ patch bit-일치 + escalated-regime witness(78°/90° absolute amplitude; site banded=∅ assert 고정). (d) mutation 4종(author: OOB -inf, first-step reset drop, accumulator/suppression 순서 교환, trunk gate vegdem2→vegdem — 전부 지정 test fail 후 byte-identical 복원). (e) purity — torch/fastmath/parallel/prange 부재 + subprocess torch-free import.
- **Lead 수동 mutation(본 세션)**: 최종 출력의 `if vbsh > zero: vbsh = one` threshold clamp 제거 → **15 failed**(frozen 74 + wallheight variants + extreme aspect). 복원 중 교훈: 두 kernel의 final block이 동일 문자열 → 첫 replace가 양쪽 모두 적용(str.replace 전역), anchor 기반 2회 복원으로 253P/2S 회복. threshold는 양 kernel 모두 존재 원본 의미 확인.
- 성능(참고, T05 입력): site_500 500×500 27-step march warm **0.0241–0.0246 s/march**(≈2.75–2.81e8 cell-steps/s). cold compile 0.235s, cache hit 0.028s. module import 4.2–5.8s(torch-heavy deps — T05에서 분리 필요). 대조: T00 full oracle time loop 315.8s.
- Sensitivity(author 문서화): tv-only OOB -inf는 gab/1000-clamp가 흡수(sh만 ta로 탐지), one-step witness는 first-step reset 불감(다중 step에서만 관측) — designated test 조정 사유 기록.
- Limitations: bush>0 wrapper refusal(원본 bush block은 marched-extent global reduction — per-target 독립 깨짐; site_500 bush=0). sparse/parallel/SIMD/fold = T05+. count-0 table은 직접 build 필요(exporter는 read-only).
- Status: PASS. 다음: T05(composed-diff corridor — C closure, DESIGN 7.3/7.4).

### T05 — sparse closure + row-run/CSR builder (PASS, 2026-09-08)

- Task: #155→#1. Owner: uf-t05-closure agent. Base: 6edc7b6 (미커밋 deliverable — lead 검증 후 이 commit에 포함).
- Files: `solweig_core/sparse_work.py`(781L, 신규, torch-free — numpy+stdlib만), `tests/ultrafast/test_sparse_closure.py`(1574L, 44 tests). Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t05/`(red_first_run, site_gate_run, suite_regression_run, final_full_run(_v2), site_measurement.json, mutations M1–M5 + lead_pre_offsets_drop/, sparse_work.lead_backup.py).
- 구현: composed pre/post value-diff C(IEEE 값-동등성 정책: NaN→changed, ±0→unchanged) → `D_p = C ∪ corridor(C, offsets_pre) ∪ corridor(C, offsets_post)`(clipped shift). 원본 guard 4종 보존(bush nonzero, clamp Case C strict `>` + 정확한 conjunct, one-step regime superset, state identity — 전부 torch 원본 대비 pin test). row-runs + 64×64 chunked bitsets 양 표현 + byte 추정 + ROUTE_DENSE_PAIR_FRACTION=0.5. empty-work shortcut은 C=∅ ∧ 전 patch table content-digest 동일일 때만(C=∅인데 table 변경 = 정직한 full-tile worklist, `table_changed_with_empty_C` flag).
- Gates: (a) RED first(ImportError 기록). (b) synthetic closure completeness — T04 dense kernel full recompute 대비 전 changed (cell,patch) ∈ closure. (c) site_500 F1 add / F2 move / F3 delete-overlap(all 153 patches, amplitude 67.49→62.497, tables differ): 0 closure misses, 0 splice mismatches. (d) untouched-state equality 매 edit 후. (e) determinism byte-identical rebuild. (f) purity(torch/fastmath 부재 + subprocess torch-free import).
- 측정: closure pair-step 비율 0.37–0.73% of full-tile dense. F1: C=13 cells, pairs 34,918, ratio 0.0037(T00 R4 dirty-window dense 대비 ~98×).
- **Lead 수동 mutation + gap 폐쇄(본 세션, 2단계)**: 1차 — pre-offsets corridor 항 제거(`out |= corridor_mask(C, offsets_pre)` drop) → **43/43 생존(SURVIVED)**. 진단: growth test는 post ⊇ pre(pre 항 구조적 여분), F3 site는 pre ⊃ post(patch 0: 322/298 steps, 24 pre-only offsets)지만 changed cell이 pre-only region에 없어 value-inert — 어느 쪽도 pre 항을 증명 못 함. Author에게 amplitude-SHRINK 방향 builder-level set-algebra witness 요청(`T = {c−d_pre} \ C \ post-corridor` 비공집합 + masks/row_runs/bitsets 멤버십). 2차 — author가 `test_amplitude_shrink_pre_only_offsets` 추가(44th), M5 지정 test. **Lead 독립 재실행**: mutation 하 FAIL(2.76s) → byte-identical 복원(sha 838e0327…) → PASS(2.41s). `tests/ultrafast` 전체 **297P/2S**(173.65s). sparse_work.py 순수성 grep 재확인.
- Mutation 총 5종 killed(author M1–M4: ∪C 삭제, old footprint, overlap-hidden deletion, post-offsets drop + lead M5 pre-offsets drop — M5만 lead 검증 라운드). Author M2 1차 witness 생존(vegdsm2 co-change) → float32 rounding-gap witness로 교체, lead에 정확히 공개됨.
- 교훈(기록): "survived mutation"의 첫 해석은 코드 결함이 아니라 **fixture 방향성 결함**일 수 있음 — growth/shrink 비대칭 확인이 진단의 핵심. 미커밋 파일 mutation은 in-repo artifact backup 필수(T04 교훈 재적용).
- Limitations: closure은 svf_shadow variant만 검증(wallheight_23 closure = T06+ march runner에서). corridor의 `slow_corridor_reference` 대칭(방향)은 reference 합치로 pin. veg_svf_state.py 미접근 확인(diff 없음). GPU 전 gate BLOCKED(장치 없음). site F3 value-inertness 자체는 미해결 물리 사실(변경 출력이 pre-only region에 안 걸림) — algebra witness가 designated killer.
- Status: PASS. 다음: T06(sparse Numba march + safe CPU parallelism — dense↔sparse↔parallel 1/2/4/8 threads raw equality).

### T06 — sparse Numba march + safe CPU parallelism (PASS, 2026-09-08)

- Task: #3. Owner: uf-t06-sparse-march agent. Base: a39c937.
- Files: `solweig_core/numba_cpu/sparse_march.py`(809L, 신규 — march.py read-only import 재사용: `_torch_maximum`/buffer seam/columns/guards), `tests/ultrafast/test_sparse_march.py`(42 tests), `tests/ultrafast/bench_sparse_march.py`(gate/bench CLI, run.py 미수정). Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t06/`.
- 구현: `_svf_shadow_runs_kernel`/`_wallheight23_runs_kernel` — T04 dense kernel body VERBATIM, cell-loop head만 row-run `(row, c0, c1_inclusive)` int32 순회로 교체. target은 (row,col) 좌표 주소 — linear index 형성 없음 → int32 overflow 구조적으로 봉쇄 + `check_work_domain` 경계 guard. 병렬: plan_chunks가 run index를 cumulative cell-count 균형 EXACT 연속 분할(중복 소유 거부, `_verify_chunks` spawn 전 재검증) — daemon `threading.Thread` × njit(nogil) — thread별 산술 = serial 산술. numba parallel/prange 미사용(purity grep + subprocess pin). SERIAL_MIN_PAIRS=16384 미만 serial routing(측정 교차점: 64 pairs 4.12× 손해 / 4096 0.53× / 65536 0.28× 이득). 취소: kernel이 매 row-run마다 shared int64 generation 재판독, `cancel()` = gen bump + join → 반환 후 write 불가(청구 창 = 1 row-run). 물리 crop 금지 — full logical domain 주소(bbox crop은 경계 읽기를 OOB 0로 바꿈).
- Gates(전부 author 기록 + lead 재검증): (a) RED first(ModuleNotFoundError). (b) dense serial ↔ sparse serial ↔ sparse 1/2/4/8 threads **raw uint32 bit 0 mismatch** — synthetic 192×384(40,539 pairs, 18,308 runs, wallheight leg 포함) + site_500 F1/F2/F3 각 153 patches + worklist 밖 stray-write 검사. (c) `tests/ultrafast` **339P/2S**(297+42). (d) purity. (e) mutation M1–M5 killed(chunk overlap[ownership property test가 killer — value-idempotent라 output gate로는 불감, 정직 기록], threshold drop, int32 boundary guard, cancel gen, bbox crop).
- **Lead 수동 mutation(본 세션)**: `_svf_shadow_runs_kernel` final graph의 `if vbsh > zero: vbsh = one` 제거(unique comment anchor + count==1 assert — T04 replace-ALL 교훈 적용) → **12 failed**(site gate + no-crop 포함). byte-identical 복원(sha aeb86ab2… = author pristine) → 42P. `tests/ultrafast` 전체 재확인 339P/2S(191.94s).
- 성능(bench_ablation.txt, n=7, load 13–29, 10-core M1 Pro — 4-core pinned reference 미확보 동일 제약): synthetic 55% mask dense 12.0ms vs sparse serial 7.1ms vs t2 3.97ms vs t4 2.14ms vs t8 1.87ms. **site_500 F1 stage: dense 14.51s vs sparse 0.80s(build_sparse_work 0.735 + march 0.065) ≈ 18×**. F2 0.86s / F3 0.94s. pair-steps 9.62M/3.29B = 0.29%(F1).
- **KEY FINDING(T07 라우팅 레버)**: sparse stage의 93%가 work-list BUILD(0.73–0.82s) — march 자체는 0.07–0.13s. thread scaling은 stage level에서 불가시(patch median ~374 pairs → serial routing); 병렬 이득은 큰 단일 work list에서만(t8 3.8×). T07은 build 비용(ComposeDiff/corridor numpy 루프)이 1차 최적화 대상.
- Limitations: wallheight_23 sparse runner는 synthetic work list만(T05 closure가 svf_shadow만 증명 — module docstring + tests에 명시). 4-core pinned ref 미확보. bench CLI가 site fixture compose를 위해 torch import(bench 파일 한정 — runner 모듈 자체 torch-free).
- Status: PASS. 다음: T07(packed state + canonical SVF fold — build 비용 레버 반영).

### T07 — packed bit state + canonical SVF fold (PASS, 2026-09-08)

- Task: #4. Owner: uf-t07-svf-fold agent. Base: d2c2f50.
- Files: `solweig_core/bitplanes.py`(379L — uint8 (rows,cols,⌈P/8⌉), patch axis LAST, little bit order, tail bits 항상 0(deterministic, layout 무관), VALUE-membership binary fence({0.0,1.0} 외 typed refuse — one-step vbsh==2.0/NaN/±inf 진입 봉쇄, regime fallback은 solver-side), CHUNK_CELLS=64 chunk 소유권 guard), `solweig_core/numba_cpu/svf_fold.py`(482L — packed byte 직접 소비, unpack-to-cube 없음), `tests/ultrafast/test_bitplanes.py`(33T) + `test_svf_fold.py`(27T) + `bench_svf_fold.py`. Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t07/`.
- 구현: patch-major/annulus-minor recurrence 원본 열거 순서 그대로(`svfveg += w_iso*v; svfaveg += w_iso*b` → E/S/W/N aniso pair), `last`=3.0459e-004 (vegdem2==0.0, -0.0 포함 NaN 제외) S/W 4-accumulator 가산, veg clamp 10종 strict `>`, `SVFtotal = svf − (1−svfveg)(1−trans)`, trans=0.03. **상수 동결(DESIGN 6.3)**: annulus weight/azimuth는 원본 torch 계산의 CAPTURED uint32 bit pattern — 재계산 없음(T01 trig 1-ULP 위험 회피), digest 0a13e80a…, test가 live recompute와 pin. Threading: T06 model IMPORT(plan_chunks/_verify_chunks read-only) — row exact 분할, daemon×njit(nogil), SERIAL_MIN_CELLS=4096.
- Gates(전부 author 기록 + lead 재검증): (1) oracle raw equality(compare_plane_bits uint32 view) — vegetated/unvegetated 40×80, non-square 64×48 negative-DEM, NaN/−0.0 scene, clamped regime(clamp 발동 assert), **site_500 500×500 full 원본 svf_calculator 대비 11 출력 전부 0 mismatch**(scene binary 검증 포함). (2) downstream 1-hop: svfbuveg = svf−(1−svfveg)(1−transVeg) utci_process.py:774/867 그대로 — 0 mismatch. (3) packed roundtrip 양방향 + tail padding + Fortran/strided source bit-exact. (4) affected-only: masked cell == full fold, unmasked == base bit-identical, empty mask → base 무변경, site_500 chunk-level 포함. (5) `tests/ultrafast` **399P/2S**(339+60). (6) purity — 두 모듈 torch-free(grep + subprocess).
- **Lead 수동 mutation(본 세션)**: S-direction aniso pair의 v/b operand 교환 → **5 failed**(clamped regime + site_500 all-outputs 포함). byte-identical 복원(sha 558aaa3a…) → 60P / 399P/2S 재확인. Author M1–M6(7 cycles: values→bool, tail nondeterminism, shared-byte race, pre-summed weights, reversed patch order, TRANS/LAST 1-ulp flip) 전부 killed, survivor 없음.
- 성능(site_500, n=7 median): 원본 svf_calculator full 69.1s(참조) → pack 0.082s(1회) + FULL fold serial 0.765s / t4 0.202s / t8 0.136s; AFFECTED 10%(24,940 cells) serial 0.082s / t4 0.024s. cold JIT 0.481s(warm 0.014s). **fold는 march와 달리 thread scaling 실제 유효**(row-parallel, cell cost 균일).
- Limitations: downstream coverage = svfbuveg 1-hop(asvf/diffsh/Tmrt = T08). one-step regime packed refuse는 typed이나 solver-side fallback wiring은 범위 밖(증인 scale=0.25 vbsh=2.0, 2,522 cells). constants pin test는 test 환경에서 torch live 재계산 필요(bench 파일 한정 torch — T06 선례). 4-core pinned ref 미확보. GPU BLOCKED.
- Status: PASS. 다음: T08(radiation hoist + ordered fusion — svf_fold.SvfFoldResult가 upstream contract, utci_process.py:867 svfbuveg부터).

### T08 — radiation hoist + ordered fusion (PASS, 2026-09-08)

- Task: #5. Owner: uf-t08-radiation agent. Base: c0e81bc.
- Files: `solweig_core/numba_cpu/radiation.py`(1884L, torch-free — frozen-input loader, hoisted mirror `mirror_radiation_timestep`/`mirror_sunonsurface`, fused cell-owned kernel `_fused_day_kernel`/`_fused_night_kernel` njit serial), `tests/ultrafast/test_radiation.py`(12T), `radiation_oracle.py`(capture producer), `radiation_frozen_pin.json`(361KB digests, self-standing), `bench_radiation.py`. Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t08/`(**5.4GB capture bundle** — commit 금지, capture-dependent tests skipif-absent로 정직 처리).
- **불변량 hoist 이중 분류(핵심 설계)**: (1) torch-exact-in-numpy(f32 plane ops, f64-adder accumulation `acc=f32(f64(acc)+P)`, cast-first 규칙)은 probe 검증 후 live numpy 재계산 — bit-identical 입증. (2) **SLEEF 의존(torch **4/sin/cos/exp ≠ 임의 torch-free 재계산)**: CAPTURED bits로 동결(static/azimuth_statics/walk_offsets/per-t tXX.npz) + digest pin + live torch-replay test(T07 pattern). **Hybrid status 기록됨** — pow4 planes·scalars, sinalt/cosalt, exp weight1, lv/Lsky/steradian/lumChi/cardinal tables, sos bool cubes, ks surfaces, night Lup = frozen; 나머지 전부 live torch-free. numpy f64 sin ≠ torch f64 sin (host probe evidence) — T09에 그대로 승계될 제약.
- Ordered fusion: per-cell dRad → 18-azimuth walk(stale-border plane-slice semantics 그대로: v_n = in_window ? src[r+dx,c+dy] : v_{n-1}, n==0 first-snapshot, keep==1 zeroing, branch1/branch2 facesh, **gn2 /second 비대칭** verbatim) → cardinal buckets → gvf tails → 6 TsWaveDelay blends → Kup → Kside 153-prefix → Kdown → dp pass 1(sky f32 plain adds / veg·sun·shaded f64-adder adds, k = kside_n + dp_rank[idx]) → **cell-local reflection barrier** `refl=((Ld_sky+lup_out)*ome)*half/pi` → dp pass 2 → Lside/Ldown 5-term → Lside_veg(tsw*0.5) → Sstr/Tmrt → POI. 2-pass sky→reflection 순서 + strict direction 부등식 원본 그대로.
- Stage manifest(T00식 exclusive): define_patch 20.39s / Kside 6.95s / sunonsurface 7.96s(incl) / gvf 0.22s(excl). T03 march 재사용 없음 — 별도 frozen-table manifest로 자체 관리(카드의 "재사용 전 별도 manifest" 조건의 재해석 — 재사용 자체가 없었음).
- Gates(author 기록 + lead 재검증): (1) intermediates **1746 ok / 0 bad**(24 timesteps 전 plane set, synthetic + site_500). (2) Tmrt/shadow plane set 포함 + synthetic 18-azimuth sunonsurface differential(40×80, 전 azimuth × 5 returns, raw uint32). (3) **fused full loop 685/685 ok, 0 bad**(state threading t0→t23). (4) `tests/ultrafast` **411P/2S**(399+12). (5) purity(AST walk + subprocess — runtime imports {__future__, dataclasses, numba, numpy, pathlib}). (6) mutation M1–M5 5/5 killed(incomplete-sky reflection 63bad, direction merge 54, f64-adder→f32 18, stale sunlit 62, halo 162 — 전부 contiguous 0-16 verify + sha 복원 재green).
- **Lead 수동 mutation(본 세션)**: fused kernel gn2의 `/second` → `/(second+1)`("sibling과 일치"라는 리팩터 함정 — source의 문서화된 비대칭) → **3 failed**(fused full-loop parity + fused==mirror 포함). byte-identical 복원 → 12P / 411P/2S 재확인.
- 성능(n=5 median, load 5.9–9.7): oracle radiation stage 43.81s/24t(full solve 49.31s의 88.9%) → hoisted mirror 28.05s(0.64×) → **fused 13.27s(0.30×, oracle stage 대비 3.22×)**. warm per-day-step 0.766s. cold JIT 18.61s(compile ~5.3s, disk cache).
- Limitations: SLEEF frozen family는 torch-free 재계산 불가(capture+pinned — T14 packaging에서 의사결정 필요). water(Twater) branch site_500 미발동(lc=0 전역 — frozen via flag, synthetic lc=0 경로만). full solve의 비-radiation 5.49s/24t 미착수(march=T04-06, utci=T09). fused kernel serial(parallelism 후속 — parity first). verify driver는 contiguous timestep 필요(문서화, runtime 제약 아님). 4-core pinned ref 미확보.
- Status: PASS. 다음: T09(UTCI compatibility kernel — T01 primitive matrix의 log/exp/pow 불일치 해결 + T08 frozen 전략 승계).

### T09 — UTCI compatibility kernel (PASS, 2026-09-08)

- Task: #6. Owner: uf-t09-utci agent. Base: ea3bd11.
- Files: `solweig_core/numba_cpu/utci.py`(635L, torch-free), `solweig_core/numba_cpu/math_compat.py`(566L — T09 신규 공유 프리미티브 층), `tests/ultrafast/test_utci.py`(552L, 12T), `tests/ultrafast/bench_utci.py`(184L). Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t09/`(probes p1–p9 + SLEEF shim 원본 소스/dylib + mutations M1–M7 + bench_kernels_r7.json / bench_oracle_stage_r5.json + utci.lead_backup.py + lead_ultrafast_full.txt).
- **핵심 결과: UTCI 레벨 primitive 전부 CLEAN torch-free** — fallback 없음, T08식 frozen 없음(T08의 SLEEF 의존과 대조적). 세 축: (1) **SLEEF 포트** — sleef_expf/xexpf, sleef_logf_u1(torch log 내부 u10 symbol → u1 body로 pin, P5b/P5c: torch==bundled-u10-symbol 0/36004), xpowf body. (2) **P9 발견 — torch pow(Tensor, Scalar) chunk-tail 레인은 opmath_type<float>=DOUBLE 경로**: `pow(f64(x), f64(e))` 결과를 f32로 single-rounding — libm powf 아님(sleef≠f64pow ≈1.8%, f32pow≠f64pow ≈0.64% 레인 존재). 정확한 layout 모델: threads_eff=min(T,⌈n/32768⌉), chunk=⌈n/threads_eff⌉ contiguous, tail=per-chunk 뒤 len%8 레인(소형 n은 단일 serial chunk 동일 규칙). e∈{−2..3}은 scalar-exponent exact forms(1/(x*x), 1/x, 1.0, x, x*x, (x*x)*x — P5f int==float dispatch 포함, NaN/±0 base 거동 핀), e∈{4,5,6}은 SLEEF body + opmath-f64 tail layout routing. **T01 pow_float divergence 메커니즘 여기서 폐쇄**. (3) **nadd/nsub/nmul/ndiv first-NaN-operand wrapper** — torch elementwise는 첫 배열 피연산자의 NaN payload를 결정적 전파, numba 스칼라 체인은 LLVM 스케줄러가 교환 가능 사이트를 commute할 수 있어 payload 승자 미정 → wrapper로 pin(유한값은 plain op와 bit-identical).
- 211-statement 다항식 커널: oracle AST에서 생성(t09/probes/gen_poly_kernel.py)되어 **커밋된 POLY_EXPRESSION_SOURCE 단일 소스**에서 exec(Python 3.14 nesting cap 우회) + njit — 커널과 소스 블록이 분리 불가(drift 구조적 봉쇄), test_utci.py가 oracle 재파싱 대비 AST 구조 동등성 pin(M1 killer). NaN 레인은 VALID 유지(`NaN <= -999` False — 마스크가 NaN을 시험 안 함). prange bit-safety by construction: 레인 값은 (k,n,T,grain)에만 의존, 스케줄 무관; dense count pass는 serial integer reduction.
- EXPRESSION_MANIFEST(schema "utci-expression-manifest/1"): 21 표현식별 primitive/dtype/context/resolution + layout_model + left_for_T10(R5 warm-start — T10이 pow≥4 도입 시 math_compat 프리미티브 재사용 + layout 규율 + T08 frozen context 선행 확인 요건).
- Gates(author 기록 + lead 재검증): (1) dense parity — synthetic 6 scenes × 204,099 lanes raw uint32 0 mismatch. (2) sparse bitidentity + odd tails n∈{209421, 8191, 13, 7, 1} 0 mismatch(tail 레인이 정확히 opmath scalar pow가 사는 곳). (3) **site_500 체인 t∈{0,5,12,23}**: march→svf→radiation→UTCI 209,422 valid lanes/timestep 전부 0 mismatch, digest 7d002c29f84e173c pin(p7_site_chain.json). (4) `tests/ultrafast` **423P/2S**(411+12, 327.57s — lead_ultrafast_full.txt). (5) purity — 두 모듈 torch-free(grep exit 1 + subprocess). (6) mutation M1–M7 7/7 killed(AST 구조, exp 포트, pow tail layout, exact-form 오류, NaN wrapper 제거, scatter 순서, 마스크 방향).
- **Lead 수동 mutation(본 세션)**: 포화 기압 체인 `es = nmul(G7, sleef_logf_u1(tk))` → `np.log(tk)`(libm logf) → **2 failed**(dense parity + sparse odd tails). byte-identical 복원(sha 76f42c71…) → 12P / 423P/2S 재확인. mutations/lead_sleef_log_swap/README.md.
- 성능(bench_kernels_r7.json, n=250,000 valid, reps 7): dense median **0.0733s vs torch 0.0727s(1.008× — 사실상 패리티)**, sparse 0.0796s. serial njit 0.52s → prange가 격차 폐쇄. cold JIT dense 4.18s / sparse 1.48s(disk cache 후 warm). oracle UTCI stage(t∈{0,5,12,23}) 0.298s(r5, load 5.7–5.9) — per-timestep ≈0.075s로 후보와 정렬.
- Limitations: bit-exactness는 **extent(n)과 thread count 의존 by construction** — layout 모델 파라미터(torch_threads=8: t08 capture manifest, grain=32768: torch 2.14.0 macOS arm64 empirical). pow Scalar routing은 e∈{4,5,6} 검증(UTCI 도메인 밖 일반화 안 됨). SLEEF 포트는 macOS arm64 libm/sleef ABI 기준(타 플랫폼 재검증 필요). M1 killer가 AST test인 점 기록(값 레벨 불감한 구조 변화의 designated killer). 4-core pinned ref 미확보. GPU BLOCKED.
- Status: PASS. 다음: T10(thermal adapter + exact sparse temporal replay — R5 활용, checkpoint fingerprint vs next_step, geometry-history/met-prefix 무효화 분리, RED witnesses 6종).

### T14a — UTCI族 시작 재컴파일 제거 (PASS, 2026-09-08)

- Task: #15(T14 감사 후속 소과제). Owner: uf-t14a-startup agent. Base: 547991f. 커밋: **본 항목과 함께**(utci.py, math_compat.py, _utci_poly_gen.py 신규, test_startup_cache.py 신규 6 gates, bench_startup_t14a.py, bench_utci.py docstring 정정).
- **근거(T14 audit)**: clean-host process-to-ready 6–11s vs 3000ms 목표(`startup_ms.cpu_aot_prebuilt_site_process_to_ready_p95_max`). 단일 주 원인 — UTCI族 전원 `cache=False`: (a) poly 커널은 `exec(compile(..., "<utci_poly_element>", ...))`로 정의되어 `co_filename`이 실재하지 않는 파일 → numba 0.67 cache locator 전멸(`os.path.exists` 요구, `RuntimeError: no locator available`) — **구조적 cache=False**, (b) 나머지 3 utci + 14 math_compat 커널은 flagship이 못 해서 전가족 blanket — **우발적**.
- 수정: (a) 17개 플래그 반전 `cache=True`(fastmath/error_model/parallel/레이아웃 무변). (b) poly 커널 → **실재 모듈 파일 `_utci_poly_gen.py`로 byte-exact 물질화** — POLY_EXPRESSION_SOURCE(sha256 `6d7b7a57…`, main↔worktree lead 재확인 동일)는 단일 소스 유지, `_poly_gen_module_source()`가 헤더(소스 sha + 재생성 명령 포함)+종래 statement builder로 재생성, **import-time drift gate**가 파일≠생성물이면 import 거부(numba staleness가 mtime+size지 content가 아닌 점을 보완 — cache artifact보다 먼저 발화).
- Gates(6, 전부 green): W1 warm-start(16개 family member .nbi 전부 + 2번째 프로세스 재컴파일 0 + 재생 ratio>2 asserted, wall-clock 비단정), W2 **cross-process bit parity**(cold-cache 첫 컴파일 = warm-cache 재생 = in-process, NaN payload/±0/-999/divergent pow bits 장면, digest 3종 pin), W3 결정성(2 fresh cache dir 동일 재고+digest) / drift gate(동일 길이 계수 drift → import 거부) / staleness(TK_OFF 편집 → 편집 bits, stale bits 아니음), W4 purity(toolchain 대비 delta 공집합).
- 성능(n=3 median, t14a/timing/): **p2r_jit_s 7.15s → 1.27s warm**(cold 11.08s — 첫 컴파일 불변, 배포 시 warmed cache dir 탑재 필요). utci_dense warm 4.312→0.0185s, sparse 1.588→0.0051s. 잔여 warm p2r는 radiation cache load 0.795s + march 0.185s(radiation/march 레인 소관, T14 참조). 3000ms D1 budget에 ~1.7s headroom.
- Mutations: author 3/3 killed(MT1 gate bypass, MT2 계수 drift, MT3 dense cache=False 복귀). **Lead 독립 mutation**: drift gate 비교자 weakening(byte `!=` → `len() !=` — 동일 길이 drift에 우회) → killer `test_drift_gate_edited_generated_module` FAIL(0.57s, returncode 0으로 import 성공) → byte-identical 복원(sha `91016f68…`) → PASS. mutations/lead_gate_len_cmp/.
- Lead 검증: footprint = 선언 surface 정확(math_compat 28줄 = 14 flag flip만, utci 99+/24-), 전 diff 읽기, POLY_EXPRESSION_SOURCE 불변 sha 대조, 생성물==생성자 byte 확인, **main에서 full suite 429P/2S**(423P/2S + 6 신규, 458.32s).
- 사고 기록: worktree에서 `test_sparse_closure.py::TestSiteClosure::test_measurement_artifact_written` FAIL — **T05 레인 선행 결함**(선행 family 테스트가 module fixture 공유 list에 append하는 구조 — isolated/스킵 환경에서 `assert []`). T14a 무관, main full suite에서 green. T05/T11 레인에서 fixture 자립화 필요.
- Limitations: cold 첫 프로세스 비용 불변(~10–11s stack JIT) — 배포 warmed cache 탑재 필수(march/radiation 포함). numba staleness = mtime+size(손written 커널은 numba stamp에 의존, 생성 모듈만 content-level gate). `_utci_poly_gen.py` header 변경 시 lineno shift로 고아 .nb* 1회 재컴파일(무해). T14 본과제 과제: R1(torch 의존 캡처), R2(SLEEF sin/cos 포트), R3(unseen-angle step-table 생산자), R9(5.3GB capture → loader-keys), radiation/march warm 시작 잔여 1s.
- Status: PASS. 원본 12-task 순서는 T10 진행 중.

### T10 — R5 warm-start + exact sparse temporal replay (PASS, 2026-09-08)

- Task: #7. Owner: uf-t10-thermal agent. Base: dc61d85. 커밋: **본 항목과 함께**(`solweig_core/numba_cpu/thermal.py` 신규 702L, `solweig_gpu/incremental/executor.py` 좁은 seam 38+/19−, tests 5종 + mutation 게이트 도구 2종).
- **구조**: on-disk 계약 무변경(SCHEMA_VERSION 2) — 전부 in-memory 확장. (1) 상태 어휘 — 6 plane(Tgmap1/E/S/W/N, TgOut1) + carried scalar(CI/firstdaytime/timeadd/Twater, Twater는 absent=None=미설정 절대 default 아님). (2) `thermal_state_from_payload` — atomic validate-then-build(coverage-only 거부, f64 거부, shape 일관성, plane별 sha256 fence). (3) `classify_anchor`/`select_warm_anchor` — **geometry-history 무효화(composed_scene/resolved_landcover/model_parameters)와 met-prefix 무효화를 분리**, met digest는 후보 자신의 next_step에서 철자(절대 r0 아님). (4) `replay_thermal`/`split_replay_thermal` — next_step에서 정확 재개, global-t scatter, phase-1 폐기/phase-2 수집, off-by-one fence 3종.
- **executor seam 의미동등(lead 확인)**: `checkpoints.thermal_fingerprint` 키 = geometry 3 + met_prefix 정확 4개 — 새 classifier가 전부 컴파일러 없이 커버(기존 전체-dict 비교와 수용/거부 동일). reason 부분문자열 보존("coverage-only (no warm state)", "beyond the first changed row", "torn/corrupt", 'met_prefix'). 인접 solweig_gpu 스위트 64P + 42P/1S(worktree; main 재확인은 suite 후).
- **G2.1 실측 답(측정 먼저)**: warm 경로는 실재하나 구조적으로 좁음 — canonical 5-edit에서 warm hit은 **[m3]만**(1/5). m1 prefix-coverage 거부, m2 첫 in-day 편집(anchor next_step=24 > r0) cold split + 재고정@20, m4 전 prefix 무효 resume 0, v1 geometry 편집은 warm 경로 미발동. full-solve anchor 구조적 사용불가, warm hit은 warm 경로 자체의 r0 재고정 체인으로만. pinning 테스트로 고정.
- 성능(t08 capture, 500×500×24, reps 3): fresh-full 24 steps/14.26s, cold-split 24/13.84s, **warm 4 steps/1.83s(resume@20)** — steps_solved 24→4. warm-vs-capture uint32 mismatch 없음.
- Gates: RED-first(ImportError 선행), warm/cold/fresh raw-bit 동등(전 24t/20 plane, uint32 직접), t-coverage/scatter 6종, steps_solved 실감축, 순도(thermal.py torch-free — lead grep exit 1 재확인). author mutation 6/6 killed(M1 geometry gate, M2 digest-at-r0, M3 off-by-one, M4 Twater default, M5 t-scatter, M6 torn fence).
- **Lead 독립 mutation — 스캔 순서 반전(reverse=True→False): 첫 시도 SURVIVED**. 적용 가능 후보 2개일 때 newest-first가 어디에도 pin 안 됨(author 테스트는 "fingerprint decides, not age"만 — 유일 적용 후보가 older). **테스트 격차 → lead가 pinning 테스트 추가**(`test_newest_applicable_selected_among_matching`, 양쪽 입력 순서, pristine 6P) → mutation 재적격 **KILLED**(0.34s) → sha 복원 `c2c2810a…` → green. mutations/lead_scan_order/.
- Limitations: 편집-met bundle은 본 host에서 torch-free 재계산 불가(T01 frozen transcendental) — 편집-suffix 동등성은 내부 differential(진짜 perturbed suffix, edit-sensitivity 입증), 외부-oracle 동등성은 무편집 capture 열. Twater는 carried/refused-not-defaulted이며 진행 않음(T08이 수분 변화를 Tg plane에 동결 — live midnight 재계산은 T11). 공간 상태 재사용 미추가(상류 radiation/GVF 의존 미증명 — 카드 범위 밖). bench wall은 host load ~15 하에서(steps_solved가 종료 기준).
- T11 인계: 편집-met frozen bits용 torch-fallback 커널, torch-free Twater midnight 진행, full-solve worker의 warm-anchor 출판 가능성(현재 warm 경로만 재고정), GPU 레인 패리티.
- Status: PASS. 다음: T11(전체 fallback + 전 편집 family) — #139 incident(server/incremental)도 executor 변경 착지 후 재개 가능.

### T12 — CUDA canonical kernels (PASS, 2026-09-08)

- Task: #8. Owner: uf-t12-cuda agent. Base: 547991f. 호스트: cura RTX A6000(sm_86, CUDA_VISIBLE_DEVICES=0 pin, torch 2.5.1+cu121). 커밋: **본 항목과 함께**(`native/cuda/` — host.py·build.py·src 6종·include 3종·tools 6종·data pin 5종 72MB, tests/ultrafast CUDA 11종).
- **Canonical 프로파일 빌드 규율**: `--fmad=false --ftz=false --prec-div=true --prec-sqrt=true` — contraction/FTZ/근사 div·sqrt 전면 차단. 상수는 전부 **exact hex float literal**(CPU 참조에서 생성 — nvcc 십진 파싱 불일치 원천 차단). FMA는 SLEEF 자체 vfma 위치의 명시적 `__fmaf_rn`만. CUDA NaN canonicalization(0x7fffffff) vs x86 payload 보존 — sw_n* wrapper·SLEEF 포트에서 CPU 규칙 명시 미러. content-hash 재빌드 모델(타임스탬프 추측 없음).
- **sw_opmath_powf**: torch opmath pow(f64 pow single-rounding)의 e∈{4,5,6}을 **3-limb(144-bit) 정확 정수 멱**으로 구현 — double 1회 반올림 후 float 1회(plain __int128은 e=6에서 2^144 wrap = pins가 잡는 silent corruption급). NaN payload·부호 케이스 glibc pow 규칙 pin.
- **5단계 게이트 전부 PASS(canonical CUDA ↔ canonical CPU raw equality)**: (1) primitives 15T(SLEEF 포트 + IEEE + subnormal 생존 + is_libm_lane 테이블), (2) march(frozen-74 + 변형 + one-step witness + site_500 per-t digest + oracle cross-anchor), (3) fold(10케이스 × 11출력 raw bits + affected-chunk-only), (4) radiation — crop pins 전 t + **full-site digest 4개 t08 capture**(day t12, nights t01/05/23), (5) UTCI 18T(live numba-CPU differential, n=40005 straddle, 결정성, 기하 불변). 통합 회귀 **61P+1S**(cura, lead 재실행).
- **Legacy**: kernel-scope PASS(oracle gpu_harness march sh/vegsh per-t digest 10t 재현, legacy==canonical march bits) / cube-scope(shadow 6c62bcea…, tmrt, utci 전 24t)는 solver cross-t 상태 반송 필요 → **T13**. legacy 캐릭터화: FMA contraction이 rad day/night lane 12–21% 이동(최대 |Δ|≤1.2e-4), UTCI 전 lane bit-identical(sw_n* wrapper가 contraction 격퇴).
- 프로파일 분리: 미지 프로파일 ValueError, 프로파일별 lib·캐치, legacy 인증 거부, cross-profile buffer RuntimeMismatch. 순도: poisoned-torch subprocess에서 host 로드·빌드·런치 PURITY_OK.
- **성능(site_500, kernel-only = CUDA events / E2E)**: march_svf 0.253ms/1.27ms, march_wall 0.299/1.30, fold 0.734/3.14, utci_dense 0.551/1.70, utci_sparse(n=237500) 0.259/1.03, **rad_day 12.99ms/43.1ms**, rad_night 0.008/13.4. rad_day H2D 202MB=26ms가 E2E 지배 → **T13 residency가 차감 과제**.
- Author mutation 4/4 killed(fold FP-reduction 재배열, utci schedule leak, expf 근사, radiation FMA contraction). **Lead 독립 mutation — is_libm_lane chunk 경계 `>=`→`>`**: killer `test_is_libm_lane_table` 6 불일치로 **KILLED** → 복원 중 **stamp-cache 함정 직접 확인**(고정 lib path가 mutated .so 유지, 원 digest 스탬프로 rebuild skip → 3 stale fail) → `rm -rf build/canonical` 재빌드 → 33P → cura 전체 61P+1S. mutations/lead_libm_lane_off_by_one/.
- 사후 발견 수정: host.py `sw_timer_end.restype` 미설정 → ctypes가 EAX 읽음 → CUDA-event 계측 전부 0.0 — 수정 + event 0.739ms≈wall 0.744ms 검증 + 회귀 재실행.
- cura 위생: 잔존 bare REPL 1건(우리 venv, GPU 0%) lead가 kill — ulg-venv viewer 2건(22일)은 사용자 타 프로젝트 무관. 검증 후 GPU 4장 유휴.
- Limitations: cube-scope legacy digest(T13), UTCI 게이트가 frozen site pin이 아닌 live differential(임의 extent 커버 — pin-only CI용 site pin 추가 여지), radiation pin이 day 1 + night 3 capture(day-path pin 확대 여지), rad_day kernel 13ms + H2D 26ms(T13 residency), fold/utci bench 입력은 합성 site-size(연산 수 고정이라 타이밍 데이터 무관).
- Status: PASS. 다음: T13(residency/graphs/dispatch — cube-scope legacy + H2D 상각).

### T11 — torch-free full-solve fallback + router (PASS, 2026-09-08)

- Task: T11(세션 task 번호는 compaction으로 소실 — worklog에는 owner/base로 특정). Owner: uf-t11-fallback agent. Base: e68e330(main은 dispatch.py 무변경 확인 후 병합). 커밋: **본 항목과 함께**.
- Files(12): `solweig_core/numba_cpu/{met_recompute,full_solve,fallback_router,sleef_trig,sleef_dp,sleef_f32_extra}.py`, `solweig_core/dispatch.py`(+prefer_backend, "numba-full-solve" 등록 — 미지 백엔드 TypedRefusal UNKNOWN_BACKEND), `tests/ultrafast/{test_met_recompute,test_full_solve,test_fallback_router,test_sleef_trig}.py`, `tests/ultrafast/bench_full_solve.py`. **bush_march.py(332L) 제외** — importer 0·테스트 0 orphan(lead가 repo-root 절대경로 재검증), artifacts 백업 `t11/orphan/bush_march.py`(sha f2a2091e…). Author report가 5개 파일 누락 보고(bush_march 포함) → lead가 import graph 수동 전수 확인으로 보완.
- **met_recompute**(1083L): 캡처 1타임스텝 + 편집 met 계열(Ta/RH/radG/radD/radI/P/Twater)로 day/night 전 met-의존 필드 재생성. 초월함수 전부 SLEEF numba 포트. dtype pin 핵심: torch `360/int64 0-dim`은 reciprocal-multiply(360/30=12.000001f, P11g), Lsky 밴드 산술은 f32 승격_dimensioned-wins(P11h/i/k), Tg ramp 혼정밀(dectime f64 − SNUP f32, P11d), CI flavor(tensor/pyfloat)가 esky 보정·Tgwall 승격을 갈림. `torch.sum` f32 = SumKernel cascade port(_VEC_F32=4, grain 32768, P10i-k). Typed refusal: landcover==1, onlyglobal≠1, anisotropic_sky≠1, patch_option≠2, elvis∉{0,1}, Perez alt<0.
- **full_solve**(414L): 캡처 전창 cold/warm 풀. ProfileFenceError(캡처↔프로파일 결속 — geometry edit은 재생성 강제), StaleCheckpointError(warm은 classify_anchor가 candidate 자신의 next_step에서 met-prefix 대조). mid-window anchor(publish_anchor_at)는 메모리 내 반환. 순도: 런타임 경로 torch transitively 0.
- **fallback_router**: 4행 결정 테이블 — (1) geometry edit → 무조건 regenerate-cold-full(anchor 있어도 veto), (2) met edit + 적용 anchor + warm이 **실측** 저렴 → warm-sparse-replay, (3) 나머지 → cold-full-solve, (4) 수집 출력 > 메모리 예산 → batched collect. 비측정/0 비용은 row2 거부. bench JSON 로딩은 `SOLWEIG_ULTRAFAST_COSTS` env(lead 수정 — 하드코딩 home 경로 제거).
- **패리티 증거**: (a) met identity — 편집 없는 캡처 met로 전 24t × 모든 재생성 필드 raw bits 0 mismatch; (b) full-solve identity — 전 21 ret 평면 + ret_CI 24t 0 mismatch(met_recompute identity ∘ T08 fused-kernel raw parity 합성); (c) edited-met — **honest limit**: 편집 met에 대한 torch 캡처 오라클이 존재하지 않아 warm-vs-fresh 내부 differential로만 검증(동일 입력 warm 재개 ≡ cold 전량재계산, raw bits). 외부 오라클 증명은 불가능한 구조 — 한계로 명시.
- Author mutation 4종 killed(Lsky 스왑, profile-fence 우회, steradian 진짜 나눗셈, router geometry veto). **Lead 독립 mutation — mid-anchor met-prefix off-by-one**(`series[:t+1]`→`series[:t]`): killer `test_warm_vs_fresh_differential_edited_met`가 StaleCheckpointError로 **KILLED**(1F/10P 32.44s) → sha 검증 복원 → 11P. 대안 후보였던 replay-one-extra-step는 deterministic-recompute 멱등성으로 생존했을 것(T05/T10 교훈) — 기각 사유 기록. `t11/mutations/lead_anchor_prefix_off_by_one/`.
- **P11l anomaly(기록, 미해결·비차단)**: 오라클 `Perez_v3`를 기록된 인자로 직접 재호출하면 frozen `kside_in_lv`와 t15(1)/t16(56)/t17(143)/t19(64)/t20(99) / 459 lane에서 1-ULP급 불일치(t08–20만 검사, t21–23 미검사). 반면 **권위 gate** — numba 포트 perez_v3 vs frozen bits, 전 24t raw — 0 mismatch. 포트는 기록 진실과 전면 일치; probe 자체 인자 재구성(또는 torch 내부 dispatch)이 원인 후보. 근본 원인 미규명, frozen-bit gate가 지배하므로 차단 아님.
- Bench(site_500 500×500 24t, **host load 35–65 — 비관 상한**): cold full-solve 중앙값 **996.98 ms/step**, warm sparse replay **695.85 ms/step**, 수집 출력 21 MB/t. t08 캡처에서 5.3GB 전체 재료 로드 잔존(~1s, T14 과제). T16 수용 목표(100 ms/step)에는 warm 레인조차 미달 — TARGET_MISSED 가능성 있는 그대로 기록, acceptance_targets.yaml 불변.
- Lead 검증 절차: footprint(전 수 읽기 + orphan 발견·처분) → 50P worktree 재실행 → lead mutation KILLED/복원 → main 병합(12파일 byte-identical, sha 대조) → **main 전체 suite 527P/54S(623s)** → 본 항목.
- Limitations: edited-met 외부 오라클 부재(위), P11l 근본 원인 미규명, geometry_fingerprint_of_capture의 static-plane digest fallback(manifest 기록 우선 — fallback 시 장면 계보 약한 표현), bench 부하 조건, 4-core pinned reference 미확보(T16).
- Status: PASS. 다음: T13(cura — residency/graphs/dispatch, cube-scope legacy, rad_day H2D 26ms 상각) / T14(R1 torch-capture 잔여, R3 step-table producer, R9 loader-keys 재발행, warm-start 잔여 ~1s).

### T13 — GPU residency, graphs, dispatch (PASS, 2026-09-08)

- Task: T13. Owner: uf-t13-gpu agent. Base: 346ac5a. 호스트: cura analytica, GPU 0 RTX A6000 pin(bench 시 load 2.56/1.60/8.00 경량). 작업 dir `~/solweig_ultrafast/work/t13/repo`(T12 오버레이 대신 committed native/cuda 기반). 커밋: **본 항목과 함께**.
- Files(11): 수정 3(`sw_api.cu` — monotonic alloc counter, `sw_utci.cu` — `sw_utci_sparse_bucket_kernel` 추가(기존 kernel 무변경), `sw_kernels_decl.cuh` — prototype) + 신규 8(`sw_residency.cu` 333L residency ABI — pinned mem·stream·event·2D async copy·기존 T12 kernel의 stream-param launch(인자 순서 동일, grid (n+255)/256 동일)·graph capture/instantiate/launch, `residency.py` 541L — `ResidentRadRunner`·`OutputLease`·`(profile_id, digest)` registry, `graphs.py` 352L — `capacity_bucket`·`GraphedUtci`·`UtciGraphPool`, `dispatch_cost.py` 117L — measured-only `Dispatcher`, 게이트 3종 32T(리드 stale-lease +1 포함), `bench_cuda_t13.py`).
- **Residency**: 정적 site 입력(~178MB — diffsh·packed cube·albedo/emis plane) 상주, timestep 입력은 **변경 row-chunk만** H2D — 변경 검출은 chunk 단위 uint8-view 비교(NaN payload·±0.0 반드시 전송 → 부호화 0 역직렬화 불가). 동일 rebind 0 byte, 40-row 편집 정확히 `3*128*cols*4` byte. 창 다운로드 == 전체 슬라이스 raw bits.
- **Fence lattice**(전부 RED-first): publish fence(`PublishPendingError`), reuse fence(`BufferPendingError`), profile fence(`(profile_id, digest)` key), padding fence(bucket kernel이 `k >= n`를 읽기·쓰기 전에 guard — 0xDEADBEEF 꼬리 생존), freed-pointer fence(`GraphBufferFreedError`), CPU-selected zero-alloc(monotonic `sw_alloc_count`). **+ lead 발견 보강: stale-lease fence(`LeaseStaleError`)** — event 완료 후 미publish lease가 동일 slot 재 download 뒤 publish하면 새 실행 bits가 낡은 lease 명의로 반환되는 계약 구멍("never publish stale revisions") — 리드가 RED test + slot generation fence로 폐쇄.
- **Parity**: resident == T12 wrapper 전 site raw bits(day 25 plane + night 7), graph-on == graph-off(UTCI bucket 경계 straddle n 포함, rad_day 3 replay). wrapper == oracle는 T12 digest gate가 동일 suite에서 green → resident == oracle **transitive**.
- **성능(E2E wall, 전송+sync 포함, GPU 0)**: rad_day wrapper 50.1/50.9ms(H2D 202MB) → **resident 18.5/18.9ms**(상주, H2D 0), 40-row 편집 모드 **19.8/19.9ms**(H2D 1.05MB/iter), graph 21.0/21.5ms(replay wall 12.97ms ≈ kernel 12.99ms — **T12의 26ms H2D가 critical path에서 제거됨**). cold bind 25.3ms 1회. rad_night 13.6→11.1ms. UTCI sparse 1.00→0.79ms(build 2.2ms, ~11 replay 상각). Dispatch: tiny edit cpu 0.0010 vs cuda 0.080ms → **cpu 선택 + device 할당 0**(exit criterion), full-site cuda 13.9 vs cpu 860.5ms → cuda. Stress 400 cycle: alloc delta 0, free-mem drift 0B, 상주 ~228MB < 1GiB cap.
- Author mutation 6/6 killed(padding guard, publish fence, reuse fence, registry key, graph liveness — segfault rc=139, dispatcher 무시). **Lead 독립 mutation — stale-lease fence 중화**: killer `TestStaleLeaseFence` **KILLED**(`DID NOT RAISE LeaseStaleError`) → sha 복원 → cura 전체 **94P**(86.58s). `t13/mutations/lead_stale_lease_fence/`.
- Lead 검증: footprint(11파일 전수 읽기 + 3파일 diff — additive 무동작 확인) → cura suite 독립 재현(93P) → RED(→`AttributeError`)→GREEN(32P)→mutation KILLED→복원→**94P** 재실행(로그 `t13_lead_final_94p.log`) → 로컬 skip 규율(4P/28S, `SW_REQUIRE_CUDA=1` 하드페일) → 본 항목.
- Limitations(보고서 기록 그대로): rad_day graph는 26 kernel scalar가 launch에 bake — 새 timestep은 rebuild(~21ms; graph leg가 plain resident보다 0.9–1.2ms 느림, graph 이득은 UTCI −21%처럼 launch 지배 국면; scalar의 device-memory routing은 미이행), rad_night 11.1ms E2E는 7-plane D2H + per-plane event fence + Python lease 오버헤드 바닥(향후 batched D2H pooling), dispatch는 실측 기록 기반(availability probing 의도적 부재), pageable H2D 반환 후 재사용 semantics에 의존. cube-scope legacy digest는 본 태스크 범위 외(T12에서 T13 이관 명시됐으나 residency/graphs/dispatch가 본체; legacy cube-scope는 별도 후속으로 남김 — honest 기록).
- Status: PASS. 다음: T15(publication/mixed edit/liveness — T10/T11/T13 완료로 final integration 가능), T14 진행 중(병렬 agent).

### T14 — torch-free runtime + step-table 일반 생산자 + RSS 다이어트 (PASS, 2026-09-08)

- Task: T14(R1 잔여 torch-capture 제거, R3 일반 step-table producer, R9 compact capture 재발행, RSS 예산, AOT 판정). Owner: uf-t14 agent. Base: 154e980 위 T14 branch(커밋 **eb4dd8a**, main 병합 **57ac464** — 이후 cube-scope 32f34c2). Worktree agent-af8eed8588b00635f(제거 예정).
- Files(18, 전부 명시적 stage): `solweig_core/numba_cpu/step_table_gen.py`(R3 producer), `solweig_core/runtime.py`(facade — RUNTIME_BACKEND="numba-full-solve", typed refusal OracleBackendRefusal/CaptureRegenerationRefusal/UnseenSceneRefusal, lazy import), `solweig_core/numba_cpu/radiation.py`(diffsh RSS 다이어트 — dense 146MiB cube 폐지, `_diffsh_plane` per-patch 재계산, DIFFSH_C97=f32(1.0−0.03), dense_diffsh opt-in, n_patches), `tests/ultrafast/compact_capture.py`(R9 — np.load 차단 녹음, 5.6GB→351MB **16×**, dropped-member 대성 refusal), `packaging/runtime-cpu/pyproject.toml`(solweig-core-cpu 2.0.0 — numpy/numba/scipy만, torch 부재 환경 설치·import·해석 전 과정 검증), 게이트 4종.
- **R3 핵심**(T03 author report 반영): svf 계열은 python-float 접힘 `SVF_FIVEPIBYFOUR=0x407B53D1`, wallheight_23은 f32-tensor 접힘 `WH_FIVEPIBYFOUR=0x407B53D2` — 225°/315° 대각에서 1-ULP branch 갈림. dz association은 quotient-first(`(ds*idx)*(tan/scale)` — 비2의 거듭제곱 scale에서 1-ULP). previous-state while-stop + 마지막 overshoot step 기록. mutation 스위치(DZ_ASSOCIATION/ABS_ON_ROUND/ANGLE_CONVERSION) module-level. 게이트: 74 frozen trace byte-equal + 임의 각도 live torch differential.
- **D-게이트(전부 PASS)**: D1 startup 1.0875s(warm)/2.6986s(cold) ≤ 3000ms; D2 torch 없는 venv 설치·부팅; D3 compact capture 동일 해석; D4 무결성; D5 AOT 판정 **native_rewrite:not_warranted**.
- **RSS**: diffsh 다이어트 후 steady 110.8 MiB / job peak 535.8 MiB(예산 256/768). raw-bit pin(diffsh_diet_pin.json) pre-change freeze와 전 timestep digest 동일.
- Suite: worktree **582P/54S**(800.19s) → main 병합 후 **586P/82S**(729.70s).
- **Lead 독립 mutation — WH flavor 접힘 혼동**(WH_FIVEPIBYFOUR를 svf 접힘으로 치환): killer frozen-trace byte gate가 az=225° wallheight fixture에서 **KILLED**(1F/4P — dz_bits step1 ref 1073578127 vs cand 1073578130) → pristine sha256 404509aa… 검증 복원 → 18P. `t14/mutations/lead_wh_flavor_conflation/`(REPORT.md는 harness가 subagent 기록 거부 → lead 전사 `~/Workspace/solweig_ultrafast_artifacts/t14/REPORT.md`).
- **RSS witness 1회성 실패(정직 기록)**: main 병합 직후 full suite 1회에서 `test_job_peak_rss_within_budget` FAIL(>768MiB 주장). 재현 안 됨 — isolated 7P green, 직접 측정 cold/warm 334–425 MiB 전부 예산 내, 원인 후보 = fresh NUMBA_CACHE_DIR가 subprocess 측정에서 cold JIT 강제(전 suite 순서 압박 가중). 재실행 full suite 586P/82S clean. 예산 완화 없음.
- Limitations: P11l anomaly(T11 항목) 계속 미해결·비차단; compact capture 녹음은 loader key 경험주의(미로드 신규 키는 dropped-member fence로 loud); bench 부하 조건(T11 동일); 4-core pinned reference 미확보(T16).
- Status: PASS. 다음: T15 검증 중(병렬), T16(수용 측정).

### Cube-scope — legacy/full-plane CUDA 대형 스코프 + 대조 게이트 (PASS w/ TARGET_MISSED fenced, 2026-09-08)

- Task: T12→T13 이관 과제 — legacy lane의 cube-scope(전 site × 24t tmrt/utci/shadow) digest와 on-device cura oracle pin 대조. Owner: cube-scope agent. 커밋 **32f34c2**(main 직접).
- Files(2): `native/cuda/cubescope.py`(296L — full_solve_capture 순서의 ResidentRadRunner 루프, day scalar carry `59/1440` 역치, met plane Ta=col11+uhii/RH=col10/va=max(0.15,col9), utci valid-mask scatter) + `tests/ultrafast/test_cuda_cubescope.py`(466L — pin 4종 하드코딩 + 제어 게이트 + xfail(strict=True) 목표 + 특성화 울타리).
- **대조 설계(핵심)**: 제어 게이트 — canonical CUDA 24t 루프가 로컬 t08 capture digests를 정확히 재현(루프 드라이버·state threading·CUDA UTCI end-to-end 신뢰 확보). 이 위에서 legacy 측정: shadow == universal pin(bit-exact), tmrt/utci vs cura pins **TARGET_MISSED** — 귀속: 두 프로파일 모두 per-t 0/24 일치 → legacy FMA가 아니라 **input-flavor regime**(frozen macOS torch-2.14 CPU-lineage bits vs on-device oracle internals). legacy-vs-canonical 편차 tmrt 6.82% lanes max 6.1e-05 / utci 5.03% max 6.9e-05 / shadow 0 — 특성화 울타리로 고정. 결정론 bit-exact.
- 게이트 방어: manifest 자체가 pin을 휴대(anti-drift), xfail(strict=True) — TARGET_MISSED가 조용히 사라지면 FAIL. pin은 테스트에 하드코딩(manifest 낡음 무관 — agent가 static.npz f6c536f0 vs 실제 65b94810=rad_pins.json 일치 불일치 보고, 정직 기록).
- cura 재현: env 재구성(`SW_T12_T08_CAPTURE`/`SW_CUBESCOPE_ARTIFACTS`/`SW_REQUIRE_CUDA=1`/`CUDA_VISIBLE_DEVICES=0`) 후 **6P/1xf @61.7s** 2회. 로컬: 7 collected(1P purity + 6S CUDA 부재).
- **Lead 독립 mutation — met column 배선 교차**(Ta/RH/va col index 의도적 혼동): killer utci cube digest `8a314b55…` ≠ pin `7d002c29…` **KILLED**(20.74s) → pristine sha256 dbcb14ab… 검증 복원. `cubescope/mutations/lead_met_column_swap/`.
- Limitations: legacy tmrt/utci on-device pin 정합은 **달성 실패 그대로**(input-flavor regime — frozen capture의 CPU-lineage 입력 비트가 근본 원인, acceptance_targets.yaml 불변); cube-scope 판정은 frozen t08 capture에 국한; N_VALID=209422 고정.
- Status: PASS(패리티 게이트는 목표 달성, legacy pin 대조는 TARGET_MISSED로 정직 fence). 다음: T15 검증, T16.

### T15 — selected-time 스트리밍·출판 펜스·liveness (PASS, 2026-09-08)

- Task: T15(publication/mixed-edit/liveness — 7 witness 해소, exit criteria). Owner: uf-t15 agent. Base: 154e980. 커밋 **85ad960**(12파일 +2139/−45), main 병합 **40f2b37**. 플래그 `SOLWEIG_RT_SELECTED_TIME_STREAMING`(별도 flag — off면 byte-identical, 전용 pin 테스트).
- Files: 수정 7(`jobs.py` — request-cut marker·disclosure·guard 선행 호출·heal 2레인·lease 출판, `executor_bridge.py` — TimeCoverageGap heal(bootstrap+full-time rebind, fallback_reason="time_coverage_heal"), `patch_codec.py` — PartialTimeScatterError, `broadcast.py` — 직렬화 1회 펜스+broadcast_selected_time, `epochs.py` — window_hint_s clamp, `routes_realtime.py` — ?time_indices= 스트림(typed 400)+GET /{id}/time-coverage, `app.py` — SelectedTimeDemand 배선) + 신규 5(`realtime/selected_time.py` 382L, `realtime/publish_fence.py` 181L, 테스트 3종 34개).
- **7 witness → 해소**: (1) dense offset 시간 mis-scatter — DEFECT, PartialTimeScatterError로 폐쇄(전-깊이는 dense-prefix 유지); (2) stale 출판 — store fence 보존 + ResultPublishLease(mint-from-running→mark_complete→1회 publish, T13 OutputLease CPU 유사체); (3) request-cut 합성 손실 — DEFECT, disclosure block+guard(캐시 probe **이전**)+양레인 heal; (4) full-coverage 허위 주장 — heal 규칙 = 전체 창 AND 전체 시간, 부분-창 전체-시간 record는 heal 불가(핀), native sparse는 guard 무관(핀); (5) mixed-family no-op wedge — r2_exact 핀 재검증 3P + #139 6P; (6) 재시작 후 epoch 영구 stuck — restart drill(running crash 시뮬→start() 복구→recoverable 비움); (7) 느린 클라이언트 compute-thread 봉쇄 — DEFECT, format_sse 1회/출판, privileged frame 우회.
- **과학 differential**(314s author/212s lead 환경): cut 편집(v1 complete:false) → 후속 편집 → guard refusal → heal → v2 full — healed 슬라이스를 fresh-oracle 전 커버리지 실행과 `np.array_equal(equal_nan=True)` raw-bit 대조. exit criteria 부가: latest_result_age_s + superseded_jobs /time-coverage 노출.
- Author mutation 10종 killed(직렬화 hoist 제거, scatter fence 삭제, guard 호출 제거, disclosure 억제, lease pending-gate 제거, restart requeue 제거, heal 재발생 억제, epoch clamp 제거, coverage 허위 complete, route flag gate 제거). **Lead 독립 mutation — heal window conjunct 제거**(`_heals_gap`에서 `record_window_is_full_site` 삭제): killer `test_partial_window_full_time_record_does_not_heal`가 **KILLED**(1F/3P 65.22s — 부분-창 전체-시간 record가 잘못 heal) → pristine sha256 658e828b… 검증 복원 → 34P(67.31s). `t15/mutations/lead_heals_gap_window_conjunct/`.
- Lead 검증: footprint(7M+5N 정합) → 12파일 전수 정독 + 시맨틱 리뷰(3실결함-수정 확인) → 독립 배치 **175P 0F**(211.88s — selected_time+publication+liveness+epochs+broadcast+server_api+incident2+r2_exact, 과학 differential 포함) → mutation KILLED → 복원 → main 병합.
- Honest limitations(author 보고 그대로 6종): pre-flag 히스토리는 block 부재로 소급 구분 불가(flag 시점부터 clean books), 변수별 heal 수렴은 다음 full 출판까지(그 사이 missing_times 정직 보고), 주입된 epoch scheduler factory는 배선 안 됨(기본 scheduler만 — 코드 문서화), SSE happy path는 hub/subscription seam에서(TestClient 한계), coverage_report 창 크기는 record manifest 기반(재계산 아님 — by design), lease 이중출판 거부는 lease 범위(재시작 간 권위는 store ResultAlreadyPublished).
- Status: PASS. 다음: T16(수용 측정 — 4-core pinned reference 확보 과제 남음, TARGET_MISSED 정직 기록).
- Main 병합 후 전체 트리 suite(lead, `pytest tests/`): **2200P/94S/2F 2677.55s** — 2F는 `test_cli.py::test_cli_help`/`test_cli_missing_required_args`(FileNotFoundError, 문서화된 선행 PATH 환경 이슈, T15 무관·비접촉). T15 34 테스트 포함 전부 통과. (종전 586P/82S 기록은 ultrafast/realtime 중심 큐레이션 목록 기준 — 전 트리 기준치는 본 2200P.)

### T16 — 수용 목표 최종 측정·보고 (PASS w/ TARGET_MISSED 정식 기록, 2026-09-08)

- Task: T16(비용 모델·목표 검증·independent release review). CPU 레인 owner: t16-cpu-bench agent(worktree agent-t16cpu, base 3b4a568, 커밋 1f693d5, 병합 **7c43586**). GPU 레인 owner: t16-gpu-bench agent(cura `~/solweig_ultrafast/work/t16/`, repo 3b4a568, 비커밋 — 리드 미러 `~/Workspace/solweig_ultrafast_artifacts/t16/gpu/`, report sha 7849c672…).
- Files(CPU): `benchmarks/ultrafast/run.py`(+522 — bench 변형 3종), `t16_cpu_lane.py`(실 JobRunner/store 경로 selected-exact + stage-sum==E2E assert + sample-phase 전용 백분위 + SamplePhaseError/SampleCountError/StageSumMismatch), `t16_memory.py`(steady/job-peak/warm20 — warm20은 대표 site_500 capture로 실측), `t16_startup.py`, `t16_run_matrix.sh`, `test_t16_bench_harness.py`(10테스트, mutation 지정 킬러 3종). 원시 아티팩트 2.6GB는 미커밋, `~/Workspace/solweig_ultrafast_artifacts/t16/cpu/` 정본.
- **판정 표(GPU, cura RTX A6000 GPU0, p95)**: cuda_selected_exact **17.29ms PASS**(목표 33.3, n=120; acceptance↔full 변형 digest 동일 120/120 — 재계산 제외가 cycle-invariant임의 증명) · cuda_startup **2,630.6ms PASS**(n=10, READY digest 10/10) · memory 단일 **216.97MiB PASS** · cuda_full_day_local 39,185.3ms TARGET_MISSED(39.2×, n=30, cube pin 32/32) · cuda_full_recompute_warm 37,383ms TARGET_MISSED(7.5×, n=30) · memory 20-시나리오 4,556.4MiB TARGET_MISSED(2.23×, 무공유 상한 227.8MiB/개).
- **판정 표(CPU, dev host M1 Pro 10코어, loadavg 13-48 전부 공개, 4-core pinned 권위 판정 = BLOCKED)**: cpu_selected_exact 33,746.6ms TARGET_MISSED(n=30, paired_expensive — 단발 30-60s; stage: solve 33.21s = svf 16.9 + time_loop 16.4; queue_wait 0.51s = 500ms coalescing 창) · cpu_full_day_local 21,397ms(cold)/20,131ms(warm) TARGET_MISSED(n=100 each; ablation: fused kernel 20.43s = 96%) · cpu_full_recompute_warm 19,384ms TARGET_MISSED(n=100; anchor-resume 보조레인 10,567ms, overlap raw-bit-equal, 본수치 아님) · steady_rss 286.9MiB TARGET_MISSED(256 예산) · job_peak **320.7MiB PASS** · warm20 **672.5MiB PASS**(1,536 예산) · process_to_ready warm **1,459.8ms PASS**(n=6; cold 9,282.7ms 공개) · 미검증: ux 16.7ms NOT_RUN(브라우저 레인), cuda 행 NOT_RUN(이 레인), SVF site-cache 빌더 in-tree 부재 공개.
- **병목 귀속(GPU misses = host 측, GPU 아님)**: kernel+sync 13.9ms(<1%). full-day 1-t당 1596ms ≈ static 재bind chunk-diff(np.array_equal bool 임시 ~200MB, 1197ms isolated) + bundle 재생성(~200MB copy, 1575ms; recompute 자체 10.5ms). 다음 후보: static bind 루프 밖 hoist + memcmp/dirty-flag + bundle 버퍼 재사용 → 추정 F2 ~900ms(목표 내). 20-시나리오 메모리는 cross-scenario static 공유 필요.
- **병목 귀속(CPU)**: selected_exact solve 33.2s = svf march 16.9s + time loop 16.4s → T17 cache-aware ray-state reuse / prefix-accumulator suffix fold / equality-triggered convergence가 지정 후보. full-day fused kernel 96%.
- **정직 기계(GPU)**: selfcheck 6종 PASS(enqueue-only 검출기 synced 13.9 > 2×0.087ms, warmup 오염 증명, post-wait event 완전성, torch 부재, 무변경 bind 0-byte H2D, edit 결정론) — selfcheck 실패는 RuntimeError로 전체 abort. 실패/superseded 실행 보존(group-90 smoke PIN 전사 오류 — fence가 적발, 파싱으로 수정; startup run1 모듈 오버라이드 crash). **정직 기계(CPU)**: 백분위 sample-phase 전용 + 개수 하한 + stage-sum, 저널 전부 JSONL, 호스트/부하/인증 sha256 기록.
- Author mutation(CPU) 3종 killed(이중계산 stage, warmup-only p95, 잘린 개수). **Lead 독립 mutation**: (GPU) `pct` p95→p50 거짓 — 지정 킬러 `warmup_contamination_detected` 발화 → RuntimeError abort **KILLED** → sha 복원 → 6/6 green (`t16/mutations/lead_pct_p50_lie/`). (CPU) `percentile_checked` 보간 제거(floor 백분위) — 지정 킬러 `test_percentile_checked_known_values`(95.05 보간 핀) **KILLED**(1F/9P) → sha 복원 → 10P (`t16/mutations/lead_percentile_floor_lie/`).
- **Lead 독립 재현**: (GPU) 원시 JSONL에서 전 판정 재계산 — 17.2920ms/39,185.3/37,383.0ms, digest 동일성 120/120, selfcheck 6/0 일치; live 재측정 n=25 → **17.121ms**. (CPU) 원시 records에서 전 판정 재계산 — 33.7466s/21.3971(cold)/20.1307(warm)/19.3839s, stage-sum==E2E 정확 일치; tests/ultrafast **597P/88S**(719.29s) 재현.
- **Lead 발견 결함 수정(별도 커밋 241b9a0)**: `Runtime.cap_set()`이 필수 `latitude` 누락 CaptureSet 생성 — facade 자체 solve 진입이 첫 호출부터 TypeError(호출자 부재로 미발견; T16 메모리 하네스가 우회하며 발견). latitude 키워드 필수화 + 전달 1회 + 회귀 테스트 2종(18P).
- Honest limitations: 단일 호스트·단일 GPU·단일 capture(교차-사이트 분산 없음); GPU enqueue-only 검출기는 selfcheck 자체 span 수비(레인 측정 루프의 span 회귀는 digest/개연성으로 간접 적발); selected-exact acceptance 벽은 목표 정의상 host 재계산 제외(전체 경로 맥락 변형 1.9s 병기); CPU 30-샘플 paired_expensive(단발 30-60s); cold rows phase 전사 버그(agent 사후 수정, import_s 키로 무손실 구분, 통계는 기록 전 산출로 정확); budget 종료 ≠ 목표 달성 — acceptance_targets.yaml 불변.
- Status: PASS(측정·보고 임무 기준; 목표 달성 여부는 표 그대로 — GPU selected_exact/startup/단일 메모리 PASS, CPU latency 3종+steady RSS TARGET_MISSED, 권위 4-core 판정 BLOCKED). 다음: 예산 내 T17 선택 후속(hoist/memcmp/bundle 재사용 등 병목 후보) 또는 종료 검토.

### T17 (레인 1/3) — 계획 준수 감사, 독립 읽기 전용 (PASS, 2026-09-08)

- Owner: plan-audit agent(opus, 읽기 전용 — 레포 쓰기 0). 전체 매트릭스 영속화: `~/Workspace/solweig_ultrafast_artifacts/t17/plan_compliance_audit.md`(섹션 A–H + GAP 10건 + 총평).
- 결과: T00–T16 전 카드 의존 순서 이행, 독립 재검증(HEAD 59fdfa4 suite 599P/88S/0F, 매니페스트 해시 동일성, 변이 로그 디스크 존재, T16 보고↔원시 JSONL 일치). **금지 지름길 12/12 전부 가드 확인. 과학 제약 위반 0건** — 원본 과학 파일(shadow/veg_svf_state/utci_process/checkpoints/gis_shadow_calculation) e0d19fc 이후 빈 diff, solver.py 순수 추가, fastmath 부재, tests/ +56,983/−0(순수 삽입).
- GAP 목록(정직 기록, 목표 미완료 유지 근거): legacy cube-scope pin TARGET_MISSED(Medium), CPU/CUDA latency 미달(High-for-goal — 미달성 주장 안 함), 4-core 권위 판정 BLOCKED(Medium), ux NOT_RUN(Low), steady RSS 미달 + D5 AOT 재평가 미재실행(Low-Medium), P11l 1-ULP 프로브 이상 공개(Low), 샘플링 편차 공개(Low), 워크로그 핸드오프 필드 일부 암시(Low), D5 명명 편차(Low).
- 총평: "59fdfa4의 레포 상태는 증거가 지지하는 바를 정직하게 주장한다" — GOAL_PROMPT §44/DESIGN 19.4 완료 조건은 T17 해소 전까지 미완료로 정확히 유지.

### T17 (레인 2/3) — 원본 레포 대비 실측 속도비 (MEASURED, 2026-09-08)

- Owner: speedup-vs-legacy agent(로컬, 레포 무변경 — 커밋/푸시/워크로그 미접촉, HEAD 59fdfa4 확인). 원시 기록: `~/Workspace/solweig_ultrafast_artifacts/t17/speedup/`(schedule_raw.jsonl 55행 hw+부하 전부 포함, driver 스크립트 3종).
- 방법: site_500 500×500×24t(2009-08-11, utci/tmrt/shadow), 동일 호스트 paired interleaved + randomized order(seed 20260908). legacy = `benchmarks/ultrafast/harness_solve.py`(T00 baseline harness, oracle e0d19fc 트리 + main 트리 양쪽), new = cpu-numba(T16 프로토콜 재사분 — fixture/config 해시 검증 후) + 당일 신규 확인 런.
- **결과(원시 JSONL에서 리드 독립 재계산 — 보고치와 소수점 일치)**: full-day — oracle p95 78.117s → new warm 16.923s = **4.62×** / cold 15.619s = **5.00×**; paired per-round ratio p50 4.19×(범위 3.12–5.62×, 호스트 부하 변동 공개). T16 대표본(n=100) 기준 3.7–4.0×(당시 loadavg 48). edit t=12 — oracle incremental 55.264s → new 33.747s = **1.64×**(원본엔 selected-time 경로 부재 — 인과 등가로 full recompute 대비 2.31×; E1-applied recompute 스팟 89.908s 대비 2.70×). main-tree legacy: full 72.110s(4.26×) / edit 57.009s(1.69×) — seam이 legacy 벽을 바꾸지 않음을 재확인.
- **비트 동일성(속도비의 전제)**: 신규 legacy 런 23회(oracle/main × full/edit) 전부 frozen T00 digest 핀과 정확 일치 — full 핀(tmrt 487a912a…/utci 8db68a0d…/shadow f8f75196…) 10/10, E1 edit 핀(shadow 3fa979c9…/utci 40a00e87…/tmrt e5c113d9…) 13/13, `benchmarks/ultrafast/baseline/raw_runs.jsonl` 동결 레코드와 교차 대조(리드 독립 검증). 당일 패리티 게이트 리드 재실행: `test_full_solve_identity` 1P(15.65s), `test_profile` 오라클 핀 16P(1.13s).
- **정직 발견**: T00 동결 full 벽 317.609s(cold_sample rep0, time_loop 315.76 — 원시 레코드 확인)은 **오늘 재현 불가**(warm-out 73.1s, 5샘플 53.6–79.9s, digest는 핀과 정확 일치) → 첫 실행 torch 일회성 비용으로 귀속, 공개 후 interleaved 측정으로 대체. 동결 edit 벽(59.0/62.9s)은 노이즈 내 재현. 제한: dev host 부하 하 측정(4-core 권위 판정 BLOCKED 지속), torch 스레드 host 기본값(양쪽 legacy에 동일), legacy는 solve-side 벽(서버 경로 부재 — new 수치가 E2E라 공정성 마진은 new에 불리), E1 recompute는 n=1 스팟.
- README ledger에 T17 속도비 행 추가. Status: MEASURED — 사용자 요구 "원본 레포 대비 실측 속도비" 충족. 다음: 레인 3/3(T17 GPU 최적화) 검증.

### T17 (레인 3/3) — CUDA full-day host-side 최적화 + 비트 패리티 (PASS, 2026-09-08)

- Task: T17(t16 병목 후보 실행 — static bind hoist / chunk-diff memcmp / bundle 재생성 제거). Owner: t17-gpu agent(opus, cura, **GPU 3 고정** `CUDA_VISIBLE_DEVICES=3`, preflight로 4 GPU 전부 idle 확인 + 1Hz 샘플러 csv로 무점유 입증). 원격 작업 전부 비커밋; 원시 기록 `~/solweig_ultrafast/work/t17/`(artifacts/ + SHA256SUMS.txt 25파일 전부 OK — 리드 검증). 로컬 미러 `~/Workspace/solweig_ultrafast_artifacts/` (티어다운 후 정본).
- **수정 3종(+63/−10, 2파일 — 커널·수학·정밀도 무변경)**: (F1) `cubescope.py` static 바인드 hoist — day/night static 번들은 pinned builder의 순수 `st` 참조(장면 불변)라 최초 1회만 바인드(사전 176/176 static 불변성 검사 identity+bytes_equal, 재바인드는 0 H2D 셀프체크). (F2) `residency.py` changed-chunk 술어를 np.array_equal(uint8 뷰) → libc memcmp fast path(C-contiguous) + array_equal fallback — uint8 뷰에선 memcmp==0 == array_equal, NaN payload·±0.0는 바이트로 비교(전송 집합 불변 — h2d 동일성으로 입증). (F3) `cubescope.py` dense diffsh를 `dense_diffsh_from_packed`로 1회 재료화(builder 자체 fallback 함수 — 비트 동일 by construction; probe로 dense_diffsh=True 경로와 memcmp 동일 입증). bundle_build 996.2→0.41 ms/t. **volatile plane in-place 변형은 의도적 기각** — runner 미러가 참조하는 배열 변형 시 diff 항상-동등 → device stale(펜스 붕괴). fresh per-t 참조 유지.
- **판정(GPU 3, p95, n=30/warmup 5)**: cuda_full_day_local resident_hoisted **677.7ms PASS**(목표 1000; T16 39,185.3 대비 **57.8×**) · cuda_full_recompute_warm optimized_scope **3,378.7ms PASS**(목표 5000; T16 37,383 대비 **11.1×**) · resident_perbind 컨텍스트 681.7(n=10). 리드 독립 재계산: 원시 run rows(status=accepted 246행)에서 p95 재산출 — 677.668/3,378.719 소수점 일치, stage-residual max 0.102ms, 전 행 digest_match=true.
- **패리티(절대 계약)**: committed(59fdfa4 pristine) vs optimized 2프로세스 2체크아웃 — per-t digest 72/72(3평면×24t) 전부 동일, ret_CI·carry 스칼라 동일, **큐브 digest 양측 == 동결 핀**(shadow 6c62bcea…/tmrt 65ef43ae…/utci 7d002c29… — `test_cuda_cubescope.py`에서 파싱, 수기 전사 아님), **h2d 바이트 동일 543,574,853**(전송 집합 불변의 최강 증거). 비시간 워크로(wall, 동일 프로세스 형상): committed 28,805.9ms → optimized 3,405.6ms(8.5×).
- **자기 파괴 시연(agent)**: Lup_pre volatile 재바인드 생략 → 펜스 first_bound_t=8 발화(KILLED digest 기록: tmrt db395c73…/utci caa2d4c0…), 다음 정상 바인드에서 자가 치유. enqueue-only 목격 6ms vs synced 663–965ms(160.8× — 벽시계 동기 측정 입증), warmup 오염 합성 증명, torch 부재.
- **리드 독립 변이**: `_chunk_bytes_equal` 항상-True(pristine 백업+sha 5f361a9d… 선행) → parity_run 재실행 **tmrt de1874ca…/utci 71eb7f1f… 핀 발산 — KILLED**(shadow 불변 = static 전용, 예측 일치) → sha 검증 복원 → 재실행 3핀 전부 일치. `results/parity_lead_mutation.json`/`parity_lead_restore.json`.
- **정직 이력**: group 1–2 p95(1,481–1,495ms)는 측정 하네스 아티팩트(런 간 digest tobytes가 host allocator 교란 → fresh 출력 버퍼 페이지 폴트; probe_apply2 PRETOUCH로 262–970→7.6ms 격리, 원인 2단계 귀속)로 폐기 — 행 전부 보존 + orchestration BLOCKED 1행 보존. F3 산포(1.3–4.2s)는 **커밋 드라이버** 내부 per-t digest 장부의 동일 메커니즘(패리티 계약상 수정 불가 — 목표 내 통과). 잔여 병목: wait_sync 286ms(GPU-bound), recompute_overrides 242ms(CPU-bound) — host 메모리 관리는 더 이상 병목 아님.
- **리드 랜딩**: t17_driver.diff(sha 130ba90c…) `git apply` 클린 → 랜딩 파일 sha == 원격 체크아웃(bdcb141e/5f361a9d) 바이트 일치. **리드 발견 결함**: 신규 술어에 레포 게이트 부재(검증이 bench selfcheck에만 존재) → `test_cuda_residency.py`에 `TestChunkBytesEqualPredicate` 4종 추가(CPU 전용 — equal/±0.0/NaN payload/무작위 비트 반전 fuzz + fallback 경로, "술어는 비트가 다르면 절대 동등 판정 불가" 핀). 지정 킬러 변이(`return True`) → **3F/1P KILLED** → sha 복원 → 5P/10S. 부수 발견: uint8 view의 마지막 축 연속성 요구는 커밋 코드와 동일(술어-동등 유지 — 완화 아님).
- Status: PASS — 두 GPU 수용 목표 신규 통과(T16 미달 2건 해소). GPU 메모리 20-시나리오 미달은 cross-scenario static 공유 미시행으로 TARGET_MISSED 유지(후속 후보). 다음: 종료 인출 + cura 완전 삭제.

### T17 종료 — 기록 인출·cura 완전 삭제·목표 상태 판정 (2026-09-09)

- **인출 2/2**: `~/solweig_ultrafast/work/t17/` → `~/Workspace/solweig_ultrafast_artifacts/t17/cura_t17_full/`(artifacts 26/26 sha OK, results 전체 — 리드 변이/복원 JSON 포함, 하네스·프로브·report.md·t17_repo.bundle·수정 드라이버 원본 2파일 — 랜딩 파일과 바이트 일치). /tmp 잔여(probe_t17a/b/c, t16_lead_spot.jsonl) 선인출 후 소거. 시작 시점 인출 1/2는 `cura_final_mirror/`(8.4GB, PULL_VERIFICATION.md).
- **cura 완전 삭제**: `rm -rf ~/solweig_ultrafast` exit 0 → 디렉터리 부재 + nvidia-smi 4장 전부 2 MiB/0% 확인. 증거: `cura_final_mirror/TEARDOWN_RECORD.md`.
- **목표 상태 판정 (DESIGN 19.4 조건 대조 — 정직 기준)**: raw-bit gates PASS(CPU 전 계약 + CUDA canonical; legacy cube-scope만 fence된 TARGET_MISSED), Torch-free runtime PASS(T14), certified GPU canonical profile PASS(T12), legacy GPU regression kernel-scope PASS/cube-scope TARGET_MISSED, concurrency/liveness PASS(T15), memory/startup 부분(job-peak·warm20·startup PASS, steady RSS·GPU 20-시나리오 TARGET_MISSED), **final exact target 미달 지속 — CPU latency 3종(selected 33,746.6ms vs 100ms, full-day 21,397ms vs 3,000ms, recompute-warm 19,384ms vs 15,000ms) TARGET_MISSED**. GPU full-day/recompute는 T17에서 미달→PASS로 전환(677.7/3,378.7ms p95).
- **결론: 전체 goal completed 아님 — 19.4 전항목 통과 요건에 CPU latency·steady RSS·GPU 20-시나리오 메모리·legacy cube pin 미달 잔존.** 계획 규정(19.4·§20 마지막 준)대로 best verified commit = **96d61c2**, 정확히 검증된 상태로 종료. 귀속된 다음 후보: CPU svf march 16.9s(cache-aware ray-state reuse/prefix-accumulator suffix fold)·time loop 16.4s·steady RSS 286.9→256MiB(DESIGN 12.4 AOT 재평가)·cross-scenario static 공유. 근사·범위 축소·정밀도 저하로 목표를 맞추는 금지 조항 위반 없음.
- 원본 레포 대비 실측 속도비(bit-identical 결과 기준): full-day 4.6×(warm)/5.0×(cold), selected-time edit 1.6×(incremental)/2.3–2.7×(recompute 등가) — CPU 레인 측정, README ledger 참조.

### T24 — adaptive epoch/debounce scheduling (§710) — 착수 (2026-09-09)

- Task: T24 (GOAL_PROMPT_T18_CPU v3 a50994f). §710: 500ms coalescing 대기 자체가 cpu_selected_exact 100ms 목표 초과 → window 축소를 reducer determinism·idempotency·conflict resolution 불변 조건 하 평가 + E2E ablation. TASKS T15: "별도 flag와 coverage test". Owner: t24 author agent. HEAD a50994f (미커밋, lead landing).
- **Semantics map (read-only 완료)**: 대기는 `jobs.py:1092-1094` 단일 지점 (`self._stop.wait(coalescing_window_ms/1000)`, interruptible by stop only). pre-sleep supersede scan(:1090) → wait → `_dispatch`(:1097) 내 post-sleep scan(:1706) → `_execute`. acceptance/fold/publication 경로는 시계를 읽지 않음 — window는 (a) dispatch 지연, (b) sleep 중 도착한 newer edit의 pre-solve supersede 기회량에만 기여. legacy 배칭은 store supersede-on-submit이, exact 배칭은 single-flight+`_supersede_queued_locked`+revision-chase가 이미 담당 (window와 독립). epoch cadence adaptive는 T15가 이미 flag로 출하 (`SOLWEIG_RT_SELECTED_TIME_STREAMING` → 50ms floor hint, epochs.py `_window_s` clamp 50-200ms) — T24 잔여분은 **debounce**(coalescing wait) 절반.
- Window-VALUE-dependent: dispatch latency, sleep-중-도착 supersede 확률(potential wasted solves), `queue_wait_s` stage. Window-INVARIANT: accepted op 순서/중복 Dedup/conflict fold/epoch 배칭/single-flight/게시 guard/result bytes.
- **설계**: (A) 순수 config — `SOLWEIG_COALESCE_MS` (이미 존재, 코드 0줄). (B) adaptive — 별도 flag `SOLWEIG_RT_ADAPTIVE_COALESCE` (default OFF, flag-off byte-identical pin test): 고정 wait 대신, burst 지시자(큐 대기 잔여 / 동일 scenario nonterminal peer / open epoch) 없으면 contract latency floor 50ms(=EPOCH_TICK_MIN_MS)로 축소, 지시자 있으면 full window 유지. mid-wait 재평가 없음(단일 wait 유지). create_app wiring은 T15 패턴 준용.
- Files 예정: jobs.py(wait 단일 지점+정책), app.py(wiring), __main__.py(env 표), tests/test_realtime_t24_adaptive_coalesce.py(신규), benchmarks/ultrafast/t24_coalesce_ablation.py(신규, n=5 paired interleaved, ~/Workspace/solweig_ultrafast_artifacts/t24/). march/radiation·oracle 미접촉.
- **구현 (미커밋)**: `jobs.py` — 모듈 플래그 `SOLWEIG_RT_ADAPTIVE_COALESCE`+`adaptive_coalescing_enabled()`(T15 `enabled()` 패턴), `ADAPTIVE_COALESCE_FLOOR_MS = EPOCH_TICK_MIN_MS`(contract 50ms 단일 floor), JobRunner ctor `adaptive_coalesce=False`/`adaptive_coalesce_floor_ms`, `_effective_coalescing_window_ms()`+`_coalesce_burst_ahead()`(지시자: 큐 잔여 / 동일 scenario nonterminal peer / open epoch — 전부 durable 신호, 시계 무관; 누락 job row=보수 full window), wait 지점 단일(:1092) 교체. `app.py` — `create_app(adaptive_coalesce=None)` env 해석 1회(T15 준용), JobRunner 전달. `__main__.py` — env 표+KNOWN_ENV_VARS. 라우트 응답 body 미변경(광고 window 그대로).
- **테스트**: `tests/test_realtime_t24_adaptive_coalesce.py` 20P — flag-off pin(wait 인수 == 0.5 정확 1회 + open_epochs probe 미호출), flag-on floor pin(wait == 0.05), 지시자 3종 full-window 유지(+읽기전용 증명), window=0 edge, 축소창 내 supersede 작동, flag 간 동일 시퀀스 → 최종 게시 bytes 동일, 중복 submit 멱등, create_app env/명시 우선 wiring. 기존 suite 전부 무손상: perf_wave1 58P · server_api 79P · server_main · realtime_operations+r2_fast_lane 66P · realtime_epochs 21P · selected_time+t15_liveness+r2_exact_lane+t16_bench_harness 63P · recovery+deployment+universal+security 75P.
- **변이(지정) 및 복원**: M1 "adaptive never shrinks"(burst always True) → 3 test FAIL로 KILLED; M2 "flag 무시 adaptive 상시" → flag-off wait pin+결정적 ≥0.5s 하한 2 test FAIL로 KILLED(단 `test_flag_off_returns_window_without_burst_probes`는 M2 하생존 — missing-job 보수분기가 probe 전 full window 반환; 결정적 killer는 wait-인수 pin). sha 1a704eea… 복원 양방향 검증, 복원 후 20P. 기록 `~/Workspace/solweig_ultrafast_artifacts/t24/mutations.json`.
- **측정(잠정)**: `benchmarks/ultrafast/t24_coalesce_ablation.py` — T16 selected-exact 흐름 재사용, 3-arm(fixed500/fixed50/adaptive) n=5 pair randomized(seed 20260909), incremental JSONL `~/Workspace/solweig_ultrafast_artifacts/t24/ablation_*/`. 스모크(pairs=1): queue_wait fixed500 0.504-0.506s(T16 0.511s 재현) / fixed50 0.055s / adaptive 0.051s — 설계 그대로. 풀런 진행중.
- **측정 결과 (잠정, n=5 pair randomized seed 20260909, loadavg 12.3 공개, 타 agent 호스트 공유)**: queue_wait(결정적 stage) — fixed500 p50 0.5023/p95 0.5057s(T16 0.511s 재현) · fixed50 p50 0.0555s · adaptive p50 0.0537s. paired delta(fixed500−신규) = 0.4463~0.4545s(15/15 rep, 전 arm). E2E p50 — fixed500 35.315s · fixed50 35.006s · adaptive 35.879s: 0.45s 감소는 stage-sum 항등식(각 rep assert)으로 E2E에 1:1 환산이 수학적으로 보장되나, n=5의 solve 노이즈(±5s, 34.8~48.0s)가 통계적 E2E p95 확인을 압도 → E2E 확정은 T29 클린 재측정. **raw-bit 목격: 15/15 rep, 전 arm에서 baseline(v0)·E1(v1) 게시 payload sha256 완전 동일**(9cbc42b4…/8b8b0d5b…) — window 정책이 게시 bytes에 유출 안 됨을 실경로에서 입증.
- **판정(카드 범위)**: 후보 A(config 500→50) 0.45s 제거 실측, 코드 0줄, 그러나 정적(버스트 중에도 배칭 보호 축소). 후보 B(adaptive flag) 동일 0.45s 제거 + burst 지시자(큐 잔여/nonterminal peer/open epoch) 존재 시 full 500ms 유지 + flag-off byte-identical pin. §710 전제 불변 확인: acceptance·fold·conflict·publication 경로는 window 미참조(코드 귀속 + 20P + 15/15 raw-bit). **cpu_selected_exact 100ms 목표는 T24 후에도 TARGET_MISSED** — E2E ≈35s 중 대기 0.50→0.05s로 "대기 자체가 목표 초과" §710 블로커는 해소, 잔여 gap은 compute(solve ~34.8s = svf march + time loop, T18/T19/T25/T27 소관)로 100% 귀속. 기본값 변경(500ms 유지 vs flag-on 기본화) 판단은 캠페인 verdict(T29) 소관 — 본 카드는 default OFF로 인도.
- **T29 재측정 지시**: (1) 클린 세션 n≥30 paired_expensive로 3-arm 재확인(특히 E2E p95의 0.45s 환산 — 본 잠정치는 solve 노이즈 하 비확정); (2) T16 대표 selected-exact 재측정 시 정책 명시(default flag-off면 수치 불변어야 함 — t16_cpu_lane.py 미수정으로 보장됨); (3) fast/visual 혼합 부하에서 adaptive arm의 wasted-solve 관찰(burst 지시자가 drag 중 full-window를 유지하는지 — 커버리지 test는 단일 지시자별 입증, 실 drag 프로파일은 미측정); (4) loadavg/4-core 조건 기록. 원시 기록: `~/Workspace/solweig_ultrafast_artifacts/t24/ablation_20260909T033213/`(ablation_raw.jsonl 16행 incremental + summary + store).
- Status: PASS (§710 평가·구현·증명·잠정 측정 완료; 커밋은 lead landing, HEAD a50994f 기반 미커밋 diff — jobs.py +107/app.py +16/__main__.py +5, 신규 tests 20P + bench 스크립트).

### T19 — f-floor post-onset suffix exit (T27-C6) + 진폭-정확 prefix 선택 ablation (§352) (PASS, 2026-09-09)

- Task: T19 (GOAL_PROMPT_T18_CPU v3). §352: CPU svf march 16.9s(cache-aware ray-state reuse 지정 후보) + wallheight_23(time loop 내부). 두 레버: L1 = T27 판정 C6 "f-floor post-onset suffix early exit"(PROVEN), L2 = step table 진폭-정확 prefix 길이 선택(R6 정책 레지스트리) ablation. Owner: t19 author agent. HEAD 511092a 기반, 미커밋(lead landing).
- **정리(고정점, svf_shadow 형태)**: 각 step 종료 시점(예외 블록 `s == 0` 포함 전체 step 이후) 상태 `(sh=1, vegsh=0, vbsh)`는 고정점 — (1) `f`는 `_torch_maximum`(torch max 의미론: NaN 흡수, tie 시 2번째 피연산자) 하에서 비교-단조라 `f > a_t`가 참으로 유지 → `sh=1` 고정, (2) post-max building suppression(`vegsh*sh > 0 → vegsh=0`)이 매 step vegsh를 재영점화 → 남은 step의 fab/gab 갱신은 무효, (3) `vbsh = 0 + vbsh`는 음이 아닌 f32 정수 누산기에 비트-정확 항등. 잔여 step은 3출력에 정확 0-기여 → 탈출은 출력-불변. **`vegsh == 0` 접속사가 하중-필수**: `s == 0` 예외 블록이 suppression 이후 vegsh=1을 세팅 가능(step 0 onset + firstvegdem 조건 + count ≥ 2 → naive `sh==1` 탈출은 vbsh 1→2 뒤집음 — 지정 RED 목격, 테스트로 사망 확인). **NaN 예외**: torch-max가 NaN을 흡수해 onset 이후 sh가 0으로 회귀 가능 → a-평면 NaN 스캔(벡터 1패스/배치, ~1MiB) 가드, NaN 발견 시 배치 전체 탈출 비활성(전체 march = 원래 비트 by construction).
- **wallheight_23 형태 정리(독립 서술)**: 동일 고정점이 성립하나 증명 경로 다름 — (a) 이 변형은 첫-step 예외가 없다(step 0 = (0,0) 자기 비교, svf의 firstvegdem/vbsh.zero_() 블록 부재) → 추가 접속사 불요, (b) `dzprev` 결합(tlf/tlg → lfa/lga → v2)은 v2 **내부에서만** 소비되고 `v2==4→0` 리셋 후 suppression이 vegsh를 재영점화하므로 상태 `(sh=1, vegsh=0, vbsh)`로의 정지성에 dzprev 항은 흡수됨(다음 step의 dzprev는 이번 step dz로 고정 — 남은 step의 tlf/tlg 갱신이 lfa/lga를 바꿔도 suppression이 매 step vegsh를 지움), (c) `vbsh` 누산은 소스 피연산자 순서 동일. site_500 실장면 6셀(진폭 eff/abs × 태양 az105/alt25·az170/alt55·az245/alt30) exit-vs-full raw-bit 동일성으로 실증.
- **구현(미커밋)**: `solweig_core/numba_cpu/march.py` — `_svf_shadow_kernel`/`_wallheight23_kernel`에 `allow_exit` 파라미터 + step 말 `if allow_exit and sh == one and vegsh == zero: break`(전체 step·예외 블록 이후). `_suffix_exit_allowed(a, suffix_exit)` NaN 가드; `march_svf_shadow`/`march_wallheight23`에 `suffix_exit: bool = True`(기본 ON, 출력은 불변). `solweig_core/numba_cpu/sparse_march.py` — 쌍둥이 run 커널 2종 동일 블록(밀집 모듈과 축자-병렬), `march_svf_shadow_sparse`/`march_wallheight23_sparse`/`SparseMarchLaunch.*`/`march_build`에 kwarg 관통. step table·진폭 정책 레지스트리·R4/R6 의미론 불변(L2는 기존 R6 선택을 소비만 함). fastmath/FMA/재배치 없음.
- **테스트**: `tests/ultrafast/test_t19_suffix_exit.py` 신규 30테스트 — naive 목격(전체 march가 목격 셀 vegsh=1/vbsh=1 산출 핀) + NaN 가드 목격(sh 핀) + NaN sparse-vs-dense(threads 1/2) + zero-step/one-step 경계 + 음수 DEM OOB 팬텀 onset(alts 12/42/78) + **동결 74 트레이스 차동**(37 svf + 37 wh, sha 검증, raw uint32 비교) + 변형 그리드 9케이스 × 2커널(az 0/1e-12/225/359.5 포함) + site 장면(6° 링 전체·index%7·escalated, absolute-amp 78/90) + **sparse threads 1/2/4/8 == dense exit**. `tests/ultrafast/test_sparse_march.py` 커널 직접호출 2곳 인자 갱신. 전체 suite: **633P/88S/0F**(733.47s).
- **변이(지정) 사망 확인 + 복원**: M-I naive `sh==1` 단독 탈출 → 목격 테스트 FAIL(vegsh/vbsh 1 원소 불일치) **KILLED** → sha 복원 byte-identical. M-II NaN 가드 제거 → NaN 목격 FAIL(sh 6원소) **KILLED** → sha 복원 byte-identical. 기록 `~/Workspace/solweig_ultrafast_artifacts/t19/mutations/`(pristine sha a5314049…, 현재 march.py와 일치).
- **A0 진단(§5-A0, site_500 합성 장면, 순수 계수 — march 상태기계 축자 복제)**: 전 153패치 `R_exact = 0.6404`(T27 공식 onset+2 대비 +0.24pp), onset-free 79.1%; **6° 링(총 step의 58%) R_exact = 0.5567**, onset-free 52.8%; 천정(alt 90) R = 0.0040. 저고도 링이 이득 집중 — R은 고도 단조 증가.
- **ablation(잠정, 공유 호스트 load 5.2-8.6, n=3 교차-순환, JSONL 원시 보존)**: march 스테이지(153패치 밀집 직렬, 500×500) — abs_off 44.50s / eff_off 16.21s / abs_on 30.68s / eff_on **10.40s** → L2(진폭 선택) **2.75×**(step 총수 35,456→13,179), L1@eff **1.56×**(16.21×0.6404=10.38 ≈ 측정 10.40 — A0 R_exact 상한 정확 실현), 합산 **4.28×**. wallheight(태양 3위치) — abs_off 0.641s / eff_off 0.168s / abs_on 0.541s / eff_on **0.146s** → L2 3.81×, L1@eff 1.15×, 합산 4.39×. site_500은 R6 banded=∅(정직 공개 — escalated 경로 0패치, 효과 전부 windowed A_eff 선택에서 발생).
- **E2E 정직 귀속**: 생산 selected-exact 레인은 torch 커널(`solver.py:77-80` shadow_fn 임포트, `:1620` 재생 호출; time loop `utci_process.py:915/946`) — numba march 복제본은 생산 호출자 부재 → **T19 diff의 E2E 기여는 라우팅 결정 전 구조적으로 0**(NOT_RUN_WITH_REASON 기록, `ablation_march_stage.jsonl` 말미). numba 직렬 march의 A_eff 전체(16.2s) ≈ torch 16.9s → 라우팅 시 예상 절감 = L1 1.56×(march 16.9→~10.8s). 라우팅 결정은 T19 카드 밖(lead 소관) — 보고로 이관.
- Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t19/` — a0_onset_histogram.{py,jsonl}+a0_summary.json, ablation_march_stage.{py,jsonl}, ablation_wallheight.{py,jsonl}, smoke_exit.py, mutations/.
- Status: PASS(레버 구현·정리·게이트 기준). L1 PROVEN+게이트 통과, L2 잠정 2.75×(march)/3.81×(wallheight), 합산 4.28×. E2E 반영은 레인 라우팅 후속 카드 과제.
### T18 — 기본 full canonical fold 최적화 (§807·§589) — PASS (ablation-grade) (2026-09-09)

- Task: T18 (GOAL_PROMPT_T18_CPU v3 a50994f). DESIGN §807 "기본 full canonical fold를 먼저 최적화" + §589 우선순위 규정의 선행 카드. 범위: 임시 배열 제거·arena 재사용(§601)·layout·dead scan 제거 — 연산 순서 동치 + 전 비트 동등 논증 하에. 금지 계약 전부 준수 (fastmath/unordered reduction/FMA/Horner/subtract-old·add-new/precision/read-reach/6° floor/계수/재정렬/cross-profile cache/golden·pin 변경 불가; in-place aliasing 금지; oracle worktree 무접촉). Owner: t18 author agent. HEAD a50994f (미커밋 — lead landing; T18분 working-tree 변경 = `solweig_core/numba_cpu/radiation.py` + `tests/ultrafast/test_radiation.py` M3 anchor 1개뿐, 나머지 tree 변경은 T19/T24분).
- **병목 귀속 (attrib.py)**: fused kernel 내 walk 33.8%, dp pass1 34.5%, Kside 10.6%, dRad 5.1%, refl 2.0% → pass1 CSE + walk dead-scan 집중.
- **채택 (K1+K2+K3+K4 — 전부 bit-exact 게이트 통과, 단일 스택으로 납품)**:
  - **K1** dp pass1 공통부분식 host 선계산: per-cell 재계산 f64 곱(veg64·ster64·cos/sin 계열 + 4방위 card 변종, day 18/night 12종 (153,) 배열)을 per-t hoist, 커널은 `pv_c[idx]` load. 동치: numpy elementwise f64 == 커널 scalar IEEE 동일 결합순서; scalar-context accumulate 그대로. 변이 M-K1 psun_c→pv_c crosswire KILLED.
  - **K2** walk f-floor dead-scan 제거: f = buildings∈{0,1} running min (실측 unique [0.,1.], −0.0 없음) → f==+0.0 이후 step의 가산항은 전부 값×0.0=±0.0. RN 덧셈에서 +0.0 초기화 누산기가 −0.0가 될 수 없음(양변 −0.0 필요) → ±0.0 skip bit-exact. latch 보존: tempb=tws·f=+0.0⟹tbub 유지, tempbwall=f·(−1)+1=+1.0⟹tbubw:=1. NaN/Inf 배제 host fence `_assert_walk_planes_finite` (비유한시 RuntimeError — 침묵 없음). 실측 elide률 25.16% steps. 변이 M-K2 stuck-zero KILLED.
  - **K3** walk window int64 table (18,11)×4: per-cell 경계 산식 host 정수 table화 — 정수연산, guard 결과 동일. 변이 M-K3 off-by-one KILLED.
  - **K4** walk step0 snapshot `/one` 제거: 유한 f32 x/1.0≡x bit-identity. walbwn_f는 출력-사망(lif=1⟺b_row=0⟹항×b_row, 수치검증 max=0.0)이나 의미 보존. 변이 M-K4 wrong-divisor KILLED.
- **REFUSED (근거 기록)**: sunwall/albshadow cross-t hoist (wallsun per-t 실측 차이); dRad lv2==0 elision (13 day-t 중 0개); lumChi elision (0개); H1 host arena out= buffer (~0.15-0.2% vs thread-safety·캐스팅 위험); C6 state-plane ping-pong (~0.3% vs +6MiB RSS); C7 night zero-plane cross-t aliasing (caller output 보유); prange 병렬화 (카드 수단 목록 외 — 관찰 기록).
- **정확성 게이트**: (1) diff gate BIT-IDENTICAL — 24t×21 plane raw uint32 + final state + mid-anchor 3 + CI series (pristine-HEAD 기준, `~/Workspace/solweig_ultrafast_artifacts/t18/fold/{gen_ref,diff_check,ref_outputs}`). (2) 변이 4종 KILLED by `tests/ultrafast/test_radiation.py::test_fused_full_loop_raw_parity`, sha256 byte-identical 복원 (`mutations.jsonl`, 초기 약한 변이 2종의 교체 이력 포함). (3) oracle differential — 본 레인 구속 계약(T08 표준): `test_fused_full_loop_raw_parity`(24t 전 plane raw == oracle-recorded capture) + `test_full_solve_identity_all_timesteps` + frozen pin digests 전부 green. raw_runs.jsonl oracle_full pin(tmrt 487a912a…/utci 8db68a0d…/shadow f8f75196…) 프로그래밍 파싱 후 capture plane과 file-digest·raw-bytes tie probe → 불일치(GPU incremental solve_window 산출물, 상이 파이프라인 product; `oracle_pin_check.json`). CPU fold는 utci 단계 미경유·미변경. (4) suite: **632P/88S/1F** (`logs/suite_final.txt`) — 1F=`test_diffsh_diet::test_job_peak_rss_within_budget`, 동시 bench 부하 환경성(단독 재실행 PASS `logs/rss_isolated_rerun.txt`, T18 변경은 transient ~30KB/t·정상태 RSS 불변). M3 anchor string 갱신(locator machinery, golden 아님 — 공개).
- **측정 (ablation-grade, n=5 rounds×5, interleaved, per-burst 독립 process 2 warmup+5 sample, load guard 8.0)**: `ab_run.sh`/`ab_burst.py`/`ab_aggregate.py`/`ab_samples.jsonl`. **full/head paired median = 0.9375 (−6.25%)**, per-round 0.929/0.937/1.022(load 8.5 outlier)/0.938/0.928; k1/head = 1.0004 (K1 단독 이 host에서 중립 — not-slower 게이트 통과, 스택의 나머지가 이득 담당); step-median 736ms vs 793ms (−7.2%). loadavg 공시 4.95-9.92 (median 6.60, 공유 host 시스템 노이즈; 동시 팀 bench load 11-13 하 1차 시도는 폐기·보관 `ab_samples_round1_polluted.jsonl`). 조용한 host quick(참고): full 13.1-13.3s vs head 14.4-14.6s (~10%). T16 baseline 환산: cold 21,397→~20,060ms, warm 20,131→~18,873ms — 목표 3,000ms에는 미달(T18은 §807 선행 카드, 도달 판정은 T29).
- **상태**: fold 최적화 PASS(정확성 전 게이트 + 측정 개선 + memory 비침해). rollback: `git checkout HEAD -- solweig_core/numba_cpu/radiation.py tests/ultrafast/test_radiation.py` (변이 복원 sha 이미 양방향 검증). next_command: T29에서 n≥100 acceptance-grade 측정 승격; §589 equality-triggered(T20)·§807 suffix fold(T21) 착수 가능.
### T19b — 생산 svf march 라우팅 (T04/T11/§796+ 마이그레이션 완성) — PASS (2026-09-09)

- Task: GOAL_PROMPT_T18_CPU v3 T19 후속. T19의 정직 귀속(생산 레인 torch 커널, numba march 생산 호출자 부재 → E2E 기여 구조적으로 0)을 문서가 정한 레인 등록 규율로 해소. Owner: t19b author agent. HEAD 666f449 (T18 착지 위 측정). Lead landing (미커밋 납품).
- **구현**: `solweig_gpu/incremental/march_router.py` (신규) — 레인 어휘 `legacy-torch-shadow`/`numba-svf-march`, env `SOLWEIG_RT_SVF_MARCH_LANE` (미설정/numba=기본, torch=base arm·kill-switch byte-identical, 미등록=MarchLaneError 라우드 — 절대 추측 안 함). `shadow_march()` = shadow()와 동일 호출 형상; numba 레인 = R3 `build_table_general` (74 frozen trace 게이트) + T04 `march_svf_shadow` (suffix_exit 기본, T19 C6 bit-invariant), table memoization key = f32 BITS(azimuth/altitude/scale/amplitude)+shape+policy; 장면 도형 거절(bush>0·dtype·shape)은 원인 인자 그대로 torch 폴백 + lane_stats 기록 (근사 없음). 호출부: S1 `solver.py:1621` 153-patch 재생 (policy provenance `r6_band_escalated`/`effective_windowed`), S2 `veg_svf_state.py:752` corridor re-march, telemetry = process-level 카운터의 before/after DELTA (동시 solve 오귀속 없음), `worker.py:1559` prepare 측 리프트 + `solve_window` 누적(+=, 다중 윈도우 공유 stage dict), `jobs.py:485` job metrics float 채널. perf_wave2 spy → router seam 이동.
- **측정 (provisional, warmup1/repeats1 ×4 rounds 교대 인터리브, base=torch lane vs routed=numba lane, HEAD 666f449+diff)**: E2E paired delta mean −9.50s (**−26.6%**: 35.76→26.26s); svf stage 15.87→6.20s (−61%, 부하 강건 신호); time_loop 양팔 ~19s 불변 (S5 NOT_RUN 귀속). loadavg 5min ~14 피크 공시(양팔 교대 노출). routing witness: routed rows `svf_march_routed=153/refused=0` (생산 경로 = S2 corridor), base rows 0. **T16 selected-exact 33.7s → ~26.3s 환산** — 100ms 목표 잔여 gap은 compute(~26s) + 대기로 T29 분리 귀속.
- **정확성 게이트**: 54 route gates (raw-bit differential az 0/37/225/312 × alt 6/42/78 × scale, NaN a-plane, strided, patch-148 vbsh==2.0, refusal fallback bits+stats, torch lane, memoization f32-bits identity, science_site window_svf_bundle 19-tuple bitwise, apply_edit_batch packed-bit parity) + executor/worker/perf_wave2 127 green. **digest parity 4/4 rounds**: payload sha256 == store checksum == across arms, decoded arrays raw-bit identical. **매니페스트 재생성 독립 검증 (lead)**: pre/post diff = source_geometry_hash 148 + trace sha 148, content_sha256 0변화 (witness: `~/Workspace/solweig_ultrafast_artifacts/t19b/manifest_regen_witness/`).
- **리드 검증**: diff read 전부 확인; 독립 변이 T19b-LM1 (unknown lane 침묵 추측) → 2 tests KILLED, sha 복원 검증(`lead_lm1_before.sha`), 72P 재실행. Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t19b/`.
- **DEFER/NOT_RUN 귀속 (T29 인용용)**: S3 `svf_calculator` full-tile 폴백(전체호출 단위, bit-pinned producer 부재), S4 compute_utci/regenerate.calculate_svf(오케스트레이터/콜드 재구축, selected-exact 외), S5 `utci_process` wallheight_23 time-loop(numba twin 존재하나 oracle 포함 전 레인이 소비하는 공유 torch core — 별도 마이그레이션 카드, T27 별도 증명 의무). 잔여 time_loop ~19s 귀속 근거.
- Status: PASS (routing adopted; kill-switch 유지). rollback: `git revert` 착지 커밋 (단일 커밋). next_command: T29 n≥100 clean-session 승격 + S5 별도 카드 여부 판단.
### T22 — 2-bit extended vbsh 인코딩 ablation + 확장 regime 증명 (§432/§842) — NO-CONTRIBUTION (성능) / 증명·capability 기록 (2026-09-09)

- Task: DESIGN 8.1/§432 (vbsh=2.0 반례가 1-bit binary 전제를 막는 regime → "2-bit로 2.0 저장 가능 ≠ corridor proof 자동 확장" 경고 포함). 카드 판정문: 상태 표현 압축의 목표 기여 ablation 판정, 기여 없으면 그 사실 기록. Owner: t22 author agent (본 항목 직접 작성). HEAD 318c417 위 dev-only (측정 창 폐쇄 준수 — 창 개방 전 벤치 없음, in-test 소규모 타이밍만 잠정 공시).
- **정리 (확장 regime 증명, 전 변종)**: 최종 그래프 `vbsh = 1 − (thr(vbsh_acc) − vegsh_final)`는 정확한 f32 정수 산술. (i) count==1 (one-step): s==0 유일 누산 기여는 `vbshvegsh.zero_()` 리셋으로 폐기 → acc==0 항상 → vbsh = 1+vegsh_final ∈ **{1,2}** (0 불가). (ii) count≥2 (multi-step): acc==0는 모든 step 누산 시점 vegsh==0 강제, final vegsh는 마지막 step의 억제-후 값 그대로(누산과 final graph 사이에 수정 없음. s==0 예외 vegsh=1은 s≥1 억제로 소거되거나 acc≥1로 누적) → vegsh_final==0 → vbsh ∈ **{0,1}** (2 불가). ⟹ **vbsh==2 ⟺ count==1 ∧ vegsh_final==1**, 패치별 vbsh는 이진 — {0,1} 또는 {1,2} — regime만으로 결정 (**regime-remap 따름정리**: value = bit + one_step_bias[patch]로 동일 정의역을 1-bit 바이트 그대로 보관). wallheight_23 변종은 리셋 부재 → vbsh ∈ {0,1} 상시 (2.0은 svf_shadow 고유). NaN a-평면에서도 출력 정수 도메인 불변 (NaN은 suffix-exit 정리에만 위협). 소비자 측 재임계 없음 — `if vbsh > zero`/vegsh 감산은 march 최종 그래프(producer 내부, march.py:161-168)에서 종결, fold는 값만 선형 소비 → decode-값 동치가 동치 논증 전부.
- **구현 (전부 가산, 생산 경로 무변경)**: `bitmask.py`에 2-bit 코덱 (`PackedVisibility2Bit`, pack/unpack/unpack_patch/set_patch_window/weighted_sum/spatial_window_view _2bit; 레이아웃 = 패치 p를 byte p>>2의 shift 2(p&3), 4패치/바이트, 153패치 → 39 B/px; {0,1,2} 외 typed refusal — truncation 없음) + `__init__.py` re-export. march.py/sparse_march.py 무수정 (producer는 이미 정확 — 도메인 증명 대상). 신규 게이트 `tests/ultrafast/test_vbsh_2bit.py` **24 gates** (A 도메인 8 / B 코덱 8 / C fold 동치 3 / D 반례 3 / E 비용 2).
- **게이트**: (A) 혼합 regime 장면(scale 0.25, A=4.0: 66/78° 링 one-step 48패치 + 저고도 multi) 전 153패치 도메인 분해 {1,2}/{0,1} 실측, fence 술어 `one_step_patch_indices` ⟺ 실행 테이블 count==1 (천정 sentinel 제외 전 패치 일치), all-multi(A=60.0, 대각 78° az 215.7의 dz₁≈26.4m 넘김 — A=20.0은 여전히 1패치 one-step으로 {1,2} 방출, 계수 실측으로 확인)에서 2.0 무방출, wallheight 양방향 {0,1}, sparse 전타일 work-list one-step {1,2}, NaN 장면 도메인+torch oracle bit-equal. (B) 라운드트립·레이아웃 핀(수동 바이트값 고정)·{3,7,255,−1,0.5,NaN} typed refusal·윈도우 갱신==repack·가중합 순서 정확. (C) **fold 오라클 differential**: 혼합 장면에서 `pack_visibility`(1-bit)는 2.0으로 ValueError(오늘의 engagement 공백 목격) ↔ 2-bit pack→decode→`_fold_veg_svf_from_planes` 스칼라 11종+svftotal **raw-bit 동일**, regime-remap 1-bit도 동일; all-multi 장면 1-bit/2-bit/remap 3방식 전부 동일(기존 이진 도메인 무섭); 샘플 5+2패치 torch oracle 대조 독립성. (D) 반례 3종 — corridor `!= 0` 붕괴(vbsh_w.numpy()!=0, veg_svf_state.py:783)가 2.0→1 침묵 오염 → fold 발산 목격(2-bit 레인은 값 변환 의무의 근거), **regime widening stale**: A 4→60 (오늘의 `_regime_transition_reason`은 narrowing-only라 widening에서 None=침묵)에서 one-step 패치 미변경 셀 값 반전(2→1/1→0) → stale 2-bit 상태 fold ≠ 신진폭 oracle fold (§432 경고의 구체적 반례 — 확장 상태는 ANY-crossing fence 필수), decode 레이아웃 변이 사망. (E) 하기 비용.
- **ablation (잠정, 단일 프로세스 warmup1/repeats3, 500×500×153)**: 바이트 — vbsh 스택 20→**39 B/px**(+95%), 상태 총 40→59 B/px(9.54→14.07 MiB, +47.5%), f32 평면 대비 291.82 MiB. 타이밍 — pack 1-bit 9.1ms vs 2-bit 152.5ms(**16.7×**) vs remap 5.1ms; decode→f32 18.9ms vs 144.7ms(**7.7×**); **fold `_fold_veg_svf_from_planes` 5,694ms** (2-bit decode의 39×) — fold는 f32 평면을 소비하므로 인코딩 독립, 코덱 전체 차이(≤153ms)는 fold 1회의 ≤2.2%. **정리: 표현 선택은 fold-경로 성능에 구조적으로 무관.**
- **판정 (T29 verdict row)**: **(a) 압축/비용 기여 = 없음** — naive 2-bit는 모든 축에서 지배당함(바이트 +95%, pack 16.7×, decode 7.7×, fold 동일) {0,1,2} 정의역은 regime-remap가 0바이트 추가·1-bit 코덱 속도로 보관(따름정리 + 게이트 C 실측). **(b) 확장 regime 증명 = 완료**(상기 정리, 전 변종, NaN 포함, fence 술어⟺실행 스텝 일치). capability(2.0-regime baseline의 corridor engagement)는 증명됐으나 (i) 생산 측정 사이트(site_500)는 baseline fence 통과 장면 — corridor 이미 engagement, 이 인코딩의 생산 E2E delta = **0**, (ii) engagement하려면 ANY-crossing fence + corridor 값-보존 쓰기(`!= 0` 대체) + 상태 버전 마커가 선행(D 반례 2종이 부재 시 오염 목격). 채택 권고 = **유보**: 필요 시 naive 2-bit 대신 remap; 본 카드는 코덱(게이트된)과 증명만 납품, 생산 wiring 없음.
- NOT_RUN/DEFER (T29 인용): 측정 창 내 E2E A/B (창 폐쇄 + 생산 사이트 engagement=0으로 귀속 무의미); `svf_fold.py`(T07 packed 직접 소비, 비생산 wiring)의 2-bit decode 변형 미구현 — 방향만 기록(decode `b = f32((byte>>shift)&1)` → 2-bit 확장 시 2bit 판독 필요); 확장 상태의 생산 fence(ANY-crossing)/corridor 쓰기/wire는 채택 결정 시 별도 카드.
- 회귀: `test_incremental_bitmask.py` 10P, `test_incremental_veg_occlusion.py` 48P, ruff clean(내 파일; `__init__.py` E402 19건은 HEAD 선행 존재 — append-only 관례).
- Status: NO-CONTRIBUTION(성능 목표) + 증명·코덱 기록 완료. rollback: `git checkout HEAD -- solweig_gpu/incremental/bitmask.py solweig_gpu/incremental/__init__.py && rm tests/ultrafast/test_vbsh_2bit.py` (본 항목 제외). next_command: T29 verdict table에 (a)/(b) 행 반영; 확장 상태 채택 여부는 lead 판단.

### T20 — equality-triggered temporal convergence (§589) — PASS (구현·게이트; 측정 창 대기) (2026-09-09)

- Task: GOAL_PROMPT_T18_CPU v3 T17 카드(§589 후보). Owner: t20-eqconv author agent. Base 666f449에서 설계·probe(05:32), 게이트 전체는 318c417→6a9a1fe 위 재검증(T19b/T22는 solweig_gpu만 수정 — solweig_core/numba_cpu/는 666f449..HEAD 0 diff로 probe 결과 head-독립 structurally 확인). dev-only(측정 창 폐쇄 — 벤치 없음, in-test 타이밍만 잠정). Lead landing (미커밋 납품).
- **§589 계약 (DESIGN.ko.md:587-589 §11.5)**: dirty recurrence가 어떤 step에서 reference future inputs가 동일하고 carried state의 모든 bits가 이전 committed state와 같으면 이후 suffix 재사용. Equality는 6 planes(Tgmap1/E/S/W/N/TgOut1) + 4 scalar(firstdaytime/timeadd f64 bits/CI f32 bits/threaded CI f64 bits)+CI flavor(night esky 분기 load-bearing)+future-input identity(MetInputs 8필드 raw bits incl. Twater 부호·list sentinel) 전부. 부분 equality·온도 차이 조기종료 금지. raw float32 bits(NaN payload·signed zero 포함)가 비교 통화 — allclose/equal_nan/1-ULP 금지.
- **구현** (`solweig_core/numba_cpu/full_solve.py`, +281행 additive): `ConvergenceBoundary`(경계 1개의 carried facts), `ConvergenceReference`(committed run + boundaries + 서빙용 outputs/CI series — 방어적 복사 서빙, reference 불변), `full_solve_capture(convergence_reference=...)`. loop-top에서 boundary별 검사(cheap-first: future rows → CI thread/flavor → plane scan last), 전부 bit-identical일 때만 [t,n) suffix 복사·종료. 외부 reference(profile/window/scene lineage/series length 불일치)는 FullSolveError typed refusal. 요청 mid-anchor가 경계 이후에 있으면 그 경계는 forfeit(anchors outrank convergence, 기록·지연 재개). ret_CI_flavors를 result에 보존(§589 키 소재). no-reference 기본 경로 바이트 불변.
- **공실 아님 실증** (`~/Workspace/solweig_ultrafast_artifacts/t20/probe_eqconv.py` + `probe_boundaries.jsonl`): 실제 driver 복제(출력 digest 24/24 tie probe)로 25 경계 스냅샷, 11 변형 추적. **5/11 히트**: V-identity@11(진입), V-radd50@12→13, V-radi50@12→13(f32 흡수로 f32 recurrence 재수렴), V-ta+1@0(night)→1, V-ta+1@5-cold→6(night aliasing+흡수). Ta/radG day-row 편집은 창 내 불수렴, multi-row 편집 불히트, 음성 통제(all-rows) 0 히트(오탐 없음).
- **게이트** `tests/ultrafast/test_t20_eqconv.py` **15/15 PASS (259s)**: (1) identity warm resume — boundary 11 firing, suffix 3중 raw-bit 동일(unassisted warm replay·reference run·final state), reference 무손상 digest; (2) radD*0.5@12 편집 — probe-측정 boundary 13 firing, 전 24t unassisted 편집 replay와 raw-bit 동일(재계산 물리, 재확용 바이트 아님); (3) 1-ULP poison grid(6 param case, clean plane 1개만 생존, 0 셀은 부호 flip) — 전부 refuse·poison 미서빙; (4) night row 22 편집 — boundary 1 state 동일에도 future mismatch로 refuse; (5) 외부 reference 4종 typed refusal; (6) anchor precedence(11 요청 → 11 지연 후 12 재개, 23 요청 → 전면 forfeit, anchor 전부 생산); (7) no-reference 기본 불변; (8) predicate pins(signed zero/NaN payload/firstdaytime/Twater ±0.0·list sentinel — capture-free).
- **변이 3종 KILLED** (`~/Workspace/solweig_ultrafast_artifacts/t20/mutations/`): M-T20-1 plane 비교 Tgmap1-only → [Tgmap1] case가 firing 포착(python poison 서빙); M-T20-2 future 검사 row-t-only → fired_at=1 stale 서빙 포착; M-T20-1b allclose tolerance → 1-ULP/±0.0 poison 수용 firing 포착. 전부 sha256 byte-identical 복원(e52dabfa…). **교훈(사고)**: 변이 복원을 `git checkout HEAD --`로 시도했다가 미커밋 T20 구현 281행이 소거됨 → 사전 저장한 `git diff`에서 변이 1행만 sed 수정 후 `git apply`로 byte-identical 복구. 미커밋 scope 파일에 checkout 금지 규칙 준수(이후 cp backup 방식).
- **정확성 등가 논증**: 재사용 정확성은 귀납법 — `recompute_timestep`+`fused_radiation_timestep`이 (state, ci_thread, flavor, met row, capture bytes)의 결정적 순수 함수이므로 bit-identical state+inputs ⟹ bit-identical suffix. flavor를 키에 포함시킨 근거: met_recompute night esky 분기(`ci64 < 0.95`)가 계산 bits를 바꿈(flavor 독립성 증명 대신 키 포함으로 봉쇄).
- **거절 기록**: 부분 equality(일부 plane/일부 future row/온도 임계) 전면 — §589 금지; CI flavor 무시 — night bits 불일치; suppression 전면 forfeit 대신 per-boundary forfeit+지연 재개 — anchor 불변식(요청 anchor 전부 생산)을 지키는 최소 포기; 텍스트 repr 기반 met 비교 대신 f64 bits — met_from_capture 왕복 동일성.
- **측정 계획(창 개방 대기, NOT_RUN)**: aux anchor-resume 레인(§589가 작동하는 유일 레인) with/without reference paired·interleaved. 예상: primary recompute-warm row는 cold full 재계산(참조 없음)이라 §589 기여 0 — 정직한 TARGET_MISSED 인계; identity case는 step-median ~736ms×13 step → copy 비용 수준 단축 후보. access width: cpu_full_day_local p95 ≤3000ms에는 무기여(레인 상이).
- 회귀: `test_full_solve.py` 11P, `test_diffsh_diet.py`+`test_radiation.py` 포함 30P(오염 없음), gate file 15P. Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t20/`(probe, mutations, 본 백업).
- Status: PASS(구현·게이트·변이·probe) / ablation NOT_RUN(측정 창 대기). rollback: `git checkout HEAD -- solweig_core/numba_cpu/full_solve.py && rm tests/ultrafast/test_t20_eqconv.py` (lead landing 전 한정 — 미커밋 상태에서 실행 금지는 위 사고 참조). next_command: T29에 (i) probe 히트 패다(5/11, night 편집 1-경계 수렴)·(ii) ablation 결과 인용, aux 레인 resume 비용 귀속.
- **측정 addendum (lead 착지 e51d6a1 이후, 2026-09-09 aux 창 실측 — provisional-polluted 판정)**: aux anchor-resume 레인 with/without reference, A/B/B/A 교대 인터리브 6라운드(24 rows), head d92e288. **A conv(warm@11+reference, boundary 11 firing, suffix [11,24) 복사): median 209ms** (n=12, 57–386ms — 273MB 출력 복사+state 복사+13 dict 복사 ≈ 0.2s) vs **B plain(warm@11, 13 step 재실행): median 9,676ms** (n=12, 8,959–12,033ms) → **median 46.28×**. 수용성 증인: 6/6 라운드 A↔B 출력 digest raw-bit 동일. **정직 오염 기록**: 전 row가 guard 8.0 초과(loadavg 5-min 27.3–30.5) — 오염원은 타 에이전트 solve가 아닌 외부 자동화(본 host ~/Workspace/MechAnim playwright/chrome-headless, 07:00 시작, 합산 200–300% CPU)로 특정. 인터리브 설계상 양팔이 동일 외부 부하를 공유하므로 비율은 부하-강건, 효과 크기(복사 ~0.2s vs 재실행 ~9.7s)는 5× 균일 왜곡 가정에도 결론 불변(lead 판정 (a): T23 선례 — row 단위 부하 공시+불변 지표(인터리브 비율+bit tie)가 주장 운반, 절대값은 formal-grade 아님, guard 8.0 유지·선례 없음). 기회 재실행 조항: T29 pre-flight 중 자연 발생하는 1-min <8.0 창에서만 ≥3 clean 라운드 추가(clean=true flag, 폴링·대기·MechAnim 접촉 금지) — 없으면 본 polluted-ratio row가 record. 정직 인계 불변: primary recompute-warm row는 cold 재계산(참조 없음)이라 §589 기여 0, full-day p95와는 무기여(레인 상이) — §589의 기여는 aux resume 레인 한정. Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t20/aux_ablation.jsonl` (24 rows + 6 round_tie + summary + meta, head d92e288), 스크립트 `aux_ablation.py`. Status 갱신: **PASS(구현·게이트·변이·probe·ablation provisional-polluted)**.
기회 재실행 실행됨(동일 창 규율): 3 clean 라운드 완수(1 mid-round breach 폐기·재대기, dip-gate 1-min 6.47–7.99) — **clean A median 202.1ms vs clean B median 8,412.6ms = 41.626×**, 3/3 round tie bit-exact → polluted row(46.28×)의 결론 재확인, `clean_rounds.py`/`aux_ablation.jsonl` head b960ba2.

### T23 — steady RSS 256 MiB + §12.4 AOT 재평가 (§4.3/§12.4·B6) — TARGET_MISSED(예산)/측정·회계 완료, trigger-1 FIRES, AOT 비교 infeasible (2026-09-09)

- Task: GOAL_PROMPT_T18_CPU v3 T23 카드. Owner: t23-rss author agent. 측정 전용 카드(생산 코드 무변경 — 공유 트리에 T23 diff 0). 측정 트리 = 별도 detached worktree `~/Workspace/solweig_t23_wt` @ **6a9a1fe**(t20-eqconv의 미커밋 full_solve.py +281행 attribution 제외; 공유 트리 런은 별도 행으로 기록 — full_solve.py는 T16 기록분과 **동일 바이트**(sha256 a9bbe583…)이므로 T16 286.9는 current-tree 회귀가 아닌 같은 코드의 재표본).
- **방법**(§4.3/line-173 스코프 — resident mmap·scratch·checkpoint·code·thread stack): (1) T16-shape 단발 프로토콜(import + CaptureSet + 1회 warm solve + 출력 해제 + gc + settle 후 `ps rss=` current) — T16 286.9와 직접 비교 가능; (2) staged ladder(import→static→solve1→solve2→settle)로 단계 기여 분리; (3) 프로세스당 **전체 vmmap**(phys_footprint·malloc zone virtual/resident/dirty·__TEXT·mapped file·stack) + libmalloc `malloc_zone_pressure_relief` 진단(회수 가능 잔여량 상한); (4) per-row loadavg + vm_stat(free/compressed). Arms: A=T16-shape warm ×5, D=settle 30s 변형 ×3, B=ladder ×3, C=fresh NUMBA_CACHE_DIR cold-compile ×1. 드라이버·원본: `~/Workspace/solweig_ultrafast_artifacts/t23/`(t23_formal.jsonl·t23_rss_ladder.jsonl·vmmap/·floors·inventory).
- **층별 계측 ledger (§173 스코프)**: bare python 17.1 / +numpy·numba·scipy 77.1 / +solweig runtime **80.9-82.0(안정, rep 간 ±0.1)** / NUMBA_DISABLE_JIT import 78.9 — **import 시점 JIT 비용 ≈ 0**(llvmlite는 어차피 로드). capture static+loop state held +30 MiB(24.0 packed static + 6.0 loop state; **112.0±0.3 안정**). **mapped file ≈ 0.1 MiB** — capture npz는 mmap이 아니라 heap-read(§4.3 "resident mmap" 항목은 사실상 미기여). thread stack 21.3M virtual/**48K resident**(Python daemon threads; 무기여). JIT 코드 자체 = 디스크 .nbc 총 **2.34 MB**(최대 커널 fused_day 572 KB) — 레지던시 병목은 생성물(코드)이 아니라 **컴파일러 작업집합+할당자 churn**. post-gc live malloc **60.0 MiB**(2-solve shape, held 30 포함) + VM_ALLOCATE resident ~41.5(numpy 대형/JIT 섹션). **분석 live+code 바닥 ≈ 190-220 MiB**(최저 ps-rss 드로우 190.1과 일치 — 256 예산은 live 집합으로는 충족).
- **본질 발견 — ps-rss 단발 값은 재량적 회수의 사행값, 프로세스 속성이 아님**: 동일 코드·입력·프로토콜 드로우 밴드 **15.5-718.4 MiB**(전 arm·탐색 합산). phys_footprint(압박 불변)는 warm **~660 MiB(636.6-664.3, fpk 650.5-678.0)**로 일정 — 그 중 live ~60-100, 나머지는 **해제됐으나 dirty로 상주하는 잔여(freed-resident residue)**: malloc zone virtual ~595-615 / resident 1.9-458 / dirty, live allocated 60 vs frag 472.7(89%) — 잔여의 원인 = LLVM/JIT 컴파일 체류(+cold-compile arm fpk **732.1**, warm 대비 **+55-80 MiB** 체류 마진) + solve 경로의 §12.2 미준수 churn(arena 재사용 부재 — solve2가 solve1 peak을 +90 MiB 상회하는 관측). 커널 회수 압박에 따라 ps-rss는 518(load 18)→252→53(load 50)→21(load 90-117)로 붕괴 — **loadavg 90-117 arm에서 fpk는 여전히 ~650-665**로 프로세스 실질 작업집합 불변 확인. settle 30s는 밴드에 무효(settle이 아니라 커널 회수 압박이 지배). pressure_relief 진단 = **no-op**(해제 대형 region 존재 시 0-135 MiB만) — 음성 결과로 기록(§4.3 회수 가능 잔여 상한의 증거).
- **T16 286.9 재평정**: 같은 드라이버 바이트의 한 드로우이며, formal A-arm(load 17-22의 "quiet-ish")에서 489.2-602.6이 최빈, 252.3이 최저 — 286.9는 밴드 하단부(압박 하 회수 상태)의 표본. **단일 ps-rss 기록을 steady의 조건으로 쓰는 프로토콜 자체가 부실** — T23의 공식 기록: ps-rss 분포 + phys_footprint(fpk) + malloc zone 분해 3중 병기(이후 재측정은 fpk를 steady의 불변 지표로 권고).
- **§12.4 trigger-1 판정: FIRES** — "JIT compiler/runtime residency가 lightweight target을 막는다"의 측정학적 실재: (i) 컴파일러 체류(cold fpk 732 vs warm ~660), (ii) numba 런타임 solve churn의 malloc 잔여(live 60 vs resident 최대 458), (iii) llvmlite 텍스트/런타임 상주(import 81 중 ~60). live 집합(~190-220)은 256을 충족하나, **프로세스 상주로는 256을 신뢰적으로 충족하는 in-document 수단 없음** — §12.2 arena 재사용은 코드 변경(카드 수단 외), allocator env 정책은 문서 미인가, NUMBA_DISABLE_JIT은 semantic 무효.
- **B6/§26 비교 판정: INFEASIBLE(in-document)** — §12.4는 "Numba와 native를 동일 ABI/trace/fixture에 놓고 비교"를 요구하나, in-tree native/ = **native/cuda/ 뿐**(site-packages 외 .cpp/.cc/CMakeLists 0, cpu-native 참조 0). cpu-native 레인 신설 = 85개 njit/vectorize 진입점(sleef_trig 27·sleef_dp 25·math_compat 14 등 bit-exact compat 층 + frozen 1-ulp trace 재구현)을 새로 빌드하는 별도 최적화 레인 — 캠페인 수단 밖. B6 row는 **NOT_RUN(구조적)**: counterfactual 레인 부재. 증거: `t23_aot_infeasibility_inventory.txt`(§12.4/§4.3/§26 인용 + 트리 인벤토리).
- **T29 verdict row — steady RSS 256**: **TARGET_MISSED**(신뢰 가능한 warm steady 기준, primary macOS host; 달성 드로우는 측정을 무효화하는 커널 압박 꼬리에서만 발생). 귀속: JIT-컴파일러/런타임 체류 + malloc churn 잔여(trigger-1). 참고 행 불변: job-peak 320.7 PASS, warm20 672.5 PASS, cold fpk 732.1 < 768 참고. context(스코프 외): torch 생산 서버 레인 peak 836.0(T16 informational — trigger-1 판정에서 제외, cpu-numba 레인은 torch 부재 확인).
- Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t23/` — t23_formal.py(A/D/B/C 정식 드라이버)·t23_formal.jsonl(행당 loadavg/vm_stat/vmmap 분해)·t23_rss_ladder.py·t23_rss_ladder.jsonl·t23_floors.py/.json·t23_pressure_relief_probe.py/.jsonl·t23_aot_infeasibility_inventory.txt·vmmap/(rep별 전체 vmmap). 측정 환경 정직 기록: "exclusive" 창 내에도 실측 loadavg 17-117(팀메이트/시스템 부하 잔존) — 행별 loadavg 동봉으로 귀속 가능; quiet 재실행이 필요하면 드라이버 재사용(jsonl 존재 시 refusal 가드).
- Status: 카드 자체 PASS(측정·회계·판정 납품), 예산 행 **TARGET_MISSED**, trigger-1 **FIRES**, B6 비교 **NOT_RUN(infeasible)**. rollback: `git worktree remove --force ~/Workspace/solweig_t23_wt && git worktree prune`(측정 worktree; artifacts·공유 트리는 T23 손대지 않음 — 공유 트리 T23 diff = 0 확인). next_command: T29에 (i) steady_rss TARGET_MISSED + 귀속 문구, (ii) ps-rss 단발 프로토콜 폐기·fpk 병기 권고, (iii) B6 NOT_RUN(infeasible) 인용, (iv) lightweight release gate의 유일 문서 내 탈출로 = §12.4 cpu-native 신설(별도 캠페인) 명시.

### T21 — prefix-accumulator suffix fold ablation (§807) — REFUSED (memory gate), exactness proven (2026-09-09)

- Task: GOAL_PROMPT_T18_CPU v3 T21. DESIGN §807 "정확한 누산기 prefix를 저장하고 그 지점부터 suffix fold를 재실행"의 정량 판정 카드 (subtract-old·add-new는 금지 계약과 별개로 본 방안에서도 부재 — recurrence의 원 비트를 그대로 잇는 방식). Owner: t21 author agent (본 항목 직접 작성). HEAD 318c417 위 dev-only (창 폐쇄 준수 — 임시 wall만 기록, formal timing arm NOT_RUN). 판정 대상 명확화: §807의 fold = temporal canonical fold (`full_solve` day loop) — 카드의 "bytes per timestep"·§589 "여섯 평면과 네 스칼라" 어휘로 svf_fold의 10 patch accumulator와 분리.
- **경계 상태 완전 열거 (Completeness proof)**: 6 f32 평면 (Tgmap1/E/S/W/N, TgOut1) + firstdaytime(int) + timeadd(f64) + CI(f32) + ci_thread(f64) + ci_flavor_is_tensor(bool) + next_step + met-prefix sha256 fence = §589의 6+4 + 부하성 flavor. Twater는 per-row 강제 입력 (미보유). day step은 6평면 전체 교체, night step은 입력 평면 객체 재게시 (kernel이 상태 평면 기록 안 함 → checkpoint 사본 변형 불가). 누락 누산기 = 침묵 발산이므로 변이로 각 항목 치아 검증.
- **동치 논증**: fold는 recurrence state_{t+1}=F(state_t, inputs_t) — 동일 prefix 입력 비트 + 동일 누산기 비트 ⇒ 중간 복원 suffix는 원 recurrence의 연속 (귀납). 유효성 fence는 classify_anchor 규칙 계승 (a ≤ r0 ∧ met-prefix digest 일치; future-prefix 거절).
- **정확성 게이트 (전부 PASS, pytest 12/12, 106s)**: (1) identity — harness loop ≡ 생산 `full_solve_capture` (24t × 21 plane raw uint32 + f64 CI series 비트 + per-t flavor + 최종 경계 비트). (2) 독립 oracle — 복원 suffix의 per-step 출력 및 per-step next-state (평면/timeadd/firstdaytime/CI) vs capture ret_* raw 비트, 불일치 0. (3) restore fidelity 8/8 bitwise (a∈{4,8,12,16} × r0∈{8,12,16}, anchor<r0 포함). (4) fence — 16 경계 존재 하에 12 선택, 16은 future-prefix 거절.
- **변이 (round 2, 완료-의미 수정 후)**: KILLED — M3 timeadd drop→t13 Tmrt, M4 firstdaytime drop→t12 Tmrt, M5 평면 NaN→t12 Tmrt. ABSORBED (본 capture에서 측정·pin, `mutation_pin.json`) — M1 f64 ci_thread→f32 붕괴 (생산 warm-path 철자) bit-equal 완주, M2 flavor flip bit-equal 완주, M5b 1-ULP normal cell poke f32 blend 흡수 (진단 pin). M1/M2 흡수는 capture 특이적 — 보편 안전 주장 아님. round 1의 결함 있는 완료 판정(24-step 기대) 폐기본 보존 `parity_probes.round1_buggy_mutation_check.jsonl`.
- **메모리 (측정 — REFUSED 근거)**: payload 6,000,069 B = 5.722 MiB/경계 (site_500 500×500). 측정 RSS k 경계 보유: k=2→273.6 MiB, k=4→278.5, k=24→356.3 (harness process, allocator 포함); 투영 286.9(캠페인 기저)+k×5.722 → 298.3…424.2 vs 256 MiB 목표 — **전 k 침해 ⇒ 카드 게이트상 REFUSED**. capture 2.7ms / restore 0.2ms — 런타임 오버헤드 무시 수준, 단일 차단변수는 상주 메모리.
- **접근 폭 (임시 wall, parity-class 부하; formal paired arm NOT_RUN-with-reason — 메모리 거절 결정적·재계산 warm 목표 이미 미충족, `timing_arm_status.json`)**: full 재접 12,974ms 대비 ms-가중 k-sweep 평균 refold k1(@12)=10,144 / k4=7,969 / k23(per-t)=7,093ms (54.6%; 하한은 13 day step × ~740ms). recompute-warm 15,000ms는 무수정 full 재접(~13.0s)으로 이미 충족; full-day 3,000ms는 본 방식으로 도달 불가. §807 자체 예측 ("이른 변화 = 이득 작음") 정량 확인 — k1 절감 22%.
- **리드 검증**: footprint 3 test-only 파일 확인 (t21_suffix_fold.py, t21_ablation_run.py, test_t21_suffix_fold.py; 생산 편집 0). 게이트 2회 재실행 12/12. 독립 변이 LM1 — 기본 복원 경로에 timeadd=0.0 주입 (mutate= 우회) → 4 tests KILLED (scalar 복원 fidelity 치아 확인), sha 복원 검증 (`t21/lead_lm1_before.sha`). paired timing 창은 lead가 DECLINE (메모리 거절 결정적).
- Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t21/` — parity_probes.jsonl (+round1 결함본), memory_ledger.jsonl, mutation_pin.json, timing_arm_status.json, parity_run{,2}.log, memory_run.log, lead_lm1_before.sha.
- Status: REFUSED (채택) / exactness PROVEN (기록). rollback: 3 파일 삭제. next_command: T29 인용 — "T21은 캠페인 기저(286.9 MiB) 기준 거절, k≤2는 17.6 MiB near-miss로 t23의 allocator residue 대역 내 → best-case allocator draw에서 near-miss"로 서술; §807 잔여 면은 svf_fold patch accumulator (spatial 유사안은 T25 영역).

### T25 — spatial/COW checkpoint 확장 (§571·§579 + TASKS T10 migration) — PASS (구현·게이트·증명; E2E timing 창 대기) (2026-09-09)

- Task: GOAL_PROMPT_T18_CPU v3 T25 카드 (DESIGN §571·§579, TASKS T10이 "별도 schema migration과 test"로 연기한 항목 §363). Owner: t25 author agent (본 항목 직접 작성). HEAD 6a9a1fe 위 dev-only (측정 창 폐쇄 — 벤치 없음, ledger형 in-test 계측만). Lead landing (미커밋 납품). HARD 계약 준수: svf-state COW와 thermal state 재사용의 분리 — 적용판정은 **digest-only** (scene revision·SVF-store chunk 상태는 planner에 불입, revision-blindness 게이트로 pin).
- **정리 (chunk COW warm start)**: cell x의 step k thermal bytes는 (met rows 0..k−1, carried scalars, x의 Chebyshev reach R 내 scene cells, x 자cell landcover, march globals)의 결정적 함수. Clean chunk (dilation 후 digest 일치)는 checkpoint bytes 복사; dirty chunk는 경계 sub-window에서 **REPLAY COLD** (zero-init 아님 — k>0의 dirty 평면은 진화 상태; elementwise chain ⟹ windowed run == full-tile crop 비트동일, splice는 구성상 bit-exact). **복원 상태의 scalar 분할 (lead 보고 조건 a, §589 11-항 명시)**: chunk COW가 소유하는 것은 6 f32 평면뿐 — §589의 4 scalars(firstdaytime/timeadd f64/CI f32/ci_thread f64)+CI flavor는 chunk 산물이 아니라 **전역 record prefix**에서 온다(met rows가 ci_thread를 loop 전체에 f64로 관통하므로 met chunking 불가 — chunking은 기하만). compose의 scalar parity fence가 이 분할을 봉쇄: dirty replay의 scalars == record scalars 강제(met-only 정리: CI/firstdaytime/timeadd/Twater 기하무의존), 불일치 = loud CheckpointError. Dilation: D = reach//cells+1 (Chebyshev chunk-거리). Landcover는 per-cell 소비(alb/emis/TgK 자cell, Twater는 met-only) ⟹ 자chunk만 dirty, scene은 dilate. march globals fence (T22 ANY-crossing 템플릿): march_amplitude_bits = f32(amaxvalue − min(a)) (solver.py:641 철자), amaxvalue_bits = global max(a.max(), vegdem.max()) (solver.py:550), scale_bits = cells/m — **1-bit 차이면 전 chunk dirty** (raster 무변경인 step-policy 변경도 march 출력을 바꾸므로 per-chunk 사면 없음). Scalar-side 강거절: met_prefix/model_parameters digest 불일치 ⟹ COW 자체 불가.
- **구현** (`solweig_gpu/incremental/checkpoints.py`, +~450행 additive; solver/veg_svf_state/full_solve 무수정): SCHEMA 2→3, LOADABLE (2,3) — v2는 spatial_cow=None으로 **행동보존** load (planner typed refusal, whole-tile 정책 불변; migration 게이트로 pin). `chunk_scene_digests`/`capture_spatial_cow` (per-chunk sha256, thermal_fingerprint 래스터 순서, landcover 별도 맵|None), `_validate_spatial_cow` (write/load 양측 fence), `plan_chunk_cow_warm_start` → `ChunkCowPlan` (clean 2-D bool, dirty_window RasterWindow, refusal_reason, cells_replayed_fraction, plane_bytes_cow_reused, page_table_bytes, dirty_causes), `compose_chunk_cow_state` (replay 필수 — "must REPLAY cold, never zero-init"; per-chunk byte-splice; **scalar parity fence**: replay scalars == record scalars, 불일치 = CheckpointError).
- **게이트** `tests/ultrafast/test_t25_chunk_cow.py` **36 gates PASS (117s)**: A digest 철자 7 (결정성·단일 chunk flip·landcover-only·ragged·absent 안정·CHUNK_CELLS==64 drift fence·validator 거절) / B migration 5 (v3 roundtrip 비트동일·v2 hand-crafted 행동보존 load+select_replay 일치·v2 planner typed refusal·LOADABLE{2,3}·payload digest 변조 거절) / C planner fences 10 (all-clean·D=1 정확 3×3 집합+dirty_window+causes·D=3 전dirty·landcover 자chunk만·absence 전이·met/params 거절·shape/scale/amax/amplitude 4종·**1-ULP amplitude fence**·revision blindness) / D compose 6 (replay 필수·inapplicable 거절·all-clean 순수 checkpoint bytes·replay shape/name fence·scalar divergence 거절·splice 격자 분할) / E oracle 실물리 5 / F ablation telemetry 2 (counter 항등식·500² ledger print + page_table_bytes == json len).
- **E (실물리 oracle, solve_window 192² site + 11.9m flat block edit)**: (1) march globals 불변 실측 (amax/canopy 동일, a만 차이 — block < 12m 기존 최고고도); (2) **정리 증명**: dilation 밖 평면 raw-bit 동일 + dirty window 내 비공허; (3) COW compose == fresh rebuild 전 6평면+scalars 비트동일; (4) COW 상태 warm continuation == cold suffix 전 스텝 비트동일; (5) **understated-reach RED witness**: 8-cell chunks, 정확 reach 59 (D=8) vs 과소 7 (D=1) → 과소 window 경계 8px < 실측 14px 태양그림자 영향권 → clean chunk가 edit이 이미 무효화한 bytes 서빙 목격 (stale>0). Witness 정직 기록: 16-cell chunks에서는 3번 반복 fixture(나무 2종·block) 모두 vacuous — 실측 thermal 영향은 block 에지 +14px뿐 (6° 링 이론 56px 아님, k=2에서 SVF 환류는 f32 누적 가시성 미달), D=1 경계 16px가 이를 덮음; dome-tree는 구조적으로 불가 (flank ~8px < 최소 ring 16px). 8-cell chunks로 해소.
- **Ablation ledger (in-test, 500²·64-cell chunks·8×8 격자)**: 중심 1-chunk scene edit → 9 chunks dirty (D=1 3×3), **36,864 cells replayed / 250,000 (fraction 0.1475)**, plane_bytes_cow_reused = 213,136 cells × 4 B × 6 planes = 5.12 MiB. **page_table 산술 (lead 보고 조건 b, T23/T29 재도출용)**: compact-json 항목당 ≈147 B = scene 64-hex + landcover 64-hex 두 digest 맵의 ("r,c":"hex" + 구문) — chunk 수 = ceil(rows/cells)×ceil(cols/cells). site_500 실측형: **64-cell(8×8=64 chunks) = 9,524 B (9.30 KiB), 16-cell(1024 chunks) = 148.9 KiB, 128-cell(16 chunks) = 2.46 KiB**; landcover 부재 시 항목당 반값(~73 B). 고정 헤더(격자 필드 + 3×8-hex bit fences) ≈150 B. 게이트는 page_table_bytes == len(json.dumps(stored)) 항등식으로 pin. ADOPTION GATE 회계: 기본 64-cell page table 9.5 KB ≪ 256 MiB 예산 (T23 baseline 대비 무의미 수준) — 단 **RSS 미측정** (카드 지시: T23과 조정은 lead 경유, 본인 직접 측정 금지). E2E 타이밍 NOT_RUN (창 폐쇄 + executor wiring 미착지 상태).
- **PRE-EXISTING 결함 발견 (보고·비수정 — fingerprint 소유권 타 카드)**: `utci_process.py:922` Twater = mean(Ta[jday == floor(dectime[i])])는 **동일 jday 미래 met rows까지 읽음**; `:929` CI는 미래 row i+rise+1에서 수립. met_prefix digest는 0..next_step−1만 커버 ⟹ 미래 동일-day Ta/RH/radG 편집 시 warm start가 stale scalar 서빙 가능 (본 카드 scalar parity fence가 노출 — COW 계층은 이를 loud 거절로 봉쇄하나 근원 r5 fingerprint의 day-window 확장은 결정 주체 lead/t20).
- **리드 정정 (2026-09-09 심의, 위 인용문의 봉쇄 서술에 대한) — "COW 계층 loud 거절 봉쇄"는 오기**: (1) COW는 미배선 (plan_chunk_cow_warm_start 생산 호출자 0, WORKLOG T25 (iii) proposals-only); (2) 배선돼도 동일-day 미래 met 편집은 anchor-ACCEPT 경로 (thermal.py:496-499 — future-prefix 거절은 next_step > r0일 때만; met-prefix digest는 candidate 자신의 next_step에서 계산)로 COW 미경유. 오늘 트리의 hazard 경로 동작 = **무거 refusal·무계측 stale 서빙**. CI 형제는 landcover 게이트 밖 (utci_process.py:925-930, 전 사이트 적용, 코드 내 주석 없음). 상세: 심의 워크플로우 wf_68692dd5 기록.
- **Executor wiring PROPOSALS (보고 전용 — full_solve.py는 t20 소유, 무수정)**: (i) classify_anchor 거절 직후·cold replay 직전에 planner 호출, (ii) dirty-window replay는 `solve_window(read_window=full, write_window=dirty_window)` 경유, (iii) `stage_thermal_capture`에 spatial_cow 동봉 (worker.py:1476 캡처 seam).
- 정직 범위: COW는 march globals 비트가 움직이는 편집(부지 최고고도 상향 등)에는 **적용 불가** — 전타일 replay로 강등 (fence가 보장, planner 거절 사유 명시). chunk_cells=64 기본 = veg_svf_state._CHUNK_CELLS 미러 (drift 게이트 pin, import 아님).
- 회귀: `test_incremental_checkpoints.py` 50P + `test_incremental_warm_start.py` 10P + `test_incremental_worker.py` 55P, ruff clean (양 파일).
- Status: PASS (스키마 migration·fence 전세트·실물리 비트동일 증명·ledger) / E2E ablation NOT_RUN (창 + wiring 대기). rollback: `git checkout HEAD -- solweig_gpu/incremental/checkpoints.py && rm tests/ultrafast/test_t25_chunk_cow.py` (미커밋 납품 — 실행 시 본 항목 잔류 주의, T20 사고 선례). next_command: T29 verdict row — "T25 COW: 적용판정 digest-only, page table 9 KB ≪ 예산, 적용 시 replay fraction ~0.15/chunk-edit (64-cell chunks), E2E 기여는 wiring 후 별도 측정"; Twater/CI day-window digest 확장 판정은 lead.

### T29 — 도달 가능성 종합 판정 + 원본 측정 규정 폐쇄 — 판정표 확정 (2026-09-09)

- Task: GOAL_PROMPT_T18_CPU v3 (a50994f) 최종 카드. acceptance_targets.yaml sampling 최소 규정(n≥100, warmup≥5, randomized paired, hw/부하 기록)으로 3 E2E 예산 행 재측정 + 목표별 판정표(현재 → 카드별 개선폭 → 잔여 gap → 도달 여부). Owner: t23-rss agent (캠페인 드라이버 `~/Workspace/solweig_ultrafast_artifacts/t29/t29_campaign.py` 설계·실행) — 저자 세션이 호스트 재부팅(19:12, API ConnectionRefused)으로 소실 ⇒ 본 항목은 리드가 원본 아티팩트에서 전사(clean-rounds 선례; 저자 복귀 시 원문 교체). 측정 트리: detached worktree **b960ba2**(머지 HEAD). 생산 코드 편집 0 — 측정 전용 카드, raw-bit 계약·oracle worktree 무접촉.
- **프로토콜(dip-harvest)**: 행별 launch gate load1<8.0 (5s 폴링, 행당 상한 1,800s), midrun breach → 행 폐기 + 세그먼트 전체 requeue(원본 행 보존), 판정은 clean 행만 인용, dip 대기 총량 공개. 20% 플래그 분할 규칙(clean-only 판정 + all-rows 병기). selected_exact은 T16 공인 계측기 무변경 (manifest row1_exception: 세그먼트 진입 게이트 + 사후 행별 hw.loadavg 플래그). 백분위 규약: **선형 보간**(summary 산출과 raw JSONL 재계산이 최종 자릿수까지 일치 — 독립 검증 확인). warmup≥5 (seg 0, yaml 최소), 이후 세그먼트 2 (fresh-process 재웜, 미샘플). randomized paired: 단일-암 예산 행(B 후보 부재)으로 manifest에 기록된 예외.
- **판정표** (백분위 ms, 선형; clean 행만):

| 목표 (final budget) | 현재 (clean) | 카드별 개선폭 | 잔여 gap | 도달 여부 |
|---|---|---|---|---|
| cpu_selected_exact (p95, 100ms) | 공인 바닥 ~25.0s — T19b round4_routed 25.01s · 캠페인 min 24,260.2ms · T16 공인 33.29s (3 독립 공인 런 교차) | T18 fold K1–K4 + T19 suffix exit + T19b 라우팅 33,746.6→25,010 = **1.35×** + T24 대기 0.50→0.05s (−0.45s — adaptive flag ON 기준; default OFF 출하, 심의 정정) | final **242.6×**, milestone(1,000ms) **24.3×** | **TARGET_MISSED** — 잔여 100% compute (대기 해소; T24 귀속, 아래 분리 귀속 행) |
| cpu_full_day_local (3,000ms) | warm p50 13,022.0 / p95 13,559.9 / max 14,272.6 (clean n=70, 7/10 segs) · cold CONTEXT p95 13,602.2 (n=10) | T16 20,131→13,022 = **1.55×** (T18/T19/T19b march+fold) | **4.52×** (milestone 10,000ms도 1.36× 미달) | **TARGET_MISSED** |
| cpu_full_recompute_warm (15,000ms) | p50 13,100.5 / p95 13,484.8 / max 14,948.3 (clean n=100, 10/10 segs — **n≥100 규정 충족**) | T16 19,384→13,485 = **1.44×** (T18/T19 — §589는 본 레인 기여 0, WORKLOG:379/:406 기록; 초기 기재 "T20 §589 포함"은 심의 정정) | — | **도달 PASS** (p95 기준 10.1% 여유; max 14,948.3 = 51.7ms 여유도 공개; breach 0.00) |
| steady_rss (256MiB) | 286.9MiB (T16/T23 측정) | 개선 0 — T21 REFUSED (메모리 게이트, k경계 전 침해), T25 checkpoint wiring 유예(future card) | 1.12× | **TARGET_MISSED** (T23 — trigger-1 FIRES 기록됨) |
| ux_local_geometry_feedback | — | — | — | NOT_RUN (브라우저 레인 — 본 캠페인 CPU E2E 범위 외, goal 규정대로 GPU 항목 제외) |

- **selected-exact compute/대기 분리 귀속 (카드 규정)**: §710 블로커("500ms coalescing 대기 자체가 100ms 목표 초과")는 T24가 해소 — queue_wait 0.50→0.05s (adaptive floor, paired 15/15, 게시 payload sha256 원비트 동일 9cbc42b4…/8b8b0d5b…). 잔여 gap ~34.8s→현재 ~25s는 **전부 compute** (svf march ~6.2s + time loop ~18.1s, job_metrics = T19b stage 귀속과 일치; S5/S3 미라우팅 잔여). 구조 발견: yaml scope `include_queue_epoch_wait: true` ⇒ 대기는 측정 범위 내 (coalescing 500ms 단독이 예산 5× — 계기 구조, 호스트 진정으로 무관). ms/s slip 가설은 반박됨 — milestone>final 정렬(양 레인), cuda 달성 17.29ms가 33.3ms 타겟과 동위수, §710 ms 일관 산술(5×).
- **측정 무결성**: clean 행 load1 최대 7.89 < 8.0 게이트 (PASS 행 오염 가능성 배제) — recompute 100/100·warm 70/70·cold 10/10 전 행 게이트 하회. selected_exact 100행 전부 오염 플래그 (load1 8.24–87.70, breach 1.0) ⇒ contaminated-evidence로만 인용. sampling n≥100: recompute **MET**, full_day warm **NOT_MET(host) 공개** — seg 7 미완료(9회 requeue, 최대 5샘플), 19:12 재부팅으로 pass 29+ 행 손실; cold n=10 CONTEXT(spec-grade 아님 명시). requeued 시도의 clean 행(complete 세그먼트 외 27/45/12행)은 프로토콜상 통계 제외 — 원본 JSONL 보존. 총 dip-harvest 대기 ~3.5h+ (호스트 밴드 5.8–149, 외부 스파이크 + 재부팅 포함) — breach 폐기 규정 없으면 PASS 행 성립 불가.
- **비-저자 리뷰**: 독립 서브에이전트(본 항목 미작성)가 5개 JSONL + summary 원본에서 전 수치 재계산 — **A–F 전 항목 PASS**, summary 선형 규약 일치 확인(§1073 상세: recompute p50/p95/max·load 게이트·10 segs, warm 70/7segs, cold summary 교차 일치, selected 100행 전 오염 + 4/4 stats 일치, manifest budget/head_sha/gate 텍스트, 도출치 242.60×/4.52×/10.101%/1.55×/1.44×/1.35×). 지적 2건 반영: 백분위 선형 규약으로 통일(초안의 nearest-rank 13,563→13,559.9), "~250×" 표기를 정확치 242.6×로 교체.
- 수용 규정: acceptance_targets.yaml 불변 (`agent_may_relax_targets: false` — yaml status는 원본 그대로). 판정 어휘 PASS/TARGET_MISSED/NOT_RUN만 사용. 롤백 불요(생산 편집 0).
- Status: **판정표 확정 — goal v3 CPU 도달성 캠페인 폐쇄.** selected_exact TARGET_MISSED(compute 귀속) · full_day TARGET_MISSED · recompute_warm **PASS** · RSS TARGET_MISSED · ux NOT_RUN. next_command: 잔여 레버는 compute 레인(S5/S3 라우팅, march 잔여 6.2s) — 신규 후보는 T28 재개 조항 경유만. README ledger 동기 반영.

### T28 — 경미 후보 비교 (§716·§801, 선택) — NOT_RUN (조건 미충달, 리드 판정) (2026-09-09)

- 카드 조건: "E2E가 목표 근처에 도달해 의미가 생길 때만 실행하고, 아니면 NOT_RUN으로 기록한다." T25(패스, wiring 유예)·T26(NO-CONTRIBUTION) 판정 도착으로 조건 판정 가능 — 병목 후보 소진 시점의 E2E 실측(머지 트리 b960ba2 기준): cpu_selected_exact ~26.3s vs 100ms 목표 (263× 격차), cpu_full_day_local ~18.9s warm vs 3,000ms (6.3× 격차), cpu_full_recompute_warm ~13.0s vs 15,000ms (이미 MET — 실행 불요).
- §716 transport dirty-rectangle/chunk-only + compression: full_day/selected_exact 행의 gap에 비례해 의미가 생기는 후보 — 양 행 모두 격차 큼. §801 compiler auto-fusion(exact harness): 커널 속도 후보 — 동일하게 격차 큼. 두 후보 모두 "목표 근처"에서만 판별력을 가지므로 현재 격차에서는 기여 판정 자체가 무의미.
- 참고: T25 wiring이 착지되더라도 COW는 재생 셀 비율(0.1475 @ center-edit) 절감이지 E2E 행의 6.3×/263× 격차를 바꾸지 못함 — 조건 판정 불변. §801 harness 소유자(t20-eqconv)는 사후 통보; 조건 판정은 격차 산술이라 의견 여지 없음. T29 캠페인이 recompute-warm 행에서 목표 회귀를 발견하면 재개 조항으로 재판정 가능(단, 그때도 §716/§801이 그 회귀를 되돌릴 후보인지 재확인 필요).
- Status: NOT_RUN(조건 미충달 — 판정 근거 기록 완료). 리드 판정, 저자 없음(카드 성격상 측정 실행 조건부). next_command: T29 verdict table에 NOT_RUN 행 + 본 산술 인용.

### T26 — whole-cell scalar bundle reuse ablation (§474) — NO-CONTRIBUTION (E2E), exactness proven (2026-09-09)

- Task: DESIGN §474/§8.3 "완전 의존성 fingerprint 하에서 완료된 셀의 svf_fold 번들을 재생" 정량 판정 카드 (T16 종료 기록이 병목 후보로 명명; T21의 temporal fold와 분리 — §807의 svf_fold 측면). Owner: t21-fold agent (본 항목 직접 작성). HEAD 99e4b97 위 dev-only 3 파일, 생산 편집 0. formal timing 창 lead DECLINE — 임시 wall + hitrate.jsonl (행별 loadavg)이 기록.
- **번들 완전 열거 (Completeness proof)**: `_fold_kernel`은 셀당 메모리리스 — [i,j] 셀 비트만 읽고 누산기는 셀마다 재초기화 ⇒ 번들 = 4 per-cell 입력의 순함수: vegsh packed 20B + vbsh packed 20B + vegdem2 f32 (오직 `==0.0` 술어로만 소비 → 1비트; -0.0은 동류이고 NaN은 배제) + svf_building f32 (svftotal에만). frozen 테이블 (W_ISO/W_ANISO/AZIMUTH/LAST_CONST/TRANS/방향 마스크)은 전역 상수 → namespace stamp로 울타리. 결론: 캐시 정확성은 fingerprint가 4 입력의 원 비트를 덮으면 recurrence 귀납 없이 순함수 인수로 성립.
- **C1 TRANS scope pin (GO 조건 1)**: trans는 scope-constant — `svf_fold.py:295`의 `svftotal = svf_building − (1−svfveg)·one_minus_trans`에서 소비, `one_minus_trans`는 :447에서 모듈 frozen `_TRANS_U32→TRANS` (:146-147)으로부터 hoist. 셀/패치 인덱싱 없음. 비트는 `frozen_tables_digest` (:195) + `FOLD_CONSTANTS_SHA256` 울타리 → 5번째 fingerprint 필드 불요, 캐시 namespace stamp (digest + FOLD_CONSTANTS + 4 방향 마스크 sha256)가 비트 보유, 이종 stamp는 CacheFenceError 거절. 변이 pin: 소비점에서 TRANS 변위 → 재계산 svftotal 이동 + fence 포착 (치아 확인).
- **Fingerprint 구현**: 변종 B (live) = veg 20B + vbsh 20B + vegdem2-영 술어 1B = 41B, 10-float 재생 (svftotal은 fresh 재계산 — 2 flops IEEE f32, kernel 비트 동일, 게이트 pin). 변종 A (§474 엄격) = + vegdem2 f32 비트 + svf_building f32 비트 = 48B, 11-float 재생. raw-bytes dict key → 해시 충돌 구성 불가. 제작 중 하네스 결함 자체 포착: `.view(np.uint32).astype(np.uint8)[:, None]`는 f32당 하위 1바이트만 남김 (A 변종 fingerprint 붕괴) → `.view(np.uint8).reshape(-1, 4)` 수정.
- **정확성 게이트 (전부 PASS, pytest 11/11; test_svf_fold.py 합동 38 passed, 112.51s)**: 순수성 (복사 입력 셀 마스크 위치 비트 동일 번들), gen-0 identity + warm (변종 B/A), 편집 세대 cross-gen hit, M1 vbsh-drop KILLED, M2 vegdem2-술어-drop KILLED, M3 vegdem2 값별 키 pin (3.25 vs 9.75 — 동일 하위바이트 0x00, 이값 — exact), M4 이종 stamp CacheFenceError 거절, C1 pin, M5 near-miss 바이트 false-hit 없음, 메모리 장부 `cache_memory_ledger.json`.
- **C2 경제학 (GO 조건 2 — hit-with-verify 포함 행, 손실 행 은닉 없음; 임시 wall)**: gen-0 전체 miss는 순 오버헤드 0.612–0.946× (per-cell Python 해싱이 이득 잠식 — lead step-0 예측 확인). dirty-cell hit은 masked baseline 대비 4.8–9.0× (structured_500 gen1–3: 225–359 cells, 5.83/6.86/8.96× over 7.01/5.58/5.31ms; structured_200 4.8–5.2×) — 그러나 baseline 자체가 5.3–7.0ms ⇒ 절대 절감 편집당 ~4–6ms. 손실 사례 기록 (random_300 gen1 0.218×). 캐시 footprint 2.37 MiB (structured_500, 12,354 classes) … 17.25 MiB (random_300, 90k). guaranteed-hit 편집 모델은 실제 march hit-rate를 과대평가 (T29 유보).
- **정직 귀속**: `fold_svf` 생산 호출자 0 (생산 svf는 torch svf_calculator lane; T19b는 march만 라우팅, S3 유예) ⇒ E2E 기여는 구조적으로 0. fold-stage 잔여가 카드의 전부. formal 창 DECLINE 정당화: 4–6ms 절대 마진은 구조적 zero-E2E 귀속을 뒤집지 못함.
- **리드 검증**: footprint 3 dev 파일 확인. 게이트 재실행 11/11 + 합동 38 passed (84.94s), ruff clean. 독립 변이 LM1 — 제작 중 하위바이트 결함 재주입 (변종 A vegdem2 값 키를 4바이트 뷰의 `[..., :1]`로 붕괴) → M3 pin이 즉시 포착 (test_pin_key_by_vegdem2_value_stays_exact FAIL, 타 10/11 PASS). 설계된 치아 분업 확인. sha 복원 검증 (`t26/lead_lm1_before.sha`).
- Artifacts: `~/Workspace/solweig_ultrafast_artifacts/t26/` — hitrate.jsonl (16행, 행별 loadavg, walls_provisional: true), cache_memory_ledger.json, lead_lm1_before.sha.
- Status: NO-CONTRIBUTION (E2E) / exactness PROVEN (기록 — §474 부활 시 T29 march instrumentation 선행 유보). rollback: 3 파일 삭제. next_command: T29 인용 — "T26: fold-stage 후보는 한계에서 소진 (4–6ms 절대, 생산 호출자 0); 실제 레버는 march lane (T19b 6.2s) + S3/S5 라우팅 + §474 부활 시 실편집 march bit-diff 계측."

### Vanilla(e0d19fc) 전 연산 동등성 캠페인 — 실행 표면 PROVEN_RAWBITS / 보편 명제 NOT PROVEN (2026-09-09)

- 지시: "계산이나 계측 혹은 시뮬레이션 관련 모든 연산에서 vanilla와 같음을 증명하고 계측" — 워크플로우 wf_e607f548-b83, 14 agents (스냅샷 1 + 레인 9 + 계측기 감사 1 + fresh 차분 2 + 적대 심의 1), 0 error. Repo HEAD 0771622 vs pinned oracle e0d19fc. 집계 proof wall ~1,828.6s. 전체 원문: `~/Workspace/solweig_ultrafast_artifacts/vanilla_eq/{CAMPAIGN_VERDICT.md,CAMPAIGN_VERDICT.json}`.
- **스냅샷 무결성**: oracle tracked tree → `vanilla_eq/oracle_copy/` (git archive, 420/420 blob-sha1 == ls-tree, 작업 전후 porcelain 동일 — pinned worktree 쓰기 0 입증). 데이터는 main repo symlink.
- **PROVEN_RAWBITS (실행 표면, 본 host)**: fresh E2E full-solve site_500/24t — shadow/tmrt/utci 0/18,000,000 uint32 mismatch per plane, 양측 sha256 == frozen raw_runs.jsonl pins (tmrt 487a912a, utci 8db68a0d, shadow f8f75196), wall 118s. Fresh edit-path — E2 tree-move 3 planes sha 동일 + met edit air_temperature@t=12 0/9,427,968 mismatch. 레인별: svf march 98/98 (frozen-74 raw incl. -0.0/NaN payload), fold 50/50 (site_500 11출력 raw == 원본 svf_calculator, 2회 실행), radiation CPU 24t fused 0 mismatch, met/full-solve 24t 전 필드+21 planes+CI 0 mismatch, UTCI 12/12 raw (NaN payload/-0.0), wallheight 186/190 (red 3 = 비수치 provenance/RSS), thermal 96+36+6 green (warm==cold byte 동일), router/capture 0 mismatch + 15.97× compact, bitplane/vbsh/SLEEF-f32 raw 동일.
- **보편 명제 NOT PROVEN — 증명 정지점 8**: (1) CUDA 전체 본 host 실행 0 (9 test 파일 0 실행; cura RTX A6000/torch 2.5.1 기록만); (2) 플랫폼 핀 macOS arm64+torch 2.14.0+8 threads; (3) fixture 단일 site_500/lc=0/2009-08-11 — water(lc=1) 분기 무실행; (4) **동일일 future met edit warm 서빙 — LIVE 무펜스 hazard** (하단); (5) edited-met은 외부 oracle 부재 — 내부 warm==fresh 차분만; (6) fenced 체제 — cube-scope legacy CUDA (xfail strict, 기록치 tmrt ≤6.1e-05/6.82% lanes, vs cura day 0.345C), 225/315 vanilla 내부 1-ULP (kernel f32 상수 0x407B53D2 cos분기 vs helper 0x407B53D1 sin분기 — numba는 kernel측 재현, 신규 이탈 아님), P11l probe; (7) digest 희소 — rad_pins.json 4/24t (WORKLOG:181 "crop pins 전 t" 과대 기재 확인), UT chain 4/24t; (8) 봉합면 — router/executor publication, full-tile forcing, E1/E3/E4, numba-full-solve dispatch 루트는 fresh 양트리 차분 없음.
- **LIVE hazard (심의 최상위)**: 동일일 future met edit — future-prefix refusal은 next_step > r0일 때만 발화, met_prefix digest는 0..next_step−1 커버 → r0 > next_step인 future 편집은 anchor-ACCEPT, Twater(utci_process.py:922, lc==1 gate)/CI(:929, 전 site)가 future 행을 읽으므로 stale 값 무경고 서빙 가능. COW는 unwired (생산 호출자 0) + met 편집은 애초 COW 미경유 — 펜스 없음. 기존 "Twater/CI record-only" 기록 강화: 심의 코드 검증으로 발화 조건 특정. Twater는 water site 필요 (fixture 없음), CI는 전 site 적용.
- **레인 간 모순 (심의 확인)**: C1 — lane-utci의 "COW layer가 loudly 거절" 서술은 거짓 (lane-fold·계측기 감사와 상충, 위 hazard 확정); C2 — lane-thermal이 tests/test_incremental_met_warm_path.py를 'executed' 근거 인용했으나 보고된 pytest 명령에 없음 (미재실행); C3 — WORKLOG:181 rad_pins 과대 기재 (4 timestep만 커밋됨); C4 — wallheight 헤드라인을 153-patch vanilla oracle gate으로 읽으면 안 됨 (numba측 env-gated sweep + subset/replay 우회); C5 — 세션 시작 git 스냅샷 HEAD 표기 stale (e480dc5 → 실제 0771622).
- **미실행 표면**: tests/ultrafast 실체는 47 test_*.py (브리프의 "62파일"은 과대 — 75 .py 전체 기준) 중 28 실행 / 19 미실행: test_cuda_{march,fold,cubescope,legacy,graphs,residency,dispatch_cost,profile}.py, test_{abi,request,status,profile}.py, test_adapter_roundtrip.py, test_bitwise_harness.py (비트 비교기 자체 증명), test_primitive_matrix.py, test_sparse_closure.py, test_startup_cache.py, test_g21_warm_measurement.py(+driver). 팬텀 5 (HEAD에 없음, 레인 명령 0 수집): test_capture.py, test_march_svf.py, test_utci_sparse.py, test_sleef_dp.py, test_sleef_f32_extra.py. sleef_dp.py(f64 dd)/sleef_f32_extra.py는 전용 게이트 부재 — SLEEF dp 비트 주장은 met_recompute 경유 전이 근거뿐.
- **provenance 잔여 red 2**: T19b(318c417) 이후 artifact source_geometry_hash 7283d87f→6cbec1b9 (frozen-74 전체) + step_table_gen.SOURCE_GEOMETRY_HASH pin 불일치 — 수치 0-mismatch와 무관하게 지속 red; 실 geometry provenance 회귀를 가리는 훈련 효과 리스크 → traces+hash 재생성 카드 필요 (본 캠페인 no-write 규정으로 미수행).
- **계측기 감사 → T29 표 지지**: workload 동등성 지지 (op 분리==vanilla 요청 경로, queue wait yaml 스코프 내, payload sha256 15/15 flag-무관, T16 계측기 certified unmodified, stage-sum assert, 선형 백분위 독립 재계산 A–F PASS). 행 등급: recompute_warm PASS 만 규정급 (clean n=100, load gate max 7.89); selected_exact 100행 전부 load 오염 → certified floor(T19b 25.01s+campaign min+T16) 근거 — 242.6× 부등식으로는 유효, 절대 p95 인용 금지; full_day warm n=70/7seg — 4.52× 부등식 유효, 절대치 host-grade; COW/T26은 생산 호출자 0이라 어떤 계측 수치도 불변.
- 소모: 서브에이전트 ~760k→88k tokens(재개 후 심의 분), 총 2회 실행 (1차 심의 FINDINGS_SCHEMA 미정의 ReferenceError → 스크립트 수정 후 resumeFromRunId 캐시 재생). Status: 캠페인 COMPLETE — 실행 표면 증명 착지, 보편 명제 정지점 기록. 후속 후보: provenance hash 재생성 카드, Twater/CI 펜스 결정(기존 Option A/C 보류 중), CUDA 게이트 별도 host 실행.
